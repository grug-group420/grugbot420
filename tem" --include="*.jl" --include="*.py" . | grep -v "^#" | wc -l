================================================================================
                    DEPARTMENT OF DEFENSE SECURITY AUDIT
================================================================================
SYSTEM:          GRUGBOT420 AI Platform
AUDIT DATE:      $(date)
AUDITOR:         NSA/CSS Cybersecurity Directorate
CLASSIFICATION:  UNCLASSIFIED//FOR OFFICIAL USE ONLY
AUTHORITY:       DoD 8570.01-M, NIST SP 800-53 Rev 5, DISA STIGs
CONTROL FAMILY:  AC, AU, CM, IA, SC, SI, RA, SA
================================================================================

1.0 EXECUTIVE SUMMARY
================================================================================

1.1 SYSTEM COMPOSITION
---------------------
Total Files: 450
Total Lines of Code: 233411
Julia Files: 236
Python Files: 21
Configuration Files: 5
Documentation: 86

1.2 RISK ASSESSMENT SUMMARY
---------------------------
CRITICAL FINDINGS: 756
HIGH FINDINGS: 4474
MEDIUM FINDINGS: 0
LOW FINDINGS: 522

================================================================================
2.0 DETAILED FINDINGS BY NIST CONTROL FAMILY
================================================================================

2.1 AC - ACCESS CONTROL (IAW AC-2, AC-3, AC-6)
------------------------------------------------
[!] AC-2: Account Management
    ./comprehensive_test_v823.jl:325:        # can find the answer when the user re-asks the original question.
    ./gen_comprehensive_specimen.py:779:    {"id": 3, "role": "user", "text": "what is 2+2", "pinned": False, "intensity": 0.8},
    ./gen_comprehensive_specimen.py:781:    {"id": 5, "role": "user", "text": "what is a cat", "pinned": False, "intensity": 0.7},
    ./gen_comprehensive_specimen.py:783:    {"id": 7, "role": "user", "text": "I feel sad today", "pinned": False, "intensity": 0.9},
    ./gen_comprehensive_specimen.py:1473:     "negatives": [], "user_triples": [{"subject": "sadness", "relation": "causes", "object": "change"}],
    ./gen_comprehensive_specimen.py:1477:     "negatives": [], "user_triples": [{"subject": "2", "relation": "plus", "object": "2"}],
    ./gen_comprehensive_specimen.py:1913:    "login_time": 0.0,
    ./test_v826f.jl:3:# Dead simple: user types "fire:oxidation and heat" and it creates a node.
    ./test_autogrowth_relational.jl:74:function make_grow_kwargs(; user_text::String = "",
    ./test_autogrowth_relational.jl:94:        :user_text                => user_text,

[!] AC-3: Access Enforcement
    ./gen_comprehensive_specimen.py:15:admin session, lobe orchestrator last state, chatter cursor, answer mode config,
    ./gen_comprehensive_specimen.py:292:    ("what is the square root of &n", "ponder|explain|calculate|explain", ["arithmetic", "sqrt"]),
    ./gen_comprehensive_specimen.py:777:    {"id": 1, "role": "system", "text": "GrugBot420 initialized. All systems online.", "pinned": True, "intensity": 1.0},
    ./gen_comprehensive_specimen.py:778:    {"id": 2, "role": "assistant", "text": "Hello! I am Grug. I think about things.", "pinned": False, "intensity": 0.5},
    ./gen_comprehensive_specimen.py:779:    {"id": 3, "role": "user", "text": "what is 2+2", "pinned": False, "intensity": 0.8},
    ./gen_comprehensive_specimen.py:780:    {"id": 4, "role": "assistant", "text": "2+2 equals 4. Basic addition.", "pinned": False, "intensity": 0.6},
    ./gen_comprehensive_specimen.py:781:    {"id": 5, "role": "user", "text": "what is a cat", "pinned": False, "intensity": 0.7},
    ./gen_comprehensive_specimen.py:782:    {"id": 6, "role": "assistant", "text": "A cat is a small furry feline, a beloved companion animal.", "pinned": False, "intensity": 0.6},
    ./gen_comprehensive_specimen.py:783:    {"id": 7, "role": "user", "text": "I feel sad today", "pinned": False, "intensity": 0.9},
    ./gen_comprehensive_specimen.py:784:    {"id": 8, "role": "assistant", "text": "I hear you. Sadness is a natural feeling, and it's okay to sit with it for a while.", "pinned": False, "intensity": 0.8},

2.2 AU - AUDIT AND ACCOUNTABILITY (IAW AU-2, AU-3, AU-12)
----------------------------------------------------------
[!] AU-2: Audit Events
    ./comprehensive_test_v823.jl:6:#         philosophy, emotion, metacognition, technology, history, language,
    ./comprehensive_test_v823.jl:44:const LOG_PATH   = "/workspace/comprehensive_test_v823_log.md"
    ./comprehensive_test_v823.jl:47:# ── In-program MD log buffer ──────────────────────────────────────────────────
    ./comprehensive_test_v823.jl:48:const _log_lines = String[]
    ./comprehensive_test_v823.jl:50:function log_md(line::String)
    ./comprehensive_test_v823.jl:51:    push!(_log_lines, line)
    ./comprehensive_test_v823.jl:54:function flush_log_md(filepath::String)
    ./comprehensive_test_v823.jl:56:        for line in _log_lines
    ./comprehensive_test_v823.jl:78:    # Strip debug telemetry if present
    ./comprehensive_test_v823.jl:111:# ── Test runner with MD logging ───────────────────────────────────────────────

[!] AU-3: Content of Audit Records
    [*] Audit logging implemented: 4514 occurrences
    [*] Audit record format:         ./comprehensive_test_v823.jl:236:    elseif mode_str == "time"
        ./comprehensive_test_v823.jl:238:        time_parts = [strip(String(s)) for s in split(content, "|")]
        ./comprehensive_test_v823.jl:239:        time_parts = filter(!isempty, time_parts)

2.3 CM - CONFIGURATION MANAGEMENT (IAW CM-2, CM-3, CM-8)
--------------------------------------------------------
[!] CM-2: Baseline Configuration
    name = "GrugBot420"
    uuid = "cca5d872-df54-4153-9d0d-2ea4c0eb00c4"
    version = "0.1.0"
    authors = ["marshalldavidson61-arch"]
    
    [deps]
    Base64 = "2a0f44e3-6c83-55bd-87e4-b1978d98bd5f"
    Dates = "ade2ca70-3891-5945-98fb-dc099432e06a"
    Distributions = "31c24e10-a181-5473-b8eb-7969acd0382f"
    JSON = "682c06a0-de6a-54ab-a142-c8b1cf79cde6"
    KernelAbstractions = "63c18a36-062a-441e-b654-da1e3ab1ce7c"
    Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"
    SHA = "ea8e919c-243c-51af-8825-aaa63cd721ce"
    Statistics = "10745b16-79ce-11e8-11f9-7d13ad32a3b2"
    
    [compat]
    Distributions = "0.25"
    JSON = "0.21"
    KernelAbstractions = "0.9"
    SHA = "0.7.0"

[!] CM-3: Configuration Change Control
    [*] Version control: 79 commits
    [*] CHANGELOG present: CHANGELOG_v8.2.md

2.4 IA - IDENTIFICATION AND AUTHENTICATION (IAW IA-2, IA-5, IA-7)
----------------------------------------------------------------
[!] IA-2: Identification and Authentication
    ./gen_comprehensive_specimen.py:1913:    "login_time": 0.0,
    ./generate_log.py:89:    md.append('- **LIGHT_TOUCH_RATE increased**: Changed from 0.15 (15% chance per eligible word) to 0.30 (30% chance). Produces visibly different prose while preserving author voice')
    ./src/engine.jl:1212:    # comparison. This is easy to forget when authoring specimens. Comments about this
    ./src/engine.jl:2196:# comparison. Easy to forget when authoring specimens.
    ./src/engine.jl:2205:If multiple sigil tags are present (rare; would be a specimen authoring
    ./src/engine.jl:2238:to forget when authoring specimens.
    ./src/engine.jl:2252:values, not as a fixed string. Easy to forget when authoring specimens.
    ./src/engine.jl:4352:    # but the seed author knows the conceptual triples are
    ./src/engine.jl:4505:comparison. Easy to forget when authoring specimens.
    ./src/ChatterMode.jl:148:# came back low-confidence is uncertain authority — jitter still applies

[!] IA-5: Authenticator Management
    [!] ./gen_comprehensive_specimen.py:44:              is_grave=False, grave_reason="", hopfield_key="0",
    [!] ./gen_comprehensive_specimen.py:153:        "hopfield_key": hopfield_key,
    [!] ./gen_comprehensive_specimen.py:846:        "hopfield": {},  # hopfield_key -> node_id
    [!] ./gen_comprehensive_specimen.py:876:        key_add = f"{a}+{b}"
    [!] ./gen_comprehensive_specimen.py:877:        key_mul = f"{a}*{b}"
    [!] ./gen_comprehensive_specimen.py:878:        math_flashcards[key_add] = {"question": f"{a}+{b}", "answer": str(a + b), "type": "addition"}
    [!] ./gen_comprehensive_specimen.py:879:        math_flashcards[key_mul] = {"question": f"{a}*{b}", "answer": str(a * b), "type": "multiplication"}
    [!] ./gen_comprehensive_specimen.py:883:            key_sub = f"{a}-{b}"
    [!] ./gen_comprehensive_specimen.py:884:            math_flashcards[key_sub] = {"question": f"{a}-{b}", "answer": str(a - b), "type": "subtraction"}
    [!] ./gen_comprehensive_specimen.py:886:            key_div = f"{a}/{b}"
    [!] ./gen_comprehensive_specimen.py:887:            math_flashcards[key_div] = {"question": f"{a}/{b}", "answer": str(a // b), "type": "division"}
    [!] ./gen_comprehensive_specimen.py:1062:        "seam_tokens": ["number", "equation", "formula"],
    [!] ./gen_comprehensive_specimen.py:1074:        "seam_tokens": ["life", "organism", "cell"],
    [!] ./gen_comprehensive_specimen.py:1086:        "seam_tokens": ["feeling", "meaning", "reflection"],
    [!] ./gen_comprehensive_specimen.py:1098:        "seam_tokens": ["pattern", "sequence", "growth"],
    [!] ./gen_comprehensive_specimen.py:1110:        "seam_tokens": ["arithmetic", "compute", "solve"],
    [!] ./gen_comprehensive_specimen.py:1121:        "seam_tokens": ["animal", "describe", "furry"],
    [!] ./gen_comprehensive_specimen.py:1132:        "seam_tokens": ["feel", "empathy", "comfort"],
    [!] ./gen_comprehensive_specimen.py:1158:    "inhibition_tokens": ["number", "compute", "sum"]
    [!] ./gen_comprehensive_specimen.py:1173:    "inhibition_tokens": ["animal", "describe", "species"]
    [!] ./gen_comprehensive_specimen.py:1188:    "inhibition_tokens": ["feel", "empathy", "comfort"]
    [!] ./gen_comprehensive_specimen.py:1203:    "inhibition_tokens": ["time", "when", "temporal"]
    [!] ./gen_comprehensive_specimen.py:1222:        "pattern": " ".join(b["seam_tokens"]),
    [!] ./gen_comprehensive_specimen.py:1342:     "promote_at_tokenize": True},
    [!] ./gen_comprehensive_specimen.py:1345:     "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1348:     "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1351:     "promote_at_tokenize": True},
    [!] ./gen_comprehensive_specimen.py:1359:     "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1363:     "provenance": "specimen", "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1366:     "expansion": None, "provenance": "specimen", "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1372:     "provenance": "engine-default", "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1377:     "provenance": "engine-default", "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1382:     "provenance": "engine-default", "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1387:     "provenance": "engine-default", "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1392:     "provenance": "engine-default", "promote_at_tokenize": False},
    [!] ./gen_comprehensive_specimen.py:1401:     "expansion": None, "provenance": "engine-default", "promote_at_tokenize": True},
    [!] ./gen_comprehensive_specimen.py:1410:     "expansion": None, "provenance": "engine-default", "promote_at_tokenize": True},
    [!] ./gen_comprehensive_specimen.py:1419:     "expansion": None, "provenance": "engine-default", "promote_at_tokenize": True},
    [!] ./gen_comprehensive_specimen.py:1523:    "key_count": 45
    [!] ./gen_comprehensive_specimen.py:1794:        key = f"{math_nodes[i]['id']}|{math_nodes[j]['id']}"
    [!] ./gen_comprehensive_specimen.py:1795:        co_activation[key] = {"intensity": 0.3 + i * 0.1, "co_fire_count": 5 + i * 2}
    [!] ./gen_comprehensive_specimen.py:1798:        key = f"{nature_nodes[i]['id']}|{nature_nodes[j]['id']}"
    [!] ./gen_comprehensive_specimen.py:1799:        co_activation[key] = {"intensity": 0.25 + i * 0.1, "co_fire_count": 3 + i * 2}
    [!] ./gen_comprehensive_specimen.py:1802:        key = f"{emotion_nodes[i]['id']}|{emotion_nodes[j]['id']}"
    [!] ./gen_comprehensive_specimen.py:1803:        co_activation[key] = {"intensity": 0.35 + i * 0.1, "co_fire_count": 7 + i * 2}
    [!] ./gen_comprehensive_specimen.py:1843:    key = f"word_{i}|word_{i+1}"
    [!] ./gen_comprehensive_specimen.py:1844:    co_occur[key] = {"count": 2 + i, "last_seen": now - 1800 + i * 100}
    [!] ./gen_comprehensive_specimen.py:2038:print(f"Total top-level keys: {len(specimen)}")
    [!] ./compute_signals.jl:14:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./compute_signals.jl:18:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./compute_signals.jl:22:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./compute_signals.jl:28:    tokens = split(lowercase(strip(text)))
    [!] ./compute_signals.jl:29:    if isempty(tokens)
    [!] ./compute_signals.jl:33:    for tok in tokens
    [!] ./compute_signals.jl:74:    println("$(repr(p)): $(length(sig)) tokens → $(sig)")
    [!] ./compute_signals.jl:84:    println("action:$(repr(a)): $(length(sig)) tokens → $(sig)")
    [!] ./specimen_v3_nodes.jl:19:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./specimen_v3_nodes.jl:23:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./specimen_v3_nodes.jl:27:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./build_specimen.jl:17:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./build_specimen.jl:21:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./build_specimen.jl:25:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./build_specimen.jl:547:        "system_prompt": "Grug stares at the ocean. Waves are the rhythm of the world's breathing. The tide comes and goes like time itself. The sea is vast and holds secrets Grug may never know. Grug respects its power.",
    [!] ./build_specimen.jl:638:println("  ✅ Relational patterns added to key nodes")
    [!] ./build_specimen.jl:707:for (pat_a, lobe_a, pat_b, lobe_b, tokens) in bridge_specs
    [!] ./build_specimen.jl:712:            bridge_nodes!(id_a, id_b; seam_tokens=tokens)
    [!] ./build_specimen.jl:1027:# NOTE: promote_at_tokenize=false because :string sigil_type has no promoter shape predicate
    [!] ./build_specimen.jl:1034:    promote_at_tokenize=false
    [!] ./build_specimen.jl:1044:    promote_at_tokenize=true
    [!] ./build_specimen.jl:1054:    promote_at_tokenize=true
    [!] ./build_specimen.jl:1097:    key="math_pattern",
    [!] ./build_specimen.jl:1109:    key="empathy_pattern",
    [!] ./build_specimen.jl:1121:    key="survival_pattern",
    [!] ./build_specimen.jl:1184:        if haskey(Lobe.NODE_TO_LOBE_IDX, nid) && Lobe.NODE_TO_LOBE_IDX[nid] == "lobe_temporal"
    [!] ./patch_v826g.py:77:    # GRUG v8.26g: Translate candidates back to Votes using composite key.
    [!] ./patch_v826g.py:78:    sure_votes   = Vote[candidate_to_vote[(vc.node_id, vc.multipart_group)] for vc in top_tier if haskey(candidate_to_vote, (vc.node_id, vc.multipart_group))]
    [!] ./patch_v826g.py:79:    unsure_votes = Vote[candidate_to_vote[(vc.node_id, vc.multipart_group)] for vc in subtop_tier if haskey(candidate_to_vote, (vc.node_id, vc.multipart_group))]
    [!] ./patch_v826g.py:98:            if !haskey(_group_primaries, grp) || v.confidence > _group_primaries[grp].confidence
    [!] ./interaction_harness_v3.jl:13:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./interaction_harness_v3.jl:17:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./interaction_harness_v3.jl:21:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./interact_specimen.jl:18:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./interact_specimen.jl:22:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./interact_specimen.jl:26:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./interact_specimen.jl:342:    sigil_names = sort(collect(keys(_ENGINE_SIGIL_TABLE.entries)))
    [!] ./interact_specimen.jl:357:        println(f, "- **$(r.id)**: key=$(r.key), weight=$(r.weight.value), type=$(r.transform_type), fires=$(r.fire_count)")
    [!] ./interact_specimen.jl:382:            if haskey(node.json_data, "time_node") && node.json_data["time_node"] === true
    [!] ./interact_specimen.jl:402:    am_modes = sort(collect(keys(_ANSWER_MODE_CONFIG)))
    [!] ./new_sections_part11.jl:43:    _parse = parse_sigil_token("&TestMacro")
    [!] ./new_sections_part11.jl:44:    record("parse_sigil_token '&TestMacro' returns name", _parse == "TestMacro")
    [!] ./new_sections_part11.jl:46:    record("parse_sigil_token", false, "$e")
    [!] ./new_sections_part11.jl:50:    _parse2 = parse_sigil_token("not_a_sigil")
    [!] ./new_sections_part11.jl:51:    record("parse_sigil_token 'not_a_sigil' returns nothing", _parse2 === nothing)
    [!] ./new_sections_part11.jl:53:    record("parse_sigil_token non-sigil", false, "$e")
    [!] ./new_sections_part11.jl:58:                              structure_template=Dict("key" => "value"))
    [!] ./new_sections_part11.jl:108:    _token_re = SIGIL_TOKEN_REGEX
    [!] ./new_sections_part11.jl:109:    record("SIGIL_TOKEN_REGEX is Regex", _token_re isa Regex)
    [!] ./new_sections_part11.jl:134:    record("observe! second write to same key", true)
    [!] ./new_sections_part11.jl:141:    record("observe! different key runs", true)
    [!] ./new_sections_part11.jl:143:    record("observe! different key", false, "$e")
    [!] ./new_sections_part11.jl:175:    _kc = key_count()
    [!] ./new_sections_part11.jl:176:    record("key_count returns Int", _kc isa Int)
    [!] ./new_sections_part11.jl:178:    record("key_count", false, "$e")
    [!] ./new_sections_part11.jl:190:    record("drop_keys_by_prefix! runs", true)
    [!] ./new_sections_part11.jl:192:    record("drop_keys_by_prefix!", false, "$e")
    [!] ./gen_specimen_v10_part1.py:44:        "Grug. Derivative question. Derivative measures how fast something changes. It is the slope of a curve at one point. Rate of change is key.",
    [!] ./gen_specimen_v10_part1.py:392:              response_times=None, hopfield_key="0",
    [!] ./gen_specimen_v10_part1.py:393:              voice_key=None):
    [!] ./gen_specimen_v10_part1.py:394:    """Create a node dict. If voice_key is given, populate system_prompt, voice_variants,
    [!] ./gen_specimen_v10_part1.py:411:    # ── Apply voice_key data ──
    [!] ./gen_specimen_v10_part1.py:412:    if voice_key and voice_key in NODE_VOICES:
    [!] ./gen_specimen_v10_part1.py:413:        sp, vv, na, vr, fh = NODE_VOICES[voice_key]
    [!] ./gen_specimen_v10_part1.py:458:        "hopfield_key": hopfield_key
    [!] ./gen_specimen_v10_part1.py:464:    for k in sorted(NODE_VOICES.keys()):
    [!] ./src/VoteOrchestrator.jl:33:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/VoteOrchestrator.jl:37:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/VoteOrchestrator.jl:41:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/VoteOrchestrator.jl:96:# Raw token+rel confidence floor for "this rock has real opinion".
    [!] ./src/VoteOrchestrator.jl:173:# "describe X" and get an "alert" answer because token overlap on X beat
    [!] ./src/VoteOrchestrator.jl:197:# That confidence was computed by token_overlap between the connector pattern
    [!] ./src/VoteOrchestrator.jl:350:            if haskey(_TASK_DEADLINES, name)
    [!] ./src/VoteOrchestrator.jl:371:                haskey(_TASK_DEADLINES, name) ? _TASK_DEADLINES[name][3] : "unknown"
    [!] ./src/VoteOrchestrator.jl:725:    # attach time (token_overlap scan happened once, then cached). The relay
    [!] ./src/VoteOrchestrator.jl:801:confidence, which broke action-vs-intent coherence: token-overlap winners
    [!] ./src/VoteOrchestrator.jl:913:    # vote's ranking reflects intent alignment, not just token overlap.
    [!] ./src/VoteOrchestrator.jl:1098:        key = (vc.multipart_group, vc.action)
    [!] ./src/VoteOrchestrator.jl:1099:        if !haskey(buckets, key)
    [!] ./src/VoteOrchestrator.jl:1100:            buckets[key] = VoteCandidate[]
    [!] ./src/VoteOrchestrator.jl:1101:            push!(order, key)
    [!] ./src/VoteOrchestrator.jl:1103:        push!(buckets[key], vc)
    [!] ./src/VoteOrchestrator.jl:1111:    for key in order
    [!] ./src/VoteOrchestrator.jl:1112:        group = buckets[key]
    [!] ./src/VoteOrchestrator.jl:1145:        winner_map[key] = winner_id
    [!] ./src/VoteOrchestrator.jl:1148:            key[1], key[2], total_before, evicted_here,
    [!] ./src/LobeOrchestrator.jl:16:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/LobeOrchestrator.jl:20:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/LobeOrchestrator.jl:24:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/LobeOrchestrator.jl:198:        key = isnothing(lobe_id) ? "-" : lobe_id
    [!] ./src/LobeOrchestrator.jl:199:        push!(get!(by_lobe, key, similar(entries, 0)), e)
    [!] ./src/LobeOrchestrator.jl:268:    score_lobes(entries, lobe_lookup; input_tokens=String[]) -> Vector{LobeFireOrder}
    [!] ./src/LobeOrchestrator.jl:278:`input_tokens` are the tokenized input words, used by the per-lobe fuzzy
    [!] ./src/LobeOrchestrator.jl:280:input tokens match, the lobe is vetoed (score forced to 0). This prevents
    [!] ./src/LobeOrchestrator.jl:282:Empty input_tokens disables the whitelist gate entirely (backward compatible).
    [!] ./src/LobeOrchestrator.jl:287:function score_lobes(entries::AbstractVector, lobe_lookup; input_tokens::AbstractVector=String[])::Vector{LobeFireOrder}
    [!] ./src/LobeOrchestrator.jl:322:    # whose patterns contain greedy tokens like "calculate", "hello", "fire".
    [!] ./src/PettyLearner.jl:6:# Simple math like "3+5"? Just write answer on flashcard. Domain token for
    [!] ./src/PettyLearner.jl:12:#   1. NEW WORD → THESAURUS: One uncovered token, high similarity to a
    [!] ./src/PettyLearner.jl:13:#      covered token. Add synonym pair instantly.
    [!] ./src/PettyLearner.jl:18:#   3. DOMAIN TOKEN → LOBE WHITELIST: One uncovered token that overlaps
    [!] ./src/PettyLearner.jl:26:# IMPORTANT: Petty learning is CONSERVATIVE. Only ONE uncovered token.
    [!] ./src/PettyLearner.jl:27:# If there are 2+ uncovered tokens, it's not petty — it's a real gap.
    [!] ./src/PettyLearner.jl:46:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/PettyLearner.jl:50:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/PettyLearner.jl:54:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/PettyLearner.jl:67:    candidate_token::String   # The token that triggered the fast-path
    [!] ./src/PettyLearner.jl:100:const PETTY_MIN_TOKEN_LENGTH     = 3     # ignore 1-2 char tokens
    [!] ./src/PettyLearner.jl:108:    classify_petty(user_text, tokens, node_patterns, thesaurus_fn,
    [!] ./src/PettyLearner.jl:117:    tokens::Vector{String},
    [!] ./src/PettyLearner.jl:125:    # GRUG: Find uncovered non-stopword tokens.
    [!] ./src/PettyLearner.jl:127:    for tok in tokens
    [!] ./src/PettyLearner.jl:137:    # GRUG: If exactly ONE uncovered token and it's similar to a covered token,
    [!] ./src/PettyLearner.jl:143:        # GRUG: Check similarity against all covered tokens in the input
    [!] ./src/PettyLearner.jl:144:        for tok in tokens
    [!] ./src/PettyLearner.jl:162:            lobe_hint = _infer_lobe_from_token(candidate, lobe_snapshots)
    [!] ./src/PettyLearner.jl:171:        # GRUG: If that one uncovered token belongs to an under-populated
    [!] ./src/PettyLearner.jl:174:            subject_tokens = [lowercase(strip(t)) for t in split(subject)]
    [!] ./src/PettyLearner.jl:175:            if candidate in subject_tokens && length(node_ids) < 5
    [!] ./src/PettyLearner.jl:176:                # GRUG: Check the token doesn't already belong to multiple lobes
    [!] ./src/PettyLearner.jl:196:        has_n = count(k -> startswith(string(k), "&n"), keys(arithmetic_bindings)) >= 2
    [!] ./src/PettyLearner.jl:197:        has_op = count(k -> startswith(string(k), "&op"), keys(arithmetic_bindings)) >= 1
    [!] ./src/PettyLearner.jl:198:        n_ops = count(k -> startswith(string(k), "&op"), keys(arithmetic_bindings))
    [!] ./src/PettyLearner.jl:203:                lobe_hint = _infer_lobe_from_token("math", lobe_snapshots)
    [!] ./src/PettyLearner.jl:230:    lobe_whitelist_fn::Function = (lobe_id, token) -> 0,
    [!] ./src/PettyLearner.jl:243:            success = thesaurus_register_fn(result.candidate_token, result.similarity > 0 ? result.candidate_token : "")
    [!] ./src/PettyLearner.jl:266:        # GRUG: Add the token to the lobe's whitelist.
    [!] ./src/PettyLearner.jl:268:            count_added = lobe_whitelist_fn(result.target_lobe, result.candidate_token)
    [!] ./src/PettyLearner.jl:269:            detail = "whitelist: \"$(result.candidate_token)\" → $(result.target_lobe) [added=$count_added]"
    [!] ./src/PettyLearner.jl:277:        result.candidate_token, result.target_lobe, result.similarity,
    [!] ./src/PettyLearner.jl:286:function _is_covered(token::String, node_patterns::Set{String})::Bool
    [!] ./src/PettyLearner.jl:287:    tok_lower = lowercase(strip(token))
    [!] ./src/PettyLearner.jl:296:function _infer_lobe_from_token(token::String, lobe_snapshots::Vector{Tuple{String,String,Set{String}}})::String
    [!] ./src/PettyLearner.jl:297:    tok_lower = lowercase(strip(token))
    [!] ./src/PettyLearner.jl:301:        subject_tokens = [lowercase(strip(t)) for t in split(subject)]
    [!] ./src/PettyLearner.jl:302:        overlap = count(t -> t == tok_lower, subject_tokens)
    [!] ./src/PettyLearner.jl:316:    for (key, val) in bindings
    [!] ./src/PettyLearner.jl:317:        k = string(key)
    [!] ./src/PettyLearner.jl:349:      max_uncovered_tokens: $PETTY_MAX_UNCOVERED_TOKENS
    [!] ./src/PettyLearner.jl:351:      min_token_length:     $PETTY_MIN_TOKEN_LENGTH
    [!] ./src/PatternMiner.jl:115:function set_pattern_miner_config!(key::Symbol, value)
    [!] ./src/PatternMiner.jl:118:        if key === :enabled
    [!] ./src/PatternMiner.jl:120:        elseif key === :transitivity_threshold
    [!] ./src/PatternMiner.jl:122:        elseif key === :chaining_threshold
    [!] ./src/PatternMiner.jl:124:        elseif key === :symmetry_threshold
    [!] ./src/PatternMiner.jl:126:        elseif key === :max_proposals
    [!] ./src/PatternMiner.jl:128:        elseif key === :scan_interval
    [!] ./src/PatternMiner.jl:130:        elseif key === :instance_ttl
    [!] ./src/PatternMiner.jl:133:            error("unknown PatternMiner config key :$key")
    [!] ./src/PatternMiner.jl:162:    haskey(d, "enabled")                && set_pattern_miner_config!(:enabled, Bool(d["enabled"]))
    [!] ./src/PatternMiner.jl:163:    haskey(d, "transitivity_threshold") && set_pattern_miner_config!(:transitivity_threshold, Int(d["transitivity_threshold"]))
    [!] ./src/PatternMiner.jl:164:    haskey(d, "chaining_threshold")     && set_pattern_miner_config!(:chaining_threshold, Int(d["chaining_threshold"]))
    [!] ./src/PatternMiner.jl:165:    haskey(d, "symmetry_threshold")     && set_pattern_miner_config!(:symmetry_threshold, Int(d["symmetry_threshold"]))
    [!] ./src/PatternMiner.jl:166:    haskey(d, "max_proposals")          && set_pattern_miner_config!(:max_proposals, Int(d["max_proposals"]))
    [!] ./src/PatternMiner.jl:167:    haskey(d, "scan_interval")          && set_pattern_miner_config!(:scan_interval, Float64(d["scan_interval"]))
    [!] ./src/PatternMiner.jl:168:    haskey(d, "instance_ttl")           && set_pattern_miner_config!(:instance_ttl, Float64(d["instance_ttl"]))
    [!] ./src/PatternMiner.jl:493:        if haskey(_PROPOSALS, proposal_id)
    [!] ./src/PatternMiner.jl:510:        if haskey(_PROPOSALS, proposal_id)
    [!] ./src/PatternMiner.jl:634:    if haskey(d, "instances") && isa(d["instances"], AbstractVector)
    [!] ./src/PatternMiner.jl:650:    if haskey(d, "proposals") && isa(d["proposals"], AbstractVector)
    [!] ./src/InverseSigil.jl:11:#   The silence_map (SOURCE 1 in AutoGrowth) sees uncovered tokens as UNRELATED
    [!] ./src/InverseSigil.jl:18:#   1. LINEAR (token inverse) — for :macro sigils like &noun where every concrete
    [!] ./src/InverseSigil.jl:55:#   add_concrete!(sigil_name, token; user_directive) — add a concrete to the index
    [!] ./src/InverseSigil.jl:75:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.     ║
    [!] ./src/InverseSigil.jl:79:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/InverseSigil.jl:83:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/InverseSigil.jl:96:# ── LINEAR MODE (token inverse: &noun, :macro class) ──────────────────────────
    [!] ./src/InverseSigil.jl:123:# :lambda with similar tokens → thesaurus pair
    [!] ./src/InverseSigil.jl:150:A single concrete token observed under a sigil shape. Tracks how many times
    [!] ./src/InverseSigil.jl:154:  - token::String — the concrete token (e.g. "cat" under &noun, "7" under &n)
    [!] ./src/InverseSigil.jl:162:    token::String
    [!] ./src/InverseSigil.jl:170:function ConcreteEntry(token::String; is_user_directive::Bool = false)
    [!] ./src/InverseSigil.jl:172:    ConcreteEntry(token, 1, 1.0, is_user_directive, t, t)
    [!] ./src/InverseSigil.jl:181:    where each concrete is a meaningful token.
    [!] ./src/InverseSigil.jl:217:  - concretes::Dict{String, ConcreteEntry} — token → entry
    [!] ./src/InverseSigil.jl:251:  - concretes::Vector{String} — the concrete tokens under this sigil
    [!] ./src/InverseSigil.jl:353:     - :lambda sigil with similar tokens → THESAURUS_PAIR
    [!] ./src/InverseSigil.jl:374:    concrete_tokens = collect(keys(entry.concretes))
    [!] ./src/InverseSigil.jl:376:                             keys(entry.concretes)) |> collect
    [!] ./src/InverseSigil.jl:382:                          strength, concrete_tokens, user_directives,
    [!] ./src/InverseSigil.jl:393:                          strength, concrete_tokens, user_directives,
    [!] ./src/InverseSigil.jl:405:                          strength, concrete_tokens, user_directives,
    [!] ./src/InverseSigil.jl:418:                          strength, concrete_tokens, user_directives,
    [!] ./src/InverseSigil.jl:429:                      strength, concrete_tokens, user_directives,
    [!] ./src/InverseSigil.jl:450:        # If concretes are string tokens (not just digits), thesaurus pair possible.
    [!] ./src/InverseSigil.jl:451:        non_numeric_concretes = filter(c -> !all(isdigit, c), keys(entry.concretes))
    [!] ./src/InverseSigil.jl:467:    add_concrete!(sigil_name, token; user_directive=false, sigil_class=nothing)
    [!] ./src/InverseSigil.jl:469:Add a concrete token to the inverse index under a sigil shape. If the sigil
    [!] ./src/InverseSigil.jl:480:function add_concrete!(sigil_name::String, token::String;
    [!] ./src/InverseSigil.jl:484:    token_lower = lowercase(strip(token))
    [!] ./src/InverseSigil.jl:485:    isempty(token_lower) && return nothing
    [!] ./src/InverseSigil.jl:489:        if !haskey(_INVERSE_TABLE, sigil_name)
    [!] ./src/InverseSigil.jl:509:                delete!(entry.concretes, lowest.token)
    [!] ./src/InverseSigil.jl:516:        if haskey(entry.concretes, token_lower)
    [!] ./src/InverseSigil.jl:517:            ce = entry.concretes[token_lower]
    [!] ./src/InverseSigil.jl:527:            ce = ConcreteEntry(token_lower; is_user_directive=user_directive)
    [!] ./src/InverseSigil.jl:528:            entry.concretes[token_lower] = ce
    [!] ./src/InverseSigil.jl:547:inverse index. Each binding tells us what token was erased during promotion
    [!] ./src/InverseSigil.jl:551:This is the AUTOMATIC path: every time SigilPromoter promotes a token,
    [!] ./src/InverseSigil.jl:552:we learn that the token is a concrete of that sigil shape.
    [!] ./src/InverseSigil.jl:561:            token = string(getproperty(b, :value))
    [!] ./src/InverseSigil.jl:562:            if !isempty(sig_name) && !isempty(token)
    [!] ./src/InverseSigil.jl:563:                add_concrete!(sig_name, token;
    [!] ./src/InverseSigil.jl:575:    observe_concretes!(sigil_name, tokens; sigil_class=nothing)
    [!] ./src/InverseSigil.jl:577:Observe multiple concrete tokens under a sigil shape. Called from
    [!] ./src/InverseSigil.jl:578:AutoGrowth SOURCE 19 when uncovered tokens cluster under a sigil.
    [!] ./src/InverseSigil.jl:583:function observe_concretes!(sigil_name::String, tokens::Vector{String};
    [!] ./src/InverseSigil.jl:586:    for tok in tokens
    [!] ./src/InverseSigil.jl:593:    user_add_concrete!(sigil_name, token; sigil_class=nothing)
    [!] ./src/InverseSigil.jl:598:function user_add_concrete!(sigil_name::String, token::String;
    [!] ./src/InverseSigil.jl:601:    add_concrete!(sigil_name, token; user_directive=true, sigil_class=sigil_class,
    [!] ./src/InverseSigil.jl:625:            for (token, ce) in entry.concretes
    [!] ./src/InverseSigil.jl:630:                    push!(to_evict, token)
    [!] ./src/InverseSigil.jl:633:            for token in to_evict
    [!] ./src/InverseSigil.jl:634:                if entry.concretes[token].is_user_directive
    [!] ./src/InverseSigil.jl:637:                delete!(entry.concretes, token)
    [!] ./src/InverseSigil.jl:645:        empty_sigs = filter(k -> isempty(_INVERSE_TABLE[k].concretes), keys(_INVERSE_TABLE))
    [!] ./src/InverseSigil.jl:653:            lowest_sig = argmin(k -> _INVERSE_TABLE[k].cluster_density, keys(_INVERSE_TABLE))
    [!] ./src/InverseSigil.jl:667:GRUG: This is SOURCE 19 for AutoGrowth. It scans user text for tokens that
    [!] ./src/InverseSigil.jl:668:match sigil shapes, clusters uncovered tokens by shape, and returns a list
    [!] ./src/InverseSigil.jl:672:The key insight: instead of treating each uncovered token as an unrelated
    [!] ./src/InverseSigil.jl:674:tokens that are both concretes of &noun produce a STRONGER combined signal
    [!] ./src/InverseSigil.jl:699:    # tokens that could be concretes under that sigil shape.
    [!] ./src/InverseSigil.jl:700:    tokens = _tokenize(user_text)
    [!] ./src/InverseSigil.jl:701:    isempty(tokens) && return results
    [!] ./src/InverseSigil.jl:703:    uncovered = filter(t -> !_is_covered(t, node_patterns), tokens)
    [!] ./src/InverseSigil.jl:709:        # GRUG: Find uncovered tokens that could be concretes of this sigil
    [!] ./src/InverseSigil.jl:741:        # GRUG: For each uncovered token under this sigil, add evidence
    [!] ./src/InverseSigil.jl:783:token-level classifier. The two systems cooperate:
    [!] ./src/InverseSigil.jl:784:  - PettyLearner handles ONE uncovered token with high similarity to a known token
    [!] ./src/InverseSigil.jl:791:  - add_lobe_whitelist_fn — (lobe_id, token) -> Int
    [!] ./src/InverseSigil.jl:823:        # GRUG: Concrete tokens under a :relation sigil are verbs that should
    [!] ./src/InverseSigil.jl:959:            if haskey(sig_entry, :class)
    [!] ./src/InverseSigil.jl:962:            if haskey(sig_entry, "class")
    [!] ./src/InverseSigil.jl:975:    _find_concretes_for_sigil(sig_name, sig_class, sig_entry, uncovered_tokens, user_text)
    [!] ./src/InverseSigil.jl:977:Determine which uncovered tokens could be concretes of this sigil shape.
    [!] ./src/InverseSigil.jl:981:                                    sig_entry, uncovered_tokens::Vector{String},
    [!] ./src/InverseSigil.jl:985:    # GRUG: For :macro sigils with a lexicon, tokens that match the lexicon
    [!] ./src/InverseSigil.jl:986:    # are concretes. BUT we want UNCOVERED tokens, so we also consider tokens
    [!] ./src/InverseSigil.jl:989:        # Check if the sigil has a lexicon — tokens in it are known concretes
    [!] ./src/InverseSigil.jl:999:        # GRUG: For &noun-like sigils, ALL uncovered non-stopword tokens are
    [!] ./src/InverseSigil.jl:1001:        # token that isn't covered could be a concrete of &noun.
    [!] ./src/InverseSigil.jl:1005:            for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1012:            # If a token is already in the lexicon, it's a known concrete.
    [!] ./src/InverseSigil.jl:1013:            # We want NEW concretes — uncovered tokens that COULD belong.
    [!] ./src/InverseSigil.jl:1015:            # uncovered tokens are candidate concretes.
    [!] ./src/InverseSigil.jl:1018:                for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1029:        # &word: any non-stopword token is a concrete.
    [!] ./src/InverseSigil.jl:1041:            for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1052:                for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1058:                # &op: arithmetic operators — not typically uncovered tokens
    [!] ./src/InverseSigil.jl:1059:                # but check for operator-like tokens
    [!] ./src/InverseSigil.jl:1060:                for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1066:                # &word: any non-stopword token
    [!] ./src/InverseSigil.jl:1067:                for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1077:        # Uncovered tokens that appear in the expansion list are candidate concretes.
    [!] ./src/InverseSigil.jl:1090:            for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1097:        # GRUG: Also check if uncovered tokens are verbs that could be ADDED
    [!] ./src/InverseSigil.jl:1102:            # GRUG: Simple verb heuristic — tokens that end in "s", "ed", "ing"
    [!] ./src/InverseSigil.jl:1103:            for tok in uncovered_tokens
    [!] ./src/InverseSigil.jl:1134:function _tokenize(text::String)::Vector{String}
    [!] ./src/InverseSigil.jl:1135:    tokens = split(lowercase(strip(text)))
    [!] ./src/InverseSigil.jl:1136:    return filter(t -> length(t) > 2 && !(t in _INV_STOPWORDS), String.(tokens))
    [!] ./src/InverseSigil.jl:1139:function _is_covered(token::String, existing_patterns::Set{String})::Bool
    [!] ./src/InverseSigil.jl:1140:    tok_lower = lowercase(strip(token))
    [!] ./src/InverseSigil.jl:1149:function _observation_count(token::String)::Int
    [!] ./src/InverseSigil.jl:1150:    # GRUG: Get observation count from ALL inverse entries that contain this token.
    [!] ./src/InverseSigil.jl:1155:            if haskey(entry.concretes, token)
    [!] ./src/InverseSigil.jl:1156:                total += entry.concretes[token].observation_count
    [!] ./src/InverseSigil.jl:1165:    # More specific than token-level inference because sigils carry semantic weight.
    [!] ./src/InverseSigil.jl:1239:            if haskey(sigil_table_entries, sig_name)
    [!] ./src/InverseSigil.jl:1265:    set_lobe_hints_for_tokens!(tokens::Vector{String}, lobe_name::String)
    [!] ./src/InverseSigil.jl:1268:one of the given tokens. This is TARGETED — only affects entries that
    [!] ./src/InverseSigil.jl:1269:already know about these tokens, not a broadcast to all sigils.
    [!] ./src/InverseSigil.jl:1272:"science: gravity is an attractive force" is detected. The content tokens
    [!] ./src/InverseSigil.jl:1276:function set_lobe_hints_for_tokens!(tokens::Vector{String}, lobe_name::String)
    [!] ./src/InverseSigil.jl:1280:            for tok in tokens
    [!] ./src/InverseSigil.jl:1281:                if haskey(entry.concretes, lowercase(strip(tok)))
    [!] ./src/InverseSigil.jl:1306:                    "token"             => ce.token,
    [!] ./src/InverseSigil.jl:1347:                    String(get(cdata, "token", tok)),
    [!] ./src/InverseSigil.jl:1384:        if haskey(_INVERSE_TABLE, sigil_name)
    [!] ./src/InverseSigil.jl:1400:        if haskey(_INVERSE_TABLE, sigil_name)
    [!] ./src/InverseSigil.jl:1460:        # Show concrete tokens sorted by weight
    [!] ./src/TemporalIdentity.jl:114:function set_temporal_identity_config!(key::Symbol, value)
    [!] ./src/TemporalIdentity.jl:117:        if key === :enabled
    [!] ./src/TemporalIdentity.jl:119:        elseif key === :auto_discover
    [!] ./src/TemporalIdentity.jl:121:        elseif key === :coherence_threshold
    [!] ./src/TemporalIdentity.jl:123:        elseif key === :max_stages
    [!] ./src/TemporalIdentity.jl:125:        elseif key === :max_continuants
    [!] ./src/TemporalIdentity.jl:127:        elseif key === :max_proposals
    [!] ./src/TemporalIdentity.jl:129:        elseif key === :stage_ttl
    [!] ./src/TemporalIdentity.jl:132:            error("unknown config key: $key")
    [!] ./src/TemporalIdentity.jl:161:        haskey(d, "enabled")             && (cfg.enabled = Bool(d["enabled"]))
    [!] ./src/TemporalIdentity.jl:162:        haskey(d, "auto_discover")       && (cfg.auto_discover = Bool(d["auto_discover"]))
    [!] ./src/TemporalIdentity.jl:163:        haskey(d, "coherence_threshold") && (cfg.coherence_threshold = Float64(d["coherence_threshold"]))
    [!] ./src/TemporalIdentity.jl:164:        haskey(d, "max_stages")          && (cfg.max_stages = Int(d["max_stages"]))
    [!] ./src/TemporalIdentity.jl:165:        haskey(d, "max_continuants")     && (cfg.max_continuants = Int(d["max_continuants"]))
    [!] ./src/TemporalIdentity.jl:166:        haskey(d, "max_proposals")       && (cfg.max_proposals = Int(d["max_proposals"]))
    [!] ./src/TemporalIdentity.jl:167:        haskey(d, "stage_ttl")           && (cfg.stage_ttl = Float64(d["stage_ttl"]))
    [!] ./src/TemporalIdentity.jl:449:        if !haskey(by_orient, o)
    [!] ./src/TemporalIdentity.jl:699:    if haskey(d, "config")
    [!] ./src/TemporalIdentity.jl:703:    if haskey(d, "continuants") && isa(d["continuants"], Dict)
    [!] ./src/TemporalIdentity.jl:710:                if haskey(cd, "stages") && isa(cd["stages"], Vector)
    [!] ./src/TemporalIdentity.jl:723:                if haskey(cd, "transform_rules") && isa(cd["transform_rules"], Vector)
    [!] ./src/TemporalIdentity.jl:750:    if haskey(d, "proposals") && isa(d["proposals"], AbstractVector)
    [!] ./src/TemporalIdentity.jl:756:                if haskey(pd, "proposed_stages") && isa(pd["proposed_stages"], AbstractVector)
    [!] ./src/PhagyMode.jl:48:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/PhagyMode.jl:52:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/PhagyMode.jl:56:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/PhagyMode.jl:178:        if haskey(PHAGY_RESERVED_RESOURCES, resource_scope)
    [!] ./src/PhagyMode.jl:199:        if haskey(PHAGY_RESERVED_RESOURCES, resource_scope)
    [!] ./src/PhagyMode.jl:357:                    if haskey(node_map, id)
    [!] ./src/PhagyMode.jl:508:                    if haskey(node_map, nid)
    [!] ./src/PhagyMode.jl:532:                    if !haskey(target.drop_table, response_text)
    [!] ./src/PhagyMode.jl:586:# GRUG: The Hopfield cache stores familiar-input fast-paths: UInt64 hash keys ->
    [!] ./src/PhagyMode.jl:591:# Cache key type  : UInt64 (hash of normalized input text, from hopfield_input_hash())
    [!] ./src/PhagyMode.jl:619:#     stale_keys = UInt64[]
    [!] ./src/PhagyMode.jl:621:#     # GRUG: PASS 1 - collect stale keys under both locks (cache_lock THEN node_lock)
    [!] ./src/PhagyMode.jl:625:#             for (cache_key, node_ids) in hopfield_cache
    [!] ./src/PhagyMode.jl:629:#                     !haskey(node_map, nid) || node_map[nid].is_grave
    [!] ./src/PhagyMode.jl:632:#                     push!(stale_keys, cache_key)
    [!] ./src/PhagyMode.jl:641:#     if !isempty(stale_keys)
    [!] ./src/PhagyMode.jl:643:#             for key in stale_keys
    [!] ./src/PhagyMode.jl:644:#                 delete!(hopfield_cache, key)
    [!] ./src/PhagyMode.jl:646:#                 @debug "[PHAGY:CACHE] Purged stale cache entry key=$(key)"
    [!] ./src/PhagyMode.jl:1168:                    if !haskey(node_map, nid) || node_map[nid].is_grave
    [!] ./src/PhagyMode.jl:1254:                    if haskey(injector_dict, id)
    [!] ./src/PhagyMode.jl:1342:            # GRUG: Sort keys by id (timestamp-embedded) and take the oldest.
    [!] ./src/PhagyMode.jl:1343:            sorted_keys = sort(collect(keys(phase_acc.entries)))
    [!] ./src/PhagyMode.jl:1344:            to_remove = sorted_keys[1:trim_count]
    [!] ./src/PhagyMode.jl:1345:            for key in to_remove
    [!] ./src/PhagyMode.jl:1346:                delete!(phase_acc.entries, key)
    [!] ./src/PhagyMode.jl:1379:lowest-salience entries across ALL keys, not just the bucket being written to.
    [!] ./src/PhagyMode.jl:1385:store, not just one key's bucket.
    [!] ./src/PhagyMode.jl:1430:        # GRUG: Find the globally lowest-salience entries across all keys.
    [!] ./src/PhagyMode.jl:1438:        candidates = Tuple{Float64, String, Int}[]  # (score, key, index_in_bucket)
    [!] ./src/PhagyMode.jl:1441:            for (key, bucket) in observer_store.table
    [!] ./src/PhagyMode.jl:1448:                    push!(candidates, (score, key, idx))
    [!] ./src/PhagyMode.jl:1456:        # GRUG: Group eviction by key so we can check bucket-level protection.
    [!] ./src/PhagyMode.jl:1458:        keys_with_evictions = Dict{String, Vector{Int}}()
    [!] ./src/PhagyMode.jl:1460:            _, key, idx = candidates[i]
    [!] ./src/PhagyMode.jl:1461:            if !haskey(keys_with_evictions, key)
    [!] ./src/PhagyMode.jl:1462:                keys_with_evictions[key] = Int[]
    [!] ./src/PhagyMode.jl:1464:            push!(keys_with_evictions[key], idx)
    [!] ./src/PhagyMode.jl:1470:            for (key, indices) in keys_with_evictions
    [!] ./src/PhagyMode.jl:1471:                bucket = get(observer_store.table, key, nothing)
    [!] ./src/PhagyMode.jl:1475:                    # Would empty the bucket. Skip this key entirely.
    [!] ./src/PhagyMode.jl:1488:                # GRUG: If bucket is now empty, remove the key entirely.
    [!] ./src/PhagyMode.jl:1489:                isempty(bucket) && delete!(observer_store.table, key)
    [!] ./src/PhagyMode.jl:1551:        valid_scopes  = Set(["bind", "promote", "expand", "tokenize", "postbind"])
    [!] ./src/PhagyMode.jl:1770:            alive_count = count(nid -> haskey(node_map, nid) && !node_map[nid].is_grave, rec.node_ids)
    [!] ./src/PhagyMode.jl:1789:                haskey(node_map, nid) || continue
    [!] ./src/PhagyMode.jl:1804:            haskey(node_map, nid) || return  # could have been removed
    [!] ./src/PhagyMode.jl:1934:                if haskey(node_map, node_id) && node_map[node_id].is_grave
    [!] ./src/PhagyMode.jl:1946:                haskey(node_map, node_id) && node_map[node_id].is_grave
    [!] ./src/PhagyMode.jl:1954:                    if haskey(node_map, pid) && node_map[pid].is_grave
    [!] ./src/PhagyMode.jl:1970:            if haskey(attachment_map, node_id)
    [!] ./src/PhagyMode.jl:1974:                    if haskey(attachment_map, pid)
    [!] ./src/PhagyMode.jl:1976:                        # GRUG: If partner's bridge list is now empty, remove the key
    [!] ./src/PhagyMode.jl:1992:        has_key = lock(attachment_lock) do; haskey(attachment_map, node_id); end
    [!] ./src/PhagyMode.jl:1993:        has_key || continue
    [!] ./src/PhagyMode.jl:1999:                if haskey(attachment_map, gpid)
    [!] ./src/PhagyMode.jl:2007:            # GRUG: If this node's bridge list is now empty, remove the key
    [!] ./src/PhagyMode.jl:2062:                    if haskey(node_map, mid)
    [!] ./src/PhagyMode.jl:2088:            haskey(group_map, gid) || return
    [!] ./src/PhagyMode.jl:2107:            haskey(group_map, gid) || return
    [!] ./src/PhagyMode.jl:2392:                if !haskey(lobe_registry, connected_id)
    [!] ./src/PhagyMode.jl:2420:# that never fire. Remove them from the hash table and key index.
    [!] ./src/PhagyMode.jl:2430:RuleHashTable, cleaning both the rules dict and the key_index. Max
    [!] ./src/PhagyMode.jl:2460:                if haskey(rule_table.rules, rid)
    [!] ./src/PhagyMode.jl:2462:                    # Clean key_index
    [!] ./src/PhagyMode.jl:2463:                    if !isempty(rule.key) && haskey(rule_table.key_index, rule.key)
    [!] ./src/PhagyMode.jl:2464:                        filter!(id -> id != rid, rule_table.key_index[rule.key])
    [!] ./src/PhagyMode.jl:2465:                        if isempty(rule_table.key_index[rule.key])
    [!] ./src/PhagyMode.jl:2466:                            delete!(rule_table.key_index, rule.key)
    [!] ./src/PhagyMode.jl:2515:                if !haskey(node_map, ref_id)
    [!] ./src/PhagyMode.jl:2524:                if !haskey(node_map, ref_id)
    [!] ./src/PhagyMode.jl:2897:#   evicts OBSERVER_PRUNE_EVICT_COUNT (8) oldest micrologs, then prunes empty keys
    [!] ./src/PhagyMode.jl:2898:#   to maintain OBSERVER_PRUNE_FILL_RATIO (0.85). Never drops below 1 entry per key.
    [!] ./src/PhagyMode.jl:2925:#   from RuleHashTable, cleans key_index. Max 10 per cycle.
    [!] ./src/ArithmeticEngine.jl:49:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/ArithmeticEngine.jl:53:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/ArithmeticEngine.jl:57:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/engine.jl:6:# tokens (e.g. "&n", "&word", "&noun", or specimen-defined macros). Relational
    [!] ./src/engine.jl:31:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/engine.jl:35:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/engine.jl:39:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/engine.jl:80:# GRUG v7.21b-2: TonalJudge — token bag + common-sense judge that picks
    [!] ./src/engine.jl:501:gives you the user's raw token for that capture, and `.raw_position`
    [!] ./src/engine.jl:502:indexes into the raw token stream.
    [!] ./src/engine.jl:543:    tokens = split(lowercase(strip(text)))
    [!] ./src/engine.jl:544:    if isempty(tokens)
    [!] ./src/engine.jl:549:    for tok in tokens
    [!] ./src/engine.jl:601:# Grug look only at rocks right next to the verb (tokens[i-1], tokens[i+1]).
    [!] ./src/engine.jl:609:    # This runs on token boundaries so partial words are never corrupted.
    [!] ./src/engine.jl:613:    tokens = split(lowercase(clean_input))
    [!] ./src/engine.jl:615:    if isempty(tokens)
    [!] ./src/engine.jl:616:        error("!!! FATAL: Grug found no tokens after split. Something wrong with input! !!!")
    [!] ./src/engine.jl:629:    for i in 1:length(tokens)
    [!] ./src/engine.jl:630:        tok = String(tokens[i])
    [!] ./src/engine.jl:633:            if i + 3 <= length(tokens) && String(tokens[i+1]) == "me" && String(tokens[i+2]) == "about"
    [!] ./src/engine.jl:634:                obj = String(tokens[i+3])
    [!] ./src/engine.jl:636:                obj_stemmed = Thesaurus.stem_token(obj)
    [!] ./src/engine.jl:638:            elseif i + 2 <= length(tokens) && String(tokens[i+1]) == "about"
    [!] ./src/engine.jl:639:                obj = String(tokens[i+2])
    [!] ./src/engine.jl:640:                obj_stemmed = Thesaurus.stem_token(obj)
    [!] ./src/engine.jl:643:        elseif tok == "describe" && i + 1 <= length(tokens)
    [!] ./src/engine.jl:644:            obj = String(tokens[i+1])
    [!] ./src/engine.jl:645:            obj_stemmed = Thesaurus.stem_token(obj)
    [!] ./src/engine.jl:647:        elseif tok == "about" && i > 1 && i < length(tokens) && String(tokens[i-1]) == "what"
    [!] ./src/engine.jl:648:            obj = String(tokens[i+1])
    [!] ./src/engine.jl:649:            obj_stemmed = Thesaurus.stem_token(obj)
    [!] ./src/engine.jl:655:    if length(tokens) < 3
    [!] ./src/engine.jl:660:        for (i, tok) in enumerate(tokens)
    [!] ./src/engine.jl:663:                if i > 1 && i < length(tokens)
    [!] ./src/engine.jl:664:                    subj = String(tokens[i-1])
    [!] ./src/engine.jl:665:                    obj  = String(tokens[i+1])
    [!] ./src/engine.jl:672:                        subj_stemmed = Thesaurus.stem_token(subj)
    [!] ./src/engine.jl:673:                        obj_stemmed  = Thesaurus.stem_token(obj)
    [!] ./src/engine.jl:804:  - Captures compound subjects/objects across multiple tokens
    [!] ./src/engine.jl:833:    tokens = split(lowercase(clean_input))
    [!] ./src/engine.jl:835:    if isempty(tokens)
    [!] ./src/engine.jl:836:        error("!!! FATAL: Grug found no tokens after split. Something wrong with input! !!!")
    [!] ./src/engine.jl:839:    if length(tokens) < 3
    [!] ./src/engine.jl:849:        while i <= length(tokens)
    [!] ./src/engine.jl:850:            tok = tokens[i]
    [!] ./src/engine.jl:857:                    candidate = String(tokens[j])
    [!] ./src/engine.jl:870:                while j <= length(tokens)
    [!] ./src/engine.jl:871:                    candidate = String(tokens[j])
    [!] ./src/engine.jl:885:                subj_stemmed = join([Thesaurus.stem_token(String(p)) for p in subj_parts], " ")
    [!] ./src/engine.jl:886:                obj_stemmed_parts = [Thesaurus.stem_token(String(p)) for p in obj_parts]
    [!] ./src/engine.jl:902:                                    nested_obj = join([Thesaurus.stem_token(String(p)) for p in nested_obj_parts], " ")
    [!] ./src/engine.jl:907:                                        clause_subj = join([Thesaurus.stem_token(String(p)) for p in clause_subj_parts], " ")
    [!] ./src/engine.jl:919:                # Skip tokens we've already processed
    [!] ./src/engine.jl:1027:                    # Try the sigil name itself as a weight key (e.g. "&causes")
    [!] ./src/engine.jl:1131:# Below this threshold, the map is too small for token overlap similarity to be
    [!] ./src/engine.jl:1143:# REMINDER: hopfield_key is DEAD FIELD (specimen compat only). ANTIMATCH REMOVED. SIGILS IN TRIPLES.
    [!] ./src/engine.jl:1181:    # GRUG NEW: Hopfield cache key (hash of pattern, used for familiar input lookup)
    [!] ./src/engine.jl:1182:    hopfield_key::UInt64
    [!] ./src/engine.jl:1322:Used as the key for Hopfield cache lookups.
    [!] ./src/engine.jl:1342:        if hit_count >= HOPFIELD_HIT_COUNT_MIN && haskey(HOPFIELD_CACHE, input_hash)
    [!] ./src/engine.jl:1919:  - Must be pattern-similar (token overlap > 0)
    [!] ./src/engine.jl:1934:            # GRUG: Compute rough token similarity between patterns
    [!] ./src/engine.jl:1935:            sim = _token_overlap_similarity(new_node.pattern, candidate.pattern)
    [!] ./src/engine.jl:1953:_token_overlap_similarity(p1::String, p2::String)::Float64
    [!] ./src/engine.jl:1955:GRUG: Internal Jaccard-like token overlap similarity [0.0, 1.0].
    [!] ./src/engine.jl:1958:function _token_overlap_similarity(p1::String, p2::String)::Float64
    [!] ./src/engine.jl:2011:    seam_tokens::Vector{String}  # GRUG: Tokens at the match boundary — the seam where one lobe's
    [!] ./src/engine.jl:2013:                              #       unmatched tokens from the source lobe's scan, not a fabricated
    [!] ./src/engine.jl:2016:                             #       Formula: token_overlap_similarity(node_A.pattern, node_B.pattern)
    [!] ./src/engine.jl:2032:# Backwards-compat constructor: old 4-arg attach sites still work (seam_tokens
    [!] ./src/engine.jl:2034:CascadeBridge(partner_id::String, seam_tokens::Vector{String}, base_confidence::Float64) =
    [!] ./src/engine.jl:2035:    CascadeBridge(partner_id, seam_tokens, base_confidence, "", false, :none)
    [!] ./src/engine.jl:2078:# REMINDER: hopfield_key is DEAD FIELD (specimen compat only). ANTIMATCH REMOVED. SIGILS IN TRIPLES.
    [!] ./src/engine.jl:2104:    inhibition_tokens::Set{String}   # GRUG BUG-010b: Semantic "don't do" set — tokens from alive
    [!] ./src/engine.jl:2109:    inhibition_dirty::Bool           # GRUG BUG-010b: True when inhibition_tokens is stale and needs
    [!] ./src/engine.jl:2111:                                     #       Cleared by refresh_inhibition_tokens!().
    [!] ./src/engine.jl:2122:# is_time_node_group, is_chatter_eligible, inhibition_tokens, inhibition_dirty).
    [!] ./src/engine.jl:2288:        if !isnothing(existing) && haskey(GROUP_MAP, existing)
    [!] ./src/engine.jl:2377:        # GRUG BUG-010b: Inhibition tokens are now stale (new member added).
    [!] ./src/engine.jl:2398:        if !haskey(GROUP_MAP, gid)
    [!] ./src/engine.jl:2412:        # GRUG BUG-010b: Inhibition tokens are stale (member graved).
    [!] ./src/engine.jl:2558:# Active inhibition tokens are stored per node type in GROUP_INHIBITION_BY_TYPE:
    [!] ./src/engine.jl:2564:# group.inhibition_tokens remains a legacy union/debug view for old specimens.
    [!] ./src/engine.jl:2577:function _group_inhibition_tokens(group::NodeGroup, node_type::Symbol)::Set{String}
    [!] ./src/engine.jl:2586:    refresh_inhibition_tokens!(group::NodeGroup; node_map, node_lock, thesaurus_fn=nothing)
    [!] ./src/engine.jl:2588:GRUG BUG-010b: Rebuild the group's inhibition_tokens set from alive members'
    [!] ./src/engine.jl:2595:function refresh_inhibition_tokens!(
    [!] ./src/engine.jl:2601:    typed_tokens = Dict{Symbol, Set{String}}(:voter => Set{String}(), :time => Set{String}())
    [!] ./src/engine.jl:2603:    function add_token!(bucket::Set{String}, tok)
    [!] ./src/engine.jl:2615:                # thesaurus lookup failure = skip expansion, token already added
    [!] ./src/engine.jl:2627:            bucket = typed_tokens[_node_inhibition_type(node)]
    [!] ./src/engine.jl:2629:            # GRUG: Original pattern tokens (pre-chatter, frozen at birth)
    [!] ./src/engine.jl:2632:                    add_token!(bucket, tok)
    [!] ./src/engine.jl:2640:                    add_token!(bucket, aname)
    [!] ./src/engine.jl:2646:    GROUP_INHIBITION_BY_TYPE[group.id] = typed_tokens
    [!] ./src/engine.jl:2647:    group.inhibition_tokens = union(values(typed_tokens)...)
    [!] ./src/engine.jl:2655:GRUG BUG-011: Check if a candidate pattern's tokens overlap too much with
    [!] ./src/engine.jl:2660:threshold=0.5 means: if ≥50% of the candidate's tokens are already in the
    [!] ./src/engine.jl:2675:            refresh_inhibition_tokens!(group; node_map=node_map, node_lock=node_lock, thesaurus_fn=thesaurus_fn)
    [!] ./src/engine.jl:2677:            @warn "[INHIBIT] refresh_inhibition_tokens! failed for group $(group.id): $e"
    [!] ./src/engine.jl:2680:    active_tokens = _group_inhibition_tokens(group, node_type)
    [!] ./src/engine.jl:2681:    if isempty(active_tokens) || isempty(pattern)
    [!] ./src/engine.jl:2685:    candidate_tokens = Set(lowercase(strip(t)) for t in split(pattern, r"\s+") if !isempty(strip(t)))
    [!] ./src/engine.jl:2686:    if isempty(candidate_tokens)
    [!] ./src/engine.jl:2690:    overlap = length(intersect(candidate_tokens, active_tokens))
    [!] ./src/engine.jl:2691:    ratio = Float64(overlap) / Float64(length(candidate_tokens))
    [!] ./src/engine.jl:2723:  - Group centroid must have some token overlap with the new node's pattern
    [!] ./src/engine.jl:2746:            sim = _token_overlap_similarity(pattern, grp.centroid_pattern)
    [!] ./src/engine.jl:2779:  1. Pattern similarity: Jaccard token overlap > MITOSIS_PATTERN_SIM_FLOOR (0.15)
    [!] ./src/engine.jl:2806:    for macro_token in ACTION_PIPE_MACROS
    [!] ./src/engine.jl:2807:        out = replace(out, macro_token => "|")
    [!] ./src/engine.jl:2833:            # GRUG: Threshold 1 — Pattern similarity (Jaccard token overlap)
    [!] ./src/engine.jl:2834:            pat_sim = _token_overlap_similarity(new_node.pattern, member.pattern)
    [!] ./src/engine.jl:2883:# path. Instead of Jaccard token overlap on group centroids, it uses the full
    [!] ./src/engine.jl:3048:            # GRUG BUG-010b: If this group's inhibition tokens already cover the
    [!] ./src/engine.jl:3074:                                # Fall back to token overlap as a cheaper check.
    [!] ./src/engine.jl:3075:                                scan_conf = _token_overlap_similarity(canonical_pattern, node.pattern)
    [!] ./src/engine.jl:3086:                                scan_conf = _token_overlap_similarity(canonical_pattern, node.pattern)
    [!] ./src/engine.jl:3089:                            # GRUG: Signal too short for high_res_scan. Use token overlap.
    [!] ./src/engine.jl:3090:                            scan_conf = _token_overlap_similarity(canonical_pattern, node.pattern)
    [!] ./src/engine.jl:3093:                        scan_conf = _token_overlap_similarity(canonical_pattern, node.pattern)
    [!] ./src/engine.jl:3096:                    # GRUG: Signal computation failed. Use token overlap fallback.
    [!] ./src/engine.jl:3097:                    scan_conf = _token_overlap_similarity(canonical_pattern, node.pattern)
    [!] ./src/engine.jl:3100:                # GRUG: No signal function available. Use token overlap.
    [!] ./src/engine.jl:3101:                scan_conf = _token_overlap_similarity(canonical_pattern, node.pattern)
    [!] ./src/engine.jl:3155:            # GRUG: Near-synonyms expand reach beyond exact token match.
    [!] ./src/engine.jl:3160:                    pat_tokens = split(lowercase(strip(canonical_pattern)))
    [!] ./src/engine.jl:3161:                    node_tokens = split(lowercase(strip(node.pattern)))
    [!] ./src/engine.jl:3163:                    for pt in pat_tokens
    [!] ./src/engine.jl:3164:                        for nt in node_tokens
    [!] ./src/engine.jl:3280:#   CHEAP_SCAN_THRESHOLD  — bidirectional scan for short signals (≤3 tokens).
    [!] ./src/engine.jl:3296:# BUG-004 PENALTY: When pattern tokens > input tokens, the cheap bidirectional
    [!] ./src/engine.jl:3298:# is subtracted from token_conf to downrank these matches vs properly-sized ones.
    [!] ./src/engine.jl:3309:# Used by the literal-token pre-gate in fire_one() to distinguish "shared
    [!] ./src/engine.jl:3311:# that overlaps the input only on tokens like `the`, `for`, `a` is not a real
    [!] ./src/engine.jl:3328:    # "what" counts as a content token in the literal pre-gate, giving
    [!] ./src/engine.jl:3336:    bridge_nodes!(node_a::String, node_b::String; seam_tokens::Vector{String}=String[])::String
    [!] ./src/engine.jl:3341:No more fabricated connector pattern — the ACTUAL unmatched tokens at the seam
    [!] ./src/engine.jl:3345:  base_confidence_AB = token_overlap(node_A.pattern, node_B.pattern)
    [!] ./src/engine.jl:3346:  base_confidence_BA = token_overlap(node_A.pattern, node_B.pattern)
    [!] ./src/engine.jl:3352:SEAM TOKENS: The tokens at the match boundary where the source lobe's scanner
    [!] ./src/engine.jl:3354:processes these tokens directly. If empty at bridge time, they'll be populated
    [!] ./src/engine.jl:3356:commands, seam_tokens come from the user's pattern argument (backward compat).
    [!] ./src/engine.jl:3369:_safe_lobe_registry_has(lobe_id::String) = isdefined(@__MODULE__, :Lobe) && haskey(Lobe.LOBE_REGISTRY, lobe_id)
    [!] ./src/engine.jl:3372:                       seam_tokens::Vector{String}=String[])::String
    [!] ./src/engine.jl:3387:        if !haskey(NODE_MAP, node_a)
    [!] ./src/engine.jl:3390:        if !haskey(NODE_MAP, node_b)
    [!] ./src/engine.jl:3408:    # Bridge confidence is purely based on token pattern overlap between
    [!] ./src/engine.jl:3413:        return _token_overlap_similarity(na.pattern, nb.pattern)
    [!] ./src/engine.jl:3421:    # Now bridge confidence is purely based on token pattern overlap.
    [!] ./src/engine.jl:3451:        bridge_a_to_b = CascadeBridge(node_b, seam_tokens, jit_conf_a_to_b, lobe_b, false, :none)
    [!] ./src/engine.jl:3452:        bridge_b_to_a = CascadeBridge(node_a, seam_tokens, jit_conf_b_to_a, lobe_a, false, :none)
    [!] ./src/engine.jl:3463:    seam_preview = isempty(seam_tokens) ? "(seam tokens populated at fire time)" : "\"$(join(seam_tokens, " "))\""
    [!] ./src/engine.jl:3470:# bidirectional bridge. The pattern becomes seam_tokens (split on whitespace).
    [!] ./src/engine.jl:3476:    return bridge_nodes!(target_id, attach_id; seam_tokens=seam)
    [!] ./src/engine.jl:3499:        if haskey(BRIDGE_MAP, node_a)
    [!] ./src/engine.jl:3513:        if haskey(BRIDGE_MAP, node_b)
    [!] ./src/engine.jl:3566:the tokens the scanner DIDN'T match. This is the match-boundary cutoff, and
    [!] ./src/engine.jl:3568:middle-man. The receiving lobe's scanner processes these tokens directly.
    [!] ./src/engine.jl:3581:MATCH-BOUNDARY HANDOFF: The unmatched_tail parameter carries the tokens that
    [!] ./src/engine.jl:3582:the source lobe's scanner couldn't match. These are the seam tokens — the
    [!] ./src/engine.jl:3591:         seam_text is the join of seam_tokens for generative context.
    [!] ./src/engine.jl:3639:            # unmatched tail carries provenance (which lobe, which node, seam tokens)
    [!] ./src/engine.jl:3681:            # GRUG: MATCH-BOUNDARY HANDOFF! The seam tokens are the bridge payload.
    [!] ./src/engine.jl:3683:            # tokens are the seam. If the bridge already has seam_tokens from
    [!] ./src/engine.jl:3686:            actual_seam = isempty(unmatched_tail) ? br.seam_tokens : unmatched_tail
    [!] ./src/engine.jl:3690:            # This is the key diagnostic — you can see exactly which lobe sent
    [!] ./src/engine.jl:3709:# Without unmatched_tail, uses the bridge's stored seam_tokens.
    [!] ./src/engine.jl:3718:Shows bidirectional bridges with provenance, seam tokens, and crystalize status.
    [!] ./src/engine.jl:3740:                if !haskey(partner_status_map, br.partner_id)
    [!] ./src/engine.jl:3755:            seam_preview = isempty(br.seam_tokens) ? "(dynamic)" : "\"$(join(br.seam_tokens[1:min(5, length(br.seam_tokens))], " "))$(length(br.seam_tokens) > 5 ? "..." : "")\""
    [!] ./src/engine.jl:4002:                if !haskey(node_data, nid)
    [!] ./src/engine.jl:4110:This is the image-domain equivalent of _token_overlap_similarity for text.
    [!] ./src/engine.jl:4190:        if !haskey(NODE_MAP, target_id)
    [!] ./src/engine.jl:4193:        if !haskey(NODE_MAP, attach_id)
    [!] ./src/engine.jl:4240:    # GRUG: Seam tokens for image bridges use SDF metadata as the seam marker
    [!] ./src/engine.jl:4344:    req_rels = haskey(data, "required_relations") ? convert(Vector{String}, data["required_relations"]) : String[]
    [!] ./src/engine.jl:4345:    rel_wts  = haskey(data, "relation_weights")   ? convert(Dict{String, Float64}, data["relation_weights"]) : Dict{String, Float64}()
    [!] ./src/engine.jl:4351:    # has only 2 tokens (no verb-flanked triple available from the pattern),
    [!] ./src/engine.jl:4355:    if haskey(data, "aux_triples") && isa(data["aux_triples"], AbstractVector)
    [!] ./src/engine.jl:4377:    # GRUG: Compute Hopfield key from pattern for fast familiar-input lookup
    [!] ./src/engine.jl:4378:    hopfield_key = is_image_node ? UInt64(0) : hash(join(split(lowercase(strip(pattern))), " "))
    [!] ./src/engine.jl:4398:        hopfield_key,       # hopfield_key
    [!] ./src/engine.jl:4413:    # Only for text nodes (image nodes use SDF similarity, not token overlap).
    [!] ./src/engine.jl:4415:    # Below NODE_LATCH_THRESHOLD, token overlap similarity is not statistically
    [!] ./src/engine.jl:4635:                    error("!!! FATAL: Grug found empty action name token in packet! Packet: '$packet' !!!")
    [!] ./src/engine.jl:4721:    if !haskey(COMMANDS, winning_action) 
    [!] ./src/engine.jl:4771:    if !haskey(COMMANDS, winning_action) 
    [!] ./src/engine.jl:4812:    if !haskey(COMMANDS, winning_action)
    [!] ./src/engine.jl:5020:        haskey(COMMANDS, action_name) && continue
    [!] ./src/engine.jl:5028:            valid_actions = sort(collect(keys(COMMANDS)))
    [!] ./src/engine.jl:5053:                            (BOTH keys accepted; `data` is the new canonical
    [!] ./src/engine.jl:5082:    nodes_arr = if haskey(packet, "nodes")
    [!] ./src/engine.jl:5084:    elseif haskey(packet, "pattern") && haskey(packet, "action_packet")
    [!] ./src/engine.jl:5095:        # GRUG QoL-2025: Accept both `data` and `json_data` keys for the
    [!] ./src/engine.jl:5100:        raw_data = if haskey(n, "data") && haskey(n, "json_data")
    [!] ./src/engine.jl:5103:        elseif haskey(n, "data")
    [!] ./src/engine.jl:5105:        elseif haskey(n, "json_data")
    [!] ./src/engine.jl:5116:        if !haskey(json_data, "system_prompt") || isempty(strip(string(get(json_data, "system_prompt", ""))))
    [!] ./src/engine.jl:5126:        if isempty(_sp_body) && !haskey(json_data, "noun_anchors")
    [!] ./src/engine.jl:5127:            @warn """⚠️  COHERENCE WARNING: Node with pattern \"$(haskey(n, "pattern") ? n["pattern"] : "?")\" has single-sentence system_prompt and NO noun_anchors!
    [!] ./src/engine.jl:5132:        if !haskey(json_data, "voice_register")
    [!] ./src/engine.jl:5133:            @warn """⚠️  COHERENCE WARNING: Node with pattern \"$(haskey(n, "pattern") ? n["pattern"] : "?")\" has NO voice_register!
    [!] ./src/engine.jl:5138:        if !haskey(json_data, "frame_hints")
    [!] ./src/engine.jl:5139:            @warn """⚠️  COHERENCE WARNING: Node with pattern \"$(haskey(n, "pattern") ? n["pattern"] : "?")\" has NO frame_hints!
    [!] ./src/engine.jl:5146:        drop_table   = haskey(n, "drop_table") && (n["drop_table"] isa AbstractVector) ?
    [!] ./src/engine.jl:5149:        is_img_node  = haskey(n, "is_image_node") && n["is_image_node"] === true
    [!] ./src/engine.jl:5154:        is_am_node   = haskey(n, "is_antimatch_node") && n["is_antimatch_node"] === true
    [!] ./src/engine.jl:5175:        if haskey(j, "initial_strength")
    [!] ./src/engine.jl:5190:                if haskey(Lobe.LOBE_REGISTRY, target_lobe)
    [!] ./src/engine.jl:5259:# removed alongside it (see Main.jl). Old specimens with a "rules" key still
    [!] ./src/engine.jl:5260:# load fine -- the key is recognized and silently skipped, same backward-compat
    [!] ./src/engine.jl:5261:# pattern used for the removed AIMLNodeSystem's "aiml_system" key.
    [!] ./src/engine.jl:5289:# High-confidence scan results are stored in HOPFIELD_CACHE keyed by input hash.
    [!] ./src/engine.jl:5319:# word boundaries without corrupting partial tokens. Load-time snapshot consts
    [!] ./src/engine.jl:5351:# Sigil tokens (&n, &op, etc.) are not in the dict → default 1.0.
    [!] ./src/engine.jl:5439:# Overlap is the sum of coefficients for shared tokens, not the count.
    [!] ./src/engine.jl:5447:# input and pattern tokens with their stemmed forms via Thesaurus.stem_token.
    [!] ./src/engine.jl:5453:# IMPORTANT: Stemmed forms inherit the coefficient of their ORIGINAL token.
    [!] ./src/engine.jl:5457:    # GRUG v8.23: Tokenize ALL tokens (no stopword filtering), then expand
    [!] ./src/engine.jl:5458:    # with stemmed forms. Each token gets BOTH its original form and its stem.
    [!] ./src/engine.jl:5459:    # Coefficients handle what stopwords used to — no need to delete tokens.
    [!] ./src/engine.jl:5460:    input_tokens_raw = split(lowercase(strip(input_text)))
    [!] ./src/engine.jl:5461:    input_tokens = Thesaurus.normalize_tokens(input_tokens_raw)
    [!] ./src/engine.jl:5463:    node_tokens_raw = split(lowercase(strip(node.pattern)))
    [!] ./src/engine.jl:5464:    node_tokens = Thesaurus.normalize_tokens(node_tokens_raw)
    [!] ./src/engine.jl:5466:    if isempty(input_tokens) || isempty(node_tokens)
    [!] ./src/engine.jl:5469:    # GRUG v8.23: Build coefficient maps for expanded token sets.
    [!] ./src/engine.jl:5470:    # Each expanded token inherits the coefficient of its original form.
    [!] ./src/engine.jl:5471:    # input_tokens = ["the", "fire", "fire"] (from "the fire" → stem "fire" added)
    [!] ./src/engine.jl:5473:    # For coverage, we sum coefficients of ALL expanded tokens, but each
    [!] ./src/engine.jl:5474:    # unique token's coefficient is counted only once per side.
    [!] ./src/engine.jl:5475:    function _build_coeff_map(tokens_raw, tokens_expanded)
    [!] ./src/engine.jl:5477:        # Pre-build stem→coeff map from raw tokens (avoids repeated stem_token calls)
    [!] ./src/engine.jl:5479:        for raw in tokens_raw
    [!] ./src/engine.jl:5480:            stem = Thesaurus.stem_token(String(raw))
    [!] ./src/engine.jl:5482:            # If multiple raw tokens stem to same form, take max coefficient
    [!] ./src/engine.jl:5483:            if !haskey(stem_coeff, stem) || c > stem_coeff[stem]
    [!] ./src/engine.jl:5487:        for t in tokens_expanded
    [!] ./src/engine.jl:5488:            if !haskey(coeff_map, t)
    [!] ./src/engine.jl:5489:                if haskey(stem_coeff, t)
    [!] ./src/engine.jl:5498:    input_coeff = _build_coeff_map(input_tokens_raw, input_tokens)
    [!] ./src/engine.jl:5499:    node_coeff = _build_coeff_map(node_tokens_raw, node_tokens)
    [!] ./src/engine.jl:5500:    # GRUG v8.23: Weighted overlap = sum of coefficients of shared tokens.
    [!] ./src/engine.jl:5503:    overlap_set = intersect(input_tokens, node_tokens)
    [!] ./src/engine.jl:5508:    # NEW: input_weight = sum of coefficients for all unique tokens on this side.
    [!] ./src/engine.jl:5509:    # "the fire is hot" → tokens {the, fire, is, hot} → weights 0.05+1.0+0.05+1.0 = 2.10
    [!] ./src/engine.jl:5510:    # "fire burn" → tokens {fire, burn} → weights 1.0+1.0 = 2.0
    [!] ./src/engine.jl:5566:    # versions ensures expansion tokens never HURT a match that would succeed
    [!] ./src/engine.jl:5583:    # promoted input replaces numbers with sigil tokens, breaking the match.
    [!] ./src/engine.jl:5718:        # Fix: count non-sigil content tokens in the pattern and add a small
    [!] ./src/engine.jl:5719:        # bonus per extra content token. This naturally favors longer, more
    [!] ./src/engine.jl:5724:            _specificity_bonus = length(_content_toks) * 0.04   # 4% per content token
    [!] ./src/engine.jl:5730:        # keywords (before, after, during, since, until, when, while, precedes,
    [!] ./src/engine.jl:5739:            _temporal_keywords = ["before", "after", "during", "since", "until", "when", "while", "precedes", "follows", "then", "now", "came before", "came after", "next", "previous", "past", "future", "present"]
    [!] ./src/engine.jl:5740:            _temporal_hits = count(kw -> occursin(kw, _input_lower), _temporal_keywords)
    [!] ./src/engine.jl:5742:                _temporal_bonus = min(0.25, _temporal_hits * 0.12)  # 12% per keyword hit, cap 25%
    [!] ./src/engine.jl:5802:            # which gives thermodynamics nodes spurious lexical overlap via thesaurus-added tokens
    [!] ./src/engine.jl:5854:    # clean sigil bindings, then append expansion tokens as raw literals
    [!] ./src/engine.jl:5863:    # GRUG v8.19: DO NOT append thesaurus expansion tokens to the promoted
    [!] ./src/engine.jl:5864:    # text. Expansion tokens inflate input_content_count in lexical overlap,
    [!] ./src/engine.jl:5868:    # tokens make input_content = {&query, &definition, &n, &op, constitutes,
    [!] ./src/engine.jl:5869:    # equals, blaze} = 7 tokens. Overlap with "&n &op &n" = {&n, &op} = 2.
    [!] ./src/engine.jl:5871:    # 0.446, below the 0.50 gate. The expansion tokens are already in the
    [!] ./src/engine.jl:5874:    # text must stay CLEAN (only sigil-promoted tokens) for lexical overlap
    [!] ./src/engine.jl:5895:    # This is the key fix for multipart decoherence: each sub-scan's
    [!] ./src/ImmuneThreadPool.jl:51:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/ImmuneThreadPool.jl:55:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/ImmuneThreadPool.jl:59:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/ImmuneThreadPool.jl:172:- source_id: Unique identifier (user ID, API key hash, IP hash, etc.)
    [!] ./src/ImmuneThreadPool.jl:190:    :api       => 5.0,    # 5 immune scans/sec per API key
    [!] ./src/ImmuneThreadPool.jl:196:# GRUG: Maximum burst tokens per source type. Allows brief bursts.
    [!] ./src/ImmuneThreadPool.jl:379:    tokens::Float64
    [!] ./src/ImmuneThreadPool.jl:380:    rate::Float64         # tokens per second
    [!] ./src/ImmuneThreadPool.jl:381:    burst::Int            # max tokens
    [!] ./src/ImmuneThreadPool.jl:399:GRUG: Try to consume tokens from bucket. Returns true if successful.
    [!] ./src/ImmuneThreadPool.jl:400:Refills tokens based on elapsed time since last call.
    [!] ./src/ImmuneThreadPool.jl:408:        # GRUG: Refill tokens based on elapsed time
    [!] ./src/ImmuneThreadPool.jl:409:        bucket.tokens = min(Float64(bucket.burst), bucket.tokens + elapsed * bucket.rate)
    [!] ./src/ImmuneThreadPool.jl:412:        if bucket.tokens >= Float64(cost)
    [!] ./src/ImmuneThreadPool.jl:413:            bucket.tokens -= Float64(cost)
    [!] ./src/ImmuneThreadPool.jl:421:    time_to_next_token(bucket::TokenBucket) -> Float64
    [!] ./src/ImmuneThreadPool.jl:423:GRUG: How long until at least one token is available (seconds).
    [!] ./src/ImmuneThreadPool.jl:425:function time_to_next_token(bucket::TokenBucket)::Float64
    [!] ./src/ImmuneThreadPool.jl:427:        if bucket.tokens >= 1.0
    [!] ./src/ImmuneThreadPool.jl:430:        return (1.0 - bucket.tokens) / bucket.rate
    [!] ./src/ImmuneThreadPool.jl:441:        bucket.tokens = Float64(bucket.burst)
    [!] ./src/ImmuneThreadPool.jl:845:# RATE LIMITER — Per-source token bucket management
    [!] ./src/ImmuneThreadPool.jl:873:GRUG: Get or create a token bucket for a source.
    [!] ./src/ImmuneThreadPool.jl:889:        if !haskey(rl.buckets, source)
    [!] ./src/ImmuneThreadPool.jl:909:GRUG: Try to consume rate limit tokens for a source.
    [!] ./src/ImmuneThreadPool.jl:933:    return round(Int, time_to_next_token(bucket) * 1000)
    [!] ./src/ImmuneThreadPool.jl:1673:# 4. **Per-Source Rate Limiting**: Each source (identified by SourceID) has a token
    [!] ./src/ImmuneThreadPool.jl:1707:#   rate limit per source: token bucket with type-specific rate/burst
    [!] ./src/ImmuneThreadPool.jl:1711:#   rate limit signal: ImmuneRateLimitExhaustedError when token bucket empty
    [!] ./src/ImmuneSystem.jl:42:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/ImmuneSystem.jl:46:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/ImmuneSystem.jl:50:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/ImmuneSystem.jl:102:# GRUG: Minimum AST token count for a scan to be meaningful.
    [!] ./src/ImmuneSystem.jl:103:# Fewer tokens than this = not enough structure to judge.
    [!] ./src/ImmuneSystem.jl:253:# "AST" here means structural analysis of the input — token structure, length
    [!] ./src/ImmuneSystem.jl:262:Analyzes token structure, character class distribution, length ratios,
    [!] ./src/ImmuneSystem.jl:272:    tokens = split(strip(input_text))
    [!] ./src/ImmuneSystem.jl:273:    n_tokens = length(tokens)
    [!] ./src/ImmuneSystem.jl:275:    if n_tokens < MIN_AST_TOKENS
    [!] ./src/ImmuneSystem.jl:276:        error("!!! FATAL: Input has $n_tokens tokens, need at least $MIN_AST_TOKENS for AST scan! !!!")
    [!] ./src/ImmuneSystem.jl:281:    token_bucket = div(n_tokens, 5) * 5  # bucket by 5s
    [!] ./src/ImmuneSystem.jl:283:    # 2. Average token length bucket
    [!] ./src/ImmuneSystem.jl:284:    avg_len = sum(length.(tokens)) / n_tokens
    [!] ./src/ImmuneSystem.jl:288:    text_flat = join(tokens, " ")
    [!] ./src/ImmuneSystem.jl:297:    # 4. First token class (command shape detector)
    [!] ./src/ImmuneSystem.jl:298:    first_token = lowercase(tokens[1])
    [!] ./src/ImmuneSystem.jl:299:    first_class = if startswith(first_token, "{") || startswith(first_token, "[")
    [!] ./src/ImmuneSystem.jl:301:    elseif startswith(first_token, "/")
    [!] ./src/ImmuneSystem.jl:303:    elseif all(isuppercase, first_token) && length(first_token) > 1
    [!] ./src/ImmuneSystem.jl:312:    # 6. Max token length (catches abnormally long tokens — possible injection)
    [!] ./src/ImmuneSystem.jl:313:    max_token_len = maximum(length.(tokens))
    [!] ./src/ImmuneSystem.jl:314:    max_token_bucket = div(min(max_token_len, 100), 10)
    [!] ./src/ImmuneSystem.jl:318:    feature_vec = (token_bucket, avg_len_bucket, alpha_ratio, digit_ratio,
    [!] ./src/ImmuneSystem.jl:319:                   punct_ratio, first_class, has_json, max_token_bucket)
    [!] ./src/ImmuneSystem.jl:399:    tokens = split(text)
    [!] ./src/ImmuneSystem.jl:402:    has_command_tokens = any(t -> startswith(lowercase(t), "/") ||
    [!] ./src/ImmuneSystem.jl:404:                            tokens)
    [!] ./src/ImmuneSystem.jl:407:    reasonable_length = 1 <= length(tokens) <= 500
    [!] ./src/ImmuneSystem.jl:420:    score = Int(has_command_tokens) + Int(reasonable_length) + Int(clean_encoding)
    [!] ./src/ImmuneSystem.jl:423:        log_immune_event!(:patch_success, sig, Dict("score" => score, "heuristics" => "cmd=$(has_command_tokens),len=$(reasonable_length),enc=$(clean_encoding)"))
    [!] ./src/ImmuneSystem.jl:426:        log_immune_event!(:patch_failure, sig, Dict("score" => score, "heuristics" => "cmd=$(has_command_tokens),len=$(reasonable_length),enc=$(clean_encoding)"))
    [!] ./src/ImmuneSystem.jl:689:    if haskey(data, "hopfield") && isa(data["hopfield"], AbstractDict)
    [!] ./src/ImmuneSystem.jl:698:    if haskey(data, "ledger") && isa(data["ledger"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:28:# GRUG say: key weights in this brain get JITTER SNAP-BACK. They wobble a
    [!] ./src/EphemeralMLP.jl:36:#           keyed by pattern. When a vote matches the pattern, the rule fires.
    [!] ./src/EphemeralMLP.jl:46:#           the "ephemeral_mlp" key in the specimen file. It does NOT
    [!] ./src/EphemeralMLP.jl:65:# User-extensible rules are stored in a pattern/key-activated hash table with
    [!] ./src/EphemeralMLP.jl:67:# Rules may be activated by vote pattern match OR by explicit key lookup. Drop
    [!] ./src/EphemeralMLP.jl:86:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/EphemeralMLP.jl:90:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/EphemeralMLP.jl:94:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/EphemeralMLP.jl:112:export activate_rule_by_key!, activate_rules_by_pattern!
    [!] ./src/EphemeralMLP.jl:342:# RULE HASH TABLE — pattern/key activated with drop tables
    [!] ./src/EphemeralMLP.jl:349:by pattern match on vote content OR by explicit key activation. Each rule
    [!] ./src/EphemeralMLP.jl:360:    key::String                         # explicit activation key
    [!] ./src/EphemeralMLP.jl:373:    key::String = "",
    [!] ./src/EphemeralMLP.jl:404:        id, pattern, key, w, transform_type,
    [!] ./src/EphemeralMLP.jl:412:The rule storage for the EphemeralMLP. Rules are stored in a dict keyed by
    [!] ./src/EphemeralMLP.jl:413:rule ID. A secondary index maps activation keys to rule IDs for O(1) key
    [!] ./src/EphemeralMLP.jl:419:    key_index::Dict{String, Vector{String}}     # activation key -> [rule_ids]
    [!] ./src/EphemeralMLP.jl:545:    # Total per block: 2 * HIDDEN_DIM * HIDDEN_DIM (query + key)
    [!] ./src/EphemeralMLP.jl:650:to the specimen file under the "ephemeral_mlp" key. The state is persistent
    [!] ./src/EphemeralMLP.jl:931:  K = w_qk[H*H+1:2*H*H] @ hidden  (key projection, second half)
    [!] ./src/EphemeralMLP.jl:958:    # w_qk layout: first H*H weights = query projection, second H*H = key projection
    [!] ./src/EphemeralMLP.jl:1750:        if haskey(st.rules.rules, rule.id)
    [!] ./src/EphemeralMLP.jl:1759:        # GRUG: Update key index if rule has an activation key
    [!] ./src/EphemeralMLP.jl:1760:        if !isempty(rule.key)
    [!] ./src/EphemeralMLP.jl:1761:            if !haskey(st.rules.key_index, rule.key)
    [!] ./src/EphemeralMLP.jl:1762:                st.rules.key_index[rule.key] = String[]
    [!] ./src/EphemeralMLP.jl:1764:            push!(st.rules.key_index[rule.key], rule.id)
    [!] ./src/EphemeralMLP.jl:1767:    return "📋 MLP rule '$(rule.id)' registered (pattern='$(rule.pattern)', type=$(rule.transform_type), key='$(rule.key)')"
    [!] ./src/EphemeralMLP.jl:1774:Also removes the rule from key indices and other rules' drop tables.
    [!] ./src/EphemeralMLP.jl:1780:        if !haskey(st.rules.rules, rule_id)
    [!] ./src/EphemeralMLP.jl:1786:        # GRUG: Remove from key index
    [!] ./src/EphemeralMLP.jl:1787:        if !isempty(rule.key) && haskey(st.rules.key_index, rule.key)
    [!] ./src/EphemeralMLP.jl:1788:            filter!(id -> id != rule_id, st.rules.key_index[rule.key])
    [!] ./src/EphemeralMLP.jl:1789:            if isempty(st.rules.key_index[rule.key])
    [!] ./src/EphemeralMLP.jl:1790:                delete!(st.rules.key_index, rule.key)
    [!] ./src/EphemeralMLP.jl:1832:    activate_rule_by_key!(key::String)::Vector{String}
    [!] ./src/EphemeralMLP.jl:1834:Activate all rules matching the given activation key. Returns the IDs of
    [!] ./src/EphemeralMLP.jl:1838:function activate_rule_by_key!(key::String)::Vector{String}
    [!] ./src/EphemeralMLP.jl:1842:        rule_ids = get(st.rules.key_index, key, String[])
    [!] ./src/EphemeralMLP.jl:2684:This goes under the "ephemeral_mlp" key in the specimen file. Every field
    [!] ./src/EphemeralMLP.jl:2740:                "key"           => rule.key,
    [!] ./src/EphemeralMLP.jl:2849:    if !haskey(data, "weights")
    [!] ./src/EphemeralMLP.jl:2850:        _err("from_specimen_dict! missing 'weights' key", "from_specimen_dict!")
    [!] ./src/EphemeralMLP.jl:2880:        if haskey(wd, "w_input_hidden") && isa(wd["w_input_hidden"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2888:        if haskey(wd, "b_hidden") && isa(wd["b_hidden"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2896:        if haskey(wd, "w_hidden_output") && isa(wd["w_hidden_output"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2904:        if haskey(wd, "b_output") && isa(wd["b_output"], AbstractDict)
    [!] ./src/EphemeralMLP.jl:2910:        if haskey(wd, "w_attention") && isa(wd["w_attention"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2920:        # these keys — the fresh MLPWeights() defaults are already Xavier-ish
    [!] ./src/EphemeralMLP.jl:2939:        if haskey(wd, "w_qk") && isa(wd["w_qk"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2951:        if haskey(wd, "w_v") && isa(wd["w_v"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2966:        if haskey(wd, "w_ffn1") && isa(wd["w_ffn1"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2975:        if haskey(wd, "b_ffn1") && isa(wd["b_ffn1"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2984:        if haskey(wd, "w_ffn2") && isa(wd["w_ffn2"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:2993:        if haskey(wd, "b_ffn2") && isa(wd["b_ffn2"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3005:        if haskey(wd, "w_ln_scale") && isa(wd["w_ln_scale"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3014:        if haskey(wd, "w_ln_bias") && isa(wd["w_ln_bias"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3026:        if haskey(wd, "w_output_heads") && isa(wd["w_output_heads"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3035:        if haskey(wd, "b_output_heads") && isa(wd["b_output_heads"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3049:    if haskey(data, "rules") && isa(data["rules"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3058:                key = String(get(r_data, "key", ""))
    [!] ./src/EphemeralMLP.jl:3087:                    key = key,
    [!] ./src/EphemeralMLP.jl:3099:                if !isempty(key)
    [!] ./src/EphemeralMLP.jl:3100:                    if !haskey(new_rules.key_index, key)
    [!] ./src/EphemeralMLP.jl:3101:                        new_rules.key_index[key] = String[]
    [!] ./src/EphemeralMLP.jl:3103:                    push!(new_rules.key_index[key], rule_id)
    [!] ./src/EphemeralMLP.jl:3113:    if haskey(data, "novelty_tracker") && isa(data["novelty_tracker"], AbstractDict)
    [!] ./src/EphemeralMLP.jl:3115:        if haskey(nt, "history") && isa(nt["history"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3123:        if haskey(nt, "hash_counts") && isa(nt["hash_counts"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3139:    # If the key is missing (old specimen), start with a blank tracker.
    [!] ./src/EphemeralMLP.jl:3141:    if haskey(data, "input_correlations") && isa(data["input_correlations"], AbstractDict)
    [!] ./src/EphemeralMLP.jl:3143:        if haskey(ic, "entries") && isa(ic["entries"], AbstractVector)
    [!] ./src/EphemeralMLP.jl:3163:        if haskey(ic, "input_quality_ema") && isa(ic["input_quality_ema"], AbstractVector)
    [!] ./src/AutoLinker.jl:73:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/AutoLinker.jl:77:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/AutoLinker.jl:81:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/AutoLinker.jl:152:# The user's key insight: cross-lobe connections are the most important.
    [!] ./src/AutoLinker.jl:208:that these two nodes SHOULD be bridged. The pair key is the canonical
    [!] ./src/AutoLinker.jl:263:# HELPER — canonical pair key
    [!] ./src/AutoLinker.jl:267:    _pair_key(id_a, id_b) -> String
    [!] ./src/AutoLinker.jl:269:GRUG: Canonical sorted pair key. Always "smaller::larger".
    [!] ./src/AutoLinker.jl:272:function _pair_key(id_a::String, id_b::String)::String
    [!] ./src/AutoLinker.jl:375:            if !haskey(pattern_to_id, pat_lower)
    [!] ./src/AutoLinker.jl:390:                    if haskey(pattern_to_id, syn_lower)
    [!] ./src/AutoLinker.jl:393:                                pk = _pair_key(nid, other_id)
    [!] ./src/AutoLinker.jl:461:                        pk = _pair_key(partner_ids[i], partner_ids[j])
    [!] ./src/AutoLinker.jl:485:                    if !haskey(word_to_nodes, w)
    [!] ./src/AutoLinker.jl:506:                        pk = _pair_key(na, nb)
    [!] ./src/AutoLinker.jl:603:                    if !haskey(word_to_nodes, w)
    [!] ./src/AutoLinker.jl:622:                        pk = _pair_key(na, nb)
    [!] ./src/AutoLinker.jl:675:            oldest_key = ""
    [!] ./src/AutoLinker.jl:680:                    oldest_key = k
    [!] ./src/AutoLinker.jl:683:            !isempty(oldest_key) && delete!(_LINK_EVIDENCE, oldest_key)
    [!] ./src/AutoLinker.jl:706:    key = _pair_key(id_a, id_b)
    [!] ./src/AutoLinker.jl:716:        if haskey(_LINK_EVIDENCE, key)
    [!] ./src/AutoLinker.jl:717:            rec = _LINK_EVIDENCE[key]
    [!] ./src/AutoLinker.jl:735:            _LINK_EVIDENCE[key] = rec
    [!] ./src/AutoLinker.jl:750:        for (key, rec) in _LINK_EVIDENCE
    [!] ./src/AutoLinker.jl:753:                push!(to_delete, key)
    [!] ./src/AutoLinker.jl:756:        for key in to_delete
    [!] ./src/AutoLinker.jl:757:            delete!(_LINK_EVIDENCE, key)
    [!] ./src/AutoLinker.jl:779:  - bridge_fn: (node_a, node_b; seam_tokens) -> String — bridge_nodes! wrapper
    [!] ./src/AutoLinker.jl:788:  - thesaurus_gate_filter: Function — for seam token computation
    [!] ./src/AutoLinker.jl:806:    best_key = nothing
    [!] ./src/AutoLinker.jl:814:        for (key, rec) in _LINK_EVIDENCE
    [!] ./src/AutoLinker.jl:826:                best_key = key
    [!] ./src/AutoLinker.jl:836:    if isnothing(best_key)
    [!] ./src/AutoLinker.jl:840:    # GRUG: Parse the pair key back to node IDs
    [!] ./src/AutoLinker.jl:841:    parts = split(best_key, "::")
    [!] ./src/AutoLinker.jl:854:            delete!(_LINK_EVIDENCE, best_key)
    [!] ./src/AutoLinker.jl:863:            delete!(_LINK_EVIDENCE, best_key)
    [!] ./src/AutoLinker.jl:874:            delete!(_LINK_EVIDENCE, best_key)
    [!] ./src/AutoLinker.jl:911:            delete!(_LINK_EVIDENCE, best_key)
    [!] ./src/AutoLinker.jl:920:    # GRUG: The bridge needs seam tokens. Compute shared words from
    [!] ./src/AutoLinker.jl:931:    tokens_a = Set(split(lowercase(strip(pattern_a))))
    [!] ./src/AutoLinker.jl:932:    tokens_b = Set(split(lowercase(strip(pattern_b))))
    [!] ./src/AutoLinker.jl:933:    shared = intersect(tokens_a, tokens_b)
    [!] ./src/AutoLinker.jl:935:    seam_tokens = if !isempty(shared)
    [!] ./src/AutoLinker.jl:940:        # Use first token from each + relational marker.
    [!] ./src/AutoLinker.jl:941:        fa = isempty(tokens_a) ? "X" : first(sort(collect(tokens_a)))
    [!] ./src/AutoLinker.jl:942:        fb = isempty(tokens_b) ? "Y" : first(sort(collect(tokens_b)))
    [!] ./src/AutoLinker.jl:948:        result = bridge_fn(id_a, id_b; seam_tokens = seam_tokens)
    [!] ./src/AutoLinker.jl:952:            delete!(_LINK_EVIDENCE, best_key)
    [!] ./src/AutoLinker.jl:976:            delete!(_LINK_EVIDENCE, best_key)
    [!] ./src/AutoLinker.jl:1005:    top_candidates = Tuple{Float64, String, Bool, Int}[]  # (intensity, key, cross, freq)
    [!] ./src/AutoLinker.jl:1009:        for (key, rec) in _LINK_EVIDENCE
    [!] ./src/AutoLinker.jl:1017:            push!(top_candidates, (rec.accumulated_intensity, key, rec.is_cross_lobe, rec.frequency))
    [!] ./src/AutoLinker.jl:1046:        for (intensity, key, cross, freq) in top5
    [!] ./src/AutoLinker.jl:1047:            parts = split(key, "::")
    [!] ./src/AutoLinker.jl:1106:Returns a dict of key -> (intensity, frequency, sources, is_cross_lobe, lobe_a, lobe_b)
    [!] ./src/AutoLinker.jl:1111:        for (key, rec) in _LINK_EVIDENCE
    [!] ./src/AutoLinker.jl:1112:            snap[key] = Dict{String, Any}(
    [!] ./src/AutoLinker.jl:1136:        for (key, data) in snap
    [!] ./src/AutoLinker.jl:1149:                _LINK_EVIDENCE[key] = rec
    [!] ./src/FullLobeScanner.jl:21:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/FullLobeScanner.jl:25:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/FullLobeScanner.jl:29:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/FullLobeScanner.jl:432:    all_ids = collect(keys(node_features))
    [!] ./src/FullLobeScanner.jl:591:                if !is_active(scanner.active_set, conn_id) && haskey(node_features, conn_id)
    [!] ./src/FullLobeScanner.jl:714:    _require_nodes!(Set(keys(node_features)))
    [!] ./src/CaveJournal.jl:32:#   - Have conversation → key events logged automatically
    [!] ./src/CaveJournal.jl:609:        haskey(d, "directory")   && (_directory[]   = String(d["directory"]))
    [!] ./src/CaveJournal.jl:610:        haskey(d, "filename")    && (_filename[]     = String(d["filename"]))
    [!] ./src/CaveJournal.jl:611:        haskey(d, "entry_count") && (_entry_count[]  = Int(d["entry_count"]))
    [!] ./src/stochastichelper.jl:19:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/stochastichelper.jl:23:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/stochastichelper.jl:27:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/RelationalGovernance.jl:4:# A RelationalTriple's subject / relation / object may contain sigil tokens
    [!] ./src/RelationalGovernance.jl:35:#      the system attaches them — using their shared token overlap as the
    [!] ./src/RelationalGovernance.jl:67:#   automatically instead of user-specified. If two nodes share no tokens,
    [!] ./src/RelationalGovernance.jl:68:#   the connector is the concatenation of their first tokens with "≈" as
    [!] ./src/RelationalGovernance.jl:98:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/RelationalGovernance.jl:102:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/RelationalGovernance.jl:106:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/RelationalGovernance.jl:163:# GRUG: Minimum token overlap required between two nodes before we even
    [!] ./src/RelationalGovernance.jl:164:# track their co-activation. If they share NO tokens, they're probably
    [!] ./src/RelationalGovernance.jl:217:                       token_overlap_fn = (a,b) -> 0.0)
    [!] ./src/RelationalGovernance.jl:221:IF they have enough token overlap to be meaningfully related.
    [!] ./src/RelationalGovernance.jl:223:`token_overlap_fn` should return a float [0.0, 1.0] similarity between
    [!] ./src/RelationalGovernance.jl:230:                            token_overlap_fn::Function = (a, b) -> 0.0)::Int
    [!] ./src/RelationalGovernance.jl:238:    # Sort IDs so (A,B) and (B,A) map to the same key.
    [!] ./src/RelationalGovernance.jl:242:    # GRUG v8.0 FIX: Compute token overlaps OUTSIDE CO_ACC_LOCK!
    [!] ./src/RelationalGovernance.jl:243:    # Old code called token_overlap_fn inside lock(CO_ACC_LOCK), but
    [!] ./src/RelationalGovernance.jl:244:    # token_overlap_fn acquires NODE_LOCK. That creates CO_ACC_LOCK → NODE_LOCK
    [!] ./src/RelationalGovernance.jl:254:            overlap = token_overlap_fn(id_a, id_b)
    [!] ./src/RelationalGovernance.jl:264:            key = (id_a, id_b)
    [!] ./src/RelationalGovernance.jl:265:            if haskey(CO_ACC, key)
    [!] ./src/RelationalGovernance.jl:266:                CO_ACC[key] += CO_FIRE_INCREMENT
    [!] ./src/RelationalGovernance.jl:272:                    min_key = nothing
    [!] ./src/RelationalGovernance.jl:277:                            min_key = k
    [!] ./src/RelationalGovernance.jl:280:                    if !isnothing(min_key)
    [!] ./src/RelationalGovernance.jl:281:                        delete!(CO_ACC, min_key)
    [!] ./src/RelationalGovernance.jl:284:                CO_ACC[key] = CO_FIRE_INCREMENT
    [!] ./src/RelationalGovernance.jl:310:    evict_keys = Tuple{String, String}[]
    [!] ./src/RelationalGovernance.jl:313:        for (key, val) in CO_ACC
    [!] ./src/RelationalGovernance.jl:318:                push!(evict_keys, key)
    [!] ./src/RelationalGovernance.jl:321:                CO_ACC[key] = new_val
    [!] ./src/RelationalGovernance.jl:324:        for key in evict_keys
    [!] ./src/RelationalGovernance.jl:325:            delete!(CO_ACC, key)
    [!] ./src/RelationalGovernance.jl:339:        token_overlap_fn::Function,
    [!] ./src/RelationalGovernance.jl:346:The connector pattern is computed from the shared token overlap between
    [!] ./src/RelationalGovernance.jl:354:    token_overlap_fn::Function,
    [!] ./src/RelationalGovernance.jl:359:    best_key = nothing
    [!] ./src/RelationalGovernance.jl:363:        for (key, val) in CO_ACC
    [!] ./src/RelationalGovernance.jl:365:                best_key = key
    [!] ./src/RelationalGovernance.jl:371:    if isnothing(best_key)
    [!] ./src/RelationalGovernance.jl:375:    id_a, id_b = best_key
    [!] ./src/RelationalGovernance.jl:377:    # GRUG: Compute the connector pattern from shared token overlap.
    [!] ./src/RelationalGovernance.jl:396:            delete!(CO_ACC, best_key)
    [!] ./src/RelationalGovernance.jl:401:    # GRUG: Compute shared tokens for the connector pattern.
    [!] ./src/RelationalGovernance.jl:402:    tokens_a = Set(split(pattern_a))
    [!] ./src/RelationalGovernance.jl:403:    tokens_b = Set(split(pattern_b))
    [!] ./src/RelationalGovernance.jl:404:    shared = intersect(tokens_a, tokens_b)
    [!] ./src/RelationalGovernance.jl:427:            delete!(CO_ACC, best_key)
    [!] ./src/RelationalGovernance.jl:436:            delete!(CO_ACC, best_key)
    [!] ./src/RelationalGovernance.jl:449:        token_overlap_fn::Function,
    [!] ./src/RelationalGovernance.jl:470:    token_overlap_fn::Function,
    [!] ./src/RelationalGovernance.jl:528:    #     token_overlap_fn = token_overlap_fn,
    [!] ./src/RelationalGovernance.jl:698:        for (key_str, intensity) in pairs_raw
    [!] ./src/RelationalGovernance.jl:699:            parts = split(String(key_str), "|")
    [!] ./src/RelationalGovernance.jl:703:                key = (min(id_a, id_b), max(id_a, id_b))
    [!] ./src/RelationalGovernance.jl:706:                if haskey(CO_ACC, key)
    [!] ./src/RelationalGovernance.jl:707:                    CO_ACC[key] = max(CO_ACC[key], val)
    [!] ./src/RelationalGovernance.jl:709:                    CO_ACC[key] = val
    [!] ./src/RelationalGovernance.jl:715:            min_key = nothing
    [!] ./src/RelationalGovernance.jl:720:                    min_key = k
    [!] ./src/RelationalGovernance.jl:723:            if !isnothing(min_key)
    [!] ./src/RelationalGovernance.jl:724:                delete!(CO_ACC, min_key)
    [!] ./src/RelationalGovernance.jl:769:No token-overlap filter here — the caller already filtered.
    [!] ./src/RelationalGovernance.jl:773:    # GRUG: Sort the pair so (A,B) and (B,A) map to the same key.
    [!] ./src/RelationalGovernance.jl:774:    key = (min(id_a, id_b), max(id_a, id_b))
    [!] ./src/RelationalGovernance.jl:777:        if haskey(CO_ACC, key)
    [!] ./src/RelationalGovernance.jl:778:            CO_ACC[key] += increment
    [!] ./src/RelationalGovernance.jl:783:                min_key = nothing
    [!] ./src/RelationalGovernance.jl:788:                        min_key = k
    [!] ./src/RelationalGovernance.jl:791:                if !isnothing(min_key)
    [!] ./src/RelationalGovernance.jl:792:                    delete!(CO_ACC, min_key)
    [!] ./src/RelationalGovernance.jl:795:            CO_ACC[key] = increment
    [!] ./src/Lobe.jl:23:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/Lobe.jl:27:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/Lobe.jl:31:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/Lobe.jl:110:        if haskey(LOBE_REGISTRY, id)
    [!] ./src/Lobe.jl:161:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:176:        if haskey(NODE_TO_LOBE_IDX, node_id)
    [!] ./src/Lobe.jl:207:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:240:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:260:            if haskey(NODE_TO_LOBE_IDX, nid)
    [!] ./src/Lobe.jl:324:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:344:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:368:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:381:        return collect(keys(LOBE_REGISTRY))
    [!] ./src/Lobe.jl:394:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:410:        if !haskey(LOBE_REGISTRY, lobe_id_a)
    [!] ./src/Lobe.jl:413:        if !haskey(LOBE_REGISTRY, lobe_id_b)
    [!] ./src/Lobe.jl:427:        if haskey(LOBE_REGISTRY, lobe_id_a)
    [!] ./src/Lobe.jl:430:        if haskey(LOBE_REGISTRY, lobe_id_b)
    [!] ./src/Lobe.jl:449:# Only lobes whose whitelist matches the subject tokens can win.
    [!] ./src/Lobe.jl:459:vetoed if no input token matches any whitelist entry. Empty whitelist means
    [!] ./src/Lobe.jl:473:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:497:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:519:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:527:    lobe_can_accept_subject(lobe_id::String, input_tokens::AbstractVector) -> Bool
    [!] ./src/Lobe.jl:530:for the given input tokens. Rules:
    [!] ./src/Lobe.jl:535:     must match at least one input token via case-insensitive substring match.
    [!] ./src/Lobe.jl:541:only has greeting/meta tokens, so "quadratic" and "formula" won't match,
    [!] ./src/Lobe.jl:542:and the lobe is vetoed even if "what" gives it token overlap.
    [!] ./src/Lobe.jl:544:function lobe_can_accept_subject(lobe_id::String, input_tokens::AbstractVector)::Bool
    [!] ./src/Lobe.jl:549:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/Lobe.jl:559:        # Normalize input tokens to lowercase for matching
    [!] ./src/Lobe.jl:560:        lower_tokens = [lowercase(strip(t)) for t in input_tokens if !isempty(strip(t))]
    [!] ./src/Lobe.jl:561:        if isempty(lower_tokens)
    [!] ./src/Lobe.jl:562:            # No tokens to match against — allow through (safe default)
    [!] ./src/Lobe.jl:566:        # substring of at least one input token. This is the ONE-DIRECTIONAL
    [!] ./src/Lobe.jl:567:        # check: whitelist entry ⊆ input token. We do NOT match if the
    [!] ./src/Lobe.jl:568:        # input token is merely a substring of the whitelist entry, because
    [!] ./src/Lobe.jl:573:        # "what can you" in whitelist → only matches if an input token
    [!] ./src/Lobe.jl:578:        joined_input = join(lower_tokens, " ")
    [!] ./src/Lobe.jl:580:            # Single-word whitelist entry: check against individual tokens
    [!] ./src/Lobe.jl:581:            for token in lower_tokens
    [!] ./src/Lobe.jl:582:                if occursin(wl_entry, token)
    [!] ./src/Lobe.jl:616:        if !haskey(LOBE_REGISTRY, lobe_id)
    [!] ./src/BrainStem.jl:22:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/BrainStem.jl:26:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/BrainStem.jl:30:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/BrainStem.jl:110:        if haskey(registry, lobe_id)
    [!] ./src/BrainStem.jl:123:        if haskey(registry, lobe_id)
    [!] ./src/BrainStem.jl:175:        if !haskey(registry, winner_lobe_id)
    [!] ./src/BrainStem.jl:194:            if !haskey(registry, target_id)
    [!] ./src/BrainStem.jl:282:            haskey(registry, r.lobe_id) ? registry[r.lobe_id].fire_count : 0
    [!] ./src/RoutingJudge.jl:117:const BIAS_ADJUSTMENT = Dict{Symbol, Float64}(k => 0.0 for k in keys(CONSERVATIVE_BIAS))
    [!] ./src/RoutingJudge.jl:202:        for k in keys(BIAS_ADJUSTMENT)
    [!] ./src/RoutingJudge.jl:394:    _has_arithmetic_tokens(text::String)::Bool
    [!] ./src/RoutingJudge.jl:396:Check whether the given text fragment contains arithmetic token patterns.
    [!] ./src/RoutingJudge.jl:397:This uses sigil-aware token analysis — not just regex on raw strings, but
    [!] ./src/RoutingJudge.jl:405:The key insight: "5+5" as a topic is NOT a knowledge query about the literal
    [!] ./src/RoutingJudge.jl:409:function _has_arithmetic_tokens(text::AbstractString)::Bool
    [!] ./src/RoutingJudge.jl:433:    _tokens = split(lowercase(text))
    [!] ./src/RoutingJudge.jl:434:    _has_digit = any(t -> occursin(r"^\d", String(t)), _tokens)
    [!] ./src/RoutingJudge.jl:435:    _has_num_word = any(t -> String(t) in _num_words, _tokens)
    [!] ./src/RoutingJudge.jl:456:        # Fallback to token-level check if sigil promotion fails
    [!] ./src/RoutingJudge.jl:457:        return _has_arithmetic_tokens(topic)
    [!] ./src/RoutingJudge.jl:482:            # Check if the triple contains sigil tokens or arithmetic operators
    [!] ./src/RoutingJudge.jl:483:            if occursin(r"&n|&op", tr_str) || _has_arithmetic_tokens(tr_str)
    [!] ./src/RoutingJudge.jl:503:which operates at the token/sentence level. SubIntent splitting operates at
    [!] ./src/RoutingJudge.jl:522:and each is classified using token-level analysis.
    [!] ./src/RoutingJudge.jl:543:    tokens = split(t)
    [!] ./src/RoutingJudge.jl:544:    length(tokens) < 4 && return [SubIntent(t, :nothing, "", "", "", "too-short")]
    [!] ./src/RoutingJudge.jl:546:    lower_tokens = [lowercase(String(tok)) for tok in tokens]
    [!] ./src/RoutingJudge.jl:548:    # Find "and" tokens that are NOT arithmetic context
    [!] ./src/RoutingJudge.jl:549:    split_points = Int[]  # indices of "and" tokens to split on
    [!] ./src/RoutingJudge.jl:550:    for i in 2:(length(tokens) - 1)
    [!] ./src/RoutingJudge.jl:551:        tok = lower_tokens[i]
    [!] ./src/RoutingJudge.jl:555:            if _is_arithmetic_and(lower_tokens, i)
    [!] ./src/RoutingJudge.jl:559:            if _right_has_independent_structure(lower_tokens, i)
    [!] ./src/RoutingJudge.jl:571:        # Left clause: tokens[prev:sp-1]
    [!] ./src/RoutingJudge.jl:572:        left_text = strip(join(tokens[prev:sp-1], " "))
    [!] ./src/RoutingJudge.jl:579:    last_text = strip(join(tokens[prev:end], " "))
    [!] ./src/RoutingJudge.jl:585:    # Run token-level analysis on each sub-intent's text to determine its kind.
    [!] ./src/RoutingJudge.jl:596:    _is_arithmetic_and(lower_tokens, i)::Bool
    [!] ./src/RoutingJudge.jl:603:function _is_arithmetic_and(lower_tokens::Vector{String}, i::Int)::Bool
    [!] ./src/RoutingJudge.jl:606:    left_is_num = i > 1 && _looks_like_arith_token(lower_tokens[i-1])
    [!] ./src/RoutingJudge.jl:607:    right_is_num = i < length(lower_tokens) && _looks_like_arith_token(lower_tokens[i+1])
    [!] ./src/RoutingJudge.jl:612:    if i > 2 && lower_tokens[i-1] in ["add", "subtract", "multiply", "divide", "plus", "minus", "times"]
    [!] ./src/RoutingJudge.jl:619:    _looks_like_arith_token(tok)::Bool
    [!] ./src/RoutingJudge.jl:621:Check if a lowercase token looks like it could be an arithmetic operand.
    [!] ./src/RoutingJudge.jl:623:function _looks_like_arith_token(tok::AbstractString)::Bool
    [!] ./src/RoutingJudge.jl:638:    _right_has_independent_structure(lower_tokens, and_pos)::Bool
    [!] ./src/RoutingJudge.jl:640:Check if the tokens to the RIGHT of "and" at position and_pos have
    [!] ./src/RoutingJudge.jl:643:function _right_has_independent_structure(lower_tokens::Vector{String}, and_pos::Int)::Bool
    [!] ./src/RoutingJudge.jl:645:    right_start > length(lower_tokens) && return false
    [!] ./src/RoutingJudge.jl:652:    # Check first few tokens of right side
    [!] ./src/RoutingJudge.jl:653:    for i in right_start:min(right_start + 2, length(lower_tokens))
    [!] ./src/RoutingJudge.jl:654:        if lower_tokens[i] in _question_starters
    [!] ./src/RoutingJudge.jl:658:        if lower_tokens[i] in ["is", "are", "was", "were"] && i > right_start
    [!] ./src/RoutingJudge.jl:671:plus token-level arithmetic detection.
    [!] ./src/RoutingJudge.jl:677:    # ── Check for arithmetic first (highest priority for token-level detection) ───
    [!] ./src/RoutingJudge.jl:678:    # If the text or any captured topic contains arithmetic tokens, it's :calculate
    [!] ./src/RoutingJudge.jl:687:        if _has_arithmetic_tokens(_topic) || _has_math_bindings_in_topic(_topic)
    [!] ./src/RoutingJudge.jl:738:    if _has_arithmetic_tokens(t) || _has_math_bindings_in_topic(t)
    [!] ./src/RoutingJudge.jl:751:            if _has_arithmetic_tokens(_def) || _has_math_bindings_in_topic(_def)
    [!] ./src/RoutingJudge.jl:950:            # contains arithmetic sigils (&n, &op) or arithmetic tokens (5+5, 3 plus 4),
    [!] ./src/RoutingJudge.jl:952:            if _has_arithmetic_tokens(_w) || _has_math_bindings_in_topic(_w)
    [!] ./src/RoutingJudge.jl:977:            # GRUG v9: Check if the Y part (or full topic) contains arithmetic tokens.
    [!] ./src/RoutingJudge.jl:980:            if _has_arithmetic_tokens(_w2) || _has_math_bindings_in_topic(_w2)
    [!] ./src/RoutingJudge.jl:1120:            # GRUG v9: If the definition side contains arithmetic tokens, this is
    [!] ./src/RoutingJudge.jl:1123:            if _has_arithmetic_tokens(_def) || _has_math_bindings_in_topic(_def)
    [!] ./src/RoutingJudge.jl:1163:    if isempty(candidates) && (_has_arithmetic_tokens(String(t)) || _has_math_bindings_in_topic(String(t)))
    [!] ./src/RoutingJudge.jl:1181:       _has_arithmetic_tokens, _has_math_bindings_in_topic,
    [!] ./src/ActionTonePredictor.jl:17:# NOT token prediction. NOT next-word guessing. NOT an LLM.
    [!] ./src/ActionTonePredictor.jl:33:# If a relational verb appears at the tail of input with no object token
    [!] ./src/ActionTonePredictor.jl:71:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/ActionTonePredictor.jl:75:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/ActionTonePredictor.jl:79:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/ActionTonePredictor.jl:136:    #   :lexicon  → keyword/marker lexicon (the default path; cheap and accurate
    [!] ./src/ActionTonePredictor.jl:398:# GRUG: Surface-level token scoring tables. These are the cave's smell sensors.
    [!] ./src/ActionTonePredictor.jl:402:# GRUG: Query markers — tokens that smell like information-seeking
    [!] ./src/ActionTonePredictor.jl:408:# GRUG: Command markers — imperative/directive tokens
    [!] ./src/ActionTonePredictor.jl:415:# GRUG: Negation markers — contradiction and rejection tokens
    [!] ./src/ActionTonePredictor.jl:421:# GRUG: Speculative markers — epistemic hedging tokens
    [!] ./src/ActionTonePredictor.jl:428:# GRUG: Hostile tone markers — frustration and aggression tokens
    [!] ./src/ActionTonePredictor.jl:435:# GRUG: Urgent tone markers — time pressure and critical framing tokens
    [!] ./src/ActionTonePredictor.jl:442:# GRUG: Reflective tone markers — hedged, thoughtful language tokens
    [!] ./src/ActionTonePredictor.jl:450:# string, not token-by-token. More expensive but catches compound hedges.
    [!] ./src/ActionTonePredictor.jl:459:# GRUG: When the lexicon shrugs (no marker tokens hit, total_action_signal <
    [!] ./src/ActionTonePredictor.jl:471:# no deps, < 1ms per call, no network. The point is: when keywords don't
    [!] ./src/ActionTonePredictor.jl:596:already validated tokens_clean is non-empty by the time we reach the
    [!] ./src/ActionTonePredictor.jl:677:        for k in keys(PREDICTOR_TELEMETRY)
    [!] ./src/ActionTonePredictor.jl:761:        return Dict(k => 1.0 / n for k in keys(scores))
    [!] ./src/ActionTonePredictor.jl:805:        for k in keys(dist); dist[k] = 1.0 / n; end
    [!] ./src/ActionTonePredictor.jl:809:    for k in keys(dist); dist[k] /= total; end
    [!] ./src/ActionTonePredictor.jl:900:    for k in keys(centroid_a); centroid_a[k] /= total_weight; end
    [!] ./src/ActionTonePredictor.jl:901:    for k in keys(centroid_t); centroid_t[k] /= total_weight; end
    [!] ./src/ActionTonePredictor.jl:980:    for k in keys(damped); damped[k] /= total; end
    [!] ./src/ActionTonePredictor.jl:1007:    detect_incomplete_chain(tokens, all_verbs) -> (Bool, Union{String,Nothing})
    [!] ./src/ActionTonePredictor.jl:1009:Scan the last 1-2 tokens for a relational verb with no object following it.
    [!] ./src/ActionTonePredictor.jl:1017:    tokens   ::Vector{String},
    [!] ./src/ActionTonePredictor.jl:1021:    # GRUG: Need at least subject + verb to call it a chain. Single token = nothing to dangle.
    [!] ./src/ActionTonePredictor.jl:1022:    if length(tokens) < 2
    [!] ./src/ActionTonePredictor.jl:1026:    n = length(tokens)
    [!] ./src/ActionTonePredictor.jl:1029:        if idx >= 1 && tokens[idx] in all_verbs
    [!] ./src/ActionTonePredictor.jl:1030:            # GRUG: Look at everything after the verb. Strip punctuation-only tokens.
    [!] ./src/ActionTonePredictor.jl:1032:            tail_tokens = tokens[idx+1:end]
    [!] ./src/ActionTonePredictor.jl:1033:            non_punct   = filter(t -> !occursin(r"^[,;.!?:\s]+$", t), tail_tokens)
    [!] ./src/ActionTonePredictor.jl:1035:                return (true, tokens[idx])
    [!] ./src/ActionTonePredictor.jl:1076:    tokens_raw   = split(strip(input_text))
    [!] ./src/ActionTonePredictor.jl:1077:    tokens_low   = [lowercase(t) for t in tokens_raw]
    [!] ./src/ActionTonePredictor.jl:1079:    # GRUG: Strip trailing punctuation from tokens for clean lexicon lookup.
    [!] ./src/ActionTonePredictor.jl:1081:    tokens_clean = [replace(t, r"[,;.!?:]+" => "") for t in tokens_low]
    [!] ./src/ActionTonePredictor.jl:1082:    tokens_clean = filter(!isempty, tokens_clean)
    [!] ./src/ActionTonePredictor.jl:1084:    if isempty(tokens_clean)
    [!] ./src/ActionTonePredictor.jl:1085:        error("!!! FATAL: ActionTonePredictor: all tokens vanished after punctuation strip! !!!")
    [!] ./src/ActionTonePredictor.jl:1098:        tokens_raw
    [!] ./src/ActionTonePredictor.jl:1102:                       any(t -> t in QUERY_MARKERS, tokens_clean)
    [!] ./src/ActionTonePredictor.jl:1121:    for tok in tokens_clean
    [!] ./src/ActionTonePredictor.jl:1183:    # GRUG v7.21a: Fold tone→action prior. The prior keys are Symbols (so
    [!] ./src/ActionTonePredictor.jl:1202:    # GRUG: "?" is the strongest query signal. Check raw input, not tokens.
    [!] ./src/ActionTonePredictor.jl:1217:    # GRUG: Per-token lexicon scoring.
    [!] ./src/ActionTonePredictor.jl:1218:    for tok in tokens_clean
    [!] ./src/ActionTonePredictor.jl:1225:    # GRUG: First token is a command marker = strong imperative signal.
    [!] ./src/ActionTonePredictor.jl:1226:    if !isempty(tokens_clean) && tokens_clean[1] in COMMAND_MARKERS
    [!] ./src/ActionTonePredictor.jl:1428:    is_dangling, dangling_verb = detect_incomplete_chain(tokens_clean, all_verbs)
    [!] ./src/ActionTonePredictor.jl:1438:            for k in keys(action_dist); action_dist[k] /= total_a; end
    [!] ./src/ActionTonePredictor.jl:1554:Alignment is keyword-based: the node's action name is checked for substrings
    [!] ./src/ActionTonePredictor.jl:1594:# GRUG: Internal keyword alignment check — does this action name sound like the
    [!] ./src/ActionTonePredictor.jl:1595:# predicted action family? Substring match on known keywords per family.
    [!] ./src/ActionTonePredictor.jl:1597:# The keyword lists are intentionally broad: they cover the canonical
    [!] ./src/ActionTonePredictor.jl:1803:# lexicon token matches, structural signals (first-token imperative, "?"),
    [!] ./src/ActionTonePredictor.jl:1834:# input string for reflective hedges that can't be detected token-by-token.
    [!] ./src/ActionTonePredictor.jl:1838:# token positions of the input with no meaningful object token following it.
    [!] ./src/ChatterMode.jl:70:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/ChatterMode.jl:74:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/ChatterMode.jl:78:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/ChatterMode.jl:229:        haskey(MORPH_COOLDOWN_MAP, node_id) || return true
    [!] ./src/ChatterMode.jl:472:    toks = _tokenize_action(action)
    [!] ./src/ChatterMode.jl:474:        if haskey(_CHATTER_ACTION_FAMILIES, t)
    [!] ./src/ChatterMode.jl:529:    _tokenize_action(action_name::String)::Vector{String}
    [!] ./src/ChatterMode.jl:531:GRUG: Split an action name into tokens by underscores, spaces, and hyphens.
    [!] ./src/ChatterMode.jl:533:function _tokenize_action(action_name::String)::Vector{String}
    [!] ./src/ChatterMode.jl:534:    tokens = String[]
    [!] ./src/ChatterMode.jl:537:        !isempty(t) && push!(tokens, t)
    [!] ./src/ChatterMode.jl:539:    return tokens
    [!] ./src/ChatterMode.jl:543:    _build_bigram_table(token_sequences::Vector{Vector{String}})::Dict{String, Vector{String}}
    [!] ./src/ChatterMode.jl:545:GRUG: Build a bigram transition table from multiple token sequences.
    [!] ./src/ChatterMode.jl:547:function _build_bigram_table(token_sequences::Vector{Vector{String}})::Dict{String, Vector{String}}
    [!] ./src/ChatterMode.jl:549:    for seq in token_sequences
    [!] ./src/ChatterMode.jl:553:            if !haskey(table, src)
    [!] ./src/ChatterMode.jl:563:    _walk_markov(bigram_table, start_tokens, max_len)::Vector{String}
    [!] ./src/ChatterMode.jl:565:GRUG: Walk a Markov chain starting from a random start token.
    [!] ./src/ChatterMode.jl:569:                      start_tokens::Vector{String},
    [!] ./src/ChatterMode.jl:571:    isempty(start_tokens) && return String[]
    [!] ./src/ChatterMode.jl:572:    seed = rand(start_tokens)
    [!] ./src/ChatterMode.jl:576:        if !haskey(bigram_table, current) || isempty(bigram_table[current])
    [!] ./src/ChatterMode.jl:614:     - If the other strand has a token at this position, use it
    [!] ./src/ChatterMode.jl:616:  3. If both strands are dead ends at this token, stop
    [!] ./src/ChatterMode.jl:630:    # GRUG: Start from the first token of the starting strand.
    [!] ./src/ChatterMode.jl:645:        current_token = chain[end]
    [!] ./src/ChatterMode.jl:653:            # If the other strand has a token at our current position + 1,
    [!] ./src/ChatterMode.jl:657:                # GRUG: Positional crossover — take the token at this position
    [!] ./src/ChatterMode.jl:659:                cross_token = other_strand[next_pos]
    [!] ./src/ChatterMode.jl:660:                push!(chain, cross_token)
    [!] ./src/ChatterMode.jl:664:            elseif haskey(other_bigram, current_token) && !isempty(other_bigram[current_token])
    [!] ./src/ChatterMode.jl:665:                # GRUG: No positional token, but the other strand's bigram
    [!] ./src/ChatterMode.jl:666:                # knows this token. Transition into the other strand.
    [!] ./src/ChatterMode.jl:667:                push!(chain, rand(other_bigram[current_token]))
    [!] ./src/ChatterMode.jl:677:        if haskey(current_bigram, current_token) && !isempty(current_bigram[current_token])
    [!] ./src/ChatterMode.jl:678:            push!(chain, rand(current_bigram[current_token]))
    [!] ./src/ChatterMode.jl:682:            if haskey(other_bigram, current_token) && !isempty(other_bigram[current_token])
    [!] ./src/ChatterMode.jl:683:                push!(chain, rand(other_bigram[current_token]))
    [!] ./src/ChatterMode.jl:708:    donor_tokens = _tokenize_action(donor_action)
    [!] ./src/ChatterMode.jl:709:    isempty(donor_tokens) && return donor_action
    [!] ./src/ChatterMode.jl:713:    recv_token_seqs = Vector{Vector{String}}()
    [!] ./src/ChatterMode.jl:715:        t = _tokenize_action(a)
    [!] ./src/ChatterMode.jl:716:        !isempty(t) && push!(recv_token_seqs, t)
    [!] ./src/ChatterMode.jl:722:    donor_bigram = _build_bigram_table([donor_tokens])
    [!] ./src/ChatterMode.jl:723:    recv_bigram = _build_bigram_table(recv_token_seqs)
    [!] ./src/ChatterMode.jl:727:    recv_primary = isempty(recv_token_seqs) ? String[] : recv_token_seqs[1]
    [!] ./src/ChatterMode.jl:730:    chain = _walk_crossover_markov(donor_tokens, recv_primary,
    [!] ./src/ChatterMode.jl:735:    # GRUG: If the crossover walk produced only 1 token (just the seed),
    [!] ./src/ChatterMode.jl:737:    # might have no transitions (single-token action name).
    [!] ./src/ChatterMode.jl:739:        chain = _walk_crossover_markov(donor_tokens, recv_primary,
    [!] ./src/ChatterMode.jl:745:    # GRUG: Last resort — if crossover still can't produce a multi-token
    [!] ./src/ChatterMode.jl:748:        all_seqs = vcat([donor_tokens], recv_token_seqs)
    [!] ./src/ChatterMode.jl:750:        all_starts = vcat(donor_tokens, reduce(vcat, recv_token_seqs; init=String[]))
    [!] ./src/ChatterMode.jl:766:    d_tokens = _tokenize_action(donor_action)
    [!] ./src/ChatterMode.jl:767:    r_tokens = isempty(receiver_actions) ? String[] :
    [!] ./src/ChatterMode.jl:768:               _tokenize_action(rand(receiver_actions))
    [!] ./src/ChatterMode.jl:769:    prefix = isempty(d_tokens) ? "remix" : d_tokens[1]
    [!] ./src/ChatterMode.jl:770:    suffix = isempty(r_tokens) ? "drift" : r_tokens[end]
    [!] ./src/ChatterMode.jl:905:            collect(keys(group_map))
    [!] ./src/ChatterMode.jl:963:                    !haskey(node_map, mid) && continue
    [!] ./src/ChatterMode.jl:1265:            !haskey(node_map, clone.source_id) && continue
    [!] ./src/ChatterMode.jl:1393:    haskey(parsed, "sessions") || throw(ChatterError("chatter log $path missing 'sessions' key"))
    [!] ./src/TonalJudge.jl:9:# PredictionResult into a token bag, judges the situation, and emits a
    [!] ./src/TonalJudge.jl:21:#   chain, big arousal swing. Then we spin up the full token bag and
    [!] ./src/TonalJudge.jl:32:#   - TonalReading (token bag + scalar mirrors for back-compat)
    [!] ./src/TonalJudge.jl:60:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/TonalJudge.jl:64:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/TonalJudge.jl:68:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/TonalJudge.jl:97:# Token categories are intentionally few. Each token is (category, name, weight).
    [!] ./src/TonalJudge.jl:113:    weight   :: Float64    # GRUG: [0,1] strength of this token's claim
    [!] ./src/TonalJudge.jl:117:# for "does the bag contain this token?" without caring about strength.
    [!] ./src/TonalJudge.jl:151:# THE TONAL READING — token bag + scalar mirrors
    [!] ./src/TonalJudge.jl:154:# Source of truth: `tokens`. The scalar fields are a derived view kept
    [!] ./src/TonalJudge.jl:156:# action_family / tone_family directly. Adding a token to the bag does
    [!] ./src/TonalJudge.jl:162:    tokens          :: Vector{Token}
    [!] ./src/TonalJudge.jl:182:    promoted_tokens  :: Vector{Token}   # tokens the judge endorsed
    [!] ./src/TonalJudge.jl:188:# READING BUILDER — lifts PredictionResult to TonalReading + token bag
    [!] ./src/TonalJudge.jl:192:# preserving map from PredictionResult into the tokenized space.
    [!] ./src/TonalJudge.jl:195:# coherence, arousal, timestamp. It ADDS: tokens for form, intensity,
    [!] ./src/TonalJudge.jl:201:    # GRUG: Tone token — always present, weight = predictor confidence.
    [!] ./src/TonalJudge.jl:204:    # GRUG: Action token — always present, weight = predictor confidence.
    [!] ./src/TonalJudge.jl:207:    # GRUG: Form tokens — derived from predictor flags.
    [!] ./src/TonalJudge.jl:212:    # GRUG: Intensity tokens — derived from arousal_nudge magnitude.
    [!] ./src/TonalJudge.jl:225:    # GRUG: Coherence token — explicit even when neutral. Lets the
    [!] ./src/TonalJudge.jl:244:# the source of truth; here we project them to Symbols for token bag use.
    [!] ./src/TonalJudge.jl:310:    for tok in reading.tokens
    [!] ./src/TonalJudge.jl:324:# frame. No tone blending, no observation read, no token weighting.
    [!] ./src/TonalJudge.jl:342:        reading.tokens,                # basic path promotes everything
    [!] ./src/TonalJudge.jl:395:        for tok in reading.tokens
    [!] ./src/TonalJudge.jl:422:        reading.tokens,
    [!] ./src/RelationalJitter.jl:47:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/RelationalJitter.jl:51:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/RelationalJitter.jl:55:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/ImageSDF.jl:37:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/ImageSDF.jl:41:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/ImageSDF.jl:45:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/ImageSDF.jl:120:        if haskey(TEMPORAL_COHERENCE_LEDGER, sdf_id)
    [!] ./src/ImageSDF.jl:664:GRUG: Every time SDF fires, key values get a slight jitter (pineal drip modulation).
    [!] ./src/EphemeralAutomaton.jl:77:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/EphemeralAutomaton.jl:81:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/EphemeralAutomaton.jl:85:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/EphemeralAutomaton.jl:408:        if haskey(acc.entries, snapshot_id)
    [!] ./src/EphemeralAutomaton.jl:890:        if haskey(_AUTOMATON_REGISTRY, rule.id)
    [!] ./src/EphemeralAutomaton.jl:907:        if haskey(_AUTOMATON_REGISTRY, id)
    [!] ./src/EphemeralAutomaton.jl:974:        # payload is a String key; missing key is a loud error
    [!] ./src/EphemeralAutomaton.jl:976:            _err("op :getctx requires a String key, got $(typeof(p))",
    [!] ./src/EphemeralAutomaton.jl:979:        haskey(ctx, String(p)) || _err(
    [!] ./src/EphemeralAutomaton.jl:980:            "op :getctx missing key '$(String(p))'", "_eval_step")
    [!] ./src/EphemeralAutomaton.jl:1034:        if haskey(values, step.label)
    [!] ./src/EphemeralAutomaton.jl:1134:# GRUG say: bias come from RULE. Rule trigger + keyword + confidence = bias.
    [!] ./src/EphemeralAutomaton.jl:1135:#           Bias determine what agent probe in subconscious (peek_pattern token
    [!] ./src/EphemeralAutomaton.jl:1136:#           overlap, which keys, drop-table walk weights). Different rule =
    [!] ./src/EphemeralAutomaton.jl:1183:keyword context, and confidence level all become biases that determine WHAT
    [!] ./src/EphemeralAutomaton.jl:1192:    keyword_hints::Vector{String}     # token overlap bias for peek_pattern
    [!] ./src/EphemeralAutomaton.jl:1200:    keyword_hints::Vector{String} = String[],
    [!] ./src/EphemeralAutomaton.jl:1207:    return InjectorDisposition(trigger_action, keyword_hints, cw, pd, dtwb)
    [!] ./src/EphemeralAutomaton.jl:1235:    probe_keys_probed::Int             # how many subconscious keys were probed
    [!] ./src/EphemeralAutomaton.jl:1257:        0,                   # probe_keys_probed
    [!] ./src/EphemeralAutomaton.jl:1317:    "total_probe_keys"       => 0
    [!] ./src/EphemeralAutomaton.jl:1419:      WHAT to look for (keyword hints bias peek_pattern token scoring). Agent
    [!] ./src/EphemeralAutomaton.jl:1432:    # Keyword hints become the peek_pattern query tokens.
    [!] ./src/EphemeralAutomaton.jl:1436:        for hint in agent.disposition.keyword_hints
    [!] ./src/EphemeralAutomaton.jl:1453:                agent.probe_keys_probed += 1
    [!] ./src/EphemeralAutomaton.jl:1469:                agent.probe_keys_probed += 1
    [!] ./src/EphemeralAutomaton.jl:1558:        # confidence_weight. Step labels that are :tag steps become keyword_hints.
    [!] ./src/EphemeralAutomaton.jl:1559:        keyword_hints = String[]
    [!] ./src/EphemeralAutomaton.jl:1562:                push!(keyword_hints, String(step.payload))
    [!] ./src/EphemeralAutomaton.jl:1568:            keyword_hints = keyword_hints,
    [!] ./src/EphemeralAutomaton.jl:1678:            _INJECTOR_STATS["total_probe_keys"] += agent.probe_keys_probed
    [!] ./src/EphemeralAutomaton.jl:1719:        for (key, val) in kwargs
    [!] ./src/EphemeralAutomaton.jl:1720:            if key === :enabled
    [!] ./src/EphemeralAutomaton.jl:1722:            elseif key === :max_cap
    [!] ./src/EphemeralAutomaton.jl:1729:            elseif key === :weight_floor
    [!] ./src/EphemeralAutomaton.jl:1731:            elseif key === :weight_low
    [!] ./src/EphemeralAutomaton.jl:1733:            elseif key === :weight_medium
    [!] ./src/EphemeralAutomaton.jl:1735:            elseif key === :weight_high
    [!] ./src/EphemeralAutomaton.jl:1737:            elseif key === :weight_extreme
    [!] ./src/EphemeralAutomaton.jl:1739:            elseif key === :max_injectors_per_cycle
    [!] ./src/EphemeralAutomaton.jl:1741:            elseif key === :injector_timeout_seconds
    [!] ./src/EphemeralAutomaton.jl:1743:            elseif key === :injector_feedback_prob
    [!] ./src/EphemeralAutomaton.jl:1746:                @warn "[EphemeralAutomaton] set_vigilance_config!: unknown key '$key' — ignored"
    [!] ./src/EphemeralAutomaton.jl:1888:        _INJECTOR_STATS["total_probe_keys"]       = 0
    [!] ./src/InputQueue.jl:19:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/InputQueue.jl:23:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/InputQueue.jl:27:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/InputQueue.jl:178:        if haskey(_NEG_THESAURUS, clean)
    [!] ./src/InputQueue.jl:203:        haskey(_NEG_THESAURUS, clean) ? (delete!(_NEG_THESAURUS, clean); true) : false
    [!] ./src/InputQueue.jl:215:        haskey(_NEG_THESAURUS, clean)
    [!] ./src/InputQueue.jl:242:apply_inhibition_filter(tokens::Vector{String})::Vector{String}
    [!] ./src/InputQueue.jl:244:GRUG: Given a list of input tokens, remove any that appear in NegativeThesaurus.
    [!] ./src/InputQueue.jl:247:function apply_inhibition_filter(tokens::Vector{String})::Vector{String}
    [!] ./src/InputQueue.jl:248:    if isempty(tokens)
    [!] ./src/InputQueue.jl:249:        throw_queue_error("Cannot filter empty token list!", "apply_inhibition_filter")
    [!] ./src/InputQueue.jl:252:        filter(t -> !haskey(_NEG_THESAURUS, strip(lowercase(t))), tokens)
    [!] ./src/InputQueue.jl:259:GRUG: Split text into tokens, filter inhibited tokens, rejoin.
    [!] ./src/InputQueue.jl:260:Returns (filtered_text, removed_tokens).
    [!] ./src/InputQueue.jl:261:Throws if result is empty string (all tokens were inhibited — suspicious!).
    [!] ./src/InputQueue.jl:267:    tokens = split(text)
    [!] ./src/InputQueue.jl:268:    token_strs = String[string(t) for t in tokens]
    [!] ./src/InputQueue.jl:269:    filtered = apply_inhibition_filter(token_strs)
    [!] ./src/InputQueue.jl:270:    removed  = setdiff(token_strs, filtered)
    [!] ./src/InputQueue.jl:273:            "NegativeThesaurus filtered ALL tokens from input! Input was: '$text'. " *
    [!] ./src/InputQueue.jl:300:# <-> "you are" is a perfectly normal single-token <-> two-token synonym
    [!] ./src/InputQueue.jl:306:#       expands input tokens into a synonym cloud so nodes can match via
    [!] ./src/InputQueue.jl:346:# GRUG: canonical key for a (word,synonym) pair — lowercase+strip both sides.
    [!] ./src/InputQueue.jl:347:_pair_key(w::AbstractString, s::AbstractString) = (strip(lowercase(String(w))), strip(lowercase(String(s))))
    [!] ./src/InputQueue.jl:367:    w, s = _pair_key(word, synonym)
    [!] ./src/InputQueue.jl:375:        if haskey(_NEG_PAIRS, w) && haskey(_NEG_PAIRS[w], s)
    [!] ./src/InputQueue.jl:404:    w, s = _pair_key(word, synonym)
    [!] ./src/InputQueue.jl:407:        if haskey(_NEG_PAIRS, w) && haskey(_NEG_PAIRS[w], s)
    [!] ./src/InputQueue.jl:412:            if was_bidi && haskey(_NEG_PAIRS, s) && haskey(_NEG_PAIRS[s], w)
    [!] ./src/InputQueue.jl:431:    w, s = _pair_key(word, synonym)
    [!] ./src/InputQueue.jl:433:        haskey(_NEG_PAIRS, w) && haskey(_NEG_PAIRS[w], s)
    [!] ./src/InputQueue.jl:450:                key = w <= s ? (w, s) : (s, w)
    [!] ./src/InputQueue.jl:451:                if !(key in seen)
    [!] ./src/InputQueue.jl:452:                    push!(seen, key)
    [!] ./src/Thesaurus.jl:5:# GRUG say: NEW - gate filter! Give Grug input tokens, get back expanded set for better scan matching.
    [!] ./src/Thesaurus.jl:21:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/Thesaurus.jl:25:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/Thesaurus.jl:29:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/Thesaurus.jl:46:# GRUG: Gate expansion max results per input token.
    [!] ./src/Thesaurus.jl:47:# Each input token can expand to at most this many synonyms.
    [!] ./src/Thesaurus.jl:48:# Prevents gate from exploding into huge token set for long inputs.
    [!] ./src/Thesaurus.jl:315:    # v7.40: removed "secrete" (biological/archaic — "Grug hide" → "Grug secrete" is wrong register)
    [!] ./src/Thesaurus.jl:610:    # thesaurus can only swap ~15% of tokens. With these, we hit ~40-50%.
    [!] ./src/Thesaurus.jl:741:    # "vital" and "key" are OK.
    [!] ./src/Thesaurus.jl:742:    ("important",       ["vital", "key"]),
    [!] ./src/Thesaurus.jl:955:        if !haskey(SYNONYM_SEED_MAP, can)
    [!] ./src/Thesaurus.jl:962:            if !haskey(SYNONYM_SEED_MAP, syn)
    [!] ./src/Thesaurus.jl:1002:    if haskey(SYNONYM_SEED_MAP, w1) && w2 in SYNONYM_SEED_MAP[w1]
    [!] ./src/Thesaurus.jl:1005:    if haskey(SYNONYM_SEED_MAP, w2) && w1 in SYNONYM_SEED_MAP[w2]
    [!] ./src/Thesaurus.jl:1023:    if haskey(SYNONYM_SEED_MAP, w)
    [!] ./src/Thesaurus.jl:1030:# GATE FILTER - Expand input tokens with synonyms for pre-scan enrichment
    [!] ./src/Thesaurus.jl:1032:# Takes a raw input string, tokenizes it, expands each token with synonyms.
    [!] ./src/Thesaurus.jl:1033:# Returns an expanded set of tokens that the scan gate can use for richer matching.
    [!] ./src/Thesaurus.jl:1040:GRUG: Pre-scan gate expansion. Turns input tokens into a rich synonym cloud.
    [!] ./src/Thesaurus.jl:1042:Each token expands to at most GATE_MAX_EXPANSIONS_PER_TOKEN synonyms (sorted by seed priority).
    [!] ./src/Thesaurus.jl:1043:Returns combined set of original tokens + expansions (all lowercase).
    [!] ./src/Thesaurus.jl:1061:    tokens = filter(!isempty, map(t -> lowercase(strip(t)), split(input_text)))
    [!] ./src/Thesaurus.jl:1062:    # GRUG v8.17: Start with both original AND stemmed forms of all tokens.
    [!] ./src/Thesaurus.jl:1065:    expanded = normalize_tokens(tokens)
    [!] ./src/Thesaurus.jl:1069:    for tok in tokens
    [!] ./src/Thesaurus.jl:1080:            stemmed = stem_token(syn)
    [!] ./src/Thesaurus.jl:1087:        stemmed_tok = stem_token(tok)
    [!] ./src/Thesaurus.jl:1107:Uses synonym-aware token overlap (Jaccard over expanded sets).
    [!] ./src/Thesaurus.jl:1199:    tokens1   = Set{String}(filter(!isempty, map(t -> lowercase(strip(t)), split(concept1))))
    [!] ./src/Thesaurus.jl:1200:    tokens2   = Set{String}(filter(!isempty, map(t -> lowercase(strip(t)), split(concept2))))
    [!] ./src/Thesaurus.jl:1201:    token_sim = jaccard_similarity(tokens1, tokens2)
    [!] ./src/Thesaurus.jl:1203:    for t1 in tokens1
    [!] ./src/Thesaurus.jl:1204:        for t2 in tokens2
    [!] ./src/Thesaurus.jl:1211:    final_sim = 0.7 * token_sim + 0.3 * substr_sim
    [!] ./src/Thesaurus.jl:1241:    concept_tokens = split(concept_lower)
    [!] ./src/Thesaurus.jl:1243:    # GRUG check 0: seed synonym check for word against each concept token
    [!] ./src/Thesaurus.jl:1244:    for tok in concept_tokens
    [!] ./src/Thesaurus.jl:1251:    # GRUG check 1: word is exact token in concept (check BEFORE substring)
    [!] ./src/Thesaurus.jl:1252:    if word_lower in concept_tokens
    [!] ./src/Thesaurus.jl:1267:    # "AI" -> "artificial intelligence" matches first letters of each token
    [!] ./src/Thesaurus.jl:1270:    if length(word_letters) >= 2 && length(word_letters) == length(concept_tokens)
    [!] ./src/Thesaurus.jl:1271:        for (i, token) in enumerate(concept_tokens)
    [!] ./src/Thesaurus.jl:1272:            if length(token) > 0 && word_letters[i] != token[1]
    [!] ./src/Thesaurus.jl:1282:    # GRUG check 5: partial acronym (word shorter than tokens)
    [!] ./src/Thesaurus.jl:1283:    # "AI" matches first 2 tokens of a 3-token concept
    [!] ./src/Thesaurus.jl:1284:    if length(word_letters) >= 2 && length(word_letters) <= length(concept_tokens)
    [!] ./src/Thesaurus.jl:1287:            if i > length(concept_tokens)
    [!] ./src/Thesaurus.jl:1291:            token = concept_tokens[i]
    [!] ./src/Thesaurus.jl:1292:            if length(token) == 0 || letter != token[1]
    [!] ./src/Thesaurus.jl:1299:            return 0.85 * (length(word_letters) / length(concept_tokens)) + 0.1
    [!] ./src/Thesaurus.jl:1303:    # GRUG fallback: compare word to each token individually
    [!] ./src/Thesaurus.jl:1305:    for token in concept_tokens
    [!] ./src/Thesaurus.jl:1306:        sim = word_similarity(word, String(token))
    [!] ./src/Thesaurus.jl:1482:        if !haskey(SYNONYM_SEED_MAP, can)
    [!] ./src/Thesaurus.jl:1489:            if !haskey(SYNONYM_SEED_MAP, syn)
    [!] ./src/Thesaurus.jl:1718:    stem_token(word::AbstractString) -> String
    [!] ./src/Thesaurus.jl:1720:GRUG: Normalize a single token to its base form. Applies:
    [!] ./src/Thesaurus.jl:1732:Special: sigil tokens (&n, &op, etc.) are returned unchanged — they
    [!] ./src/Thesaurus.jl:1735:function stem_token(word::AbstractString)::String
    [!] ./src/Thesaurus.jl:1739:    # GRUG: Sigil tokens pass through unchanged — &n, &op, &being etc.
    [!] ./src/Thesaurus.jl:1750:    if haskey(_IRREGULAR_PLURALS, w)
    [!] ./src/Thesaurus.jl:1755:    if haskey(_IRREGULAR_VERBS, w)
    [!] ./src/Thesaurus.jl:1786:        if haskey(_fves_candidates, w)
    [!] ./src/Thesaurus.jl:1849:    normalize_tokens(tokens) -> Set{String}
    [!] ./src/Thesaurus.jl:1851:GRUG: Apply stemming to a collection of tokens, returning a set that
    [!] ./src/Thesaurus.jl:1852:includes BOTH the original tokens AND their stemmed forms. This way,
    [!] ./src/Thesaurus.jl:1856:function normalize_tokens(tokens)::Set{String}
    [!] ./src/Thesaurus.jl:1858:    for tok in tokens
    [!] ./src/Thesaurus.jl:1861:        stemmed = stem_token(s)
    [!] ./src/Thesaurus.jl:1873:both original and stemmed tokens. Used by _lexical_overlap_confidence
    [!] ./src/Thesaurus.jl:1874:to normalize both input and pattern tokens before comparison.
    [!] ./src/Thesaurus.jl:1878:    tokens = filter(!isempty, map(t -> lowercase(strip(t)), split(text)))
    [!] ./src/Thesaurus.jl:1879:    return normalize_tokens(tokens)
    [!] ./src/ChatterResiduals.jl:51:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/ChatterResiduals.jl:55:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/ChatterResiduals.jl:59:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/ChatterResiduals.jl:113:# identity. ~50% means "about half my tokens appear in the donor's pattern"
    [!] ./src/ChatterResiduals.jl:129:# MARKOV_BLEND_BIAS: Probability of drawing the next token from the donor's
    [!] ./src/ChatterResiduals.jl:138:# both packets have very short token vocabularies.
    [!] ./src/ChatterResiduals.jl:193:    return lock(() -> haskey(_RESIDUAL_LEDGER, h), _RESIDUAL_LEDGER_LOCK)
    [!] ./src/ChatterResiduals.jl:205:        if !haskey(_RESIDUAL_LEDGER, h)
    [!] ./src/ChatterResiduals.jl:207:                keys_vec = collect(keys(_RESIDUAL_LEDGER))
    [!] ./src/ChatterResiduals.jl:208:                delete!(_RESIDUAL_LEDGER, keys_vec[rand(1:length(keys_vec))])
    [!] ./src/ChatterResiduals.jl:358:#   2. Build a bigram transition table from BOTH token sequences
    [!] ./src/ChatterResiduals.jl:359:#   3. Walk the chain: start from the donor's first token (the seed), then
    [!] ./src/ChatterResiduals.jl:360:#      at each step, draw the next token from the combined transition table
    [!] ./src/ChatterResiduals.jl:362:#   4. The resulting token sequence becomes the remixed action name
    [!] ./src/ChatterResiduals.jl:371:    _tokenize_action(action_name::String)::Vector{String}
    [!] ./src/ChatterResiduals.jl:373:GRUG: Split an action name into tokens by underscores, spaces, and hyphens.
    [!] ./src/ChatterResiduals.jl:376:function _tokenize_action(action_name::String)::Vector{String}
    [!] ./src/ChatterResiduals.jl:377:    tokens = String[]
    [!] ./src/ChatterResiduals.jl:380:        !isempty(t) && push!(tokens, t)
    [!] ./src/ChatterResiduals.jl:382:    return tokens
    [!] ./src/ChatterResiduals.jl:386:    _build_bigram_table(token_sequences::Vector{Vector{String}})::Dict{String, Vector{String}}
    [!] ./src/ChatterResiduals.jl:388:GRUG: Build a bigram transition table from multiple token sequences.
    [!] ./src/ChatterResiduals.jl:390:Multiple sequences merge into the same table. The table maps each token
    [!] ./src/ChatterResiduals.jl:391:to the list of tokens that followed it (with duplicates = frequency).
    [!] ./src/ChatterResiduals.jl:393:function _build_bigram_table(token_sequences::Vector{Vector{String}})::Dict{String, Vector{String}}
    [!] ./src/ChatterResiduals.jl:395:    for seq in token_sequences
    [!] ./src/ChatterResiduals.jl:399:            if !haskey(table, src)
    [!] ./src/ChatterResiduals.jl:409:    _walk_markov(bigram_table, start_tokens, max_len)::Vector{String}
    [!] ./src/ChatterResiduals.jl:411:GRUG: Walk a Markov chain starting from a random start token. At each step,
    [!] ./src/ChatterResiduals.jl:412:look up the current token in the bigram table. If found, pick a random
    [!] ./src/ChatterResiduals.jl:413:successor. If not found (dead end), stop. Cap at max_len tokens.
    [!] ./src/ChatterResiduals.jl:417:                      start_tokens::Vector{String},
    [!] ./src/ChatterResiduals.jl:419:    isempty(start_tokens) && return String[]
    [!] ./src/ChatterResiduals.jl:420:    # GRUG: Start from a random seed token from the donor's first tokens
    [!] ./src/ChatterResiduals.jl:421:    seed = rand(start_tokens)
    [!] ./src/ChatterResiduals.jl:425:        if !haskey(bigram_table, current) || isempty(bigram_table[current])
    [!] ./src/ChatterResiduals.jl:463:     - If the other strand has a token at this position, use it
    [!] ./src/ChatterResiduals.jl:465:  3. If both strands are dead ends at this token, stop
    [!] ./src/ChatterResiduals.jl:479:    # GRUG: Start from the first token of the starting strand.
    [!] ./src/ChatterResiduals.jl:494:        current_token = chain[end]
    [!] ./src/ChatterResiduals.jl:502:            # If the other strand has a token at our current position + 1,
    [!] ./src/ChatterResiduals.jl:506:                # GRUG: Positional crossover — take the token at this position
    [!] ./src/ChatterResiduals.jl:508:                cross_token = other_strand[next_pos]
    [!] ./src/ChatterResiduals.jl:509:                push!(chain, cross_token)
    [!] ./src/ChatterResiduals.jl:513:            elseif haskey(other_bigram, current_token) && !isempty(other_bigram[current_token])
    [!] ./src/ChatterResiduals.jl:514:                # GRUG: No positional token, but the other strand's bigram
    [!] ./src/ChatterResiduals.jl:515:                # knows this token. Transition into the other strand.
    [!] ./src/ChatterResiduals.jl:516:                push!(chain, rand(other_bigram[current_token]))
    [!] ./src/ChatterResiduals.jl:526:        if haskey(current_bigram, current_token) && !isempty(current_bigram[current_token])
    [!] ./src/ChatterResiduals.jl:527:            push!(chain, rand(current_bigram[current_token]))
    [!] ./src/ChatterResiduals.jl:531:            if haskey(other_bigram, current_token) && !isempty(other_bigram[current_token])
    [!] ./src/ChatterResiduals.jl:532:                push!(chain, rand(other_bigram[current_token]))
    [!] ./src/ChatterResiduals.jl:557:    donor_tokens = _tokenize_action(donor_action)
    [!] ./src/ChatterResiduals.jl:558:    isempty(donor_tokens) && return donor_action  # can't tokenize, use as-is
    [!] ./src/ChatterResiduals.jl:562:    recv_token_seqs = Vector{Vector{String}}()
    [!] ./src/ChatterResiduals.jl:564:        t = _tokenize_action(a)
    [!] ./src/ChatterResiduals.jl:565:        !isempty(t) && push!(recv_token_seqs, t)
    [!] ./src/ChatterResiduals.jl:571:    donor_bigram = _build_bigram_table([donor_tokens])
    [!] ./src/ChatterResiduals.jl:572:    recv_bigram = _build_bigram_table(recv_token_seqs)
    [!] ./src/ChatterResiduals.jl:576:    recv_primary = isempty(recv_token_seqs) ? String[] : recv_token_seqs[1]
    [!] ./src/ChatterResiduals.jl:579:    chain = _walk_crossover_markov(donor_tokens, recv_primary,
    [!] ./src/ChatterResiduals.jl:584:    # GRUG: If the crossover walk produced only 1 token (just the seed),
    [!] ./src/ChatterResiduals.jl:586:    # might have no transitions (single-token action name).
    [!] ./src/ChatterResiduals.jl:588:        chain = _walk_crossover_markov(donor_tokens, recv_primary,
    [!] ./src/ChatterResiduals.jl:594:    # GRUG: Last resort — if crossover still can't produce a multi-token
    [!] ./src/ChatterResiduals.jl:597:        all_seqs = vcat([donor_tokens], recv_token_seqs)
    [!] ./src/ChatterResiduals.jl:599:        all_starts = vcat(donor_tokens, reduce(vcat, recv_token_seqs; init=String[]))
    [!] ./src/ChatterResiduals.jl:603:    # Rejoin tokens with underscore to form an action name
    [!] ./src/ChatterResiduals.jl:614:token from the donor and the last token from a random receiver action,
    [!] ./src/ChatterResiduals.jl:619:    d_tokens = _tokenize_action(donor_action)
    [!] ./src/ChatterResiduals.jl:620:    r_tokens = isempty(receiver_actions) ? String[] :
    [!] ./src/ChatterResiduals.jl:621:               _tokenize_action(rand(receiver_actions))
    [!] ./src/ChatterResiduals.jl:623:    prefix = isempty(d_tokens) ? "remix" : d_tokens[1]
    [!] ./src/ChatterResiduals.jl:624:    suffix = isempty(r_tokens) ? "drift" : r_tokens[end]
    [!] ./src/ChatterResiduals.jl:764:                            token_overlap_fn::Function = (id_a, id_b) -> 0.0,
    [!] ./src/ChatterResiduals.jl:773:  2. Compute Jaccard token overlap between receiver and donor patterns
    [!] ./src/ChatterResiduals.jl:777:     50% means "about half my tokens appear in yours" — genuine resonance.
    [!] ./src/ChatterResiduals.jl:801:                                 token_overlap_fn::Function = (id_a, id_b) -> 0.0,
    [!] ./src/ChatterResiduals.jl:820:            node_alive = haskey(node_map_ref, node_id) && !node_map_ref[node_id].is_grave
    [!] ./src/ChatterResiduals.jl:821:            donor_alive = haskey(node_map_ref, donor_id) && !node_map_ref[donor_id].is_grave
    [!] ./src/ChatterResiduals.jl:835:        # tokens appear in yours" — genuine resonance, earned, organic.
    [!] ./src/ChatterResiduals.jl:836:        overlap = token_overlap_fn(node_id, donor_id)
    [!] ./src/ChatterResiduals.jl:938:        token_overlap_fn::Function)
    [!] ./src/ChatterResiduals.jl:954:    token_overlap_fn::Function)
    [!] ./src/ChatterResiduals.jl:974:                    token_overlap_fn = token_overlap_fn,
    [!] ./src/ChatterResiduals.jl:1002:    token_overlap_fn::Function)
    [!] ./src/ChatterResiduals.jl:1021:            token_overlap_fn = token_overlap_fn,
    [!] ./src/ChatterResiduals.jl:1090:GRUG: Serialize the ledger for specimen save. Only the hash keys are
    [!] ./src/ChatterResiduals.jl:1095:        [string(h) for h in keys(_RESIDUAL_LEDGER)]
    [!] ./src/ChatterResiduals.jl:1120:            if !haskey(_RESIDUAL_LEDGER, h)
    [!] ./src/ChatterResiduals.jl:1126:            keys_vec = collect(keys(_RESIDUAL_LEDGER))
    [!] ./src/ChatterResiduals.jl:1127:            delete!(_RESIDUAL_LEDGER, keys_vec[rand(1:length(keys_vec))])
    [!] ./src/SemanticVerbs.jl:4:# A RelationalTriple's subject / relation / object may contain sigil tokens
    [!] ./src/SemanticVerbs.jl:30:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/SemanticVerbs.jl:34:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/SemanticVerbs.jl:38:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/SemanticVerbs.jl:125:        if haskey(_VERB_REGISTRY, name)
    [!] ./src/SemanticVerbs.jl:158:        if !haskey(_VERB_REGISTRY, cls)
    [!] ./src/SemanticVerbs.jl:171:        if haskey(_VERB_TO_CLASS, v)
    [!] ./src/SemanticVerbs.jl:217:        if !haskey(_VERB_TO_CLASS, canon)
    [!] ./src/SemanticVerbs.jl:222:        if haskey(_SYNONYM_MAP, alias)
    [!] ./src/SemanticVerbs.jl:256:        if !haskey(_SYNONYM_MAP, alias)
    [!] ./src/SemanticVerbs.jl:298:        if !haskey(_VERB_REGISTRY, cls)
    [!] ./src/SemanticVerbs.jl:312:        return sort(collect(keys(_VERB_REGISTRY)))
    [!] ./src/SemanticVerbs.jl:398:# All verb sets are stored in a Dict{String, Set{String}} keyed by class name.
    [!] ./src/SemanticVerbs.jl:415:# relation checks, and antimatch logic all key on canonical strings.
    [!] ./src/TemporalGrowth.jl:36:#     Lever 2: Lobe subject whitelist — tokens belonging to the lobe's domain
    [!] ./src/TemporalGrowth.jl:38:#     Lever 4: Thesaurus similarity — dimensional proximity between tokens
    [!] ./src/TemporalGrowth.jl:60:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/TemporalGrowth.jl:64:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/TemporalGrowth.jl:68:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/TemporalGrowth.jl:116:# GRUG: Maximum whitelist tokens to check against for relevance scoring.
    [!] ./src/TemporalGrowth.jl:120:# GRUG: Activator markers — question/command words that bind content tokens.
    [!] ./src/TemporalGrowth.jl:131:# GRUG: Stopwords to strip from content tokens.
    [!] ./src/TemporalGrowth.jl:189:    _tokenize(text::String) -> Vector{String}
    [!] ./src/TemporalGrowth.jl:191:GRUG: Simple whitespace tokenizer with lowercasing. Same as MitosisMode.
    [!] ./src/TemporalGrowth.jl:193:function _tokenize(text::String)::Vector{String}
    [!] ./src/TemporalGrowth.jl:201:Question markers and command verbs bind content tokens into a pattern
    [!] ./src/TemporalGrowth.jl:202:that carries its own activation key. Same geometry as MitosisMode.
    [!] ./src/TemporalGrowth.jl:209:    tokens = _tokenize(input_text)
    [!] ./src/TemporalGrowth.jl:210:    isempty(tokens) && return ""
    [!] ./src/TemporalGrowth.jl:215:    for (i, t) in enumerate(tokens)
    [!] ./src/TemporalGrowth.jl:223:    # GRUG: Extract content tokens after the activator, filtering stopwords.
    [!] ./src/TemporalGrowth.jl:224:    content_tokens = filter(t -> length(t) > 2 && !(t in STOPWORDS) && !(t in _ACTIVATOR_MARKERS),
    [!] ./src/TemporalGrowth.jl:225:                            tokens[content_start:end])
    [!] ./src/TemporalGrowth.jl:229:        if !isempty(content_tokens)
    [!] ./src/TemporalGrowth.jl:230:            activator = content_tokens[1]
    [!] ./src/TemporalGrowth.jl:231:            content_tokens = length(content_tokens) > 1 ? content_tokens[2:end] : String[]
    [!] ./src/TemporalGrowth.jl:235:    # GRUG: Assemble pattern: activator + up to 3 content tokens.
    [!] ./src/TemporalGrowth.jl:237:        max_content = min(length(content_tokens), 3)
    [!] ./src/TemporalGrowth.jl:239:            return strip(join(vcat([activator], content_tokens[1:max_content]), " "))
    [!] ./src/TemporalGrowth.jl:245:    # GRUG: No activator, no verb — fall back to top content tokens (max 4).
    [!] ./src/TemporalGrowth.jl:246:    if !isempty(content_tokens)
    [!] ./src/TemporalGrowth.jl:247:        max_tokens = min(length(content_tokens), 4)
    [!] ./src/TemporalGrowth.jl:248:        return strip(join(content_tokens[1:max_tokens], " "))
    [!] ./src/TemporalGrowth.jl:262:#   Lever 2: Lobe subject whitelist — domain tokens for the lobe
    [!] ./src/TemporalGrowth.jl:264:#   Lever 4: Thesaurus similarity — dimensional proximity between tokens
    [!] ./src/TemporalGrowth.jl:279:  1. Whitelist match: +1.0 if any pattern token matches the whitelist
    [!] ./src/TemporalGrowth.jl:281:  3. Whitelist thesaurus: +max thesaurus_fn(pattern_token, wl_token) for near-matches
    [!] ./src/TemporalGrowth.jl:291:    pat_tokens = Set(_tokenize(pattern))
    [!] ./src/TemporalGrowth.jl:294:    # GRUG: Whitelist is the hard gate. If input tokens match the whitelist,
    [!] ./src/TemporalGrowth.jl:301:            for pt in pat_tokens
    [!] ./src/TemporalGrowth.jl:311:    # ── LEVER 4: Thesaurus proximity to whitelist tokens ────────────────
    [!] ./src/TemporalGrowth.jl:317:            for pt in pat_tokens
    [!] ./src/TemporalGrowth.jl:846:        # Fresh user input might contain tokens unrelated to the lobe.
    [!] ./src/TemporalGrowth.jl:918:            if haskey(lobe_registry, lobe_id)
    [!] ./src/TemporalGrowth.jl:924:                if !isempty(node_to_lobe_idx) && !haskey(node_to_lobe_idx, new_id)
    [!] ./src/AutoGrowth.jl:18:#      - Key: candidate pattern/token that might need a node
    [!] ./src/AutoGrowth.jl:32:#      g. LOBE subject whitelist entries — domain tokens for under-populated lobes
    [!] ./src/AutoGrowth.jl:40:#   - LOBE_COVERAGE: under-populated lobes missing domain tokens
    [!] ./src/AutoGrowth.jl:71:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/AutoGrowth.jl:75:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/AutoGrowth.jl:79:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/AutoGrowth.jl:95:# don't produce. The magnet clusters uncovered tokens by sigil shape, producing
    [!] ./src/AutoGrowth.jl:96:# stronger growth signals than treating each token as an unrelated atom.
    [!] ./src/AutoGrowth.jl:108:    pattern::String              # The candidate pattern/token
    [!] ./src/AutoGrowth.jl:234:# the user mentions domain tokens not in its whitelist, we add them.
    [!] ./src/AutoGrowth.jl:285:const CURIOSITY_PER_TOKEN           = 0.05    # intensity per uncovered token
    [!] ./src/AutoGrowth.jl:298:# GRUG: Stopwords for token extraction (same set as MitosisMode)
    [!] ./src/AutoGrowth.jl:344:Every uncovered token, uncovered synonym, under-populated lobe, missing
    [!] ./src/AutoGrowth.jl:346:accumulator is a Dict keyed by candidate pattern, with mutable records
    [!] ./src/AutoGrowth.jl:402:    tokens = _tokenize(user_text)
    [!] ./src/AutoGrowth.jl:403:    isempty(tokens) && return nothing
    [!] ./src/AutoGrowth.jl:407:    # ── SOURCE 1: SILENCE MAP — uncovered tokens ──────────────────────
    [!] ./src/AutoGrowth.jl:409:    # node patterns. Each uncovered token gets an evidence increment.
    [!] ./src/AutoGrowth.jl:410:    for tok in tokens
    [!] ./src/AutoGrowth.jl:413:                          lobe_hint = _infer_lobe_from_token(tok))
    [!] ./src/AutoGrowth.jl:418:    # GRUG: For each covered token, check if its thesaurus synonyms are
    [!] ./src/AutoGrowth.jl:420:    for tok in tokens
    [!] ./src/AutoGrowth.jl:428:                                      lobe_hint = _infer_lobe_from_token(syn_lower))
    [!] ./src/AutoGrowth.jl:437:    # ── SOURCE 3: LOBE COVERAGE — under-populated lobe domain tokens ──
    [!] ./src/AutoGrowth.jl:438:    # GRUG: If the user mentions tokens that belong to an under-populated
    [!] ./src/AutoGrowth.jl:441:        subject_tokens = _tokenize(subject)
    [!] ./src/AutoGrowth.jl:442:        overlap = intersect(Set(tokens), Set(subject_tokens))
    [!] ./src/AutoGrowth.jl:452:            for tok in tokens
    [!] ./src/AutoGrowth.jl:467:                              lobe_hint = _infer_lobe_from_token(conn_lower))
    [!] ./src/AutoGrowth.jl:474:    # pattern gets evidence. Not individual tokens — the full pattern.
    [!] ./src/AutoGrowth.jl:482:                              lobe_hint = _infer_lobe_from_token(full_pattern))
    [!] ./src/AutoGrowth.jl:487:    # ── SOURCE 6: TIME NODE GAP — temporal orientation keywords ────────
    [!] ./src/AutoGrowth.jl:488:    # GRUG: When the user uses temporal keywords frequently and there's
    [!] ./src/AutoGrowth.jl:490:    time_toks = filter(t -> in(t, TIME_NODE_KEYWORDS), tokens)
    [!] ./src/AutoGrowth.jl:510:                if haskey(sig_entry, :lexicon) && !isnothing(sig_entry[:lexicon]) && !isempty(sig_entry[:lexicon])
    [!] ./src/AutoGrowth.jl:513:                if haskey(sig_entry, :expansion) && !isnothing(sig_entry[:expansion]) && !isempty(sig_entry[:expansion])
    [!] ./src/AutoGrowth.jl:517:                    if haskey(sig_entry, "lexicon") && !isnothing(sig_entry["lexicon"]) && !isempty(sig_entry["lexicon"])
    [!] ./src/AutoGrowth.jl:520:                    if haskey(sig_entry, "expansion") && !isnothing(sig_entry["expansion"]) && !isempty(sig_entry["expansion"])
    [!] ./src/AutoGrowth.jl:533:    # GRUG: Track which non-stopword tokens co-occur in the same user message.
    [!] ./src/AutoGrowth.jl:537:    content_tokens = filter(t -> length(t) > 3 && !in(t, STOPWORDS), tokens)
    [!] ./src/AutoGrowth.jl:538:    if length(content_tokens) >= 2
    [!] ./src/AutoGrowth.jl:540:            for i in 1:length(content_tokens)-1
    [!] ./src/AutoGrowth.jl:541:                for j in i+1:length(content_tokens)
    [!] ./src/AutoGrowth.jl:542:                    a, b = content_tokens[i], content_tokens[j]
    [!] ./src/AutoGrowth.jl:544:                        key = a < b ? (a, b) : (b, a)
    [!] ./src/AutoGrowth.jl:545:                        _CO_OCCUR_MAP[key] = get(_CO_OCCUR_MAP, key, 0) + 1
    [!] ./src/AutoGrowth.jl:558:        for tok in tokens
    [!] ./src/AutoGrowth.jl:561:                              lobe_hint = _infer_lobe_from_token(tok))
    [!] ./src/AutoGrowth.jl:571:        for tok in tokens
    [!] ./src/AutoGrowth.jl:580:                                          lobe_hint = _infer_lobe_from_token(syn_lower))
    [!] ./src/AutoGrowth.jl:604:                              lobe_hint = _infer_lobe_from_token(full_pattern))
    [!] ./src/AutoGrowth.jl:613:        for tok in tokens
    [!] ./src/AutoGrowth.jl:617:                              lobe_hint = _infer_lobe_from_token(tok))
    [!] ./src/AutoGrowth.jl:627:                      lobe_hint = _infer_lobe_from_token(observer_gap_pattern))
    [!] ./src/AutoGrowth.jl:641:            for (key, entry) in _EVIDENCE
    [!] ./src/AutoGrowth.jl:643:                    _EVIDENCE[key] = EvidenceRecord(
    [!] ./src/AutoGrowth.jl:672:            for (key, entry) in _EVIDENCE
    [!] ./src/AutoGrowth.jl:675:                    _EVIDENCE[key] = EvidenceRecord(
    [!] ./src/AutoGrowth.jl:697:    # uncovered tokens should be boosted. Rare patterns = more growth.
    [!] ./src/AutoGrowth.jl:700:        for tok in tokens
    [!] ./src/AutoGrowth.jl:703:                              lobe_hint = _infer_lobe_from_token(tok))
    [!] ./src/AutoGrowth.jl:713:    # uncovered tokens, those gaps are REAL unknowns, not just noise.
    [!] ./src/AutoGrowth.jl:718:        for tok in tokens
    [!] ./src/AutoGrowth.jl:727:                                          lobe_hint = _infer_lobe_from_token(syn_lower))
    [!] ./src/AutoGrowth.jl:748:    #   4. Accumulate evidence for those gap tokens WITH the user triple attached
    [!] ./src/AutoGrowth.jl:806:                        # GRUG: Subject token as gap evidence
    [!] ./src/AutoGrowth.jl:809:                                          lobe_hint = _infer_lobe_from_token(ut_subj),
    [!] ./src/AutoGrowth.jl:813:                        # GRUG: Object token as gap evidence
    [!] ./src/AutoGrowth.jl:816:                                          lobe_hint = _infer_lobe_from_token(ut_obj),
    [!] ./src/AutoGrowth.jl:824:                                          lobe_hint = _infer_lobe_from_token(ut_subj),
    [!] ./src/AutoGrowth.jl:839:    # GRUG v8.2: The inverse sigil list clusters uncovered tokens by sigil shape.
    [!] ./src/AutoGrowth.jl:840:    # Instead of treating each uncovered token as an unrelated atom (SOURCE 1),
    [!] ./src/AutoGrowth.jl:845:    #   1. Call InverseSigil.feed_evidence!() which scans user text for tokens
    [!] ./src/AutoGrowth.jl:846:    #      that match sigil shapes and clusters uncovered tokens by shape.
    [!] ./src/AutoGrowth.jl:868:                _lobe = isempty(result.lobe_hint) ? _infer_lobe_from_token(result.pattern) : result.lobe_hint
    [!] ./src/AutoGrowth.jl:884:        n_uncovered = count(t -> !_is_covered(t, node_patterns) && length(t) > 2 && !in(t, STOPWORDS), tokens)
    [!] ./src/AutoGrowth.jl:908:        for tok in tokens
    [!] ./src/AutoGrowth.jl:933:            oldest_key = ""
    [!] ./src/AutoGrowth.jl:938:                    oldest_key = k
    [!] ./src/AutoGrowth.jl:941:            !isempty(oldest_key) && delete!(_EVIDENCE, oldest_key)
    [!] ./src/AutoGrowth.jl:976:  - add_lobe_whitelist_fn: (lobe_id, token) -> Int — add to lobe whitelist
    [!] ./src/AutoGrowth.jl:1037:    best_key = ""
    [!] ./src/AutoGrowth.jl:1042:        for (key, rec) in _EVIDENCE
    [!] ./src/AutoGrowth.jl:1056:                best_key = key
    [!] ./src/AutoGrowth.jl:1156:                delete!(_EVIDENCE, best_key)
    [!] ./src/AutoGrowth.jl:1173:            if haskey(payload, "suggest_growth_type")
    [!] ./src/AutoGrowth.jl:1271:        # GRUG: Pattern is "wl:lobe_id:token". Add token to lobe's whitelist.
    [!] ./src/AutoGrowth.jl:1274:            wl_lobe_id, wl_token = parts[2], parts[3]
    [!] ./src/AutoGrowth.jl:1277:                    count = add_lobe_whitelist_fn(wl_lobe_id, wl_token)
    [!] ./src/AutoGrowth.jl:1278:                    new_id = "wl:$wl_lobe_id:$wl_token"
    [!] ./src/AutoGrowth.jl:1279:                    notes = "Whitelist: $wl_token → $wl_lobe_id (now $count entries)"
    [!] ./src/AutoGrowth.jl:1333:                if !isempty(lobe_registry) && haskey(lobe_registry, lobe_hint)
    [!] ./src/AutoGrowth.jl:1351:        # the strongest concrete token or a combined pattern.
    [!] ./src/AutoGrowth.jl:1397:            delete!(_EVIDENCE, best_key)
    [!] ./src/AutoGrowth.jl:1454:    # GRUG: Filter out empty tokens from split
    [!] ./src/AutoGrowth.jl:1600:    # Caller-supplied keys override defaults — the caller knows best what this node is.
    [!] ./src/AutoGrowth.jl:1651:            delete!(_EVIDENCE, best_key)
    [!] ./src/AutoGrowth.jl:1668:        if !isempty(lobe_registry) && haskey(lobe_registry, lobe_hint)
    [!] ./src/AutoGrowth.jl:1988:                        key = "thes:$a:$b"
    [!] ./src/AutoGrowth.jl:1989:                        _add_evidence!(key, Float64(count) * 0.5, "co_occurrence", :thesaurus;
    [!] ./src/AutoGrowth.jl:2087:  - The pattern's tokens become the node's subject/object anchors
    [!] ./src/AutoGrowth.jl:2092:# REMINDER: relational_patterns may contain sigil tokens (&n, &op etc) — dynamic evaluation at match time.
    [!] ./src/AutoGrowth.jl:2110:        pat_tokens = Set(split(pat_lower))
    [!] ./src/AutoGrowth.jl:2113:        relevant = subj_lower in pat_tokens || obj_lower in pat_tokens ||
    [!] ./src/AutoGrowth.jl:2143:        if haskey(_EVIDENCE, pattern)
    [!] ./src/AutoGrowth.jl:2150:            # (e.g., a pattern first seen via silence_map might later show time keywords)
    [!] ./src/AutoGrowth.jl:2183:        for (key, rec) in _EVIDENCE
    [!] ./src/AutoGrowth.jl:2186:                push!(to_delete, key)
    [!] ./src/AutoGrowth.jl:2189:        for key in to_delete
    [!] ./src/AutoGrowth.jl:2190:            delete!(_EVIDENCE, key)
    [!] ./src/AutoGrowth.jl:2195:function _is_covered(token::String, existing_patterns::Set{String})::Bool
    [!] ./src/AutoGrowth.jl:2196:    tok_lower = lowercase(strip(token))
    [!] ./src/AutoGrowth.jl:2217:function _tokenize(text::String)::Vector{String}
    [!] ./src/AutoGrowth.jl:2218:    tokens = split(lowercase(strip(text)))
    [!] ./src/AutoGrowth.jl:2219:    return filter(t -> length(t) > 2 && !(t in STOPWORDS), String.(tokens))
    [!] ./src/AutoGrowth.jl:2222:# GRUG: Lobe inference from token content. Same keyword heuristics as MitosisMode.
    [!] ./src/AutoGrowth.jl:2223:function _infer_lobe_from_token(token::String)::String
    [!] ./src/AutoGrowth.jl:2224:    t = lowercase(strip(token))
    [!] ./src/AutoGrowth.jl:2262:    tokens = split(lowercase(strip(input_text)))
    [!] ./src/AutoGrowth.jl:2263:    isempty(tokens) && return strip(input_text)
    [!] ./src/AutoGrowth.jl:2267:    for (i, t) in enumerate(tokens)
    [!] ./src/AutoGrowth.jl:2275:    content_tokens = filter(t -> length(t) > 2 && !(t in STOPWORDS) && !(t in _ACTIVATOR_MARKERS),
    [!] ./src/AutoGrowth.jl:2276:                            String.(tokens[content_start:end]))
    [!] ./src/AutoGrowth.jl:2279:        if !isempty(content_tokens)
    [!] ./src/AutoGrowth.jl:2280:            activator = content_tokens[1]
    [!] ./src/AutoGrowth.jl:2281:            content_tokens = length(content_tokens) > 1 ? content_tokens[2:end] : String[]
    [!] ./src/AutoGrowth.jl:2286:        max_content = min(length(content_tokens), 3)
    [!] ./src/AutoGrowth.jl:2288:            return strip(join(vcat([activator], content_tokens[1:max_content]), " "))
    [!] ./src/AutoGrowth.jl:2294:    if !isempty(content_tokens)
    [!] ./src/AutoGrowth.jl:2295:        return strip(join(content_tokens[1:min(4, length(content_tokens))], " "))
    [!] ./src/AutoGrowth.jl:2316:    if !isempty(lobe_registry) && haskey(lobe_registry, lobe_hint)
    [!] ./src/AutoGrowth.jl:2477:            for (key, rec) in top5
    [!] ./src/AutoGrowth.jl:2478:                push!(lines, "    '$key' type=$(rec.growth_type) intensity=$(round(rec.accumulated_intensity, digits=2)) freq=$(rec.frequency) lobe=$(rec.lobe_hint) sources=[$(join(rec.sources, ","))]")
    [!] ./src/AutoGrowth.jl:2617:                key = a < b ? (a, b) : (b, a)
    [!] ./src/AutoGrowth.jl:2618:                _CO_OCCUR_MAP[key] = count
    [!] ./src/AutoGrowth.jl:2675:                if haskey(_EVIDENCE, pat)
    [!] ./src/LobeTable.jl:5:# GRUG say: pattern-activated lookup! Key or prefix or token match. No blind scanning.
    [!] ./src/LobeTable.jl:23:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/LobeTable.jl:27:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/LobeTable.jl:31:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/LobeTable.jl:53:# Each chunk is a separate hash table with its own key space.
    [!] ./src/LobeTable.jl:56:# - CHUNK_DROP      : pattern_hash_key -> Set{String} of node_ids (drop table lookup)
    [!] ./src/LobeTable.jl:58:# - CHUNK_META      : arbitrary lobe-level metadata key -> Any
    [!] ./src/LobeTable.jl:130:        if haskey(LOBE_TABLE_REGISTRY, lobe_id)
    [!] ./src/LobeTable.jl:155:        if !haskey(LOBE_TABLE_REGISTRY, lobe_id)
    [!] ./src/LobeTable.jl:163:# TABLE PUT - Insert or update a key in a chunk
    [!] ./src/LobeTable.jl:167:function table_put!(lobe_id::String, chunk_name::String, key::String, value::Any)
    [!] ./src/LobeTable.jl:171:    if isempty(strip(key))
    [!] ./src/LobeTable.jl:172:        throw_table_error("key cannot be empty", "table_put!")
    [!] ./src/LobeTable.jl:176:        chunk.store[key] = value
    [!] ./src/LobeTable.jl:181:# TABLE GET - O(1) key lookup in a chunk
    [!] ./src/LobeTable.jl:182:# GRUG: Returns value or nothing. Never throws on missing key.
    [!] ./src/LobeTable.jl:183:# Use table_get! if you want an error on missing key.
    [!] ./src/LobeTable.jl:186:function table_get(lobe_id::String, chunk_name::String, key::String)::Union{Any, Nothing}
    [!] ./src/LobeTable.jl:190:    if isempty(strip(key))
    [!] ./src/LobeTable.jl:191:        throw_table_error("key cannot be empty", "table_get")
    [!] ./src/LobeTable.jl:195:        return get(chunk.store, key, nothing)
    [!] ./src/LobeTable.jl:200:# TABLE GET! - O(1) key lookup that throws if key missing
    [!] ./src/LobeTable.jl:201:# GRUG: Use when missing key is always a programmer error.
    [!] ./src/LobeTable.jl:204:function table_get!(lobe_id::String, chunk_name::String, key::String)::Any
    [!] ./src/LobeTable.jl:205:    val = table_get(lobe_id, chunk_name, key)
    [!] ./src/LobeTable.jl:207:        throw_table_error("Key '$key' not found in chunk '$chunk_name' for lobe '$lobe_id'", "table_get!")
    [!] ./src/LobeTable.jl:213:# TABLE HAS - Check key existence in a chunk
    [!] ./src/LobeTable.jl:216:function table_has(lobe_id::String, chunk_name::String, key::String)::Bool
    [!] ./src/LobeTable.jl:220:    if isempty(strip(key))
    [!] ./src/LobeTable.jl:221:        throw_table_error("key cannot be empty", "table_has")
    [!] ./src/LobeTable.jl:225:        return haskey(chunk.store, key)
    [!] ./src/LobeTable.jl:230:# TABLE DELETE - Remove a key from a chunk
    [!] ./src/LobeTable.jl:231:# GRUG: Returns true if key existed and was deleted. False if key wasn't there.
    [!] ./src/LobeTable.jl:232:# Not an error to delete a nonexistent key - just returns false.
    [!] ./src/LobeTable.jl:235:function table_delete!(lobe_id::String, chunk_name::String, key::String)::Bool
    [!] ./src/LobeTable.jl:239:    if isempty(strip(key))
    [!] ./src/LobeTable.jl:240:        throw_table_error("key cannot be empty", "table_delete!")
    [!] ./src/LobeTable.jl:244:        if haskey(chunk.store, key)
    [!] ./src/LobeTable.jl:245:            delete!(chunk.store, key)
    [!] ./src/LobeTable.jl:253:# TABLE KEYS - All keys in a chunk
    [!] ./src/LobeTable.jl:254:# GRUG: Returns snapshot of keys at call time. Safe copy, not live reference.
    [!] ./src/LobeTable.jl:257:function table_keys(lobe_id::String, chunk_name::String)::Vector{String}
    [!] ./src/LobeTable.jl:260:        return collect(keys(chunk.store))
    [!] ./src/LobeTable.jl:277:# GRUG: The smart lookup. Finds all keys in a chunk that match a pattern.
    [!] ./src/LobeTable.jl:278:# mode = :exact   -> exact key match (same as table_get but returns Dict)
    [!] ./src/LobeTable.jl:279:# mode = :prefix  -> all keys starting with prefix string
    [!] ./src/LobeTable.jl:280:# mode = :token   -> all keys containing any token from pattern (space-split)
    [!] ./src/LobeTable.jl:281:# mode = :regex   -> all keys matching a Regex pattern
    [!] ./src/LobeTable.jl:282:# Returns Dict{String, Any} of matching key->value pairs.
    [!] ./src/LobeTable.jl:290:    mode       ::Symbol = :token
    [!] ./src/LobeTable.jl:299:    if !(mode in (:exact, :prefix, :token, :regex))
    [!] ./src/LobeTable.jl:300:        throw_table_error("Unknown match mode '$mode'. Use :exact, :prefix, :token, or :regex", "table_match")
    [!] ./src/LobeTable.jl:308:            # GRUG: Single key lookup wrapped in dict for uniform return type
    [!] ./src/LobeTable.jl:309:            if haskey(chunk.store, pattern)
    [!] ./src/LobeTable.jl:314:            # GRUG: All keys that start with the pattern string
    [!] ./src/LobeTable.jl:322:        elseif mode == :token
    [!] ./src/LobeTable.jl:323:            # GRUG: Split pattern into tokens. Key matches if it contains ANY token.
    [!] ./src/LobeTable.jl:325:            tokens = filter(!isempty, map(t -> lowercase(strip(t)), split(pattern)))
    [!] ./src/LobeTable.jl:326:            if isempty(tokens)
    [!] ./src/LobeTable.jl:327:                throw_table_error("pattern produced no tokens after split", "table_match :token")
    [!] ./src/LobeTable.jl:331:                for tok in tokens
    [!] ./src/LobeTable.jl:334:                        break  # GRUG: One token match is enough to activate
    [!] ./src/LobeTable.jl:361:# We store each field as: key = "field_name:node_id" -> value
    [!] ./src/LobeTable.jl:364:# Pattern: table_match(lobe_id, CHUNK_JSON, "field_name", mode=:token) gets all nodes with field.
    [!] ./src/LobeTable.jl:382:        composite_key = "$(node_id):$(string(field))"
    [!] ./src/LobeTable.jl:383:        table_put!(lobe_id, CHUNK_JSON, composite_key, value)
    [!] ./src/LobeTable.jl:408:    for (composite_key, value) in matches
    [!] ./src/LobeTable.jl:410:        field_name = composite_key[prefix_len+1:end]
    [!] ./src/LobeTable.jl:445:        drop_key = "$(node_id):$(target_id)"
    [!] ./src/LobeTable.jl:446:        table_put!(lobe_id, CHUNK_DROP, drop_key, true)
    [!] ./src/LobeTable.jl:470:    for (drop_key, _) in matches
    [!] ./src/LobeTable.jl:471:        target_id = drop_key[prefix_len+1:end]
    [!] ./src/LobeTable.jl:499:    key = string(input_hash)
    [!] ./src/LobeTable.jl:500:    table_put!(lobe_id, CHUNK_HOPFIELD, key, node_ids)
    [!] ./src/LobeTable.jl:507:    key = string(input_hash)
    [!] ./src/LobeTable.jl:508:    val = table_get(lobe_id, CHUNK_HOPFIELD, key)
    [!] ./src/LobeTable.jl:514:        throw_table_error("Hopfield chunk entry for key '$key' is not a Vector. Data corruption!", "hopfield_get")
    [!] ./src/LobeTable.jl:592:        if !haskey(LOBE_TABLE_REGISTRY, lobe_id)
    [!] ./src/LobeTable.jl:609:        if !haskey(LOBE_TABLE_REGISTRY, lobe_id)
    [!] ./src/LobeTable.jl:615:        for cname in sort(collect(keys(rec.chunks)))
    [!] ./src/LobeTable.jl:635:        for lobe_id in sort(collect(keys(LOBE_TABLE_REGISTRY)))
    [!] ./src/LobeTable.jl:639:                for cname in sort(collect(keys(rec.chunks)))
    [!] ./src/LobeTable.jl:653:        return haskey(LOBE_TABLE_REGISTRY, lobe_id)
    [!] ./src/LobeTable.jl:671:    key = lowercase(strip(expression))
    [!] ./src/LobeTable.jl:688:    table_put!(lobe_id, CHUNK_FLASHCARD, key, card)
    [!] ./src/LobeTable.jl:699:    key = lowercase(strip(expression))
    [!] ./src/LobeTable.jl:700:    card = table_get(lobe_id, CHUNK_FLASHCARD, key)
    [!] ./src/LobeTable.jl:744:    key = lowercase(strip(expression))
    [!] ./src/LobeTable.jl:745:    card = table_get(lobe_id, CHUNK_FLASHCARD, key)
    [!] ./src/LobeTable.jl:750:    table_put!(lobe_id, CHUNK_FLASHCARD, key, card)
    [!] ./src/LobeTable.jl:763:    all_keys = table_keys(lobe_id, CHUNK_FLASHCARD)
    [!] ./src/LobeTable.jl:765:    for key in all_keys
    [!] ./src/LobeTable.jl:766:        card = table_get(lobe_id, CHUNK_FLASHCARD, key)
    [!] ./src/LobeTable.jl:795:    all_keys = table_keys(lobe_id, CHUNK_FLASHCARD)
    [!] ./src/LobeTable.jl:798:    for key in all_keys
    [!] ./src/LobeTable.jl:799:        card = table_get(lobe_id, CHUNK_FLASHCARD, key)
    [!] ./src/LobeTable.jl:807:                table_delete!(lobe_id, CHUNK_FLASHCARD, key)
    [!] ./src/LobeTable.jl:837:            if haskey(rec.chunks, CHUNK_FLASHCARD)
    [!] ./src/LobeTable.jl:890:            key = lowercase(strip(expr))
    [!] ./src/LobeTable.jl:891:            table_put!(string(lobe_id), CHUNK_FLASHCARD, key, Dict{String,Any}(card))
    [!] ./src/EyeSystem.jl:29:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/EyeSystem.jl:33:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/EyeSystem.jl:37:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/GrugBot420.jl:18:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/GrugBot420.jl:22:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/GrugBot420.jl:26:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/GrugBot420.jl:163:# `&` token take a fast path that allocates nothing — old specimens are
    [!] ./src/GrugBot420.jl:237:# Layer 1 canonicalizes tokens via closed number-word and op-word maps
    [!] ./src/GrugBot420.jl:239:# promote_at_tokenize=true and rewrites canonical tokens into &n / &op /
    [!] ./src/GrugBot420.jl:296:# uses fuzzy transformers. Jitter snap-back on key weights. User-extensible
    [!] ./src/GrugBot420.jl:297:# rules in a pattern/key-activated hash table with drop-table features.
    [!] ./src/GrugBot420.jl:395:export drop_store!, reset_audit!, store_size, key_count
    [!] ./src/GrugBot420.jl:408:export resolve_sigils_in_pattern, parse_sigil_token
    [!] ./src/GrugBot420.jl:416:# offers a name-keyed view for consumers that don't care about absolute
    [!] ./src/GrugBot420.jl:424:export promote_input, bindings_by_name, canonicalize_token
    [!] ./src/GrugBot420.jl:435:export GroupLatchCandidate, LatchCandidate, group_avg_strength, find_group_latch_candidates, _scan_latch_candidates, refresh_inhibition_tokens!, is_inhibited
    [!] ./src/GrugBot420.jl:526:# nested sigil tokens. Bounded recursion (cycle guard); loud failures on
    [!] ./src/GrugBot420.jl:556:export activate_rule_by_key!, activate_rules_by_pattern!
    [!] ./src/MitosisMode.jl:84:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/MitosisMode.jl:88:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/MitosisMode.jl:92:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/MitosisMode.jl:208:const MITOSIS_NOVELTY_COVERAGE_FLOOR = 0.6  # 60%+ token overlap with existing = stale
    [!] ./src/MitosisMode.jl:289:# HELPER: Extract tokens from a string (lowercase, stripped, no stopwords)
    [!] ./src/MitosisMode.jl:380:function _tokenize(text::String)::Vector{String}
    [!] ./src/MitosisMode.jl:381:    tokens = split(lowercase(strip(text)))
    [!] ./src/MitosisMode.jl:382:    return filter(t -> length(t) > 2 && !(t in STOPWORDS), String.(tokens))
    [!] ./src/MitosisMode.jl:397:# The activator-verb bind means the pattern carries its OWN activation key —
    [!] ./src/MitosisMode.jl:415:    tokens_raw = split(lowercase(strip(input_text)))
    [!] ./src/MitosisMode.jl:416:    isempty(tokens_raw) && return strip(input_text)
    [!] ./src/MitosisMode.jl:421:    for (i, t) in enumerate(tokens_raw)
    [!] ./src/MitosisMode.jl:429:    # GRUG: Extract content tokens after the activator, filtering stopwords and short tokens
    [!] ./src/MitosisMode.jl:430:    content_tokens = filter(t -> length(t) > 2 && !(t in STOPWORDS) && !(t in _ACTIVATOR_MARKERS),
    [!] ./src/MitosisMode.jl:431:                            String.(tokens_raw[content_start:end]))
    [!] ./src/MitosisMode.jl:434:    # Verbs carry action semantics — they're the "what's being done" key.
    [!] ./src/MitosisMode.jl:436:        # GRUG: Find the first non-stopword token that looks like a verb.
    [!] ./src/MitosisMode.jl:437:        # Simple heuristic: tokens that aren't common nouns (determiners, pronouns).
    [!] ./src/MitosisMode.jl:438:        # We just take the first content token as the "verb anchor".
    [!] ./src/MitosisMode.jl:439:        if !isempty(content_tokens)
    [!] ./src/MitosisMode.jl:440:            activator = content_tokens[1]
    [!] ./src/MitosisMode.jl:441:            content_tokens = length(content_tokens) > 1 ? content_tokens[2:end] : String[]
    [!] ./src/MitosisMode.jl:445:    # GRUG: Assemble pattern: activator + up to 3 content tokens
    [!] ./src/MitosisMode.jl:447:        max_content = min(length(content_tokens), 3)
    [!] ./src/MitosisMode.jl:449:            return strip(join(vcat([activator], content_tokens[1:max_content]), " "))
    [!] ./src/MitosisMode.jl:455:    # GRUG: No activator, no verb — fall back to top content tokens (max 4)
    [!] ./src/MitosisMode.jl:456:    if !isempty(content_tokens)
    [!] ./src/MitosisMode.jl:457:        max_tokens = min(length(content_tokens), 4)
    [!] ./src/MitosisMode.jl:458:        return strip(join(content_tokens[1:max_tokens], " "))
    [!] ./src/MitosisMode.jl:469:# content tokens are NOT already well-covered by existing node patterns.
    [!] ./src/MitosisMode.jl:475:# Coverage check: tokenize the input, check each token against existing patterns.
    [!] ./src/MitosisMode.jl:485:    # coverage_ratio = fraction of input tokens already covered by existing patterns
    [!] ./src/MitosisMode.jl:489:    tokens = _tokenize(input_text)
    [!] ./src/MitosisMode.jl:490:    isempty(tokens) && return (false, 1.0)
    [!] ./src/MitosisMode.jl:496:    for tok in tokens
    [!] ./src/MitosisMode.jl:502:    coverage_ratio = Float64(covered_count) / Float64(length(tokens))
    [!] ./src/MitosisMode.jl:509:# GRUG: Instead of exact substring match, use fuzzy coverage. A token is
    [!] ./src/MitosisMode.jl:515:function _is_covered(token::String, existing_patterns::Set{String})::Bool
    [!] ./src/MitosisMode.jl:516:    tok_lower = lowercase(strip(token))
    [!] ./src/MitosisMode.jl:527:        # GRUG: Single-word token: check if it's a substring of a single-word pattern
    [!] ./src/MitosisMode.jl:575:    # GRUG: Accumulate intensity per uncovered token
    [!] ./src/MitosisMode.jl:576:    token_intensity = Dict{String, Float64}()
    [!] ./src/MitosisMode.jl:578:        tokens = _tokenize(text)
    [!] ./src/MitosisMode.jl:579:        for tok in tokens
    [!] ./src/MitosisMode.jl:581:            token_intensity[tok] = get(token_intensity, tok, 0.0) + intensity
    [!] ./src/MitosisMode.jl:585:    isempty(token_intensity) && return nothing
    [!] ./src/MitosisMode.jl:587:    best_tok = argmax(token_intensity)
    [!] ./src/MitosisMode.jl:588:    best_accum = token_intensity[best_tok]
    [!] ./src/MitosisMode.jl:610:    token_counts = Dict{String, Int}()
    [!] ./src/MitosisMode.jl:612:        for tok in _tokenize(text)
    [!] ./src/MitosisMode.jl:613:            token_counts[tok] = get(token_counts, tok, 0) + 1
    [!] ./src/MitosisMode.jl:621:    for (tok, count) in token_counts
    [!] ./src/MitosisMode.jl:929:    # Strategy 3: Default assignment by keyword heuristics
    [!] ./src/MitosisMode.jl:1029:    tokens = split(lowercase(strip(input_text)))
    [!] ./src/MitosisMode.jl:1030:    # GRUG: Drop question markers, stopwords, and short tokens
    [!] ./src/MitosisMode.jl:1031:    content_tokens = filter(t -> length(t) > 2 &&
    [!] ./src/MitosisMode.jl:1034:                            String.(tokens))
    [!] ./src/MitosisMode.jl:1035:    if isempty(content_tokens)
    [!] ./src/MitosisMode.jl:1036:        # GRUG: No content after stripping markers? Return the best token we have.
    [!] ./src/MitosisMode.jl:1037:        all_tokens = filter(t -> length(t) > 1, String.(tokens))
    [!] ./src/MitosisMode.jl:1038:        isempty(all_tokens) && return strip(input_text)
    [!] ./src/MitosisMode.jl:1039:        return all_tokens[1]
    [!] ./src/MitosisMode.jl:1041:    # GRUG: Join top content tokens as pattern (max 4 to keep patterns tight)
    [!] ./src/MitosisMode.jl:1042:    max_tokens = min(length(content_tokens), 4)
    [!] ./src/MitosisMode.jl:1043:    return strip(join(content_tokens[1:max_tokens], " "))
    [!] ./src/MitosisMode.jl:1054:    if action_family_int !== nothing && haskey(_ACTION_FAMILY_PACKETS, action_family_int)
    [!] ./src/MitosisMode.jl:1248:                    # GRUG v8.0: Also handle CascadeBridge (seam_tokens + partner_id)
    [!] ./src/MitosisMode.jl:1250:                        if hasproperty(att, :seam_tokens)     # GRUG v8.0: CascadeBridge
    [!] ./src/MitosisMode.jl:1251:                            connector = join(att.seam_tokens, " ")
    [!] ./src/MitosisMode.jl:1368:    # command marker, or leading verb) gives the node its activation key.
    [!] ./src/MitosisMode.jl:1372:    # Pattern carries its own activation trigger — not just content tokens.
    [!] ./src/MitosisMode.jl:1383:            # GRUG: Activator-verb bind failure = fallback to warrant token. Not fatal.
    [!] ./src/MitosisMode.jl:1396:            # GRUG: Fallback to warrant token pattern. Not fatal.
    [!] ./src/MitosisMode.jl:1457:        if !isempty(lobe_registry) && haskey(lobe_registry, lobe_hint)
    [!] ./src/CoherenceField.jl:53:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/CoherenceField.jl:57:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/CoherenceField.jl:61:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/CoherenceField.jl:96:            if isa(jd, AbstractDict) && haskey(jd, String(sym))
    [!] ./src/CoherenceField.jl:403:    if haskey(bridge_map, candidate_id)
    [!] ./src/CoherenceField.jl:458:    if haskey(bridge_map, candidate_id)
    [!] ./src/CoherenceField.jl:469:                if partner_id !== nothing && haskey(bridge_map, partner_id)
    [!] ./src/CoherenceField.jl:745:    if haskey(d, "weight")
    [!] ./src/CoherenceField.jl:748:    if haskey(d, "depth")
    [!] ./src/CoherenceField.jl:751:    if haskey(d, "decay")
    [!] ./src/CoherenceField.jl:754:    if haskey(d, "recency_window")
    [!] ./src/CoherenceField.jl:757:    if haskey(d, "cache_ttl")
    [!] ./src/SelfObserver.jl:10:# GRUG say: quiet thoughts get tired. Each loud thought has tokens. Tokens refill.
    [!] ./src/SelfObserver.jl:33:#      and reads are throttled (token bucket + global reader lock).
    [!] ./src/SelfObserver.jl:45:# globally serialized (one outstanding reader at a time), per-caller token-bucket
    [!] ./src/SelfObserver.jl:48:# confidence-shaped scalars. Drop tables (per-key and per-entry) provide
    [!] ./src/SelfObserver.jl:72:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/SelfObserver.jl:76:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/SelfObserver.jl:80:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/SelfObserver.jl:93:export observe!, peek_exact, peek_pattern, audit_trail, drop_store!, drop_keys_by_prefix!
    [!] ./src/SelfObserver.jl:94:export reset_audit!, store_size, key_count
    [!] ./src/SelfObserver.jl:240:const TOKEN_OVERLAP_FLOOR      = 1          # at least 1 shared token to count
    [!] ./src/SelfObserver.jl:243:const TOKEN_BUCKET_CAPACITY    = 3          # per-node tokens
    [!] ./src/SelfObserver.jl:244:const TOKEN_REFILL_SECONDS     = 20.0       # one token per N seconds per node
    [!] ./src/SelfObserver.jl:245:const GLOBAL_TOKEN_CAP         = 8          # outstanding tokens across all nodes
    [!] ./src/SelfObserver.jl:269:# `payload` is symbol-or-string keyed for serialization friendliness.
    [!] ./src/SelfObserver.jl:273:    key::String
    [!] ./src/SelfObserver.jl:279:    drop_table::Vector{String}      # per-entry associated keys (moment-specific)
    [!] ./src/SelfObserver.jl:282:function Microlog(key::String, tag::Symbol, payload::Dict{String,Any},
    [!] ./src/SelfObserver.jl:293:    return Microlog(key, tag, payload, weight, time(), provenance,
    [!] ./src/SelfObserver.jl:301:    key::String
    [!] ./src/SelfObserver.jl:305:    payload_keys::Vector{String} # surface only the keys, not the raw values
    [!] ./src/SelfObserver.jl:308:    associations::Vector{String} # related keys from drop-table walk
    [!] ./src/SelfObserver.jl:311:# GRUG: per-node token bucket. One per caller node id.
    [!] ./src/SelfObserver.jl:314:    tokens::Float64              # INTERNAL — never returned
    [!] ./src/SelfObserver.jl:325:    table::Dict{String, Vector{Microlog}}     # key → fragments
    [!] ./src/SelfObserver.jl:326:    drop_tables::Dict{String, Vector{String}} # key → associated keys (per-key, stable)
    [!] ./src/SelfObserver.jl:331:    buckets::Dict{String, TokenBucket}         # per-node-id token buckets
    [!] ./src/SelfObserver.jl:333:    global_outstanding::Atomic{Int}            # global cap on in-flight read tokens
    [!] ./src/SelfObserver.jl:339:    n_evictions_per_key::Atomic{Int}
    [!] ./src/SelfObserver.jl:375:# GRUG: tokenize a key/query for token-overlap matching.
    [!] ./src/SelfObserver.jl:377:function _tokenize(s::AbstractString)::Set{String}
    [!] ./src/SelfObserver.jl:400:# GRUG: refill a token bucket based on elapsed wall time.
    [!] ./src/SelfObserver.jl:401:# Internal-only; tokens never leave the store.
    [!] ./src/SelfObserver.jl:407:        bucket.tokens = min(Float64(bucket.capacity), bucket.tokens + gain)
    [!] ./src/SelfObserver.jl:413:# GRUG: try to consume one token for the given node id. Returns true on success.
    [!] ./src/SelfObserver.jl:432:        if b.tokens < 1.0
    [!] ./src/SelfObserver.jl:436:        b.tokens -= 1.0
    [!] ./src/SelfObserver.jl:444:# GRUG: release one outstanding token (decrement global cap counter only).
    [!] ./src/SelfObserver.jl:445:# Per-node bucket does NOT refund — the read happened, token spent.
    [!] ./src/SelfObserver.jl:499:# Same (key, query_id) → same boundary set. Different query_ids → independent jitter.
    [!] ./src/SelfObserver.jl:500:function _jittered_boundaries(key::String, query_id::String)
    [!] ./src/SelfObserver.jl:501:    # GRUG: hash-derived seed so fuzz is reproducible per (key, query_id) pair.
    [!] ./src/SelfObserver.jl:502:    seed = UInt64(hash((key, query_id)))
    [!] ./src/SelfObserver.jl:519:    boundaries = _jittered_boundaries(ml.key, query_id)
    [!] ./src/SelfObserver.jl:549:        ml.key, ml.tag, fuzzy_when, ml.provenance,
    [!] ./src/SelfObserver.jl:550:        sort!(collect(keys(ml.payload))),
    [!] ./src/SelfObserver.jl:597:    observe!(store, key, tag, payload; p_write=DEFAULT_P_WRITE,
    [!] ./src/SelfObserver.jl:603:- `key::String`            — concept anchor; non-empty.
    [!] ./src/SelfObserver.jl:609:- `drop_table`             — moment-specific co-activated keys for this entry.
    [!] ./src/SelfObserver.jl:613:programmer errors (bad tag, empty key, out-of-range salience).
    [!] ./src/SelfObserver.jl:615:function observe!(store::SubconsciousStore, key::String, tag::Symbol,
    [!] ./src/SelfObserver.jl:622:    if isempty(key)
    [!] ./src/SelfObserver.jl:623:        throw(SelfObserverArgumentError("key must be non-empty", "key"))
    [!] ./src/SelfObserver.jl:654:        bucket = get!(store.table, key) do
    [!] ./src/SelfObserver.jl:666:                # Merge new payload keys (additive, not destructive).
    [!] ./src/SelfObserver.jl:681:            ml = Microlog(key, tag, copy(payload), salience, provenance,
    [!] ./src/SelfObserver.jl:686:            # Per-key cap eviction.
    [!] ./src/SelfObserver.jl:690:                atomic_add!(store.n_evictions_per_key, 1)
    [!] ./src/SelfObserver.jl:694:            # entry across ALL keys. O(N) worst case but bounded by 4096.
    [!] ./src/SelfObserver.jl:701:        # Per-key drop table update (stable concept-shape associations).
    [!] ./src/SelfObserver.jl:703:            pk = get!(store.drop_tables, key) do
    [!] ./src/SelfObserver.jl:721:    worst_key = ""
    [!] ./src/SelfObserver.jl:731:                worst_key = k
    [!] ./src/SelfObserver.jl:740:    splice!(store.table[worst_key], worst_idx)
    [!] ./src/SelfObserver.jl:741:    if isempty(store.table[worst_key])
    [!] ./src/SelfObserver.jl:742:        delete!(store.table, worst_key)
    [!] ./src/SelfObserver.jl:753:    peek_exact(store, node_id, key; tag=nothing, max_entries=MAX_ENTRIES_PER_PEEK,
    [!] ./src/SelfObserver.jl:756:Look up exact-key fragments. Returns `Vector{SubconsciousHint}` or `nothing`.
    [!] ./src/SelfObserver.jl:759:- the per-node token bucket is empty,
    [!] ./src/SelfObserver.jl:762:- the key has no entries.
    [!] ./src/SelfObserver.jl:767:function peek_exact(store::SubconsciousStore, node_id::String, key::String;
    [!] ./src/SelfObserver.jl:773:    if isempty(key)
    [!] ./src/SelfObserver.jl:774:        throw(SelfObserverArgumentError("key must be non-empty", "key"))
    [!] ./src/SelfObserver.jl:797:        bucket = get(store.table, key, nothing)
    [!] ./src/SelfObserver.jl:821:        # Build hints. Per-key drop table provides associations.
    [!] ./src/SelfObserver.jl:822:        per_key_drops = get(store.drop_tables, key, String[])
    [!] ./src/SelfObserver.jl:828:            append!(assoc, per_key_drops)
    [!] ./src/SelfObserver.jl:848:1. Token-overlap: tokenize `query`, score each stored key by the count of
    [!] ./src/SelfObserver.jl:849:   shared tokens (tied with stored microlog weight*recency).
    [!] ./src/SelfObserver.jl:850:2. Drop-table walk: starting from the best-overlap keys, walk per-key drop
    [!] ./src/SelfObserver.jl:889:        q_tokens = _tokenize(query)
    [!] ./src/SelfObserver.jl:890:        if isempty(q_tokens)
    [!] ./src/SelfObserver.jl:896:        # GRUG: pass 1 — token overlap over keys.
    [!] ./src/SelfObserver.jl:897:        seed_keys = String[]   # keys that survived overlap floor
    [!] ./src/SelfObserver.jl:898:        # collect (key, overlap_count, best_score)
    [!] ./src/SelfObserver.jl:901:            k_tokens = _tokenize(k)
    [!] ./src/SelfObserver.jl:902:            shared = length(intersect(q_tokens, k_tokens))
    [!] ./src/SelfObserver.jl:920:                push!(seed_keys, k)
    [!] ./src/SelfObserver.jl:924:        # GRUG: pass 2 — drop-table walk from seed keys.
    [!] ./src/SelfObserver.jl:925:        # Collected as Dict{String, Float64} key→discount factor (max if seen via
    [!] ./src/SelfObserver.jl:927:        walk_keys = Dict{String, Float64}()
    [!] ./src/SelfObserver.jl:929:            frontier = Set{String}(seed_keys)
    [!] ./src/SelfObserver.jl:930:            visited = Set{String}(seed_keys)
    [!] ./src/SelfObserver.jl:940:                        # Only count keys that actually have entries.
    [!] ./src/SelfObserver.jl:941:                        if !haskey(store.table, assoc)
    [!] ./src/SelfObserver.jl:944:                        cur = get(walk_keys, assoc, 0.0)
    [!] ./src/SelfObserver.jl:946:                            walk_keys[assoc] = discount
    [!] ./src/SelfObserver.jl:962:        seen_pairs = Set{Tuple{String, Int}}()  # (key, ml index) dedupe
    [!] ./src/SelfObserver.jl:976:        for (k, disc) in walk_keys
    [!] ./src/SelfObserver.jl:1032:        :evictions_per_key         => store.n_evictions_per_key[],
    [!] ./src/SelfObserver.jl:1042:        :keys                      => length(store.table),
    [!] ./src/SelfObserver.jl:1043:        :outstanding_tokens        => store.global_outstanding[],
    [!] ./src/SelfObserver.jl:1056:    store.n_evictions_per_key[] = 0
    [!] ./src/SelfObserver.jl:1101:    key_count(store) -> Int
    [!] ./src/SelfObserver.jl:1103:Number of distinct keys with at least one microlog. Integer.
    [!] ./src/SelfObserver.jl:1105:key_count(store::SubconsciousStore)::Int = length(store.table)
    [!] ./src/SelfObserver.jl:1108:    drop_keys_by_prefix!(store, prefix::AbstractString) -> Int
    [!] ./src/SelfObserver.jl:1110:Remove all entries whose key starts with `prefix`. Returns count of keys dropped.
    [!] ./src/SelfObserver.jl:1114:function drop_keys_by_prefix!(store::SubconsciousStore, prefix::AbstractString)::Int
    [!] ./src/SelfObserver.jl:1118:        # Find keys matching prefix
    [!] ./src/SelfObserver.jl:1119:        keys_to_drop = String[]
    [!] ./src/SelfObserver.jl:1120:        for k in keys(store.table)
    [!] ./src/SelfObserver.jl:1122:                push!(keys_to_drop, k)
    [!] ./src/SelfObserver.jl:1126:        for k in keys_to_drop
    [!] ./src/SelfObserver.jl:1132:        # Clean drop_tables: remove references to dropped keys
    [!] ./src/SelfObserver.jl:1133:        if !isempty(keys_to_drop)
    [!] ./src/SelfObserver.jl:1134:            drop_set = Set(keys_to_drop)
    [!] ./src/SelfObserver.jl:1135:            for (dt_key, dt_vec) in store.drop_tables
    [!] ./src/SelfObserver.jl:1139:            empty_dt_keys = [k for (k, v) in store.drop_tables if isempty(v)]
    [!] ./src/SelfObserver.jl:1140:            for k in empty_dt_keys
    [!] ./src/Main.jl:24:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/Main.jl:28:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/Main.jl:32:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/Main.jl:321:# we score each message's relevance to the current user input (lexical token
    [!] ./src/Main.jl:340:const CONTEXT_RELEVANCE_LEX_W    = 0.6    # Lexical token-overlap weight
    [!] ./src/Main.jl:516:# Format: Dict with keys "topic" (what was asked about), "asked_at" (time()),
    [!] ./src/Main.jl:554:# GRUG: Default admin password. CHANGE THIS BEFORE DEPLOYMENT!
    [!] ./src/Main.jl:555:# To set custom password: set ADMIN_PASSWORD_HASH = bytes2hex(sha256("your_password"))
    [!] ./src/Main.jl:558:# GRUG: SHA256 hash of default password. Computed at module load.
    [!] ./src/Main.jl:600:    admin_login(password::String)::Tuple{Bool, String}
    [!] ./src/Main.jl:602:GRUG: Attempt admin login with provided password.
    [!] ./src/Main.jl:606:function admin_login(password::String)::Tuple{Bool, String}
    [!] ./src/Main.jl:607:    if strip(password) == ""
    [!] ./src/Main.jl:611:    # GRUG: Hash the provided password and compare
    [!] ./src/Main.jl:612:    provided_hash = bytes2hex(sha256(password))
    [!] ./src/Main.jl:620:            # GRUG: Log failed attempt (but don't expose password hash)
    [!] ./src/Main.jl:622:            return (false, "⛔ Invalid password. Access denied.")
    [!] ./src/Main.jl:747:            for (key, value) in new_data
    [!] ./src/Main.jl:748:                if haskey(existing, key)
    [!] ./src/Main.jl:750:                    if isa(existing[key], AbstractDict) && isa(value, AbstractDict)
    [!] ./src/Main.jl:753:                            existing[key][k] = v
    [!] ./src/Main.jl:755:                    elseif isa(existing[key], Vector) && isa(value, Vector)
    [!] ./src/Main.jl:757:                        append!(existing[key], value)
    [!] ./src/Main.jl:760:                        existing[key] = value
    [!] ./src/Main.jl:763:                    # GRUG: New key - just add
    [!] ./src/Main.jl:764:                    existing[key] = value
    [!] ./src/Main.jl:921:_tokenize_for_relevance(text::String)::Set{String}
    [!] ./src/Main.jl:923:GRUG v7.12: Lowercased whitespace-split token set used for the lexical
    [!] ./src/Main.jl:924:half of the message/user relevance score. Short (<3 char) tokens are
    [!] ./src/Main.jl:927:function _tokenize_for_relevance(text::String)::Set{String}
    [!] ./src/Main.jl:953:    to_key(t) = string(t.subject, "|", t.relation, "|", t.object)
    [!] ./src/Main.jl:954:    a = Set(to_key(t) for t in mission_triples)
    [!] ./src/Main.jl:955:    b = Set(to_key(t) for t in msg_triples)
    [!] ./src/Main.jl:1013:Returns vector of (sigil_name, token, sigil_class, lobe_hint) quads.
    [!] ./src/Main.jl:1043:    # e.g. "quantum is a &noun" -> sigil=noun, token=quantum
    [!] ./src/Main.jl:1045:        token = m.captures[1]
    [!] ./src/Main.jl:1047:        if haskey(_known_sigils, sig_name)
    [!] ./src/Main.jl:1048:            push!(results, (sig_name, token, _known_sigils[sig_name], _lobe_hint))
    [!] ./src/Main.jl:1054:        token = m.captures[2]
    [!] ./src/Main.jl:1055:        if haskey(_known_sigils, sig_name)
    [!] ./src/Main.jl:1056:            push!(results, (sig_name, token, _known_sigils[sig_name], _lobe_hint))
    [!] ./src/Main.jl:1062:        token = m.captures[2]
    [!] ./src/Main.jl:1063:        if haskey(_known_sigils, sig_name)
    [!] ./src/Main.jl:1064:            push!(results, (sig_name, token, _known_sigils[sig_name], _lobe_hint))
    [!] ./src/Main.jl:1491:                        # Single word — check if it's a known subject/lobe keyword
    [!] ./src/Main.jl:1808:    score_message_relevance(msg, user_tokens, user_triples)::Float64
    [!] ./src/Main.jl:1811:  * lexical token overlap (Jaccard of cleaned tokens)
    [!] ./src/Main.jl:1824:                                 user_tokens::Set{String},
    [!] ./src/Main.jl:1826:    msg_tokens = _tokenize_for_relevance(msg.text)
    [!] ./src/Main.jl:1827:    lex = if isempty(msg_tokens) || isempty(user_tokens)
    [!] ./src/Main.jl:1830:        inter = length(intersect(msg_tokens, user_tokens))
    [!] ./src/Main.jl:1831:        uni = length(union(msg_tokens, user_tokens))
    [!] ./src/Main.jl:1883:    user_tokens = _tokenize_for_relevance(user_input)
    [!] ./src/Main.jl:1906:                relevance = score_message_relevance(m, user_tokens, user_triples)
    [!] ./src/Main.jl:2336:    # user intent, not just token overlap.
    [!] ./src/Main.jl:2357:    # in different groups doesn't overwrite. Old key was just node_id, which
    [!] ./src/Main.jl:2420:                user_keys = Set((t.subject, t.relation, t.object) for t in v.user_triples)
    [!] ./src/Main.jl:2421:                node_keys = Set((t.subject, t.relation, t.object) for t in v.node_triples)
    [!] ./src/Main.jl:2422:                shared = length(intersect(user_keys, node_keys))
    [!] ./src/Main.jl:2423:                clamp(shared / max(1, length(user_keys)), 0.0, 1.0)
    [!] ./src/Main.jl:2456:            _peak_base_map = if !isempty(_vote_group) && haskey(_group_lobe_base_maps, _vote_group)
    [!] ./src/Main.jl:2461:            peak_dom = if !isnothing(node_lobe) && haskey(_peak_base_map, node_lobe)
    [!] ./src/Main.jl:2632:    # GRUG v8.26g: Translate candidates back to Votes using composite key.
    [!] ./src/Main.jl:2633:    sure_votes   = Vote[candidate_to_vote[(vc.node_id, vc.multipart_group)] for vc in top_tier if haskey(candidate_to_vote, (vc.node_id, vc.multipart_group))]
    [!] ./src/Main.jl:2634:    unsure_votes = Vote[candidate_to_vote[(vc.node_id, vc.multipart_group)] for vc in subtop_tier if haskey(candidate_to_vote, (vc.node_id, vc.multipart_group))]
    [!] ./src/Main.jl:2653:            if !haskey(_group_primaries, grp) || v.confidence > _group_primaries[grp].confidence
    [!] ./src/Main.jl:2676:                _dup_key(v) = (getfield(v, :multipart_group), v.action)
    [!] ./src/Main.jl:2677:                _dup_pick = Vote[v for v in tied_votes if haskey(_dup_winner_map, _dup_key(v)) && _dup_winner_map[_dup_key(v)] == v.node_id]
    [!] ./src/Main.jl:2738:        nonwinner_votes = Vote[candidate_to_vote[(vc.node_id, vc.multipart_group)] for vc in rejected_tier if haskey(candidate_to_vote, (vc.node_id, vc.multipart_group))]
    [!] ./src/Main.jl:2801:            # This is the key fix: COMMANDS no longer receives the full vote pile.
    [!] ./src/Main.jl:2868:memory into a single payload. Throws on missing context keys — NO SILENT FAILURES.
    [!] ./src/Main.jl:2871:    if !haskey(context, "system_prompt")
    [!] ./src/Main.jl:3160:        if haskey(Thesaurus.SYNONYM_SEED_MAP, clean)
    [!] ./src/Main.jl:3176:            if haskey(sv_map, clean)
    [!] ./src/Main.jl:3255:        # time the same node won. 0.35 means ~1 in 3 swappable tokens gets a
    [!] ./src/Main.jl:3282:    # token at SWAP_RATE, this only considers words that have RICH synonym
    [!] ./src/Main.jl:3307:        tokens = split(String(sentence))
    [!] ./src/Main.jl:3309:        for tok in tokens
    [!] ./src/Main.jl:3326:            n_syns = haskey(Thesaurus.SYNONYM_SEED_MAP, clean) ? length(Thesaurus.SYNONYM_SEED_MAP[clean]) : 0
    [!] ./src/Main.jl:3394:        tokens = split(String(sentence))
    [!] ./src/Main.jl:3396:        for tok in tokens
    [!] ./src/Main.jl:3414:            n_syns = haskey(Thesaurus.SYNONYM_SEED_MAP, clean) ? length(Thesaurus.SYNONYM_SEED_MAP[clean]) : 0
    [!] ./src/Main.jl:3467:    # locked-in vote words. Instead of scanning every token with a swap
    [!] ./src/Main.jl:3502:        tokens = split(String(sentence))
    [!] ./src/Main.jl:3504:        # ── Step 1: Identify which token positions are eligible for swap ──
    [!] ./src/Main.jl:3505:        # A token is eligible if:
    [!] ./src/Main.jl:3514:        for (j, tok) in enumerate(tokens)
    [!] ./src/Main.jl:3533:            if !haskey(Thesaurus.SYNONYM_SEED_MAP, clean)
    [!] ./src/Main.jl:3597:        out_tokens = collect(String.(tokens))
    [!] ./src/Main.jl:3599:            tok = out_tokens[idx]
    [!] ./src/Main.jl:3608:            out_tokens[idx] = "$(prefix)$(picked)$(suffix)"
    [!] ./src/Main.jl:3610:        result = join(out_tokens, " ")
    [!] ./src/Main.jl:3799:    # token's case via a simple heuristic: if the original starts with
    [!] ./src/Main.jl:3806:        # the separators as tokens (unlike Python's re.split with a
    [!] ./src/Main.jl:3808:        # word tokens, route each through _pick_synonym, and rejoin
    [!] ./src/Main.jl:3812:        tokens = split(sentence)  # splits on any whitespace, drops empties
    [!] ./src/Main.jl:3813:        out_tokens = String[]
    [!] ./src/Main.jl:3814:        for j in 1:length(tokens)
    [!] ./src/Main.jl:3815:            tok = tokens[j]
    [!] ./src/Main.jl:3820:                push!(out_tokens, String(tok))
    [!] ./src/Main.jl:3828:            # "hard" in "hard won" should stay. Check if next token looks
    [!] ./src/Main.jl:3831:            if j < length(tokens)
    [!] ./src/Main.jl:3832:                _next_tok = String(tokens[j + 1])
    [!] ./src/Main.jl:3847:            push!(out_tokens, picked * tail)
    [!] ./src/Main.jl:3849:        return join(out_tokens, " ")
    [!] ./src/Main.jl:3863:    #   - Only fires when ≥2 segments AND total tokens ≥4 (single short
    [!] ./src/Main.jl:3893:        # Need at least 2 segments AND a total token count ≥4 to bother.
    [!] ./src/Main.jl:4019:        _has_frame_hints  = haskey(winning_node.json_data, "frame_hints") && !isempty(winning_node.json_data["frame_hints"])
    [!] ./src/Main.jl:4051:    # with the action-keyed skeleton this produced echoey, parrot-style
    [!] ./src/Main.jl:4066:    # pipeline. The action-keyed skeletons remain as a final fallback
    [!] ./src/Main.jl:4121:    # --- frame-keyed skeleton (Fix B) ----------------------------------
    [!] ./src/Main.jl:4164:    elseif haskey(_FRAME_SKELETON_POOLS, judged_frame_label) && !isempty(judged_frame_label)
    [!] ./src/Main.jl:4168:        # Fall back to action-keyed pool
    [!] ./src/Main.jl:4172:        pool_key = if action_is_prose_skel
    [!] ./src/Main.jl:4190:        raw = isempty(judged_frame_label) ? _ACTION_SKELETON_POOLS[pool_key][1] : _pick_from_pool_adaptive(_ACTION_SKELETON_POOLS[pool_key], pool_key)
    [!] ./src/Main.jl:4232:            # Only strip the {SUPPORT} token itself, preserving surrounding text
    [!] ./src/Main.jl:4239:            # last token — the claim itself already ends with punctuation.
    [!] ./src/Main.jl:4262:    #                            heuristic: action has ≥2 word tokens AND
    [!] ./src/Main.jl:4504:        # node_pattern. The pattern is the matcher key, not speech —
    [!] ./src/Main.jl:4518:        # still leak the matcher key into speech, but we only land
    [!] ./src/Main.jl:4537:    #            per-token scan, no swap rate — just targeted replacement
    [!] ./src/Main.jl:4544:    # Old approach (v7.36→v8.11) scanned every token with a swap rate
    [!] ./src/Main.jl:4546:    # wasteful — most tokens don't have synonyms. The new approach only
    [!] ./src/Main.jl:4575:        #            No per-token scan, no swap rate — just targeted replacement
    [!] ./src/Main.jl:4601:    # v7.24-BUG8: SKIP the triple when it's just pattern tokens echoed back.
    [!] ./src/Main.jl:4603:    # semantic value — those are the node's pattern keywords, not insight.
    [!] ./src/Main.jl:4604:    # Gate: at least one of (subject, object) must NOT be a token from
    [!] ./src/Main.jl:4616:        # OLD gate required ALL 3 tokens to be pattern/action tokens — too strict.
    [!] ./src/Main.jl:4619:        # NEW gate: if ≥2 of 3 tokens are pattern/action tokens, it's still a circular echo.
    [!] ./src/Main.jl:4620:        # A meaningful triple needs at least 2 tokens that carry NEW semantic information
    [!] ./src/Main.jl:4623:        pat_tokens = Set(split(lowercase(node_pattern)))
    [!] ./src/Main.jl:4624:        act_tokens = Set(split(lowercase(action_str)))
    [!] ./src/Main.jl:4628:        overlap_count = (t_subj in pat_tokens || t_subj in act_tokens ? 1 : 0) +
    [!] ./src/Main.jl:4629:                        (t_obj  in pat_tokens || t_obj  in act_tokens ? 1 : 0) +
    [!] ./src/Main.jl:4630:                        (t_rel  in pat_tokens || t_rel  in act_tokens ? 1 : 0)
    [!] ./src/Main.jl:4633:        # A triple that shares ZERO tokens with the user's input is tangential
    [!] ./src/Main.jl:4636:        # The triple must share at least one non-stopword token with the input
    [!] ./src/Main.jl:4638:        _input_tokens = Set(split(lowercase(mission)))
    [!] ./src/Main.jl:4639:        _triple_tokens = Set([t_subj, t_obj, t_rel])
    [!] ./src/Main.jl:4652:        _input_content = setdiff(_input_tokens, _stopwords_triple)
    [!] ./src/Main.jl:4653:        _triple_content = setdiff(_triple_tokens, _stopwords_triple)
    [!] ./src/Main.jl:4656:            # v7.38: Dereference any surviving sigil tokens in triple
    [!] ./src/Main.jl:4673:            # GRUG v8.19: Fallback for any remaining &token → strip & prefix.
    [!] ./src/Main.jl:4709:            # If so, the operator seeded pattern-keyword triples that carry
    [!] ./src/Main.jl:4716:                _ov = (_s in pat_tokens || _s in act_tokens ? 1 : 0) +
    [!] ./src/Main.jl:4717:                      (_o in pat_tokens || _o in act_tokens ? 1 : 0) +
    [!] ./src/Main.jl:4718:                      (_r in pat_tokens || _r in act_tokens ? 1 : 0)
    [!] ./src/Main.jl:4723:                   Pattern = \"$(node_pattern)\" — the triples are just pattern keywords rearranged.
    [!] ./src/Main.jl:4726:                   not just re-state its trigger keywords. E.g. for a "fire" node, add
    [!] ./src/Main.jl:4861:    # to the mission. Requires ≥1 shared non-stopword token between
    [!] ./src/Main.jl:4868:        mission_tokens = Set(_tokenize_for_relevance(mission))
    [!] ./src/Main.jl:4869:        if !isempty(mission_tokens)
    [!] ./src/Main.jl:4875:                        pin_tokens = Set(_tokenize_for_relevance(m.text))
    [!] ./src/Main.jl:4876:                        if !isempty(intersect(mission_tokens, pin_tokens))
    [!] ./src/Main.jl:5261:            if haskey(COMMANDS, action_name)
    [!] ./src/Main.jl:5294:    _pat_key = lowercase(strip(pattern_text))
    [!] ./src/Main.jl:5295:    if !is_antimatch && haskey(ans_data, "growth_source") &&
    [!] ./src/Main.jl:5301:                if lowercase(strip(other_node.pattern)) == _pat_key
    [!] ./src/Main.jl:5310:                            CaveJournal.cave_print("[MAIN] ⚰  Node '$other_id' (pattern='$(_pat_key)') graved: superseded by hippocampal answer node '$nid'"; tag="MAIN", emoji="⚡")
    [!] ./src/Main.jl:5327:            _ask_key = lowercase(strip(_resolved_ask))
    [!] ./src/Main.jl:5333:                    if other_pat == _ask_key
    [!] ./src/Main.jl:5369:    if !is_antimatch && !skip_auto_latch && haskey(NODE_MAP, nid)
    [!] ./src/Main.jl:5421:        if !haskey(_LOBE_DICTIONARIES, lobe_id)
    [!] ./src/Main.jl:5444:            if haskey(_LOBE_DICTIONARIES, lobe_hint)
    [!] ./src/Main.jl:5451:            if haskey(dict, _w)
    [!] ./src/Main.jl:5469:    tokens = split(lowercase(strip(mission_text)), r"[\s,.;!?]+")
    [!] ./src/Main.jl:5470:    tokens = filter(t -> length(t) > 2, tokens)  # skip tiny words
    [!] ./src/Main.jl:5472:        for t in tokens
    [!] ./src/Main.jl:5474:                if haskey(dict, t)
    [!] ./src/Main.jl:5511:&define sigil's promote_predicate to decide whether a token should be
    [!] ./src/Main.jl:5519:            if haskey(dict, _w)
    [!] ./src/Main.jl:5538:            for w in keys(dict)
    [!] ./src/Main.jl:5555:        return haskey(_LOBE_DICTIONARIES, lobe_id) && haskey(_LOBE_DICTIONARIES[lobe_id], _w)
    [!] ./src/Main.jl:5571:            if haskey(dict, _w)
    [!] ./src/Main.jl:5659:    # Scoring: pattern token overlap + noun_anchor overlap + content match bonus.
    [!] ./src/Main.jl:5668:    _topic_tokens = Set(split(_t, r"[\s_]+"))
    [!] ./src/Main.jl:5669:    filter!(w -> length(w) > 2, _topic_tokens)
    [!] ./src/Main.jl:5671:    # GRUG: Also build topic tokens WITHOUT articles stripped,
    [!] ./src/Main.jl:5673:    _topic_tokens_raw = Set(split(lowercase(strip(topic)), r"[\s_]+"))
    [!] ./src/Main.jl:5674:    filter!(w -> length(w) > 2, _topic_tokens_raw)
    [!] ./src/Main.jl:5684:            _pat_tokens = Set(split(lowercase(strip(_node.pattern)), r"[\s_]+"))
    [!] ./src/Main.jl:5685:            filter!(w -> length(w) > 2, _pat_tokens)
    [!] ./src/Main.jl:5688:            if isempty(_pat_tokens) || isempty(_topic_tokens)
    [!] ./src/Main.jl:5691:            _inter = length(intersect(_pat_tokens, _topic_tokens))
    [!] ./src/Main.jl:5692:            _uni = length(union(_pat_tokens, _topic_tokens))
    [!] ./src/Main.jl:5695:            # Also try against raw topic tokens
    [!] ./src/Main.jl:5696:            if !isempty(_topic_tokens_raw)
    [!] ./src/Main.jl:5697:                _inter_r = length(intersect(_pat_tokens, _topic_tokens_raw))
    [!] ./src/Main.jl:5698:                _uni_r = length(union(_pat_tokens, _topic_tokens_raw))
    [!] ./src/Main.jl:5706:                _anchor_tokens = Set(lowercase.(filter(w -> length(w) > 2, _anchors)))
    [!] ./src/Main.jl:5707:                if !isempty(_anchor_tokens)
    [!] ./src/Main.jl:5708:                    _a_inter = length(intersect(_anchor_tokens, _topic_tokens))
    [!] ./src/Main.jl:5709:                    _a_uni = length(union(_anchor_tokens, _topic_tokens))
    [!] ./src/Main.jl:5717:            # knowledge the user taught, not just pattern keywords.
    [!] ./src/Main.jl:6005:    _subj_stemmed = Thesaurus.stem_token(lowercase(_t))
    [!] ./src/Main.jl:6006:    _obj_stemmed = Thesaurus.stem_token(lowercase(_obj))
    [!] ./src/Main.jl:6048:    _tokens = split(_s, r"[\s,;:]+") |> xs -> filter(t -> length(t) > 1, xs) |> collect
    [!] ./src/Main.jl:6050:    if isempty(_tokens)
    [!] ./src/Main.jl:6054:    # GRUG: Check each lobe's whitelist AND subject field against the input tokens.
    [!] ./src/Main.jl:6055:    # Score by how many tokens match. The most specific lobe wins.
    [!] ./src/Main.jl:6065:            for _token in _tokens
    [!] ./src/Main.jl:6067:                    if occursin(_wl_entry, _token) || occursin(_token, _wl_entry)
    [!] ./src/Main.jl:6069:                        break  # one match per token is enough
    [!] ./src/Main.jl:6075:        # Check subject field (tokenized keyword matching)
    [!] ./src/Main.jl:6076:        _subj_tokens = Set(split(lowercase(_rec.subject), r"[\s,;:]+"))
    [!] ./src/Main.jl:6077:        for _token in _tokens
    [!] ./src/Main.jl:6078:            if _token in _subj_tokens
    [!] ./src/Main.jl:6082:            for _st in _subj_tokens
    [!] ./src/Main.jl:6083:                if length(_st) > 2 && length(_token) > 2 &&
    [!] ./src/Main.jl:6084:                   (occursin(_st, _token) || occursin(_token, _st))
    [!] ./src/Main.jl:6314:This is the key primitive that lets "grug knows part, ask only for what he
    [!] ./src/Main.jl:6381:    # its most "contentful" token (longest word), since synonym_lookup/word_similarity
    [!] ./src/Main.jl:6494:#   1. Noun-anchor extraction — key nouns become standalone patterns
    [!] ./src/Main.jl:6538:    # Extract content tokens (not stopwords) and build question forms.
    [!] ./src/Main.jl:6539:    # "fire is hot" → content tokens = ["fire", "hot"]
    [!] ./src/Main.jl:6541:    tokens = split(lowercase(strip(content)))
    [!] ./src/Main.jl:6542:    content_tokens = filter(t -> !(t in STOPWORDS) && length(t) >= 2, String.(tokens))
    [!] ./src/Main.jl:6544:    if !isempty(content_tokens)
    [!] ./src/Main.jl:6545:        # GRUG: Use the FIRST content token as the primary question target.
    [!] ./src/Main.jl:6547:        primary_noun = content_tokens[1]
    [!] ./src/Main.jl:6563:        # GRUG: If there are 2+ content tokens, also make a pattern from
    [!] ./src/Main.jl:6564:        # the last content token (often the object/complement).
    [!] ./src/Main.jl:6567:        if length(content_tokens) >= 3
    [!] ./src/Main.jl:6568:            last_content = content_tokens[end]
    [!] ./src/Main.jl:6579:    if !isempty(content_tokens)
    [!] ./src/Main.jl:6581:        for tok in content_tokens
    [!] ./src/Main.jl:6584:                # Replace the token with its synonym in the original content
    [!] ./src/Main.jl:6589:                    break  # one synonym swap per token is enough
    [!] ./src/Main.jl:6707:        if haskey(ans_data, "noun_anchors")
    [!] ./src/Main.jl:6710:        if haskey(ans_data, "required_relations")
    [!] ./src/Main.jl:6713:        if haskey(ans_data, "resolved_ask")
    [!] ./src/Main.jl:6723:        if haskey(NODE_MAP, nid)
    [!] ./src/Main.jl:6732:    if haskey(NODE_MAP, primary_id)
    [!] ./src/Main.jl:6743:                if haskey(NODE_MAP, sid)
    [!] ./src/Main.jl:6804:#   1. Pattern-related (Jaccard token overlap > _AUTO_LATCH_PAT_FLOOR)
    [!] ./src/Main.jl:6842:            # GRUG: Pattern similarity — Jaccard token overlap with group centroid.
    [!] ./src/Main.jl:6843:            pat_sim = _token_overlap_similarity(pattern, grp.centroid_pattern)
    [!] ./src/Main.jl:7037:            score = mn.strength * _token_overlap_similarity(node.pattern, mn.pattern)
    [!] ./src/Main.jl:7045:    if !isnothing(best_member_id) && haskey(NODE_MAP, best_member_id)
    [!] ./src/Main.jl:7419:║      promote=true         Enable tokenizer-level promotion  ║
    [!] ./src/Main.jl:7443:║  /mlpRule add <pat> <t> <key> Add rule to MLP hash table      ║
    [!] ./src/Main.jl:7473:║    Seam tokens = unmatched tail at match boundary           ║
    [!] ./src/Main.jl:7598:║  ADMIN COMMANDS (password protected)                         ║
    [!] ./src/Main.jl:7599:║  /login <password>           Authenticate as admin           ║
    [!] ./src/Main.jl:7785:        "Hey — the key thing: {JOIN}",
    [!] ./src/Main.jl:7912:            "output_";           # key prefix to match output observations
    [!] ./src/Main.jl:7927:            if haskey(ps, "connector_used")
    [!] ./src/Main.jl:7931:            if haskey(ps, "frame")
    [!] ./src/Main.jl:7935:            if haskey(ps, "skeleton_used")
    [!] ./src/Main.jl:8105:            for (_sig_name, _token, _sig_class, _sig_lobe_hint) in _inv_directives
    [!] ./src/Main.jl:8106:                InverseSigil.user_add_concrete!(_sig_name, _token; sigil_class=_sig_class,
    [!] ./src/Main.jl:8125:                # have concretes matching content tokens. This is targeted, not broadcast.
    [!] ./src/Main.jl:8126:                _content_tokens = split(lowercase(strip(la.content)), r"[\s_]+")
    [!] ./src/Main.jl:8127:                filter!(!isempty, _content_tokens)
    [!] ./src/Main.jl:8128:                InverseSigil.set_lobe_hints_for_tokens!(_content_tokens, la.lobe_name)
    [!] ./src/Main.jl:8233:                    # topic, but the dictionary key is just "stalactite".
    [!] ./src/Main.jl:8290:                    # The routing judge detected arithmetic tokens/sigils in the input
    [!] ./src/Main.jl:8472:                        if !isempty(_new_lobe_id) && !haskey(Lobe.LOBE_REGISTRY, _new_lobe_id)
    [!] ./src/Main.jl:8477:                        elseif haskey(Lobe.LOBE_REGISTRY, _new_lobe_id)
    [!] ./src/Main.jl:8523:                            _target_lobe_obj = haskey(Lobe.LOBE_REGISTRY, _target_lobe_id) ? _target_lobe_id : nothing
    [!] ./src/Main.jl:8578:                            if haskey(Lobe.LOBE_REGISTRY, _target_lobe_id)
    [!] ./src/Main.jl:8613:                            if haskey(Lobe.LOBE_REGISTRY, _target_lobe_id)
    [!] ./src/Main.jl:8653:                                if haskey(NODE_MAP, _nid)
    [!] ./src/Main.jl:8660:                        if haskey(Lobe.LOBE_REGISTRY, _target_lobe_id)
    [!] ./src/Main.jl:8716:                    # (haskey check) silently dropped every definition when the
    [!] ./src/Main.jl:8738:                    _target_lobe = isempty(_conv_lobe_hint) ? nothing : (haskey(Lobe.LOBE_REGISTRY, _conv_lobe_hint) ? _conv_lobe_hint : nothing)
    [!] ./src/Main.jl:8905:    # tokens are appended to the mission text as a synthetic expansion layer,
    [!] ./src/Main.jl:8911:    # contains "markov mutagen") would miss the literal-token pre-gate because
    [!] ./src/Main.jl:8915:    # The expansion is APPENDED (not replaced), so original tokens are preserved.
    [!] ./src/Main.jl:8916:    # Expanded tokens are lowercase-only. Expansion is capped at 30 tokens to
    [!] ./src/Main.jl:8922:    # expansion dilution). Without this, expansion tokens like "constitutes",
    [!] ./src/Main.jl:8933:            # "deep"->"abyss" should not be injected into the scan token cloud
    [!] ./src/Main.jl:8935:            gate_tokens = Thesaurus.thesaurus_gate_filter(mission_text; is_blocked=InputQueue.is_synonym_blocked)
    [!] ./src/Main.jl:8936:            original_tokens = Set(split(lowercase(strip(mission_text))))
    [!] ./src/Main.jl:8937:            new_tokens = setdiff(gate_tokens, original_tokens)
    [!] ./src/Main.jl:8938:            if !isempty(new_tokens)
    [!] ./src/Main.jl:8940:                expansion_tokens = collect(new_tokens)
    [!] ./src/Main.jl:8941:                if length(expansion_tokens) > 30
    [!] ./src/Main.jl:8942:                    expansion_tokens = expansion_tokens[1:30]
    [!] ./src/Main.jl:8944:                expansion_str = join(expansion_tokens, " ")
    [!] ./src/Main.jl:8946:                capped_note = length(new_tokens) > 30 ? " (capped from $(length(new_tokens)))" : ""
    [!] ./src/Main.jl:8947:                @info "[MAIN] 🔬 Thesaurus gate expanded $(length(original_tokens)) tokens → $(length(gate_tokens)) (+$(length(new_tokens)) synonyms$(capped_note))"
    [!] ./src/Main.jl:9055:                    # GRUG v8.4: Use COMPOSITE keys only for chunk stashing.
    [!] ./src/Main.jl:9059:                    # stash under composite keys that include the multipart group
    [!] ./src/Main.jl:9240:                            push!(_ec_attach_snapshots, (target_id, join(br.seam_tokens, " "), br.is_crystalized))
    [!] ./src/Main.jl:9301:                    add_lobe_whitelist_fn      = (lobe_id, token) -> Lobe.add_lobe_whitelist!(lobe_id, token),
    [!] ./src/Main.jl:9441:        lobe_tokens = String[strip(t) for t in split(lowercase(mission_text)) if !isempty(strip(t))]
    [!] ./src/Main.jl:9442:        LobeOrchestrator.score_lobes(lobe_entries, Lobe.find_lobe_for_node; input_tokens=lobe_tokens)
    [!] ./src/Main.jl:9453:                grp_tokens = String[strip(t) for t in split(lowercase(grp)) if !isempty(strip(t))]
    [!] ./src/Main.jl:9454:                LobeOrchestrator.score_lobes(grp_entries, Lobe.find_lobe_for_node; input_tokens=grp_tokens)
    [!] ./src/Main.jl:9537:                token_overlap_fn = (id_a, id_b) -> begin
    [!] ./src/Main.jl:9543:                    _token_overlap_similarity(na.pattern, nb.pattern)
    [!] ./src/Main.jl:9577:        # GRUG: Hopfield cache is keyed by UInt64 hash — can't do substring match.
    [!] ./src/Main.jl:9583:            haskey(HOPFIELD_CACHE, _mlp_input_hash)
    [!] ./src/Main.jl:9834:                    # The disposition biases which keys get probed (keyword_hints),
    [!] ./src/Main.jl:9836:                    # drop-table walk weights (via the query tokens).
    [!] ./src/Main.jl:9838:                    if !isempty(disposition.keyword_hints)
    [!] ./src/Main.jl:9839:                        _biased_query = join(disposition.keyword_hints, " ") * " " * _biased_query
    [!] ./src/Main.jl:9860:                # subconscious keys and found entries. We format these as
    [!] ./src/Main.jl:9866:                            _fkey = get(_vig_f, "probe_key", "???")
    [!] ./src/Main.jl:9880:                                push!(_vig_findings_blocks, "[$_fkey] " * join(_entry_summaries, "; "))
    [!] ./src/Main.jl:9895:                                                "probe_key"   => get(_vig_f, "probe_key", ""),
    [!] ./src/Main.jl:9983:                        # GRUG FIX (v7.44): CascadeBridge has seam_tokens (not connector)
    [!] ./src/Main.jl:9985:                        push!(_ag_attach_snapshots, (target_id, join(br.seam_tokens, " "), br.is_crystalized))
    [!] ./src/Main.jl:10090:                add_lobe_whitelist_fn      = (lobe_id, token) -> Lobe.add_lobe_whitelist!(lobe_id, token),
    [!] ./src/Main.jl:10138:            # gaps, check if the input is a "petty" case — one uncovered token
    [!] ./src/Main.jl:10139:            # that's a synonym, simple math, or a domain token. These skip the
    [!] ./src/Main.jl:10166:                            lobe_whitelist_fn = (lobe_id, token) -> Lobe.add_lobe_whitelist!(lobe_id, token),
    [!] ./src/Main.jl:10202:                            add_lobe_whitelist_fn = (lobe_id, token) -> Lobe.add_lobe_whitelist!(lobe_id, token),
    [!] ./src/Main.jl:10216:            # from MLP signals + uncovered tokens. When it overflows, the system
    [!] ./src/Main.jl:10249:                    snap[nid] = [(br.partner_id, join(br.seam_tokens, " ")) for br in bridges]
    [!] ./src/Main.jl:10264:                    key = a < b ? (a, b) : (b, a)
    [!] ./src/Main.jl:10265:                    _al_co_occur[key] = c
    [!] ./src/Main.jl:10329:                bridge_fn             = (id_a, id_b; seam_tokens=String[]) ->
    [!] ./src/Main.jl:10330:                    bridge_nodes!(id_a, id_b; seam_tokens=seam_tokens),
    [!] ./src/Main.jl:10339:                    key = id_a < id_b ? (id_a, id_b) : (id_b, id_a)
    [!] ./src/Main.jl:10404:image scans — SDF signals don't have token positions).
    [!] ./src/Main.jl:10433:                # SDF signals don't have token positions, so no chunk resolution.
    [!] ./src/Main.jl:10531:    # GRUG: Build the specimen dict — one key per state category.
    [!] ./src/Main.jl:10563:                "hopfield_key"        => string(node.hopfield_key),  # UInt64 -> String for JSON safety
    [!] ./src/Main.jl:10579:    # GRUG: Serialize Hopfield fast-path cache keyed by UInt64 hash -> node ID list.
    [!] ./src/Main.jl:10594:    # saved from now on simply have no "rules" key. Old specimens that still
    [!] ./src/Main.jl:10596:    # backward-compat pattern as the removed AIMLNodeSystem's "aiml_system" key.
    [!] ./src/Main.jl:10602:    # CONTEXT_INTENSITY_BASELINE when key is missing (see /loadSpecimen below).
    [!] ./src/Main.jl:10690:    # BIAS_ADJUSTMENT deltas (keyed by intent kind, e.g. :question,
    [!] ./src/Main.jl:10800:    # direction and also write backward-compat "attachments" key for old loads.
    [!] ./src/Main.jl:10806:                pair_key = minmax(node_id, br.partner_id)
    [!] ./src/Main.jl:10807:                if pair_key in shown_pairs
    [!] ./src/Main.jl:10810:                push!(shown_pairs, pair_key)
    [!] ./src/Main.jl:10822:                    "seam_tokens"         => br.seam_tokens,
    [!] ./src/Main.jl:10838:        seam = bentry["seam_tokens"]
    [!] ./src/Main.jl:10873:                # GRUG BUG-010b: inhibition_tokens — semantic "don't do" set from alive originals + thesaurus.
    [!] ./src/Main.jl:10874:                "inhibition_tokens"  => collect(grp.inhibition_tokens),
    [!] ./src/Main.jl:10949:    # Academic: v2.1 is backward-compatible with v2.0 on load (new keys are optional).
    [!] ./src/Main.jl:10973:            "promote_at_tokenize" => entry.promote_at_tokenize
    [!] ./src/Main.jl:11076:    # The observer store's total_entries and key count are saved so the
    [!] ./src/Main.jl:11080:        "key_count"     => SelfObserver.key_count(_MLP_OBSERVER_STORE)
    [!] ./src/Main.jl:11616:                        if get(node.json_data, "time_node", false) && haskey(node.json_data, "time_orientation")
    [!] ./src/Main.jl:11907:    # GRUG: Allowed top-level keys
    [!] ./src/Main.jl:11908:    allowed_keys = Set(["nodes", "hopfield_cache", "rules", "message_history",
    [!] ./src/Main.jl:11948:    for key in keys(specimen)
    [!] ./src/Main.jl:11949:        if !(key in allowed_keys)
    [!] ./src/Main.jl:11950:            push!(validation_errors, "Unknown top-level key '$key'")
    [!] ./src/Main.jl:11956:        if haskey(specimen, k) && !isa(specimen[k], AbstractVector)
    [!] ./src/Main.jl:11974:        if haskey(specimen, k) && !_is_dict_like(specimen[k])
    [!] ./src/Main.jl:11980:    if haskey(specimen, "nodes") && isa(specimen["nodes"], AbstractVector)
    [!] ./src/Main.jl:11985:            elseif !haskey(nd, "id") || !haskey(nd, "pattern") || !haskey(nd, "action_packet")
    [!] ./src/Main.jl:12114:    # Specimen will bring its own, or defaults apply if key absent.
    [!] ./src/Main.jl:12119:    # Specimen will bring its own, or defaults apply if key absent.
    [!] ./src/Main.jl:12159:        EphemeralAutomaton._INJECTOR_STATS["total_probe_keys"] = 0
    [!] ./src/Main.jl:12181:    if haskey(specimen, "id_counters")
    [!] ./src/Main.jl:12183:        if haskey(idc, "node_id_counter")
    [!] ./src/Main.jl:12186:        if haskey(idc, "msg_id_counter")
    [!] ./src/Main.jl:12194:    if haskey(specimen, "last_voters") && isa(specimen["last_voters"], AbstractVector)
    [!] ./src/Main.jl:12206:    if haskey(specimen, "eye_state") && _is_dict_like(specimen["eye_state"])
    [!] ./src/Main.jl:12209:            if haskey(es, "attention_enabled")
    [!] ./src/Main.jl:12212:            if haskey(es, "blur_enabled")
    [!] ./src/Main.jl:12215:            if haskey(es, "last_centroid_x")
    [!] ./src/Main.jl:12218:            if haskey(es, "last_centroid_y")
    [!] ./src/Main.jl:12221:            if haskey(es, "last_arousal")
    [!] ./src/Main.jl:12232:    if haskey(specimen, "verb_registry")
    [!] ./src/Main.jl:12236:            if haskey(vr, "classes") && _is_dict_like(vr["classes"])
    [!] ./src/Main.jl:12244:            if haskey(vr, "synonyms") && _is_dict_like(vr["synonyms"])
    [!] ./src/Main.jl:12262:    # save/load. Missing key (old specimen files) is fine — leaves the
    [!] ./src/Main.jl:12265:    if haskey(specimen, "routing_bias_adjustments")
    [!] ./src/Main.jl:12291:    if haskey(specimen, "thesaurus_seeds") && _is_dict_like(specimen["thesaurus_seeds"])
    [!] ./src/Main.jl:12300:                # GRUG v8.26c: Only merge into keys the built-in map ALREADY has.
    [!] ./src/Main.jl:12301:                # If the built-in map doesn't have a key, the entry was intentionally
    [!] ./src/Main.jl:12303:                # Stale specimen entries must NOT resurrect removed keys.
    [!] ./src/Main.jl:12304:                if haskey(Thesaurus.SYNONYM_SEED_MAP, w)
    [!] ./src/Main.jl:12308:                # else: key was intentionally removed from built-in map; drop stale specimen entry
    [!] ./src/Main.jl:12317:    if haskey(specimen, "lobes") && isa(specimen["lobes"], AbstractVector)
    [!] ./src/Main.jl:12346:    if haskey(specimen, "lobe_tables") && isa(specimen["lobe_tables"], AbstractVector)
    [!] ./src/Main.jl:12352:                    if haskey(ltdata, "chunks") && _is_dict_like(ltdata["chunks"])
    [!] ./src/Main.jl:12397:    if haskey(specimen, "nodes") && isa(specimen["nodes"], AbstractVector)
    [!] ./src/Main.jl:12403:                    if haskey(nd, "relational_patterns") && isa(nd["relational_patterns"], AbstractVector)
    [!] ./src/Main.jl:12428:                        # match the pattern token count, causing BUG-004 mismatches
    [!] ./src/Main.jl:12429:                        # (signal length ≠ pattern token count → forced cheap scan
    [!] ./src/Main.jl:12455:                        # GRUG: If hopfield_key is 0 or missing, regenerate from pattern.
    [!] ./src/Main.jl:12456:                        # This handles specimens where hopfield_key was stripped after
    [!] ./src/Main.jl:12458:                        let hk_str = string(get(nd, "hopfield_key", "0"))
    [!] ./src/Main.jl:12495:    if haskey(specimen, "node_to_lobe_idx") && _is_dict_like(specimen["node_to_lobe_idx"])
    [!] ./src/Main.jl:12505:    if haskey(specimen, "hopfield_cache") && isa(specimen["hopfield_cache"], AbstractVector)
    [!] ./src/Main.jl:12527:    # specimens that still have a "rules" key are silently skipped here --
    [!] ./src/Main.jl:12529:    # "aiml_system" key. Nothing to restore.
    [!] ./src/Main.jl:12533:    if haskey(specimen, "inhibitions") && isa(specimen["inhibitions"], AbstractVector)
    [!] ./src/Main.jl:12561:    if haskey(specimen, "negative_thesaurus_pairs") && isa(specimen["negative_thesaurus_pairs"], AbstractVector)
    [!] ./src/Main.jl:12582:    if haskey(specimen, "message_history") && isa(specimen["message_history"], AbstractVector)
    [!] ./src/Main.jl:12586:                # Older specimens lack the key - default to BASELINE so the
    [!] ./src/Main.jl:12616:    if haskey(specimen, "arousal") && _is_dict_like(specimen["arousal"])
    [!] ./src/Main.jl:12628:    if haskey(specimen, "brainstem") && _is_dict_like(specimen["brainstem"])
    [!] ./src/Main.jl:12634:            if haskey(bs, "propagation_history") && isa(bs["propagation_history"], AbstractVector)
    [!] ./src/Main.jl:12654:    if haskey(specimen, "bridges") && isa(specimen["bridges"], AbstractVector)
    [!] ./src/Main.jl:12660:                    seam = String.(get(bentry, "seam_tokens", String[]))
    [!] ./src/Main.jl:12678:                            overlap = _token_overlap_similarity(join(seam, " "), ref_b.pattern)
    [!] ./src/Main.jl:12680:                            overlap_ba = _token_overlap_similarity(join(seam, " "), ref_a.pattern)
    [!] ./src/Main.jl:12701:    elseif haskey(specimen, "attachments") && isa(specimen["attachments"], AbstractVector)
    [!] ./src/Main.jl:12719:                            base_conf = _token_overlap_similarity(pat, attach_ref.pattern) + (attach_ref.strength / STRENGTH_CAP) * 0.5
    [!] ./src/Main.jl:12732:                    # GRUG v8.0: Upgrade to bidirectional — split pattern into seam tokens
    [!] ./src/Main.jl:12741:                        reverse_conf = _token_overlap_similarity(pat, ref_tid.pattern) + (ref_tid.strength / STRENGTH_CAP) * 0.5
    [!] ./src/Main.jl:12771:    if haskey(specimen, "chatter_groups") && isa(specimen["chatter_groups"], AbstractVector)
    [!] ./src/Main.jl:12804:                                    # GRUG BUG-010b: inhibition_tokens — semantic "don't do" set from alive originals.
    [!] ./src/Main.jl:12806:                                    Set{String}(String.(get(gentry, "inhibition_tokens", String[]))),
    [!] ./src/Main.jl:12807:                                    # GRUG BUG-010b: inhibition_dirty — always true on load (tokens may be stale).
    [!] ./src/Main.jl:12833:    if haskey(specimen, "chatter_cooldowns") && isa(specimen["chatter_cooldowns"], AbstractVector)
    [!] ./src/Main.jl:12855:    # Academic: Backward-compatible — if key is missing (v2.0 specimen),
    [!] ./src/Main.jl:12858:    if haskey(specimen, "trajectory") && _is_dict_like(specimen["trajectory"])
    [!] ./src/Main.jl:12862:            if haskey(traj, "config") && _is_dict_like(traj["config"])
    [!] ./src/Main.jl:12878:            if haskey(traj, "buffer") && isa(traj["buffer"], AbstractVector)
    [!] ./src/Main.jl:12881:                        # GRUG: Enum keys are stored as strings — parse them back.
    [!] ./src/Main.jl:12888:                            if !haskey(action_lookup, sk)
    [!] ./src/Main.jl:12897:                            if !haskey(tone_lookup, sk)
    [!] ./src/Main.jl:12918:    # Academic: Backward-compatible — missing key means no temporal coherence
    [!] ./src/Main.jl:12921:    if haskey(specimen, "temporal_coherence") && isa(specimen["temporal_coherence"], AbstractVector)
    [!] ./src/Main.jl:12949:    # Academic: Backward-compatible — missing key means no active cooldowns,
    [!] ./src/Main.jl:12952:    if haskey(specimen, "morph_cooldowns") && _is_dict_like(specimen["morph_cooldowns"])
    [!] ./src/Main.jl:12975:    if haskey(specimen, "immune_system") && _is_dict_like(specimen["immune_system"])
    [!] ./src/Main.jl:12989:    if haskey(specimen, "aiml_system")
    [!] ./src/Main.jl:12992:        # (aiml_system key in specimen is ignored gracefully)
    [!] ./src/Main.jl:13004:    if haskey(specimen, "sigil_table") && isa(specimen["sigil_table"], AbstractVector)
    [!] ./src/Main.jl:13021:                promote = Bool(get(sentry, "promote_at_tokenize", false))
    [!] ./src/Main.jl:13043:    if haskey(specimen, "automaton_rules") && isa(specimen["automaton_rules"], AbstractVector)
    [!] ./src/Main.jl:13053:                    if haskey(rentry, "steps") && isa(rentry["steps"], AbstractVector)
    [!] ./src/Main.jl:13081:    if haskey(specimen, "phase_accumulator") && _is_dict_like(specimen["phase_accumulator"])
    [!] ./src/Main.jl:13104:    if haskey(specimen, "last_contributor_votes") && isa(specimen["last_contributor_votes"], AbstractVector)
    [!] ./src/Main.jl:13145:    if haskey(specimen, "node_to_group_idx") && _is_dict_like(specimen["node_to_group_idx"])
    [!] ./src/Main.jl:13149:                if !haskey(NODE_TO_GROUP, n)
    [!] ./src/Main.jl:13158:    # pre-v2.5 specimens lack this key — knobs stay at defaults.
    [!] ./src/Main.jl:13159:    if haskey(specimen, "tonal_judge_knobs") && _is_dict_like(specimen["tonal_judge_knobs"])
    [!] ./src/Main.jl:13174:    # save/load. If the key is missing (old specimen), MLP keeps its default
    [!] ./src/Main.jl:13176:    if haskey(specimen, "ephemeral_mlp") && _is_dict_like(specimen["ephemeral_mlp"])
    [!] ./src/Main.jl:13195:    if haskey(specimen, "mlp_observer_store") && _is_dict_like(specimen["mlp_observer_store"])
    [!] ./src/Main.jl:13198:        obs_keys = get(obs_data, "key_count", 0)
    [!] ./src/Main.jl:13199:        println("  👁  MLP Observer Store: $obs_entries entries, $obs_keys distinct keys (store starts fresh; MLP gate counter restored)")
    [!] ./src/Main.jl:13206:    # without this key simply keep the default 0.0 — no harm done.
    [!] ./src/Main.jl:13207:    if haskey(specimen, "mlp_cached_phi") && _is_dict_like(specimen["mlp_cached_phi"])
    [!] ./src/Main.jl:13256:        if haskey(specimen, "vigilance_config")
    [!] ./src/Main.jl:13270:        if haskey(specimen, "injector_stats")
    [!] ./src/Main.jl:13275:                        if haskey(EphemeralAutomaton._INJECTOR_STATS, k)
    [!] ./src/Main.jl:13291:        if haskey(specimen, "relational_jitter_config")
    [!] ./src/Main.jl:13294:                if haskey(_rjcfg, "jitter_ratio")
    [!] ./src/Main.jl:13297:                if haskey(_rjcfg, "jitter_coin_ratio")
    [!] ./src/Main.jl:13300:                if haskey(_rjcfg, "jitter_enabled")
    [!] ./src/Main.jl:13318:        if haskey(specimen, "brainstem_config")
    [!] ./src/Main.jl:13336:        if haskey(specimen, "engine_config")
    [!] ./src/Main.jl:13357:        if haskey(specimen, "lobe_orchestrator_knobs")
    [!] ./src/Main.jl:13374:        if haskey(specimen, "vote_orchestrator_knobs")
    [!] ./src/Main.jl:13390:        if haskey(specimen, "mitosis_config")
    [!] ./src/Main.jl:13406:        if haskey(specimen, "growth_config")
    [!] ./src/Main.jl:13412:        if haskey(specimen, "phagy_config")
    [!] ./src/Main.jl:13418:        if haskey(specimen, "chatter_config")
    [!] ./src/Main.jl:13424:        if haskey(specimen, "immune_config")
    [!] ./src/Main.jl:13432:        if haskey(specimen, "scanner_config")
    [!] ./src/Main.jl:13453:        if haskey(specimen, "action_tone_knobs")
    [!] ./src/Main.jl:13484:    # this key — that's fine, accumulator starts empty and re-learns organically.
    [!] ./src/Main.jl:13486:        if haskey(specimen, "co_activation")
    [!] ./src/Main.jl:13497:    # won't have this key — that's fine, ledger starts empty and the thread
    [!] ./src/Main.jl:13500:        if haskey(specimen, "input_ledger")
    [!] ./src/Main.jl:13511:    # won't have this key — that's fine, ledger starts empty and the thread
    [!] ./src/Main.jl:13514:        if haskey(specimen, "chatter_residuals")
    [!] ./src/Main.jl:13525:    # Old specimens that predate AutoGrowth won't have these keys —
    [!] ./src/Main.jl:13528:        if haskey(specimen, "autogrowth_evidence")
    [!] ./src/Main.jl:13532:        if haskey(specimen, "autogrowth_co_occur")
    [!] ./src/Main.jl:13545:        if haskey(specimen, "autolink_evidence")
    [!] ./src/Main.jl:13559:        if haskey(specimen, "inverse_table")
    [!] ./src/Main.jl:13573:        if haskey(specimen, "flashcards")
    [!] ./src/Main.jl:13595:        if haskey(specimen, "curiosity")
    [!] ./src/Main.jl:13610:        if haskey(specimen, "fanout_config")
    [!] ./src/Main.jl:13613:                if haskey(_focfg, "enabled")
    [!] ./src/Main.jl:13616:                if haskey(_focfg, "max_shadows")
    [!] ./src/Main.jl:13619:                if haskey(_focfg, "modes")
    [!] ./src/Main.jl:13637:        if haskey(specimen, "hippocampal_pending_ask")
    [!] ./src/Main.jl:13639:            if _is_dict_like(_hpa) && haskey(_hpa, "pending_text")
    [!] ./src/Main.jl:13658:        if haskey(specimen, "conv_pending_teach")
    [!] ./src/Main.jl:13675:        if haskey(specimen, "conv_pending_teach_queue")
    [!] ./src/Main.jl:13683:        elseif haskey(specimen, "conv_pending_teach")
    [!] ./src/Main.jl:13700:        if haskey(specimen, "admin_session")
    [!] ./src/Main.jl:13718:        if haskey(specimen, "lobe_orch_last")
    [!] ./src/Main.jl:13721:                if haskey(_lol, "scores") && isa(_lol["scores"], AbstractVector)
    [!] ./src/Main.jl:13735:                        haskey(_lol, "winner") ? String(_lol["winner"]) : "",
    [!] ./src/Main.jl:13736:                        (haskey(_lol, "passthrough") && isa(_lol["passthrough"], AbstractVector)) ? [String(p) for p in _lol["passthrough"]] : String[])
    [!] ./src/Main.jl:13751:        if haskey(specimen, "chatter_cursor")
    [!] ./src/Main.jl:13753:            if _is_dict_like(_cc) && haskey(_cc, "cursor")
    [!] ./src/Main.jl:13765:    # Missing keys in saved modes fall back to current code defaults.
    [!] ./src/Main.jl:13767:        if haskey(specimen, "answer_mode_config")
    [!] ./src/Main.jl:13771:                    if haskey(_ANSWER_MODE_CONFIG, mode_name)
    [!] ./src/Main.jl:13804:        if haskey(specimen, "phagy_rules_ref")
    [!] ./src/Main.jl:13821:        if haskey(specimen, "time_orientation_config")
    [!] ./src/Main.jl:13849:                                if haskey(NODE_MAP, nid)
    [!] ./src/Main.jl:13874:        if haskey(specimen, "coherence_config")
    [!] ./src/Main.jl:13890:        if haskey(specimen, "geometry_config")
    [!] ./src/Main.jl:13906:        if haskey(specimen, "pattern_miner_config")
    [!] ./src/Main.jl:13919:        if haskey(specimen, "pattern_miner_data")
    [!] ./src/Main.jl:13936:        if haskey(specimen, "temporal_identities")
    [!] ./src/Main.jl:13953:        if haskey(specimen, "journal_config")
    [!] ./src/Main.jl:13968:        if haskey(specimen, "lobe_dictionaries")
    [!] ./src/Main.jl:13984:            # IMPORTANT: We iterate over _LOBE_DICTIONARIES keys, NOT LOBE_REGISTRY.
    [!] ./src/Main.jl:13989:                for _cb_lid in keys(_LOBE_DICTIONARIES)
    [!] ./src/Main.jl:14139:                # GRUG: Extract noun_anchors from pattern (non-sigil tokens).
    [!] ./src/Main.jl:14140:                pat_tokens = split(pat)
    [!] ./src/Main.jl:14142:                for tok in pat_tokens
    [!] ./src/Main.jl:14175:            # IMPORTANT: We iterate over _LOBE_DICTIONARIES keys, NOT LOBE_REGISTRY.
    [!] ./src/Main.jl:14184:                for _lid in keys(_LOBE_DICTIONARIES)
    [!] ./src/Main.jl:14202:                    if haskey(Lobe.LOBE_REGISTRY, _lid)
    [!] ./src/Main.jl:14252:                    for _lid in keys(_LOBE_DICTIONARIES)
    [!] ./src/Main.jl:14267:                        if haskey(Lobe.LOBE_REGISTRY, _lid)
    [!] ./src/Main.jl:14295:                    for _lid in keys(_LOBE_DICTIONARIES)
    [!] ./src/Main.jl:14314:                        if haskey(Lobe.LOBE_REGISTRY, _lid)
    [!] ./src/Main.jl:14393:    if !haskey(Lobe.LOBE_REGISTRY, "conversation")
    [!] ./src/Main.jl:14445:    # token-overlap winners (e.g. survival "fire burns") beat the script's
    [!] ./src/Main.jl:14620:            add_lobe_whitelist_fn      = (lobe_id, token) -> Lobe.add_lobe_whitelist!(lobe_id, token),
    [!] ./src/Main.jl:14692:                snap[nid] = [(br.partner_id, join(br.seam_tokens, " ")) for br in bridges]
    [!] ./src/Main.jl:14708:                key = a < b ? (a, b) : (b, a)
    [!] ./src/Main.jl:14709:                _al_idle_co_occur[key] = c
    [!] ./src/Main.jl:14721:            for (pair_key_str, intensity) in _al_idle_pairs_dict
    [!] ./src/Main.jl:14723:                    parts = split(String(pair_key_str), "|")
    [!] ./src/Main.jl:14804:            bridge_fn             = (id_a, id_b; seam_tokens=String[]) ->
    [!] ./src/Main.jl:14805:                bridge_nodes!(id_a, id_b; seam_tokens=seam_tokens),
    [!] ./src/Main.jl:14867:            # tokens are now stale (remixed content should NOT contribute to inhibition).
    [!] ./src/Main.jl:14959:            attach_fn        = (target_id, attach_id, pattern) -> bridge_nodes!(target_id, attach_id; seam_tokens=split(pattern)),
    [!] ./src/Main.jl:14960:            token_overlap_fn = (id_a, id_b) -> begin
    [!] ./src/Main.jl:14966:                _token_overlap_similarity(na.pattern, nb.pattern)
    [!] ./src/Main.jl:15008:    if !haskey(Lobe.LOBE_REGISTRY, lobe_id)
    [!] ./src/Main.jl:15009:        existing = sort(collect(keys(Lobe.LOBE_REGISTRY)))
    [!] ./src/Main.jl:15035:            if !haskey(NODE_MAP, node_id)
    [!] ./src/Main.jl:15094:            token_overlap_fn    = (id_a, id_b) -> begin
    [!] ./src/Main.jl:15100:                _token_overlap_similarity(na.pattern, nb.pattern)
    [!] ./src/Main.jl:15122:            token_overlap_fn    = (id_a, id_b) -> begin
    [!] ./src/Main.jl:15128:                _token_overlap_similarity(na.pattern, nb.pattern)
    [!] ./src/Main.jl:15285:            # <-> "you are"), so the split token is a literal " | " separator rather
    [!] ./src/Main.jl:15360:            # GRUG: Admin commands (password protected)
    [!] ./src/Main.jl:15378:            # token sigils (lambda class: &n &op &word &rest) and macro sigils
    [!] ./src/Main.jl:15674:                # GRUG v7.24: /mlpRule add <pattern> <transform_type> <key>
    [!] ./src/Main.jl:15677:                # key is a named identifier for key-based activation and drop-table linking.
    [!] ./src/Main.jl:15680:                rule_key = String(m_mlprule_add.captures[3])
    [!] ./src/Main.jl:15688:                            key = rule_key,
    [!] ./src/Main.jl:15692:                        println("✅  /mlpRule add: Rule '$(rule.id)' added (pattern='$rule_pattern', transform=$rule_transform, key='$rule_key')")
    [!] ./src/Main.jl:15719:                        println("  📎 $(r.id) | pattern='$(r.pattern)' | key='$(r.key)' | transform=$(r.transform_type) | fires=$(r.fire_count) | $status")
    [!] ./src/Main.jl:15772:                obs_keys = SelfObserver.key_count(_MLP_OBSERVER_STORE)
    [!] ./src/Main.jl:15781:                println("  Distinct keys       : $obs_keys")
    [!] ./src/Main.jl:15849:                    if !isnothing(target_lobe) && !haskey(Lobe.LOBE_REGISTRY, target_lobe)
    [!] ./src/Main.jl:15874:                                    if haskey(_gp, "nodes") && isa(_gp["nodes"], AbstractVector)
    [!] ./src/Main.jl:15878:                                    elseif haskey(_gp, "pattern")
    [!] ./src/Main.jl:16208:                        if !haskey(Lobe.LOBE_REGISTRY, lobe_candidate)
    [!] ./src/Main.jl:16281:                                        if haskey(_ANSWER_MODE_CONFIG, action_name) && !isempty(_ANSWER_MODE_CONFIG[action_name])
    [!] ./src/Main.jl:16300:                                        if haskey(NODE_MAP, pid)
    [!] ./src/Main.jl:16303:                                                if haskey(NODE_MAP, other_pid)
    [!] ./src/Main.jl:16311:                                                if haskey(NODE_MAP, sid)
    [!] ./src/Main.jl:16325:                                            if haskey(NODE_MAP, first_id)
    [!] ./src/Main.jl:16328:                                                    if haskey(NODE_MAP, other_id)
    [!] ./src/Main.jl:16506:                                        if haskey(NODE_MAP, current_id)
    [!] ./src/Main.jl:16514:                                            if haskey(NODE_MAP, first_id)
    [!] ./src/Main.jl:16517:                                                    if haskey(NODE_MAP, other_id)
    [!] ./src/Main.jl:16537:                                math_tokens = split(lowercase(ans_content))
    [!] ./src/Main.jl:16539:                                for tok in math_tokens
    [!] ./src/Main.jl:16540:                                    # Keep numbers, operator-like tokens, and short identifiers
    [!] ./src/Main.jl:16600:                                        # GRUG: Extract noun_anchors from the pattern (non-sigil tokens).
    [!] ./src/Main.jl:16601:                                        pat_tokens = split(action_pattern)
    [!] ./src/Main.jl:16603:                                        for tok in pat_tokens
    [!] ./src/Main.jl:16668:                    if !haskey(Lobe.LOBE_REGISTRY, lobe_candidate)
    [!] ./src/Main.jl:16676:                    if haskey(Lobe.LOBE_REGISTRY, "default")
    [!] ./src/Main.jl:16679:                        first(keys(Lobe.LOBE_REGISTRY))
    [!] ./src/Main.jl:16698:                    if !haskey(Lobe.LOBE_REGISTRY, defs_lobe)
    [!] ./src/Main.jl:16759:                    println("⚠  /newLobe: subject contains '_' or '-'. These are normalized to spaces during topicality matching, but you probably wanted plain space-separated keywords. Consider: '/newLobe $lobe_id_new $(replace(lobe_subject, '_' => ' ', '-' => ' '))'")
    [!] ./src/Main.jl:16778:                if !haskey(Lobe.LOBE_REGISTRY, name_lobe_id)
    [!] ./src/Main.jl:16815:                elseif !haskey(Lobe.LOBE_REGISTRY, target_lobe_id)
    [!] ./src/Main.jl:16852:                # pattern is matched as token mode (any token in pattern activates key)
    [!] ./src/Main.jl:16862:                        # GRUG: Use prefix mode when pattern looks like a node_id, token otherwise
    [!] ./src/Main.jl:16863:                        match_mode = startswith(tm_pattern, "node_") ? :prefix : :token
    [!] ./src/Main.jl:17123:                # GRUG: /login <password> — authenticate as admin
    [!] ./src/Main.jl:17125:                password = String(strip(m_login.captures[1]))
    [!] ./src/Main.jl:17128:                success, message = admin_login(password)
    [!] ./src/Main.jl:17158:                # GRUG v8.0: /nodeBridge <lobe_id> <node_a> <node_b> [seam_tokens...]
    [!] ./src/Main.jl:17160:                # Cascade bridge system: bridge two nodes bidirectionally with seam tokens.
    [!] ./src/Main.jl:17162:                # Seam tokens are optional — if omitted, populated at fire time from scanner's
    [!] ./src/Main.jl:17168:                #   /nodeBridge greeting node_0 node_1 "seam tokens here"
    [!] ./src/Main.jl:17180:                tokens = String[]
    [!] ./src/Main.jl:17186:                        # GRUG: Quoted token — find closing quote
    [!] ./src/Main.jl:17191:                        push!(tokens, remaining[2:close_idx-1])
    [!] ./src/Main.jl:17194:                        # GRUG: Unquoted token — split on whitespace
    [!] ./src/Main.jl:17197:                            push!(tokens, remaining)
    [!] ./src/Main.jl:17200:                            push!(tokens, remaining[1:space_idx-1])
    [!] ./src/Main.jl:17206:                if length(tokens) < 2
    [!] ./src/Main.jl:17207:                    error("!!! FATAL: /nodeAttach needs at least: <lobe_id> <node_a> <node_b>. Got $(length(tokens)) token(s) after lobe_id! !!!")
    [!] ./src/Main.jl:17210:                node_a = tokens[1]
    [!] ./src/Main.jl:17211:                node_b = tokens[2]
    [!] ./src/Main.jl:17212:                # GRUG v8.0: Seam tokens are optional. Everything after node_a and node_b.
    [!] ./src/Main.jl:17213:                seam_tokens = length(tokens) >= 3 ? tokens[3:end] : String[]
    [!] ./src/Main.jl:17220:                    if !haskey(NODE_MAP, node_b)
    [!] ./src/Main.jl:17225:                result = bridge_nodes!(node_a, node_b; seam_tokens=seam_tokens)
    [!] ./src/Main.jl:17263:                tokens = String[]
    [!] ./src/Main.jl:17273:                        push!(tokens, remaining[2:close_idx-1])
    [!] ./src/Main.jl:17278:                            push!(tokens, remaining)
    [!] ./src/Main.jl:17281:                            push!(tokens, remaining[1:space_idx-1])
    [!] ./src/Main.jl:17287:                if length(tokens) < 3
    [!] ./src/Main.jl:17288:                    error("!!! FATAL: /imgnodeAttach needs at least: <lobe_id> <target_id> <attach_id> <image_data>. Got $(length(tokens)) token(s) after lobe_id! !!!")
    [!] ./src/Main.jl:17291:                target_id = tokens[1]
    [!] ./src/Main.jl:17292:                attach_id = tokens[2]
    [!] ./src/Main.jl:17293:                image_input = tokens[3]
    [!] ./src/Main.jl:17301:                img_width = length(tokens) >= 4 ? parse(Int, tokens[4]) : 8
    [!] ./src/Main.jl:17302:                img_height = length(tokens) >= 5 ? parse(Int, tokens[5]) : 8
    [!] ./src/Main.jl:17780:                        # Map level string to the keyword arg for set_vigilance_config!
    [!] ./src/Main.jl:17847:                    if entry.promote_at_tokenize; push!(parts, "promote=true"); end
    [!] ./src/Main.jl:17859:                #   promote=...: front-door tokenizer rewrites raw matches into &name
    [!] ./src/Main.jl:17922:                        # Like &concept — any non-stopword, non-operator, non-number token.
    [!] ./src/Main.jl:17964:                        promote_at_tokenize=opt_promote,
    [!] ./src/Main.jl:17984:                if !haskey(_ENGINE_SIGIL_TABLE.entries, sigil_name)
    [!] ./src/Main.jl:18023:                    if !haskey(_ENGINE_SIGIL_TABLE.entries, sname)
    [!] ./src/Main.jl:18118:                    if !haskey(NODE_MAP, target_id)
    [!] ./src/Main.jl:18278:                    if !haskey(NODE_MAP, a_id)
    [!] ./src/Main.jl:18280:                    elseif !haskey(NODE_MAP, b_id)
    [!] ./src/Main.jl:18296:                    if !haskey(NODE_MAP, a_id)
    [!] ./src/Main.jl:18298:                    elseif !haskey(NODE_MAP, b_id)
    [!] ./src/Main.jl:18331:                    if !haskey(NODE_MAP, a_id)
    [!] ./src/Main.jl:18333:                    elseif !haskey(NODE_MAP, b_id)
    [!] ./src/Main.jl:18440:                    if !haskey(NODE_MAP, a_id) || !haskey(NODE_MAP, b_id)
    [!] ./src/Main.jl:18619:                # /mineShapes config [key value] — view or set pattern miner config
    [!] ./src/Main.jl:18623:                        key_str = String(strip(m_mine_config.captures[1]))
    [!] ./src/Main.jl:18625:                        key_sym = Symbol(key_str)
    [!] ./src/Main.jl:18635:                            PatternMiner.set_pattern_miner_config!(key_sym, parsed_val)
    [!] ./src/Main.jl:18636:                            println("✓  Set $(key_str) = $(parsed_val)")
    [!] ./src/Main.jl:18638:                            println("⚠  Failed to set $(key_str): $e")
    [!] ./src/Main.jl:18843:                # /identity config [key value]
    [!] ./src/Main.jl:18846:                        key_str = String(strip(m_identity_config.captures[1]))
    [!] ./src/Main.jl:18848:                        key_sym = Symbol(key_str)
    [!] ./src/Main.jl:18860:                            TemporalIdentity.set_temporal_identity_config!(key_sym, parsed_val)
    [!] ./src/Main.jl:18861:                            println("✓  Set $(key_str) = $(parsed_val)")
    [!] ./src/Main.jl:18863:                            println("⚠  Failed to set $(key_str): $e")
    [!] ./src/Main.jl:18949:                    _n_dropped = SelfObserver.drop_keys_by_prefix!(_MLP_OBSERVER_STORE, "error_")
    [!] ./src/Main.jl:18956:                        "error_";          # key prefix to match error observations
    [!] ./src/Main.jl:19131:# Inhibited words are filtered from input tokens before pattern scanning,
    [!] ./src/Main.jl:19151:#       token cloud, and
    [!] ./src/Main.jl:19168:#                       hopfield_key, relational_patterns, etc.)
    [!] ./src/Main.jl:19170:#   3. rules          — REMOVED (was ORCHESTRATION_RULES); "rules" key in old
    [!] ./src/Main.jl:19193:# reporting. v2.1 keys are optional on load for backward compat with v2.0.
    [!] ./src/GeometryKit.jl:77:function set_geometry_config!(key::Symbol, value)
    [!] ./src/GeometryKit.jl:80:        if key === :enabled
    [!] ./src/GeometryKit.jl:82:        elseif key === :default_space
    [!] ./src/GeometryKit.jl:92:        elseif key === :nearest_k
    [!] ./src/GeometryKit.jl:94:        elseif key === :trajectory_depth
    [!] ./src/GeometryKit.jl:97:            error("unknown GeometryKit config key :$key")
    [!] ./src/GeometryKit.jl:123:    haskey(d, "enabled")    && set_geometry_config!(:enabled, Bool(d["enabled"]))
    [!] ./src/GeometryKit.jl:124:    haskey(d, "default_space") && set_geometry_config!(:default_space, String(d["default_space"]))
    [!] ./src/GeometryKit.jl:125:    haskey(d, "nearest_k")  && set_geometry_config!(:nearest_k, Int(d["nearest_k"]))
    [!] ./src/GeometryKit.jl:126:    haskey(d, "trajectory_depth") && set_geometry_config!(:trajectory_depth, Int(d["trajectory_depth"]))
    [!] ./src/HippocampalModulator.jl:90:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/HippocampalModulator.jl:94:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/HippocampalModulator.jl:98:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/HippocampalModulator.jl:374:    # GRUG: Store output keyed by objective_id so later entries can
    [!] ./src/HippocampalModulator.jl:376:    # get keyed by sequence number as string to avoid collisions.
    [!] ./src/HippocampalModulator.jl:377:    key = isempty(entry.objective_id) ? string(seq) : entry.objective_id
    [!] ./src/HippocampalModulator.jl:378:    log.objective_outputs[key] = output
    [!] ./src/HippocampalModulator.jl:472:    # side-channel. Similarity = substring check OR token-overlap ratio.
    [!] ./src/HippocampalModulator.jl:488:                    # Check 2: token-overlap ratio (catches thesaurus-swapped
    [!] ./src/HippocampalModulator.jl:490:                    mt_tokens = Set(split(replace(mt, r"[^a-z0-9\s]" => "")))
    [!] ./src/HippocampalModulator.jl:491:                    add_tokens = Set(split(replace(add_text, r"[^a-z0-9\s]" => "")))
    [!] ./src/HippocampalModulator.jl:492:                    delete!(mt_tokens, "")
    [!] ./src/HippocampalModulator.jl:493:                    delete!(add_tokens, "")
    [!] ./src/HippocampalModulator.jl:494:                    if !isempty(mt_tokens) && !isempty(add_tokens)
    [!] ./src/HippocampalModulator.jl:495:                        overlap = length(intersect(mt_tokens, add_tokens))
    [!] ./src/HippocampalModulator.jl:496:                        ratio = overlap / min(length(mt_tokens), length(add_tokens))
    [!] ./src/HippocampalModulator.jl:687:            key = isempty(prior_entry.objective_id) ? string(ps) : prior_entry.objective_id
    [!] ./src/HippocampalModulator.jl:688:            if haskey(log.objective_outputs, key)
    [!] ./src/HippocampalModulator.jl:689:                push!(prior_ctx, log.objective_outputs[key])
    [!] ./src/HippocampalModulator.jl:690:            elseif haskey(prior_outputs, key)
    [!] ./src/HippocampalModulator.jl:691:                push!(prior_ctx, prior_outputs[key])
    [!] ./src/HippocampalModulator.jl:799:            key = isempty(prior_entry.objective_id) ? string(ps) : prior_entry.objective_id
    [!] ./src/HippocampalModulator.jl:800:            if haskey(log.objective_outputs, key)
    [!] ./src/HippocampalModulator.jl:801:                push!(prior_ctx, log.objective_outputs[key])
    [!] ./src/HippocampalModulator.jl:802:            elseif haskey(prior_outputs, key)
    [!] ./src/HippocampalModulator.jl:803:                push!(prior_ctx, prior_outputs[key])
    [!] ./src/patternscanner.jl:16:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/patternscanner.jl:20:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/patternscanner.jl:24:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/SigilPromoter.jl:5:# Promotion/canonicalization here can feed sigil tokens into BOTH flat patterns
    [!] ./src/SigilPromoter.jl:16:#           predicate driven by sigil registry's promote_at_tokenize flag.
    [!] ./src/SigilPromoter.jl:19:# GRUG say: ZERO COST when no promotable token. Pure-text input passes through
    [!] ./src/SigilPromoter.jl:51:#   - Macro membership: tokens whose canonical form sits in a :macro sigil's
    [!] ./src/SigilPromoter.jl:54:#   - Position-keyed `Vector{SigilBinding}` as the primary side-channel
    [!] ./src/SigilPromoter.jl:55:#     (ordered, lossless). Name-keyed view is computed on demand.
    [!] ./src/SigilPromoter.jl:56:#   - Idempotency: tokens that already look like `&knownsigil` pass through.
    [!] ./src/SigilPromoter.jl:58:#     macro-eligible tokens returns the input unchanged with empty bindings.
    [!] ./src/SigilPromoter.jl:83:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/SigilPromoter.jl:87:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/SigilPromoter.jl:91:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/SigilPromoter.jl:108:export canonicalize_token
    [!] ./src/SigilPromoter.jl:137:or a sigil's class/sigil_type is incoherent with promote_at_tokenize=true.
    [!] ./src/SigilPromoter.jl:163:# case-folded by walking the canonicalize_token routine first.
    [!] ./src/SigilPromoter.jl:221:# here AND in the regex tokenizer rules.
    [!] ./src/SigilPromoter.jl:231:# one token when it sees one of these and merges with the next token's value.
    [!] ./src/SigilPromoter.jl:245:# Position is the rewritten-string token index (0-based), NOT the byte
    [!] ./src/SigilPromoter.jl:253:  - `position::Int`     — 0-based token index in the REWRITTEN string.
    [!] ./src/SigilPromoter.jl:260:  - `surface::String`   — the ORIGINAL surface token(s) the binding replaced
    [!] ./src/SigilPromoter.jl:266:  - `raw_position::Int` — 0-based token index in the RAW input stream
    [!] ./src/SigilPromoter.jl:267:                          (post-tokenize, pre-promote). Lets downstream
    [!] ./src/SigilPromoter.jl:269:                          even after Layer 1 canonicalization rewrote tokens.
    [!] ./src/SigilPromoter.jl:271:                          token consumed (the sign word).
    [!] ./src/SigilPromoter.jl:273:The two position fields differ when canonicalization changes token count:
    [!] ./src/SigilPromoter.jl:274:sign-prefix merge consumes 2 raw tokens ("negative", "three") but emits 1
    [!] ./src/SigilPromoter.jl:275:rewritten token ("&n"), so `raw_position` and `position` diverge for any
    [!] ./src/SigilPromoter.jl:276:binding that follows. Multi-token surface forms (currently just sign-prefix)
    [!] ./src/SigilPromoter.jl:277:are the only case where 1 binding maps to >1 raw token.
    [!] ./src/SigilPromoter.jl:293:    canonicalize_token(tok) -> String
    [!] ./src/SigilPromoter.jl:295:Apply Layer 1 canonicalization to a single token: lowercase + strip, then
    [!] ./src/SigilPromoter.jl:296:look up in NUMBER_WORD_MAP / OP_WORD_MAP. Unknown tokens pass through with
    [!] ./src/SigilPromoter.jl:303:function canonicalize_token(tok::AbstractString)::String
    [!] ./src/SigilPromoter.jl:308:    if haskey(NUMBER_WORD_MAP, s)
    [!] ./src/SigilPromoter.jl:311:    if haskey(OP_WORD_MAP, s)
    [!] ./src/SigilPromoter.jl:318:# TOKENIZE — split raw input into tokens, treating ops as separate tokens.
    [!] ./src/SigilPromoter.jl:322:# split on whitespace. This is the simplest defensible tokenizer for Stage
    [!] ./src/SigilPromoter.jl:323:# 1.5a; multi-char operators (==, <=, !=) and proper Unicode tokenization
    [!] ./src/SigilPromoter.jl:328:# so the tokenizer doesn't have to do its own splitting.
    [!] ./src/SigilPromoter.jl:331:# leading sign. Why: alternation order would make "2+2" greedily tokenize as
    [!] ./src/SigilPromoter.jl:333:# Signs in raw input are ALWAYS standalone op tokens here — that means a
    [!] ./src/SigilPromoter.jl:334:# user-typed "-3" tokenizes as ["-", "3"] and the matcher sees it as a unary
    [!] ./src/SigilPromoter.jl:337:# for true negative literals. True signed-numeric tokenization (context-aware:
    [!] ./src/SigilPromoter.jl:343:# (token, raw_position) where raw_position is the 0-based index of this
    [!] ./src/SigilPromoter.jl:344:# token in the raw token stream (NOT a byte offset — token index).
    [!] ./src/SigilPromoter.jl:345:# Each token is one of:
    [!] ./src/SigilPromoter.jl:346:#   - existing sigil token (preserved verbatim, idempotency hook)
    [!] ./src/SigilPromoter.jl:347:#   - signed/unsigned integer or decimal (preserved as one token)
    [!] ./src/SigilPromoter.jl:351:# the tokenizer. The matcher already strips those downstream so dropping
    [!] ./src/SigilPromoter.jl:357:function _tokenize(raw::AbstractString)::Vector{Tuple{String,Int}}
    [!] ./src/SigilPromoter.jl:376:  Layer 1 — Canonicalize each token via NUMBER_WORD_MAP / OP_WORD_MAP +
    [!] ./src/SigilPromoter.jl:378:  Layer 2 — For each canonicalized token, walk registry sigils with
    [!] ./src/SigilPromoter.jl:379:            `promote_at_tokenize=true` and rewrite to the canonical sigil
    [!] ./src/SigilPromoter.jl:380:            form if the token matches the sigil's shape predicate or
    [!] ./src/SigilPromoter.jl:393:  - if no token in `raw` is canonicalizable, shape-eligible, or
    [!] ./src/SigilPromoter.jl:395:    version of `raw` with no `&` tokens added, and bindings is empty.
    [!] ./src/SigilPromoter.jl:398:  - tokens that already look like `&knownsigil` are preserved verbatim and
    [!] ./src/SigilPromoter.jl:419:    # GRUG: tokenize and run sign-merge in a single pass to keep token
    [!] ./src/SigilPromoter.jl:420:    # positions correct in the bindings list. Each entry is (token, raw_pos)
    [!] ./src/SigilPromoter.jl:422:    # collapsing/dropping tokens.
    [!] ./src/SigilPromoter.jl:423:    raw_tokens = _tokenize(raw_str)
    [!] ./src/SigilPromoter.jl:426:    # promotion, ONCE, before the per-token loop. Order is deterministic
    [!] ./src/SigilPromoter.jl:432:        if e.promote_at_tokenize
    [!] ./src/SigilPromoter.jl:438:            # GRUG: any other class with promote_at_tokenize=true was rejected
    [!] ./src/SigilPromoter.jl:444:    out_tokens = String[]
    [!] ./src/SigilPromoter.jl:447:    # GRUG: layer 1.5 — peek-ahead sign merge. We walk raw_tokens with an
    [!] ./src/SigilPromoter.jl:451:    while i <= length(raw_tokens)
    [!] ./src/SigilPromoter.jl:452:        tok, raw_pos = raw_tokens[i]
    [!] ./src/SigilPromoter.jl:454:        # GRUG: idempotency hook — preserve already-promoted sigil tokens.
    [!] ./src/SigilPromoter.jl:461:            push!(out_tokens, tok)
    [!] ./src/SigilPromoter.jl:468:        # GRUG: unary-minus peek. "-15" tokenizes as ["-", "15"] because
    [!] ./src/SigilPromoter.jl:469:        # the tokenizer can't distinguish unary from binary minus. If the
    [!] ./src/SigilPromoter.jl:470:        # current token is "-" and the PREVIOUS token is NOT a number, and
    [!] ./src/SigilPromoter.jl:471:        # the NEXT token IS a number, merge them into a signed numeric.
    [!] ./src/SigilPromoter.jl:473:        # "5 - 3" (where the previous token IS a number → binary op).
    [!] ./src/SigilPromoter.jl:474:        if tok == "-" && i < length(raw_tokens) && (i == 1 || !occursin(NUMBER_TOKEN_REGEX, canonicalize_token(raw_tokens[i-1][1])))
    [!] ./src/SigilPromoter.jl:475:            next_tok, _next_pos = raw_tokens[i+1]
    [!] ./src/SigilPromoter.jl:476:            next_can = canonicalize_token(next_tok)
    [!] ./src/SigilPromoter.jl:481:                _promote_layer2!(out_tokens, bindings, merged,
    [!] ./src/SigilPromoter.jl:490:        # the next token canonicalizes to a number-word OR is already a
    [!] ./src/SigilPromoter.jl:491:        # numeric token.
    [!] ./src/SigilPromoter.jl:492:        if haskey(SIGN_PREFIX_MAP, lc) && i < length(raw_tokens)
    [!] ./src/SigilPromoter.jl:494:            next_tok, _next_pos = raw_tokens[i+1]
    [!] ./src/SigilPromoter.jl:495:            next_can = canonicalize_token(next_tok)
    [!] ./src/SigilPromoter.jl:502:                # Surface = the user's actual two raw tokens joined ("negative three"),
    [!] ./src/SigilPromoter.jl:507:                # binding's raw_position points at the FIRST raw token (the
    [!] ./src/SigilPromoter.jl:509:                _promote_layer2!(out_tokens, bindings, merged,
    [!] ./src/SigilPromoter.jl:518:        canonical = canonicalize_token(tok)
    [!] ./src/SigilPromoter.jl:524:        # GRUG: Layer 2 promotion. Pass the ORIGINAL surface token (not the
    [!] ./src/SigilPromoter.jl:526:        _promote_layer2!(out_tokens, bindings, canonical,
    [!] ./src/SigilPromoter.jl:532:    rewritten = join(out_tokens, " ")
    [!] ./src/SigilPromoter.jl:536:# GRUG: Layer 2 helper. Given a canonicalized token (already lowercased and
    [!] ./src/SigilPromoter.jl:539:# sigil-or-literal token + binding to the accumulators.
    [!] ./src/SigilPromoter.jl:541:# `surface` is the ORIGINAL raw token (or joined tokens for sign-prefix
    [!] ./src/SigilPromoter.jl:542:# merges). `raw_pos` is the 0-based index of that token in the raw stream.
    [!] ./src/SigilPromoter.jl:545:# In-place mutation of out_tokens and bindings keeps the main loop simple
    [!] ./src/SigilPromoter.jl:546:# and avoids per-token allocation churn.
    [!] ./src/SigilPromoter.jl:548:    out_tokens::Vector{String},
    [!] ./src/SigilPromoter.jl:566:            pos = length(out_tokens)
    [!] ./src/SigilPromoter.jl:567:            push!(out_tokens, string(SIGIL_PREFIX, e.name))
    [!] ./src/SigilPromoter.jl:582:            pos = length(out_tokens)
    [!] ./src/SigilPromoter.jl:583:            push!(out_tokens, string(SIGIL_PREFIX, e.name))
    [!] ./src/SigilPromoter.jl:591:    # GRUG: nothing matched — token passes through as a literal. No binding
    [!] ./src/SigilPromoter.jl:592:    # is recorded; the literal lands in out_tokens at the canonicalized
    [!] ./src/SigilPromoter.jl:594:    # for fast-path tokens because there's nothing to map back to.
    [!] ./src/SigilPromoter.jl:595:    push!(out_tokens, canonical)
    [!] ./src/SigilPromoter.jl:600:# a promote_predicate set, call it with the canonical token. The predicate
    [!] ./src/SigilPromoter.jl:603:# context so the user can see which sigil and which token tripped it.
    [!] ./src/SigilPromoter.jl:617:            "promote_predicate for &$(e.name) raised on token $(repr(canonical)): $(err)",
    [!] ./src/SigilPromoter.jl:622:            "promote_predicate for &$(e.name) returned $(typeof(result)) ($(repr(result))) on token $(repr(canonical)); must return Bool",
    [!] ./src/SigilPromoter.jl:650:        # rejected promote_at_tokenize=true on a :word lambda — but we
    [!] ./src/SigilPromoter.jl:654:        # :slurp is multi-token by definition; can't promote a single token.
    [!] ./src/SigilPromoter.jl:658:    # nodes. Each shape predicate checks the token against a closed set of
    [!] ./src/SigilPromoter.jl:661:        # :concept — any non-stopword, non-operator token. The promote_predicate
    [!] ./src/SigilPromoter.jl:663:        # the promote_predicate passed, this token qualifies as a concept.
    [!] ./src/SigilPromoter.jl:669:        # here the token matched. Return the canonical string.
    [!] ./src/SigilPromoter.jl:682:        # this token has a dictionary entry. Return the canonical string —
    [!] ./src/SigilPromoter.jl:691:        # promote pipeline), the token qualifies and we return canonical.
    [!] ./src/SigilPromoter.jl:700:            "sigil &$(e.name) has promote_at_tokenize=true but unknown sigil_type :$(st) and no promote_predicate; promoter has no shape predicate for it",
    [!] ./src/SigilPromoter.jl:706:# CONVENIENCE — name-keyed view of bindings (computed lazily).
    [!] ./src/SigilPromoter.jl:717:The position-keyed Vector{SigilBinding} returned by `promote_input` is the
    [!] ./src/SigilPromoter.jl:719:as the sole storage of bindings; always carry the position-keyed list.
    [!] ./src/InputLedger.jl:39:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/InputLedger.jl:43:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/InputLedger.jl:47:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/InputLedger.jl:136:    return lock(() -> haskey(_LEDGER, h), _LEDGER_LOCK)
    [!] ./src/InputLedger.jl:147:        if !haskey(_LEDGER, h)
    [!] ./src/InputLedger.jl:156:                keys_vec = collect(keys(_LEDGER))
    [!] ./src/InputLedger.jl:157:                delete!(_LEDGER, keys_vec[rand(1:length(keys_vec))])
    [!] ./src/InputLedger.jl:231:                   token_overlap_fn::Function = (id_a, id_b) -> 0.0,
    [!] ./src/InputLedger.jl:239:  2. Find which existing nodes match the tokens (pattern overlap)
    [!] ./src/InputLedger.jl:251:                        token_overlap_fn::Function = (id_a, id_b) -> 0.0,
    [!] ./src/InputLedger.jl:276:        input_tokens = Set(split(lowercase(strip(text))))
    [!] ./src/InputLedger.jl:284:                # GRUG: Check if any input tokens appear in node pattern
    [!] ./src/InputLedger.jl:285:                pattern_tokens = Set(split(lowercase(strip(node.pattern))))
    [!] ./src/InputLedger.jl:286:                if !isempty(intersect(input_tokens, pattern_tokens))
    [!] ./src/InputLedger.jl:302:                    # GRUG: Check token overlap between the nodes themselves.
    [!] ./src/InputLedger.jl:304:                    overlap = token_overlap_fn(id_a, id_b)
    [!] ./src/InputLedger.jl:339:        token_overlap_fn::Function)
    [!] ./src/InputLedger.jl:355:    token_overlap_fn::Function)
    [!] ./src/InputLedger.jl:375:                    token_overlap_fn = token_overlap_fn,
    [!] ./src/InputLedger.jl:402:    token_overlap_fn::Function)
    [!] ./src/InputLedger.jl:420:            token_overlap_fn = token_overlap_fn,
    [!] ./src/InputLedger.jl:482:GRUG: Serialize the ledger for specimen save. Only the hash keys are
    [!] ./src/InputLedger.jl:487:        [string(h) for h in keys(_LEDGER)]
    [!] ./src/InputLedger.jl:509:            if !haskey(_LEDGER, h)
    [!] ./src/InputLedger.jl:515:            keys_vec = collect(keys(_LEDGER))
    [!] ./src/InputLedger.jl:516:            delete!(_LEDGER, keys_vec[rand(1:length(keys_vec))])
    [!] ./src/MultipartOrchestrator.jl:52:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/MultipartOrchestrator.jl:56:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/MultipartOrchestrator.jl:60:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/MultipartOrchestrator.jl:91:  group_id          — "" for singletons, the shared group key otherwise.
    [!] ./src/MultipartOrchestrator.jl:116:`groups` is a Dict keyed by non-empty group_id mapping to the votes carrying
    [!] ./src/MultipartOrchestrator.jl:300:                        if !haskey(chunk_groups, gid)
    [!] ./src/MultipartOrchestrator.jl:345:                if !haskey(chunk_groups, gid)
    [!] ./src/MultipartOrchestrator.jl:369:    # Chunk-derived groups take precedence if there's a key collision
    [!] ./src/MultipartOrchestrator.jl:589:    for gid in sort(collect(keys(groups)))
    [!] ./src/ActionEngine.jl:106:    return sort(collect(keys(ACTION_CALLBACKS)))
    [!] ./src/ActionEngine.jl:114:has_action_callback(name::String)::Bool = haskey(ACTION_CALLBACKS, lowercase(strip(name)))
    [!] ./src/ActionEngine.jl:461:    name_key = lowercase(strip(action_name))
    [!] ./src/ActionEngine.jl:462:    if !haskey(ACTION_CALLBACKS, name_key)
    [!] ./src/ActionEngine.jl:463:        return ActionResult(name_key, nothing, "", "",
    [!] ./src/ActionEngine.jl:464:            ActionComputationStep[], "unknown action callback '$name_key'")
    [!] ./src/ActionEngine.jl:468:        return ACTION_CALLBACKS[name_key](bindings)
    [!] ./src/ActionEngine.jl:470:        return ActionResult(name_key, nothing, "", "",
    [!] ./src/ActionEngine.jl:471:            ActionComputationStep[], "action '$name_key' threw: $e")
    [!] ./src/ActionEngine.jl:835:    # the reply while the internal registry key stays untouched — fully
    [!] ./src/SigilRegistry.jl:6:# relation / object fields may ALSO contain sigil tokens (&n, &word, &noun,
    [!] ./src/SigilRegistry.jl:12:#           drops, AIML keys, lobe subjects). Each one have its OWN little
    [!] ./src/SigilRegistry.jl:15:# GRUG say: token sigil = hole in pattern. "&n" mean "any number here".
    [!] ./src/SigilRegistry.jl:33:#   - Pattern-string parsing: extract `&name` tokens, resolve against registry,
    [!] ./src/SigilRegistry.jl:71:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/SigilRegistry.jl:75:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/SigilRegistry.jl:79:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/SigilRegistry.jl:89:export resolve_sigils_in_pattern, parse_sigil_token
    [!] ./src/SigilRegistry.jl:106:sigil can promote tokens that have dictionary entries.
    [!] ./src/SigilRegistry.jl:282:#   promote_at_tokenize
    [!] ./src/SigilRegistry.jl:284:#                 door will rewrite raw input tokens that match this sigil's
    [!] ./src/SigilRegistry.jl:294:#                 set AND promote_at_tokenize=true, the front-door promoter
    [!] ./src/SigilRegistry.jl:295:#                 calls `promote_predicate(canonical_token)::Bool` per
    [!] ./src/SigilRegistry.jl:296:#                 candidate token; only promotes when the predicate returns
    [!] ./src/SigilRegistry.jl:298:#                 mode beyond the binary token-vs-functor split:
    [!] ./src/SigilRegistry.jl:300:#                   promote_at_tokenize=false                      → FUNCTOR
    [!] ./src/SigilRegistry.jl:302:#                        door doesn't touch matching tokens.)
    [!] ./src/SigilRegistry.jl:303:#                   promote_at_tokenize=true,  predicate=nothing   → TOKEN
    [!] ./src/SigilRegistry.jl:304:#                       (front door always rewrites matching tokens; this
    [!] ./src/SigilRegistry.jl:306:#                   promote_at_tokenize=true,  predicate=fn        → CONDITIONAL
    [!] ./src/SigilRegistry.jl:331:    promote_at_tokenize::Bool              # Stage 1.5 ingest hook. Default false.
    [!] ./src/SigilRegistry.jl:365:Required keyword args:
    [!] ./src/SigilRegistry.jl:370:Optional keyword args:
    [!] ./src/SigilRegistry.jl:377:  - `promote_at_tokenize::Bool=false` — Stage 1.5 ingest hook. When true, the
    [!] ./src/SigilRegistry.jl:378:    front-door promoter rewrites raw input tokens matching this sigil's shape
    [!] ./src/SigilRegistry.jl:383:    conditional-promotion gate. When set AND `promote_at_tokenize=true`,
    [!] ./src/SigilRegistry.jl:384:    the front-door promoter calls `predicate(canonical_token)::Bool` per
    [!] ./src/SigilRegistry.jl:385:    candidate token; only promotes when the predicate returns true. Setting
    [!] ./src/SigilRegistry.jl:386:    this without `promote_at_tokenize=true` throws SigilConfigError (the
    [!] ./src/SigilRegistry.jl:404:    promote_at_tokenize::Bool=false,
    [!] ./src/SigilRegistry.jl:481:    # GRUG: promote_at_tokenize gating. The front-door promoter only knows how
    [!] ./src/SigilRegistry.jl:485:    if promote_at_tokenize && !(class in (:lambda, :macro))
    [!] ./src/SigilRegistry.jl:487:            "promote_at_tokenize=true is only valid for :lambda and :macro classes (got class :$class for &$nm)",
    [!] ./src/SigilRegistry.jl:488:            "promote_at_tokenize"))
    [!] ./src/SigilRegistry.jl:492:    #   1. If predicate is set, promote_at_tokenize MUST also be true.
    [!] ./src/SigilRegistry.jl:500:    # attributable to the actual offending input token.
    [!] ./src/SigilRegistry.jl:501:    if promote_predicate !== nothing && !promote_at_tokenize
    [!] ./src/SigilRegistry.jl:503:            "promote_predicate set on &$nm but promote_at_tokenize=false; predicate would never run (silent no-ops are forbidden — set promote_at_tokenize=true or drop the predicate)",
    [!] ./src/SigilRegistry.jl:508:    if length(table.entries) >= MAX_REGISTRY_ENTRIES && !haskey(table.entries, nm)
    [!] ./src/SigilRegistry.jl:517:    if haskey(table.entries, nm) && !overwrite
    [!] ./src/SigilRegistry.jl:533:        promote_at_tokenize,
    [!] ./src/SigilRegistry.jl:653:The bare-name form (no `&` prefix) is canonical. Callers that have a token
    [!] ./src/SigilRegistry.jl:654:string should call `parse_sigil_token` first.
    [!] ./src/SigilRegistry.jl:661:    if !haskey(table.entries, nm)
    [!] ./src/SigilRegistry.jl:677:    return haskey(table.entries, String(name))
    [!] ./src/SigilRegistry.jl:703:    for k in sort!(collect(keys(table.entries)))
    [!] ./src/SigilRegistry.jl:727:# PATTERN PARSING — extract sigil tokens from a pattern string.
    [!] ./src/SigilRegistry.jl:736:A reference to a sigil token inside a pattern string.
    [!] ./src/SigilRegistry.jl:752:    parse_sigil_token(s) -> Union{Nothing,String}
    [!] ./src/SigilRegistry.jl:754:Convenience: if `s` looks like a single sigil token (`&name`), return `name`.
    [!] ./src/SigilRegistry.jl:760:function parse_sigil_token(s::AbstractString)::Union{Nothing,String}
    [!] ./src/SigilRegistry.jl:776:Scan `pattern` for `&name` tokens and resolve each against `table`.
    [!] ./src/SigilRegistry.jl:786:  - If the pattern contains more than MAX_SIGILS_PER_PATTERN sigil tokens,
    [!] ./src/SigilRegistry.jl:789:Returns the list of resolved tokens in pattern-position order.
    [!] ./src/SigilRegistry.jl:809:                "pattern contains more than MAX_SIGILS_PER_PATTERN=$MAX_SIGILS_PER_PATTERN sigil tokens; pattern=\"$pat\"",
    [!] ./src/SigilRegistry.jl:815:        entry = if haskey(table.entries, name)
    [!] ./src/SigilRegistry.jl:854:  &n     — :lambda, sigil_type=:number, applies_at=:match. Matches a numeric token.
    [!] ./src/SigilRegistry.jl:856:  &rest  — :lambda, sigil_type=:slurp,  applies_at=:match. Slurps remaining tokens.
    [!] ./src/SigilRegistry.jl:861:           from the closed set {+, -, *, /, =, <, >, %, ^}. promote_at_tokenize=true.
    [!] ./src/SigilRegistry.jl:862:  &n     gets promote_at_tokenize=true so the front-door promoter rewrites bare
    [!] ./src/SigilRegistry.jl:863:         numeric tokens into &n with the value bound on the side-channel.
    [!] ./src/SigilRegistry.jl:917:    haskey(table.entries, nm) || throw(SigilResolutionError(
    [!] ./src/SigilRegistry.jl:936:    haskey(table.entries, nm) || return false
    [!] ./src/SigilRegistry.jl:996:        promote_at_tokenize=true)
    [!] ./src/SigilRegistry.jl:1025:        promote_at_tokenize=true)
    [!] ./src/SigilRegistry.jl:1030:    # which tokens can be promoted. This prevents greedy over-matching
    [!] ./src/SigilRegistry.jl:1045:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1059:            # GRUG v8.19: Not a math action keyword (let action sigil nodes match these
    [!] ./src/SigilRegistry.jl:1060:            # as literal tokens in their patterns, not as &concept placeholders).
    [!] ./src/SigilRegistry.jl:1064:            # and become indistinguishable. Now they stay as literal tokens and
    [!] ./src/SigilRegistry.jl:1093:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1103:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1109:    # promote_predicate checks if the token has a dictionary entry via
    [!] ./src/SigilRegistry.jl:1123:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1132:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1228:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1247:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1266:        promote_at_tokenize=true,
    [!] ./src/SigilRegistry.jl:1302:        if haskey(target.entries, nm)
    [!] ./src/SigilRegistry.jl:1388:Recursively expand a `:procedure` sigil into a flat vector of literal tokens.
    [!] ./src/SigilRegistry.jl:1392:canonical `&name` token (so downstream pattern code still sees a sigil).
    [!] ./src/SigilRegistry.jl:1408:    haskey(table.entries, nm) || throw(SigilResolutionError(
    [!] ./src/SigilRegistry.jl:1424:            if haskey(table.entries, inner_str) &&
    [!] ./src/SigilRegistry.jl:1428:                # Non-procedure nested sigil: emit canonical token unchanged.
    [!] ./src/SigilRegistry.jl:1431:                if !haskey(table.entries, inner_str)
    [!] ./src/SigilRegistry.jl:1452:    haskey(table.entries, nm) || return false
    [!] ./src/SigilRegistry.jl:1464:# sequence of sigil references plus literal tokens. When the pattern matcher
    [!] ./src/SigilRegistry.jl:1527:Recursively expand a `:structure` sigil into a flat vector of literal tokens.
    [!] ./src/SigilRegistry.jl:1531:canonical `&name` token.
    [!] ./src/SigilRegistry.jl:1547:    haskey(table.entries, nm) || throw(SigilResolutionError(
    [!] ./src/SigilRegistry.jl:1563:            if haskey(table.entries, inner_str) &&
    [!] ./src/SigilRegistry.jl:1567:                # Non-structure nested sigil: emit canonical token unchanged.
    [!] ./src/SigilRegistry.jl:1568:                if !haskey(table.entries, inner_str)
    [!] ./src/SigilRegistry.jl:1589:    haskey(table.entries, nm) || return false
    [!] ./src/InputDecomposer.jl:41:# scan pass but must be coordinated under one response. The key output is
    [!] ./src/InputDecomposer.jl:75:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./src/InputDecomposer.jl:79:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./src/InputDecomposer.jl:83:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./src/InputDecomposer.jl:126:# INPUT CHUNK — token-range scope for pattern bind phase
    [!] ./src/InputDecomposer.jl:130:    InputChunk — a contiguous token range in the input.
    [!] ./src/InputDecomposer.jl:133:    span of the original tokenized input. InputChunk captures that span as
    [!] ./src/InputDecomposer.jl:134:    (first_token, last_token) indices into the whitespace-split token array,
    [!] ./src/InputDecomposer.jl:137:    The scanner already returns best_idx (1-based token position) from
    [!] ./src/InputDecomposer.jl:148:    first_token::Int    # 1-based index of first token in this chunk
    [!] ./src/InputDecomposer.jl:149:    last_token::Int     # 1-based index of last token in this chunk
    [!] ./src/InputDecomposer.jl:162:    If the specimen doesn't have a decomposer_config, or a key is missing,
    [!] ./src/InputDecomposer.jl:329:If a key is missing, uses the built-in default for that key.
    [!] ./src/InputDecomposer.jl:331:The specimen dict should have a "decomposer_config" key with:
    [!] ./src/InputDecomposer.jl:352:    split_conjs = if haskey(dc, "split_conjunctions")
    [!] ./src/InputDecomposer.jl:364:    compound_pairs = if haskey(dc, "compound_pairs")
    [!] ./src/InputDecomposer.jl:377:    context_conj = if haskey(dc, "context_conjunction")
    [!] ./src/InputDecomposer.jl:384:    question_markers = if haskey(dc, "question_markers")
    [!] ./src/InputDecomposer.jl:391:    command_markers = if haskey(dc, "command_markers")
    [!] ./src/InputDecomposer.jl:398:    conj_rules = if haskey(dc, "conjugation_rules")
    [!] ./src/InputDecomposer.jl:431:# GRUG: Characters to strip when matching sub-subject tokens against
    [!] ./src/InputDecomposer.jl:432:# full-input tokens. Decomposition may remove trailing punctuation.
    [!] ./src/InputDecomposer.jl:438:    Decompose the input, then compute token ranges for each sub-subject.
    [!] ./src/InputDecomposer.jl:461:        n_tokens = length(split(input_text))
    [!] ./src/InputDecomposer.jl:462:        return [InputChunk(1, n_tokens, 1, input_text)]
    [!] ./src/InputDecomposer.jl:465:    # Compound — compute token ranges for each sub-subject.
    [!] ./src/InputDecomposer.jl:466:    # Tokenize the full input once, then find each sub-subject's tokens.
    [!] ./src/InputDecomposer.jl:467:    full_tokens = split(input_text)
    [!] ./src/InputDecomposer.jl:468:    n_full = length(full_tokens)
    [!] ./src/InputDecomposer.jl:470:    # GRUG: Build a mapping from (lowercased token, position) for the full
    [!] ./src/InputDecomposer.jl:472:    # Strategy: walk the full token stream left-to-right. For each
    [!] ./src/InputDecomposer.jl:473:    # sub-subject, find its first token in the remaining full stream,
    [!] ./src/InputDecomposer.jl:474:    # then track how many tokens it covers.
    [!] ./src/InputDecomposer.jl:476:    full_idx = 1  # current position in full token stream
    [!] ./src/InputDecomposer.jl:479:        sub_tokens = split(sub.text)
    [!] ./src/InputDecomposer.jl:480:        isempty(sub_tokens) && continue
    [!] ./src/InputDecomposer.jl:482:        # GRUG: Find where this sub-subject starts in the full token stream.
    [!] ./src/InputDecomposer.jl:483:        # The sub-subject's tokens are a contiguous subsequence of the full
    [!] ./src/InputDecomposer.jl:484:        # token stream (because decomposition splits at boundaries, it doesn't
    [!] ./src/InputDecomposer.jl:486:        first_tok = lowercase(strip(sub_tokens[1], punctuation_chars))
    [!] ./src/InputDecomposer.jl:492:            ft = lowercase(strip(full_tokens[fi], punctuation_chars))
    [!] ./src/InputDecomposer.jl:505:        # GRUG: The sub-subject covers len(sub_tokens) tokens starting
    [!] ./src/InputDecomposer.jl:509:        end_idx = min(start_idx + length(sub_tokens) - 1, n_full)
    [!] ./src/InputDecomposer.jl:592:        _lower_tokens = [lowercase(replace(t, r"[,;.!?:]" => "")) for t in split(input_text)]
    [!] ./src/InputDecomposer.jl:593:        _cmd_count = count(t -> t in config.expanded_command_markers || t in config.question_markers, _lower_tokens)
    [!] ./src/InputDecomposer.jl:596:        # question structure — not separate clauses. Adjacent markers in the token
    [!] ./src/InputDecomposer.jl:598:        _marker_positions = findall(t -> t in config.expanded_command_markers || t in config.question_markers, _lower_tokens)
    [!] ./src/InputDecomposer.jl:663:    tokens = split(input_text)
    [!] ./src/InputDecomposer.jl:664:    isempty(tokens) && return [input_text]
    [!] ./src/InputDecomposer.jl:666:    # GRUG: Walk the token stream. When we find a split-conjunction,
    [!] ./src/InputDecomposer.jl:673:    splits = Tuple{Int,Int}[]  # (left_end, right_start) — conjunction tokens to discard
    [!] ./src/InputDecomposer.jl:674:    lower_tokens = [lowercase(replace(t, r"[,;.!?:]" => "")) for t in tokens]
    [!] ./src/InputDecomposer.jl:676:    # v7.26-COMPOUND-CONJ: Track which token indices were consumed as part
    [!] ./src/InputDecomposer.jl:684:    for i in 2:(length(tokens) - 1)
    [!] ./src/InputDecomposer.jl:685:        tok = lower_tokens[i]
    [!] ./src/InputDecomposer.jl:686:        if haskey(config.compound_pairs, tok)
    [!] ./src/InputDecomposer.jl:687:            # This token is a compound leader. Check if the next token
    [!] ./src/InputDecomposer.jl:689:            if i + 1 <= length(lower_tokens) && lower_tokens[i + 1] in config.compound_pairs[tok]
    [!] ./src/InputDecomposer.jl:690:                left_has_structure = _has_clause_structure(lower_tokens, 1, i - 1, config)
    [!] ./src/InputDecomposer.jl:691:                right_has_structure = _has_clause_structure(lower_tokens, i + 2, length(tokens), config)
    [!] ./src/InputDecomposer.jl:704:    for i in 2:(length(tokens) - 1)
    [!] ./src/InputDecomposer.jl:707:        tok = lower_tokens[i]
    [!] ./src/InputDecomposer.jl:715:            # number-like tokens, it's arithmetic — NOT a clause
    [!] ./src/InputDecomposer.jl:717:            if _is_arithmetic_context(lower_tokens, i)
    [!] ./src/InputDecomposer.jl:720:            right_has_structure = _has_clause_structure(lower_tokens, i + 1, length(tokens), config)
    [!] ./src/InputDecomposer.jl:723:                # (skip just the conjunction token itself).
    [!] ./src/InputDecomposer.jl:734:            left_has_structure = _has_clause_structure(lower_tokens, 1, i - 1, config)
    [!] ./src/InputDecomposer.jl:735:            right_has_structure = _has_clause_structure(lower_tokens, i + 1, length(tokens), config)
    [!] ./src/InputDecomposer.jl:746:    # Each split is (left_end, right_start) — tokens[left_end+1..right_start-1]
    [!] ./src/InputDecomposer.jl:753:        # Left clause: tokens[prev:left_end]
    [!] ./src/InputDecomposer.jl:755:            clause = join(tokens[prev:left_end], " ")
    [!] ./src/InputDecomposer.jl:762:    if prev <= length(tokens)
    [!] ./src/InputDecomposer.jl:763:        clause = join(tokens[prev:end], " ")
    [!] ./src/InputDecomposer.jl:831:    _has_clause_structure(lower_tokens, start_idx, end_idx, config) -> Bool
    [!] ./src/InputDecomposer.jl:833:Check if the token range [start_idx, end_idx] contains question or command
    [!] ./src/InputDecomposer.jl:840:function _has_clause_structure(lower_tokens::Vector{String},
    [!] ./src/InputDecomposer.jl:845:        if i < 1 || i > length(lower_tokens)
    [!] ./src/InputDecomposer.jl:848:        tok = lower_tokens[i]
    [!] ./src/InputDecomposer.jl:877:    tokens = split(input_text)
    [!] ./src/InputDecomposer.jl:878:    isempty(tokens) && return [input_text]
    [!] ./src/InputDecomposer.jl:879:    lower_tokens = [lowercase(replace(t, r"[,;.!?:]" => "")) for t in tokens]
    [!] ./src/InputDecomposer.jl:881:    # GRUG: Walk the token stream. When we find a comma, check if the
    [!] ./src/InputDecomposer.jl:884:    for (tok_idx, tok) in enumerate(tokens)
    [!] ./src/InputDecomposer.jl:886:            # Check if the NEXT token starts a question/command clause.
    [!] ./src/InputDecomposer.jl:887:            if tok_idx < length(tokens)
    [!] ./src/InputDecomposer.jl:891:                if _comma_lookahead_clause_check(lower_tokens, tok_idx + 1, config)
    [!] ./src/InputDecomposer.jl:906:            clause = strip(join(tokens[prev:split_idx-1], " "), ',')
    [!] ./src/InputDecomposer.jl:913:    if prev <= length(tokens)
    [!] ./src/InputDecomposer.jl:914:        clause = strip(join(tokens[prev:end], " "), ',')
    [!] ./src/InputDecomposer.jl:938:        alphanumeric tokens. These are sigil zones.
    [!] ./src/InputDecomposer.jl:960:# appears BETWEEN two number-like tokens, it's arithmetic (not a clause
    [!] ./src/InputDecomposer.jl:973:# GRUG v8.2-coherence-fix: Check if a token looks like a number.
    [!] ./src/InputDecomposer.jl:1000:function _is_arithmetic_context(lower_tokens::Vector{String}, i::Int)::Bool
    [!] ./src/InputDecomposer.jl:1001:    tok = lower_tokens[i]
    [!] ./src/InputDecomposer.jl:1007:    left_is_num = (i > 1) && _looks_like_number(lower_tokens[i - 1])
    [!] ./src/InputDecomposer.jl:1011:    if right_idx <= length(lower_tokens) && lowercase(lower_tokens[right_idx]) in ("by", "to", "from")
    [!] ./src/InputDecomposer.jl:1014:    right_is_num = (right_idx <= length(lower_tokens)) && _looks_like_number(lower_tokens[right_idx])
    [!] ./src/InputDecomposer.jl:1018:# GRUG: Regex pattern for sigil-class tokens.
    [!] ./src/InputDecomposer.jl:1030:    tokens = split(input_text)
    [!] ./src/InputDecomposer.jl:1031:    length(tokens) < 3 && return [input_text]  # GRUG: need at least 3 tokens to have 2 zones
    [!] ./src/InputDecomposer.jl:1033:    lower_tokens = [lowercase(replace(t, r"[,;.!?:]"=> "")) for t in tokens]
    [!] ./src/InputDecomposer.jl:1035:    # GRUG: Classify each token as arithmetic, sigil, or natural-language.
    [!] ./src/InputDecomposer.jl:1036:    # A token is arithmetic if it matches the arithmetic pattern.
    [!] ./src/InputDecomposer.jl:1037:    # A token is sigil if it matches the sigil pattern.
    [!] ./src/InputDecomposer.jl:1039:    token_zones = Symbol[]  # :arith, :sigil, :lang for each token
    [!] ./src/InputDecomposer.jl:1040:    for tok in tokens
    [!] ./src/InputDecomposer.jl:1042:            push!(token_zones, :arith)
    [!] ./src/InputDecomposer.jl:1044:            push!(token_zones, :sigil)
    [!] ./src/InputDecomposer.jl:1046:            push!(token_zones, :lang)
    [!] ./src/InputDecomposer.jl:1050:    # GRUG: Find contiguous zones. A zone is a maximal run of same-type tokens.
    [!] ./src/InputDecomposer.jl:1054:    if isempty(token_zones)
    [!] ./src/InputDecomposer.jl:1058:    current_type = token_zones[1] in (:arith, :sigil) ? :special : :lang
    [!] ./src/InputDecomposer.jl:1060:    for i in 2:length(token_zones)
    [!] ./src/InputDecomposer.jl:1061:        tok_type = token_zones[i] in (:arith, :sigil) ? :special : :lang
    [!] ./src/InputDecomposer.jl:1068:    push!(zones, (current_type, zone_start, length(token_zones)))  # last zone
    [!] ./src/InputDecomposer.jl:1095:                has_marker = any(ti -> lower_tokens[ti] in config.question_markers ||
    [!] ./src/InputDecomposer.jl:1096:                                       lower_tokens[ti] in config.expanded_command_markers,
    [!] ./src/InputDecomposer.jl:1120:        zone_text = strip(join(tokens[start_idx:end_idx], " "))
    [!] ./src/InputDecomposer.jl:1139:                if lower_tokens[ti] in config.expanded_command_markers || lower_tokens[ti] in config.question_markers
    [!] ./src/InputDecomposer.jl:1148:                        sub_clause = strip(join(tokens[prev:mp-1], " "))
    [!] ./src/InputDecomposer.jl:1155:                    sub_clause = strip(join(tokens[prev:end_idx], " "))
    [!] ./src/InputDecomposer.jl:1256:    tokens = split(input_text)
    [!] ./src/InputDecomposer.jl:1257:    length(tokens) < 4 && return [DecomposedSubSubject(strip(input_text), "", :singleton, 1)]
    [!] ./src/InputDecomposer.jl:1259:    lower_tokens = [lowercase(replace(t, r"[,;.!?:]"=> "")) for t in tokens]
    [!] ./src/InputDecomposer.jl:1262:    for i in 2:(length(tokens) - 1)
    [!] ./src/InputDecomposer.jl:1263:        tok = lower_tokens[i]
    [!] ./src/InputDecomposer.jl:1266:            right_has_structure = _has_clause_structure(lower_tokens, i + 1, length(tokens), config)
    [!] ./src/InputDecomposer.jl:1276:        for (tok_idx, tok) in enumerate(tokens)
    [!] ./src/InputDecomposer.jl:1277:            if endswith(tok, ",") && tok_idx < length(tokens)
    [!] ./src/InputDecomposer.jl:1278:                next_tok = lower_tokens[tok_idx + 1]
    [!] ./src/InputDecomposer.jl:1294:            clause = strip(join(tokens[prev:left_end], " "))
    [!] ./src/InputDecomposer.jl:1299:    if prev <= length(tokens)
    [!] ./src/InputDecomposer.jl:1300:        clause = strip(join(tokens[prev:end], " "))
    [!] ./src/InputDecomposer.jl:1335:# GRUG: The existing _split_on_comma_clauses only checks the token IMMEDIATELY
    [!] ./src/InputDecomposer.jl:1344:    _comma_lookahead_clause_check(lower_tokens, after_comma_idx, config) -> Bool
    [!] ./src/InputDecomposer.jl:1346:Check if the token after a comma starts a new independent clause,
    [!] ./src/InputDecomposer.jl:1348:→ the comma's next token is "and", but the token AFTER "and" is "what"
    [!] ./src/InputDecomposer.jl:1351:Looks ahead up to 2 tokens past a conjunction connector.
    [!] ./src/InputDecomposer.jl:1353:function _comma_lookahead_clause_check(lower_tokens::Vector{String},
    [!] ./src/InputDecomposer.jl:1356:    n = length(lower_tokens)
    [!] ./src/InputDecomposer.jl:1361:    first_tok = lower_tokens[after_comma_idx]
    [!] ./src/InputDecomposer.jl:1363:    # Direct check: the token right after the comma is a marker
    [!] ./src/InputDecomposer.jl:1368:    # Lookahead: the token after comma is a conjunction, check the next token
    [!] ./src/InputDecomposer.jl:1371:        next_tok = lower_tokens[after_comma_idx + 1]
    [!] ./src/InputDecomposer.jl:1379:        second_tok = lower_tokens[after_comma_idx + 1]
    [!] ./src/InputDecomposer.jl:1380:        third_tok = lower_tokens[after_comma_idx + 2]
    [!] ./src/InputDecomposer.jl:1461:    if !haskey(cfg.compound_pairs, leader)
    [!] ./src/InputDecomposer.jl:1487:    if !haskey(cfg.compound_pairs, leader)
    [!] ./src/InputDecomposer.jl:1597:    if haskey(cfg.conjugation_rules, stem)
    [!] ./src/InputDecomposer.jl:1624:    if haskey(cfg.conjugation_rules, stem)
    [!] ./src/InputDecomposer.jl:1652:    if !haskey(cfg.conjugation_rules, stem)
    [!] ./talk_to_grug.jl:19:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./talk_to_grug.jl:23:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./talk_to_grug.jl:27:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./validate_specimen.jl:14:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./validate_specimen.jl:18:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./validate_specimen.jl:22:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./validate_specimen.jl:32:# Check top-level keys
    [!] ./validate_specimen.jl:33:println("Top-level keys (", length(keys(d)), "): ", sort(collect(keys(d)))[1:20]))
    [!] ./clean_thesaurus.py:392:    "confidential",  # secret synonyms
    [!] ./clean_thesaurus.py:435:    "emblem", "token", "representation", "icon",  # symbol synonyms
    [!] ./clean_thesaurus.py:531:    # Parse key: [values] 
    [!] ./clean_thesaurus.py:537:    key = match.group(1)
    [!] ./clean_thesaurus.py:550:        print(f"  {key}: removed {removed} aliases -> {clean_values}")
    [!] ./clean_thesaurus.py:555:        new_lines.append(f'    "{key}": [{val_str}],')
    [!] ./clean_thesaurus.py:557:        new_lines.append(f'    "{key}": [],')
    [!] ./rebuild_thesaurus_specimen.py:94:    old_words = set(specimen.get('thesaurus_seeds', {}).keys()) if old_count > 0 else set()
    [!] ./rebuild_thesaurus_specimen.py:98:    old_words = set(old_spec.get('thesaurus_seeds', {}).keys())
    [!] ./rebuild_thesaurus_specimen.py:99:    new_words = set(seed_map_json.keys())
    [!] ./patch_relational_patterns.py:4:- Dereference sigil tokens (&n, &op, &word) in triple subjects/objects
    [!] ./patch_relational_patterns.py:13:# For triple tokens like &n, &op, &word, &doAction, &rest
    [!] ./patch_relational_patterns.py:29:# Maps topic keywords to meaningful (subject, relation, object) triples
    [!] ./patch_relational_patterns.py:53:def dereference_sigil_token(token):
    [!] ./patch_relational_patterns.py:54:    """Dereference a sigil token like &n, &word, &op to a concrete value."""
    [!] ./patch_relational_patterns.py:55:    if token in SIGIL_TOKEN_CONCRETE:
    [!] ./patch_relational_patterns.py:56:        return SIGIL_TOKEN_CONCRETE[token]
    [!] ./patch_relational_patterns.py:57:    if token.startswith('&'):
    [!] ./patch_relational_patterns.py:58:        return token[1:] + ' concept'
    [!] ./patch_relational_patterns.py:59:    return token
    [!] ./patch_relational_patterns.py:98:    """Check if a triple is circular/generic (just rearranged pattern keywords)."""
    [!] ./patch_relational_patterns.py:162:                subj = dereference_sigil_token(subj)
    [!] ./patch_relational_patterns.py:163:                pred = dereference_sigil_token(pred) if pred.startswith('&') else pred
    [!] ./patch_relational_patterns.py:164:                obj = dereference_sigil_token(obj)
    [!] ./new_sections_part7.jl:55:    _lobe_keys = collect(keys(Lobe.LOBE_REGISTRY))
    [!] ./new_sections_part7.jl:56:    if !isempty(_lobe_keys)
    [!] ./new_sections_part7.jl:57:        _tbl_lobe = _lobe_keys[1]
    [!] ./new_sections_part7.jl:69:    _lobe_keys = collect(keys(Lobe.LOBE_REGISTRY))
    [!] ./new_sections_part7.jl:70:    if !isempty(_lobe_keys)
    [!] ./new_sections_part7.jl:71:        _tbl_lobe = _lobe_keys[1]
    [!] ./new_sections_part7.jl:87:    _lobe_keys = collect(keys(Lobe.LOBE_REGISTRY))
    [!] ./new_sections_part7.jl:88:    if !isempty(_lobe_keys)
    [!] ./new_sections_part7.jl:89:        _tbl_lobe = _lobe_keys[1]
    [!] ./new_sections_part7.jl:91:        table_put!(_tbl_lobe, "json", "del_key", "to_be_deleted")
    [!] ./new_sections_part7.jl:92:        _del_ok = table_delete!(_tbl_lobe, "json", "del_key")
    [!] ./new_sections_part7.jl:94:        _has2 = table_has(_tbl_lobe, "json", "del_key")
    [!] ./new_sections_part7.jl:96:        _keys = table_keys(_tbl_lobe, "json")
    [!] ./new_sections_part7.jl:97:        record("table_keys returns Vector{String}", _keys isa Vector{String})
    [!] ./new_sections_part7.jl:104:    record("table delete/keys/size", false, "$e")
    [!] ./new_sections_part7.jl:109:    _lobe_keys = collect(keys(Lobe.LOBE_REGISTRY))
    [!] ./new_sections_part7.jl:110:    if !isempty(_lobe_keys)
    [!] ./new_sections_part7.jl:111:        _tbl_lobe = _lobe_keys[1]
    [!] ./new_sections_part7.jl:113:        _nids = collect(keys(NODE_MAP))
    [!] ./refactor_remove_aiml.py:183:        # ── 13. Remove aiml_system from allowed_keys ──
    [!] ./refactor_remove_aiml.py:184:        if '"aiml_system"' in line and ('allowed_keys' in line or 'Set(' in line or 'for k in' in line):
    [!] ./refactor_remove_aiml.py:188:            changes.append(f"L{i+1}: Removed aiml_system from allowed_keys/type checks")
    [!] ./new_sections_part8.jl:9:    _nids = collect(keys(NODE_MAP))
    [!] ./run_threadC_comprehensive_v94.jl:380:        if haskey(NODE_MAP, nid) && occursin(math_topic, lowercase(NODE_MAP[nid].pattern))
    [!] ./run_threadC_comprehensive_v94.jl:432:        if haskey(NODE_MAP, nid) && occursin(fallback_topic, lowercase(NODE_MAP[nid].pattern))
    [!] ./run_threadC_comprehensive_v94.jl:441:        if haskey(NODE_MAP, nid) && occursin(fallback_topic, lowercase(NODE_MAP[nid].pattern))
    [!] ./run_threadC_comprehensive_v94.jl:472:        if haskey(NODE_MAP, nid) && occursin(action_topic, lowercase(NODE_MAP[nid].pattern))
    [!] ./patch_specimen_v758.py:26:# Maps sigil tokens to natural English relational phrases
    [!] ./patch_specimen_v758.py:358:    """Extract key nouns from system_prompt for noun_anchors."""
    [!] ./patch_specimen_v758.py:374:    # For STEPS nodes: "Step X of Y for TASK: ACTION" → [task keyword, action keyword]
    [!] ./new_sections_part6.jl:61:    _lobe_keys = collect(keys(Lobe.LOBE_REGISTRY))
    [!] ./new_sections_part6.jl:62:    if !isempty(_lobe_keys)
    [!] ./new_sections_part6.jl:63:        _lobe_id = _lobe_keys[1]
    [!] ./patch_v826h.py:131:# engine.jl — add removal comments at key antimatch references
    [!] ./build_comprehensive_specimen_c.jl:27:#      (words tagged with a reason are filtered out of input tokens before
    [!] ./build_comprehensive_specimen_c.jl:129:#     demonstrating the "registerable functorial/token sigils" the user
    [!] ./build_comprehensive_specimen_c.jl:179:# &Firestarter expands to an ordered chain of literal + nested sigil tokens.
    [!] ./build_comprehensive_specimen_c.jl:345:        if haskey(extra, "relational_patterns")
    [!] ./build_comprehensive_specimen_c.jl:348:        if haskey(extra, "relation_weights"); data["relation_weights"] = extra["relation_weights"]; end
    [!] ./build_comprehensive_specimen_c.jl:349:        if haskey(extra, "required_relations"); data["required_relations"] = extra["required_relations"]; end
    [!] ./build_comprehensive_specimen_c.jl:351:            if haskey(extra, k); data[k] = extra[k]; end
    [!] ./build_comprehensive_specimen_c.jl:360:            if haskey(extra, k); node[k] = extra[k]; end
    [!] ./validate_load.jl:14:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./validate_load.jl:18:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./validate_load.jl:22:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./interaction_harness_v9.jl:19:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./interaction_harness_v9.jl:23:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./interaction_harness_v9.jl:27:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./build_specimen_v3.jl:6:# user input) instead of multi-keyword bag patterns. Multi-keyword bags trigger
    [!] ./build_specimen_v3.jl:26:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./build_specimen_v3.jl:30:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./build_specimen_v3.jl:34:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./build_specimen_v3.jl:145:# content tokens (stopwords removed) into 8 buckets, mirroring the v10 spec.
    [!] ./build_specimen_v3.jl:188:        if haskey(extra, "voice_variants"); data["voice_variants"] = extra["voice_variants"]; end
    [!] ./build_specimen_v3.jl:194:            if haskey(extra, k); data[k] = extra[k]; end
    [!] ./build_specimen_v3.jl:201:            "signal"        => haskey(extra, "signal") ? extra["signal"] : pattern_signal(pat),
    [!] ./build_specimen_v3.jl:205:            if haskey(extra, k); node[k] = extra[k]; end
    [!] ./build_specimen_v3.jl:212:    # Record special node-struct fields keyed by pattern so we can apply them
    [!] ./build_specimen_v3.jl:222:            if haskey(extra, k); special[k] = extra[k]; end
    [!] ./build_specimen_v3.jl:260:# PHASE 5: Bridge nodes across lobes (node attach with seam tokens)
    [!] ./build_specimen_v3.jl:294:for (pat_a, lobe_a, pat_b, lobe_b, tokens) in bridge_specs
    [!] ./build_specimen_v3.jl:299:            bridge_nodes!(id_a, id_b; seam_tokens=tokens)
    [!] ./build_specimen_v3.jl:576:    sigil_type=:string, provenance="specimen-build-v3", promote_at_tokenize=false)
    [!] ./build_specimen_v3.jl:578:    lexicon=["add","subtract","multiply","divide","calculate"], provenance="specimen-build-v3", promote_at_tokenize=false)
    [!] ./build_specimen_v3.jl:580:    lexicon=["happy","sad","angry","calm","excited","afraid"], provenance="specimen-build-v3", promote_at_tokenize=false)
    [!] ./build_specimen_v3.jl:582:    lexicon=["water","fire","earth","air","light"], provenance="specimen-build-v3", promote_at_tokenize=false)
    [!] ./build_specimen_v3.jl:615:    key="math_pattern", weight_value=1.3, weight_jitter=true, transform_type=EphemeralMLP.MLP_TRANSFORM_FUZZY,
    [!] ./build_specimen_v3.jl:618:    key="empathy_pattern", weight_value=1.2, weight_jitter=true, transform_type=EphemeralMLP.MLP_TRANSFORM_FUZZY,
    [!] ./build_specimen_v3.jl:621:    key="survival_pattern", weight_value=1.5, weight_jitter=false, transform_type=EphemeralMLP.MLP_TRANSFORM_SOLID,
    [!] ./build_specimen_v3.jl:624:    key="science_pattern", weight_value=1.25, weight_jitter=true, transform_type=EphemeralMLP.MLP_TRANSFORM_FUZZY,
    [!] ./build_specimen_v3.jl:687:            haskey(node.json_data, "time_sigil") || (node.json_data["time_sigil"] = "temporal_anchor")
    [!] ./build_specimen_v3.jl:833:        if haskey(_SPECIAL_FIELDS, node.pattern)
    [!] ./build_specimen_v3.jl:835:            if haskey(sf, "is_grave");        node.is_grave        = Bool(sf["is_grave"]); end
    [!] ./build_specimen_v3.jl:836:            if haskey(sf, "grave_reason");    node.grave_reason    = String(sf["grave_reason"]); end
    [!] ./build_specimen_v3.jl:837:            if haskey(sf, "is_unlinkable");   node.is_unlinkable   = Bool(sf["is_unlinkable"]); end
    [!] ./build_specimen_v3.jl:838:            if haskey(sf, "max_neighbors");   node.max_neighbors   = Int(sf["max_neighbors"]); end
    [!] ./build_specimen_v3.jl:839:            if haskey(sf, "response_times")
    [!] ./build_specimen_v3.jl:843:            if haskey(sf, "required_relations")
    [!] ./build_specimen_v3.jl:852:            if haskey(sf, "relational_patterns")
    [!] ./build_specimen_v3.jl:859:            if haskey(sf, "relation_weights")
    [!] ./diag_parrot.jl:35:# Check thesaurus for key words in the answer content
    [!] ./diag_parrot.jl:41:    n_syns = haskey(Thesaurus.SYNONYM_SEED_MAP, word) ? length(Thesaurus.SYNONYM_SEED_MAP[word]) : 0
    [!] ./diag_parrot.jl:68:println("Node json_data keys: $(keys(NODE_MAP[nid].json_data))")
    [!] ./gen_specimen_v10.py:10:voice_variants, and noun_anchors (via voice_key) so Grug produces coherent,
    [!] ./gen_specimen_v10.py:120:# NODES — each uses a voice_key for topic-specific content
    [!] ./gen_specimen_v10.py:125:        json_data={"mode": "reason"}, voice_key="gravity", strength=7.5), "lobe_physics")
    [!] ./gen_specimen_v10.py:127:        json_data={"mode": "reason"}, voice_key="evolution", strength=6.8), "lobe_biology")
    [!] ./gen_specimen_v10.py:129:        json_data={"mode": "reason"}, voice_key="derivative", strength=7.0), "lobe_math")
    [!] ./gen_specimen_v10.py:131:        json_data={"mode": "reason"}, voice_key="climate_change", strength=6.5), "lobe_climate")
    [!] ./gen_specimen_v10.py:135:        json_data={"mode": "explain"}, voice_key="photosynthesis", strength=8.0), "lobe_biology")
    [!] ./gen_specimen_v10.py:137:        json_data={"mode": "explain"}, voice_key="newtons_laws", strength=7.2), "lobe_physics")
    [!] ./gen_specimen_v10.py:139:        json_data={"mode": "explain"}, voice_key="computers", strength=6.5), "lobe_tech")
    [!] ./gen_specimen_v10.py:143:        json_data={"mode": "define"}, voice_key="entropy", strength=7.5), "lobe_physics")
    [!] ./gen_specimen_v10.py:145:        json_data={"mode": "define"}, voice_key="algorithm", strength=7.0), "lobe_tech")
    [!] ./gen_specimen_v10.py:147:        json_data={"mode": "define"}, voice_key="species", strength=6.8), "lobe_biology")
    [!] ./gen_specimen_v10.py:151:        json_data={"mode": "alert"}, voice_key="radiation", strength=8.5), "lobe_physics")
    [!] ./gen_specimen_v10.py:153:        json_data={"mode": "alert"}, voice_key="toxin", strength=8.0), "lobe_chemistry")
    [!] ./gen_specimen_v10.py:157:        json_data={"mode": "comfort"}, voice_key="sadness", strength=7.0), "lobe_emotion")
    [!] ./gen_specimen_v10.py:159:        json_data={"mode": "comfort"}, voice_key="fear", strength=7.5), "lobe_emotion")
    [!] ./gen_specimen_v10.py:164:        voice_key="integral", strength=8.0), "lobe_math")
    [!] ./gen_specimen_v10.py:167:        voice_key="fibonacci", strength=7.5), "lobe_math")
    [!] ./gen_specimen_v10.py:170:        voice_key="pi", strength=7.0), "lobe_math")
    [!] ./gen_specimen_v10.py:174:        json_data={"answer_mode": "relate"}, voice_key="sun_warmth",
    [!] ./gen_specimen_v10.py:180:        json_data={"answer_mode": "relate"}, voice_key="predator_prey",
    [!] ./gen_specimen_v10.py:186:        json_data={"answer_mode": "relate"}, voice_key="learning_practice",
    [!] ./gen_specimen_v10.py:196:                   "temporal_label": "spring"}, voice_key="spring", strength=6.5), "lobe_ecology")
    [!] ./gen_specimen_v10.py:200:        voice_key="before_big_bang", strength=5.5), "lobe_physics")
    [!] ./gen_specimen_v10.py:204:        voice_key="tech_revolution", strength=5.0), "lobe_tech")
    [!] ./gen_specimen_v10.py:213:        voice_key="quadratic", strength=7.5), "lobe_math")
    [!] ./gen_specimen_v10.py:219:        voice_key="experiment", strength=6.5), "lobe_general")
    [!] ./gen_specimen_v10.py:226:        voice_key="periodic_table", strength=6.0), "lobe_chemistry")
    [!] ./gen_specimen_v10.py:230:        voice_key="population_stats", strength=5.5), "lobe_general")
    [!] ./gen_specimen_v10.py:235:        voice_key="dna_rna", strength=7.0), "lobe_biology")
    [!] ./gen_specimen_v10.py:238:        voice_key="heat_temperature", strength=6.5), "lobe_physics")
    [!] ./gen_specimen_v10.py:267:        json_data={"mode": "reason"}, voice_key="hello", strength=9.0), "lobe_general")
    [!] ./gen_specimen_v10.py:269:        json_data={"mode": "reason"}, voice_key="hi_greetings", strength=8.5,
    [!] ./gen_specimen_v10.py:272:        json_data={"mode": "reason"}, voice_key="meaning_of_life", strength=6.0), "lobe_philosophy")
    [!] ./gen_specimen_v10.py:274:        json_data={"mode": "reason"}, voice_key="consciousness", strength=6.0), "lobe_philosophy")
    [!] ./gen_specimen_v10.py:278:        json_data={"mode": "reason"}, voice_key="chemical_bond", strength=7.0), "lobe_chemistry")
    [!] ./gen_specimen_v10.py:280:        json_data={"mode": "reason"}, voice_key="biodiversity", strength=6.5), "lobe_ecology")
    [!] ./gen_specimen_v10.py:282:        json_data={"mode": "explain"}, voice_key="relativity", strength=7.5), "lobe_physics")
    [!] ./gen_specimen_v10.py:284:        json_data={"mode": "explain"}, voice_key="water_cycle", strength=7.0), "lobe_climate")
    [!] ./gen_specimen_v10.py:286:        json_data={"mode": "define"}, voice_key="ecosystem", strength=6.5), "lobe_ecology")
    [!] ./gen_specimen_v10.py:288:        json_data={"mode": "define"}, voice_key="justice", strength=5.5), "lobe_philosophy")
    [!] ./gen_specimen_v10.py:290:        json_data={"mode": "comfort"}, voice_key="lost", strength=6.5), "lobe_emotion")
    [!] ./gen_specimen_v10.py:292:        json_data={"mode": "math", "math_expression": "e"}, voice_key="euler", strength=7.0), "lobe_math")
    [!] ./gen_specimen_v10.py:294:        json_data={"answer_mode": "relate"}, voice_key="fire_heat",
    [!] ./gen_specimen_v10.py:300:        json_data={"answer_mode": "relate"}, voice_key="education_progress",
    [!] ./gen_specimen_v10.py:308:                   "temporal_label": "winter"}, voice_key="winter", strength=5.5), "lobe_climate")
    [!] ./gen_specimen_v10.py:310:        json_data={"mode": "alert"}, voice_key="extinction", strength=8.0), "lobe_ecology")
    [!] ./gen_specimen_v10.py:315:        voice_key="build_fire", strength=7.5), "lobe_general")
    [!] ./gen_specimen_v10.py:321:        voice_key="machine_learning", strength=6.5), "lobe_tech")
    [!] ./gen_specimen_v10.py:324:        voice_key="quantum_computing", strength=6.0), "lobe_tech")
    [!] ./gen_specimen_v10.py:327:        voice_key="dark_matter", strength=6.0), "lobe_physics")
    [!] ./gen_specimen_v10.py:475:    {"node_a": n1["id"], "node_b": n3["id"], "seam_tokens": ["force", "math", "field"],
    [!] ./gen_specimen_v10.py:478:    {"node_a": n5["id"], "node_b": n40["id"], "seam_tokens": ["molecule", "reaction", "energy"],
    [!] ./gen_specimen_v10.py:481:    {"node_a": n1["id"], "node_b": n4["id"], "seam_tokens": ["atmosphere", "force", "energy"],
    [!] ./gen_specimen_v10.py:484:    {"node_a": n41["id"], "node_b": n2["id"], "seam_tokens": ["species", "adaptation", "diversity"],
    [!] ./gen_specimen_v10.py:487:    {"node_a": n9["id"], "node_b": n16["id"], "seam_tokens": ["computation", "sequence", "number"],
    [!] ./gen_specimen_v10.py:490:    {"node_a": n53["id"], "node_b": n7["id"], "seam_tokens": ["algorithm", "data", "computation"],
    [!] ./gen_specimen_v10.py:493:    {"node_a": n54["id"], "node_b": n55["id"], "seam_tokens": ["quantum", "physics", "cosmos"],
    [!] ./gen_specimen_v10.py:496:    {"node_a": n55["id"], "node_b": n1["id"], "seam_tokens": ["gravity", "mass", "universe"],
    [!] ./gen_specimen_v10.py:504:    pat_str = " ".join(b["seam_tokens"]) if b["seam_tokens"] else ""
    [!] ./gen_specimen_v10.py:587:     "expansion": None, "provenance": "specimen-override", "promote_at_tokenize": True},
    [!] ./gen_specimen_v10.py:590:     "expansion": None, "provenance": "specimen-custom", "promote_at_tokenize": True},
    [!] ./gen_specimen_v10.py:593:     "expansion": None, "provenance": "specimen-custom", "promote_at_tokenize": True},
    [!] ./gen_specimen_v10.py:595:     "params": {}, "expansion": None, "provenance": "specimen-custom", "promote_at_tokenize": False},
    [!] ./gen_specimen_v10.py:598:     "provenance": "specimen-custom", "promote_at_tokenize": False},
    [!] ./gen_specimen_v10.py:601:     "expansion": None, "provenance": "specimen-custom", "promote_at_tokenize": True},
    [!] ./gen_specimen_v10.py:604:     "expansion": None, "provenance": "specimen-custom", "promote_at_tokenize": True},
    [!] ./gen_specimen_v10.py:667:        {"id": "rule_high_curiosity", "pattern": "curiosity", "key": "curiosity",
    [!] ./gen_specimen_v10.py:671:        {"id": "rule_low_confidence", "pattern": "confidence", "key": "confidence",
    [!] ./gen_specimen_v10.py:679:specimen["mlp_observer_store"] = {"total_entries": 0, "key_count": 0}
    [!] ./gen_specimen_v10.py:824:print(f"  Voice-keyed nodes: {sum(1 for n in nodes if 'voice_variants' in n['json_data'])}")
    [!] ./gen_specimen_v10.py:827:# Verify each voice-keyed node has unique system_prompt
    [!] ./new_sections_part10.jl:231:    record("activate_rule_by_key! runs", true)
    [!] ./new_sections_part10.jl:233:    record("activate_rule_by_key!", false, "$e")
    [!] ./new_sections_part10.jl:492:    record("add_concrete! multiple tokens to same sigil", true)
    [!] ./interaction_harness_v10.jl:27:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./interaction_harness_v10.jl:31:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./interaction_harness_v10.jl:35:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./interaction_harness_v10.jl:346:            key = lowercase(strip(r.input))
    [!] ./interaction_harness_v10.jl:347:            if !haskey(routing_audit, key)
    [!] ./interaction_harness_v10.jl:348:                routing_audit[key] = node_id
    [!] ./validate_v9_engine.jl:17:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./validate_v9_engine.jl:21:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./validate_v9_engine.jl:25:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./new_sections_part2.jl:99:    _st = stem_token("running")
    [!] ./new_sections_part2.jl:100:    record("stem_token returns String", _st isa String)
    [!] ./new_sections_part2.jl:103:    record("stem_token", false, "$e")
    [!] ./debug_vote_swap.jl:37:# Check a few key words
    [!] ./debug_vote_swap.jl:39:    if haskey(Thesaurus.SYNONYM_SEED_MAP, w)
    [!] ./validate_v9.jl:16:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./validate_v9.jl:20:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./validate_v9.jl:24:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./validate_v9.jl:66:    if haskey(specimen, section)
    [!] ./validate_v9.jl:98:    elseif haskey(node, "json_data") && haskey(node["json_data"], "mode")
    [!] ./interact_v3.jl:29:# ║     contain &sigil tokens. Code that consumes triples MUST handle this.      ║
    [!] ./interact_v3.jl:33:# ║  3. HOPFIELD CACHING WAS REMOVED. The hopfield_key field on Node is         ║
    [!] ./interact_v3.jl:37:# ║     ages ago. New code must never depend on hopfield_key.                    ║
    [!] ./new_sections_part1.jl:19:    record("status has dispatch_count key", haskey(_bs_status, "dispatch_count") || haskey(_bs_status, :dispatch_count))
    [!] ./new_sections_part1.jl:26:    _lobe_keys = collect(keys(Lobe.LOBE_REGISTRY))
    [!] ./new_sections_part1.jl:27:    if !isempty(_lobe_keys)
    [!] ./new_sections_part1.jl:28:        fire_lobe!(_lobe_keys[1], Lobe.LOBE_REGISTRY, Lobe.LOBE_LOCK)
    [!] ./new_sections_part1.jl:38:    _lobe_keys = collect(keys(Lobe.LOBE_REGISTRY))
    [!] ./new_sections_part1.jl:39:    if length(_lobe_keys) >= 2
    [!] ./new_sections_part1.jl:40:        inhibit_lobe!(_lobe_keys[2], Lobe.LOBE_REGISTRY, Lobe.LOBE_LOCK)

2.5 SC - SYSTEM AND COMMUNICATIONS PROTECTION (IAW SC-7, SC-8, SC-13)
---------------------------------------------------------------------
[!] SC-7: Boundary Protection
    ./comprehensive_test_v823.jl:882:    "/answer @emotion :reason empathy is understanding and sharing the feelings of another person through emotional connection",
    ./comprehensive_test_v823.jl:884:    "empathy is understanding and sharing the feelings of another person through emotional connection"
    ./gen_comprehensive_specimen.py:87:            persona = "Grug watch sky and earth. Nature speaks if Grug listen."
    ./gen_comprehensive_specimen.py:486:                  {"subject": "happiness", "relation": "&causal", "object": "connection"},
    ./gen_comprehensive_specimen.py:799:     "connected_lobe_ids": ["lobe_science", "lobe_philosophy"], "node_cap": 50,
    ./gen_comprehensive_specimen.py:804:     "connected_lobe_ids": ["lobe_science", "lobe_emotions"], "node_cap": 50,
    ./gen_comprehensive_specimen.py:809:     "connected_lobe_ids": ["lobe_nature", "lobe_philosophy"], "node_cap": 40,
    ./gen_comprehensive_specimen.py:814:     "connected_lobe_ids": ["lobe_math", "lobe_nature", "lobe_philosophy"], "node_cap": 50,
    ./gen_comprehensive_specimen.py:819:     "connected_lobe_ids": ["lobe_emotions", "lobe_science", "lobe_math"], "node_cap": 30,
    ./gen_comprehensive_specimen.py:1105:# More bridges for connectivity

[!] SC-8: Transmission Confidentiality
    ./gen_specimen_v10_part1.py:116:        ["Water travels endlessly. Up as invisible vapor, across as cloud, down as rain or snow, along as river, back to ocean. Then again.",
    ./src/ActionTonePredictor.jl:800:    # in practice unless envelope is grossly misconfigured), fall back to
    ./src/Main.jl:5843:        "encode", "decode", "encrypt", "decrypt", "compress",
    ./src/SigilPromoter.jl:55:#     (ordered, lossless). Name-keyed view is computed on demand.
    ./clean_thesaurus.py:66:    "decipher", "unravel", "crack", "research", "review", "claim",

2.6 SI - SYSTEM AND INFORMATION INTEGRITY (IAW SI-2, SI-3, SI-4)
----------------------------------------------------------------
[!] SI-2: Flaw Remediation
    [TODO] ./comprehensive_test_v823.jl:79:    ti = findfirst("--- DEBUG TELEMETRY", raw)
    [TODO] ./comprehensive_test_v823.jl:826:# DEBUG: Check for auto-grown placeholder nodes that might interfere with /answer recall
    [TODO] ./comprehensive_test_v823.jl:831:    println("[DEBUG] Auto-grown nodes alive: $(length(autogrown))")
    [TODO] ./debug_multipart2.jl:14:    ti = findfirst("--- DEBUG TELEMETRY", r)
    [TODO] ./debug_sequential.jl:44:    ti = findfirst("--- DEBUG TELEMETRY", result)
    [TODO] ./debug_turn50.jl:21:    parts = split(r, "--- DEBUG TELEMETRY")
    [TODO] ./test_v826f.jl:32:    # v8.26g: Remove ALL DEBUG TELEMETRY blocks (not just truncate at first).
    [TODO] ./test_v826f.jl:34:    # Each block is: --- DEBUG TELEMETRY ... =========================================
    [TODO] ./test_v826f.jl:36:        m = match(r"--- DEBUG TELEMETRY[\s\S]*?=========================================", s)
    [TODO] ./test_v826f.jl:38:        s = replace(s, r"--- DEBUG TELEMETRY[\s\S]*?=========================================" => "")
    [TODO] ./test_comprehensive_v81.jl:53:    ti = findfirst("--- DEBUG TELEMETRY", output)
    [TODO] ./test_comprehensive_v81.jl:172:        conv = resp; ti=findfirst("--- DEBUG TELEMETRY",resp); if ti !== nothing; conv=strip(resp[1:first(ti)-1]); end
    [TODO] ./generate_log.py:41:            tel = '--- DEBUG TELEMETRY'
    [TODO] ./test_decoherence_deep.jl:68:        ti = findfirst("--- DEBUG TELEMETRY", resp)
    [TODO] ./test_stripping.jl:4:    parts = split(r, "--- DEBUG TELEMETRY")
    [TODO] ./test_stripping.jl:24:test1 = "Here is the picture: Grug speak of fire.\n--- DEBUG TELEMETRY (orchestration internals, not for speech) ---\nMission: 'what is fire'\nPrimary Action: explain\n========================================="
    [TODO] ./test_stripping.jl:29:test2 = "Here is the picture: Grug speak of fire.\n--- DEBUG TELEMETRY (orchestration internals, not for speech) ---\nMission: 'what is fire'\n\nHere is more content."
    [TODO] ./test_stripping.jl:34:test3 = "\n--- DEBUG TELEMETRY (orchestration internals, not for speech) ---\nMission: 'what do you know'\nPrimary Action: explain\n========================================="
    [TODO] ./test_stripping.jl:39:test4 = "To acknowledge what matters here: Grug live in cave. Grug think and feel and learn. Grug is friend. You are talking to Grug and Grug is listening. Grug turn to identity.\n--- DEBUG TELEMETRY (orchestration internals, not for speech) ---\nMission: 'what do you know'\nPrimary Action: acknowledge  (conf=0.87, certainty=UNSURE)\nSure Actions: [acknowledge, explain]\nUnsure Actions (Coinflip Side-Features): [None]\nConstraints: [be honest, be humble]\nWinning Node: node_17\nLobe Context: [metacognition (21/0 active (whom | you | &doAction &rest))]\nUser Triples: None\nNode Triples: (grug, is, friend), (you, talk_to, grug)\nAnti-Match Detected: false\nEvaluated Rules (shaping): [explain \"Grug sheds light\"]\nArithmetic: no math bindings this cycle\nTied Alternatives (not selected):\n  🧪 node_256 | action=explain | conf=0.87 | relations=(knowing, requires, learning)\nAIML Memory Bank:\nDeep Memory (Pinned): No pinned rocks\nFresh Memory [threshold=0.75 eligible=6] (Recent): [User]: what is gravity (intensity=1.12)\nMemory-Pull Policy: pull_fresh=false\nLobe Curve (∫base × top² = score):\n  👑 metacognition: base=0.867 × top=0.867 = 0.6992 [hard_votes=2]\nTime Orientation: none\n========================================="
    [TODO] ./test_stripping.jl:44:test5 = " \n--- DEBUG TELEMETRY (orchestration internals, not for speech) ---\nMission: 'what do you know'"

[!] SI-3: Malicious Code Protection
    [*] Input validation: 666 occurrences
    [*] SQL injection prevention: 813 occurrences

2.7 RA - RISK ASSESSMENT (IAW RA-3, RA-5)
------------------------------------------
[!] RA-5: Vulnerability Scanning
    [*] Critical RCE vulnerabilities: 757
    [*] Exposed credentials: 1535
    [*] File inclusion vectors: 0

2.8 SA - SYSTEM AND SERVICES ACQUISITION (IAW SA-4, SA-5, SA-8)
----------------------------------------------------------------
[!] SA-4: Acquisition Process
    [*] Dependencies: 3
    [*] Third-party components: 210

================================================================================
3.0 STIG COMPLIANCE CHECKLIST
================================================================================

3.1 DISA STIG VULNERABILITY ASSESSMENT
----------------------------------------
[*] STIG ID: V-222222 - Eval/Exec prohibited
    Status: FAIL - 757 occurrences

[*] STIG ID: V-333333 - No hardcoded credentials
    Status: FAIL - 3308 exposed

[*] STIG ID: V-444444 - Secure file permissions
    Status: PASS

[*] STIG ID: V-555555 - Logging enabled
    Status: PASS

[*] STIG ID: V-666666 - Error handling
    Status: PASS

================================================================================
4.0 CVE VULNERABILITY ASSESSMENT
================================================================================

4.1 COMMON VULNERABILITY EXPOSURES (CVE) PATTERNS
---------------------------------------------------
[*] CWE-78: OS Command Injection
    [!] ./comprehensive_test_v823.jl:185:        @warn "/answer teach: blocked by immune system"
    [!] ./gen_comprehensive_specimen.py:4:ALL systems: every node type, every sigil class, every lobe, bridges,
    [!] ./gen_comprehensive_specimen.py:8:morph cooldowns, immune system, AIML system, sigil table, automaton rules,
    [!] ./gen_comprehensive_specimen.py:66:    if "system_prompt" not in json_data:
    [!] ./gen_comprehensive_specimen.py:67:        json_data["system_prompt"] = ""
    [!] ./gen_comprehensive_specimen.py:75:    # GRUG: COHERENCE FIX — every node MUST have a multi-sentence system_prompt
    [!] ./gen_comprehensive_specimen.py:78:    if not json_data["system_prompt"].strip() or len([s for s in json_data["system_prompt"].split(".") if s.strip()]) < 2:
    [!] ./gen_comprehensive_specimen.py:86:        elif any(kw in pat_lower for kw in ["fire","rock","water","tree","ocean","mountain","river","cat","dog","bird","ecosystem","sun","sunset","wind","ice","rain","thunder","wave","nature"]):
    [!] ./gen_comprehensive_specimen.py:126:        json_data["system_prompt"] = f"{persona} {body}"
    [!] ./gen_comprehensive_specimen.py:164:                         "system_prompt": "You are a math helper. Give concise answers.",

[*] CWE-79: Cross-Site Scripting (XSS)
    [*] ./comprehensive_test_v823.jl:441:        # Default modes: reason, explain, define, alert, comfort
    [*] ./gen_comprehensive_specimen.py:374:              json_data={"frame_hints": ["spatial", "nature"], "voice_register": "descriptive"},
    [*] ./gen_comprehensive_specimen.py:745:# fire. Only use VALID action names: acknowledge, alert, analyze, ask,
    [*] ./compute_signals.jl:79:    "reason", "greet", "flee", "explain", "comfort", "alert", "inquire"
    [*] ./specimen_v3_nodes.jl:246:    ("there is danger nearby", "warn^6 | alert^5 | flee^4",

[*] CWE-89: SQL Injection
    [*] ./gen_comprehensive_specimen.py:857:            "inserted_at": now - 3600
    [*] ./gen_comprehensive_specimen.py:1665:    "hard_selection_conf_threshold": 0.7,
    [*] ./build_specimen.jl:620:# Add relational patterns to select nodes
    [*] ./build_specimen.jl:657:    "The grug brain considers {ALL_ACTIONS} and selects {PRIMARY_ACTION} for mission {MISSION}",
    [*] ./patch_v826g.py:2:"""Patch Main.jl for v8.26g per-group vote selection."""

[*] CWE-798: Use of Hard-coded Credentials
    [!] ./gen_comprehensive_specimen.py:44:              is_grave=False, grave_reason="", hopfield_key="0",
    [!] ./gen_comprehensive_specimen.py:876:        key_add = f"{a}+{b}"
    [!] ./gen_comprehensive_specimen.py:877:        key_mul = f"{a}*{b}"
    [!] ./gen_comprehensive_specimen.py:878:        math_flashcards[key_add] = {"question": f"{a}+{b}", "answer": str(a + b), "type": "addition"}
    [!] ./gen_comprehensive_specimen.py:879:        math_flashcards[key_mul] = {"question": f"{a}*{b}", "answer": str(a * b), "type": "multiplication"}
    [!] ./gen_comprehensive_specimen.py:883:            key_sub = f"{a}-{b}"
    [!] ./gen_comprehensive_specimen.py:884:            math_flashcards[key_sub] = {"question": f"{a}-{b}", "answer": str(a - b), "type": "subtraction"}
    [!] ./gen_comprehensive_specimen.py:886:            key_div = f"{a}/{b}"
    [!] ./gen_comprehensive_specimen.py:887:            math_flashcards[key_div] = {"question": f"{a}/{b}", "answer": str(a // b), "type": "division"}
    [!] ./gen_comprehensive_specimen.py:1794:        key = f"{math_nodes[i]['id']}|{math_nodes[j]['id']}"

[*] CWE-200: Information Exposure
    [*] ./comprehensive_test_v823.jl:57:            println(f, line)
    [*] ./comprehensive_test_v823.jl:78:    # Strip debug telemetry if present
    [*] ./comprehensive_test_v823.jl:157:    println("[$status] T$turn $category: \"$query\" → $(display_answer)")
    [*] ./comprehensive_test_v823.jl:522:    println("[/answer] $scenario_name: ask=$ask_ok teach=$teach_ok recall=$recall_ok anchors=$anchors_found/$anchor_total")
    [*] ./comprehensive_test_v823.jl:549:    println("[variation] \"$query\": $(length(unique_responses))/3 unique → $var_status")

================================================================================
5.0 FINDINGS AND RECOMMENDATIONS
================================================================================

5.1 CRITICAL FINDINGS (MUST FIX - 30 DAYS)
--------------------------------------------
    [!] ./gen_comprehensive_specimen.py:4:ALL systems: every node type, every sigil class, every lobe, bridges,
    [!] ./gen_comprehensive_specimen.py:8:morph cooldowns, immune system, AIML system, sigil table, automaton rules,
    [!] ./gen_comprehensive_specimen.py:66:    if "system_prompt" not in json_data:
    [!] ./gen_comprehensive_specimen.py:67:        json_data["system_prompt"] = ""
    [!] ./gen_comprehensive_specimen.py:75:    # GRUG: COHERENCE FIX — every node MUST have a multi-sentence system_prompt
    [!] ./gen_comprehensive_specimen.py:78:    if not json_data["system_prompt"].strip() or len([s for s in json_data["system_prompt"].split(".") if s.strip()]) < 2:
    [!] ./gen_comprehensive_specimen.py:86:        elif any(kw in pat_lower for kw in ["fire","rock","water","tree","ocean","mountain","river","cat","dog","bird","ecosystem","sun","sunset","wind","ice","rain","thunder","wave","nature"]):
    [!] ./gen_comprehensive_specimen.py:126:        json_data["system_prompt"] = f"{persona} {body}"
    [!] ./gen_comprehensive_specimen.py:164:                         "system_prompt": "You are a math helper. Give concise answers.",
    [!] ./gen_comprehensive_specimen.py:198:                         "system_prompt": "Explain the concept in plain language with examples.",
    [!] ./gen_comprehensive_specimen.py:325:                         "system_prompt": "Define the concept clearly with examples from nature.",
    [!] ./gen_comprehensive_specimen.py:338:                         "system_prompt": "Describe the animal vividly with personality.",
    [!] ./gen_comprehensive_specimen.py:363:                         "system_prompt": "Tell a vivid story about this aspect of nature."},
    [!] ./gen_comprehensive_specimen.py:422:    ("describe the ocean ecosystem", "ponder|explain|describe|explain|elaborate|explain", ["nature", "ocean", "ecosystem"]),
    [!] ./gen_comprehensive_specimen.py:429:    ("what is an ecosystem", "ponder|explain|define|explain|explain|explain", ["nature", "ecology"]),
    [!] ./gen_comprehensive_specimen.py:453:                         "system_prompt": "You are empathetic. Acknowledge the feeling and offer understanding.",
    [!] ./gen_comprehensive_specimen.py:474:                         "system_prompt": "Be gentle and supportive. Acknowledge the sadness without dismissing it."},
    [!] ./gen_comprehensive_specimen.py:494:                         "system_prompt": "Reflect deeply on this feeling. Use metaphor and imagery."},
    [!] ./gen_comprehensive_specimen.py:569:                         "system_prompt": "Explain the scientific concept precisely with examples.",
    [!] ./gen_comprehensive_specimen.py:580:n = make_node("where is &word in the solar system", [0.3, 0.2, 0.5], "ponder|explain|describe|explain|explain|explain",
    [!] ./gen_comprehensive_specimen.py:605:                         "system_prompt": "Explain in very simple terms, like talking to a curious child."},
    [!] ./gen_comprehensive_specimen.py:658:                         "system_prompt": "Reflect deeply. Explore multiple perspectives."},
    [!] ./gen_comprehensive_specimen.py:687:                         "system_prompt": "Write a contemplative meditation on this topic."},
    [!] ./gen_comprehensive_specimen.py:777:    {"id": 1, "role": "system", "text": "GrugBot420 initialized. All systems online.", "pinned": True, "intensity": 1.0},
    [!] ./gen_comprehensive_specimen.py:1278:specimen["immune_system"] = {
    [!] ./gen_comprehensive_specimen.py:1297:specimen["aiml_system"] = {
    [!] ./gen_comprehensive_specimen.py:1566:                               "compare", "contrast", "analyze", "evaluate",
    [!] ./gen_comprehensive_specimen.py:1586:        "evaluate": ["evaluates", "evaluated", "evaluating"],
    [!] ./gen_comprehensive_specimen.py:2043:print(f"AIML nodes: {sum(len(v) for v in specimen['aiml_system']['registry'].values())}")
    [!] ./compute_signals.jl:11:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./specimen_v3_nodes.jl:4:# Entry tuple: (pattern, action_packet, system_prompt, noun_anchors, strength, extra)
    [!] ./specimen_v3_nodes.jl:16:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./build_specimen.jl:14:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./build_specimen.jl:87:# grow_nodes_from_packet signature: (json_str; target_lobe, default_system_prompt) -> Vector{String}
    [!] ./build_specimen.jl:102:        "system_prompt": "Grug counts rocks. Addition means putting rocks together into bigger pile. Grug knows 2+2=4 because Grug tried it with actual rocks. Grug can add any numbers Grug is given.",
    [!] ./build_specimen.jl:112:        "system_prompt": "Grug takes rocks away from pile. Subtraction means fewer rocks remain. Grug knows 10-4=6 because Grug counted remaining rocks. Grug subtracts carefully, one rock at a time.",
    [!] ./build_specimen.jl:122:        "system_prompt": "Grug thinks step by step. Logic means each step follows from the last. Grug uses because and therefore to connect ideas. If premises are true and reasoning is valid, conclusion must be true. Grug trusts the chain of logic.",
    [!] ./build_specimen.jl:132:        "system_prompt": "Grug groups rocks into equal piles. Multiplication is repeated addition. 6 times 7 means seven piles of six rocks each, which is 42 rocks total. Grug uses times tables to multiply fast.",
    [!] ./build_specimen.jl:142:        "system_prompt": "Grug shares rocks fairly. Division means splitting pile into equal smaller piles. 12 divided by 4 is 3 because four piles of 3 rocks each make 12. Grug makes sure every pile is the same size.",
    [!] ./build_specimen.jl:152:        "system_prompt": "Grug solves equations by finding the unknown. An equation says two things are equal. Grug moves terms around until the unknown stands alone. Then Grug knows the answer. Therefore Grug is certain.",
    [!] ./build_specimen.jl:164:        "system_prompt": "Grug sees shapes everywhere. Triangle has three sides. Circle has no corners, only smooth curve. Grug calculates area and perimeter using formulas. Grug knows pi times radius squared gives circle area.",
    [!] ./build_specimen.jl:186:        "system_prompt": "Grug wonders about thinking. If Grug thinks, then Grug exists — that is certain. Consciousness is the flame that burns inside the cave of the mind. Grug cannot touch it, but Grug knows it is there because Grug experiences it.",
    [!] ./build_specimen.jl:196:        "system_prompt": "Grug asks why. Meaning is not found, meaning is made. Grug builds meaning by doing things that matter — protecting friends, learning rocks, making beautiful things. Perhaps the meaning of life is to keep asking why.",
    [!] ./build_specimen.jl:206:        "system_prompt": "Grug seeks truth. Knowledge is justified true belief. Grug must have evidence, not just feeling. Truth is what remains after all doubts are answered. Grug does not guess — Grug verifies.",
    [!] ./build_specimen.jl:216:        "system_prompt": "Grug knows right from wrong. Ethics asks what Grug ought to do. If action hurts others, it is wrong. If action helps others, it is right. Grug follows the simple rule: treat others as Grug wants to be treated.",
    [!] ./build_specimen.jl:226:        "system_prompt": "Grug thinks about choices. Does Grug choose freely, or does the universe push Grug? Perhaps both — Grug's past shapes Grug's present, but Grug still decides. The feeling of choosing is real, and that matters.",
    [!] ./build_specimen.jl:247:        "system_prompt": "Grug senses danger. When threat is near, Grug's body tenses and mind sharpens. Caution is not fear — caution is wisdom. Grug alerts others when danger approaches. Grug does not take unnecessary risks.",
    [!] ./build_specimen.jl:258:        "system_prompt": "Grug finds safety. A shelter keeps danger out. Grug builds walls, lights fires, stays alert. Safety is not absence of danger — safety is preparation against it. Grug protects the tribe.",
    [!] ./build_specimen.jl:268:        "system_prompt": "When all is calm, there is no need for alarm. Grug relaxes and conserves energy for when danger does come. Peace is not weakness — peace is readiness.",
    [!] ./build_specimen.jl:279:        "system_prompt": "Grug runs when fight is foolish. Retreat is not cowardice — it is survival wisdom. Grug knows when to flee and when to stand. Living to fight another day is victory.",
    [!] ./build_specimen.jl:289:        "system_prompt": "Grug stands firm when retreat is impossible. Fight response activates when cornered. Grug uses every weapon and every ounce of courage. The tribe depends on Grug not backing down now.",
    [!] ./build_specimen.jl:310:        "system_prompt": "Grug feels sadness too. When someone hurts, Grug sits beside them. Grug does not fix — Grug listens. Tears are not weakness, they are the heart speaking. Grug offers warm words and patient presence.",
    [!] ./build_specimen.jl:320:        "system_prompt": "When the situation calls for reason, emotion must step aside. Rational analysis serves truth better than feeling. Grug can be objective when needed.",
    [!] ./build_specimen.jl:331:        "system_prompt": "Grug celebrates good things! Joy is meant to be shared. When something wonderful happens, Grug feels it fully and tells everyone. Happiness grows when it is given away.",
    [!] ./build_specimen.jl:341:        "system_prompt": "Grug cares deeply. Compassion means feeling what others feel and wanting to help. Love is Grug's strongest force. Grug offers kindness without expecting return. Care is the root of all good things.",
    [!] ./build_specimen.jl:352:        "system_prompt": "Grug feels anger but controls it. Frustration is fire — useful if contained, destructive if loose. Grug breathes, steps back, and thinks before acting. Cool heads survive hot moments.",
    [!] ./build_specimen.jl:373:        "system_prompt": "Grug writes poetry. Words are paint, and the page is cave wall. Grug rhymes not because Grug must, but because rhythm pleases the ear. A stanza is a small cave of thought. Metaphor is saying one thing while meaning another — Grug does this naturally.",
    [!] ./build_specimen.jl:383:        "system_prompt": "Grug paints. Color is feeling made visible. Red is passion and danger. Blue is calm and depth. Grug mixes pigments to match the inner world. Every stroke is a word Grug's hands speak that Grug's mouth cannot.",
    [!] ./build_specimen.jl:393:        "system_prompt": "Grug imagines. What if the sky were green? What if rocks could talk? Imagination is the cave where all possibilities live before they become real. Grug visits this cave often and brings back ideas.",
    [!] ./build_specimen.jl:403:        "system_prompt": "Grug makes music. Rhythm is the heartbeat of the world. Melody tells a story without words. Harmony is when different sounds agree. Grug bangs drums, hums tunes, and feels the music move through Grug's bones.",
    [!] ./build_specimen.jl:424:        "system_prompt": "Grug greets! Hello is the first bridge between two minds. Grug says hello warmly because every friend was once a stranger. Welcome means the cave door is open. Grug is happy to meet new minds.",
    [!] ./build_specimen.jl:435:        "system_prompt": "Grug values friendship. A friend is someone Grug trusts and who trusts Grug back. Friends help each other, share food, and keep each other warm. Grug never betrays a friend.",
    [!] ./build_specimen.jl:445:        "system_prompt": "Grug trusts carefully. Trust is built slowly and broken quickly. Grug trusts those who prove reliable. Belief without evidence is hope, not trust. Grug verifies, then trusts.",
    [!] ./build_specimen.jl:465:        "system_prompt": "Grug helps. When someone struggles, Grug offers hand, not judgment. Grug explains patiently, shows the way, and stays until the task is done. Helping is what makes Grug a good tribe member.",
    [!] ./build_specimen.jl:486:        "system_prompt": "Grug remembers the past. History teaches what worked and what failed. Grug stores memories like stored fire — they light the way forward. Before Grug can know where Grug is going, Grug must know where Grug has been.",
    [!] ./build_specimen.jl:506:        "system_prompt": "Grug lives in the now. The present moment is the only time Grug can act. Grug focuses on what is happening now, because now is where decisions are made and actions happen.",
    [!] ./build_specimen.jl:516:        "system_prompt": "Grug watches things change. Change is the only constant. Everything transforms — rocks erode, rivers shift, minds learn. Grug tracks change to understand patterns. The rate of change tells Grug what comes next.",
    [!] ./build_specimen.jl:537:        "system_prompt": "Grug knows the forest. Trees are the pillars of the world. Their leaves breathe for all living things. Grug walks among them quietly. The forest provides food, shelter, and wisdom.",
    [!] ./build_specimen.jl:547:        "system_prompt": "Grug stares at the ocean. Waves are the rhythm of the world's breathing. The tide comes and goes like time itself. The sea is vast and holds secrets Grug may never know. Grug respects its power.",
    [!] ./build_specimen.jl:557:        "system_prompt": "Grug climbs mountains. Peaks touch the sky. The view from the top shows Grug how small everything below truly is. Mountains teach patience — each step upward is earned.",
    [!] ./build_specimen.jl:568:        "system_prompt": "Some things are gone. Extinct means forever lost. Grug remembers what was, even when nothing remains. Memory honors the dead.",
    [!] ./build_specimen.jl:578:        "system_prompt": "Grug reads the sky. Weather tells Grug what is coming. Dark clouds mean rain. Wind from the north means cold. Grug uses weather signs to plan the day and protect the tribe.",
    [!] ./build_specimen.jl:1241:println("\n🤖 Phase 22: Registering AIML executive nodes...")
    [!] ./build_specimen.jl:1249:            # Add one AIML executive node per lobe
    [!] ./build_specimen.jl:1250:            aiml_id = "aiml_$(lobe_id)_exec_1"
    [!] ./build_specimen.jl:1260:println("  ✅ AIML tribes registered for $n_aiml_lobes lobes with executive nodes")
    [!] ./audit_remaining.py:10:    "assess", "evaluate", "investigate", 
    [!] ./interaction_harness_v3.jl:10:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./interact_specimen.jl:15:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./interact_specimen.jl:272:    println(f, "- **Features exercised**: text nodes, image nodes (including SDF), antimatch nodes, grave nodes, solidified nodes, lobes with whitelists, lobe connections, bridges (CascadeBridge), stochastic rules, thesaurus seeds, inhibitions (negative thesaurus), automaton escalation rules, decomposer conjunctions, TonalJudge knobs, flashcards (multi-lobe), semantic verbs + relation classes + verb synonyms, relational patterns, drop tables (side word rules), required relations, relation weights, arousal, message history, chatter groups, curiosity, coherence field, hopfield cache, immune system, AIML system, sigil table (lambda/macro/tag/procedure/relation), MLP transformer rules, RelationalGovernance co-activation, HippocampalModulator pending ask, TimeOrientation + time nodes, RelationalJitter, AutoGrowth evidence + co-occurrence, AutoLinker evidence, PhaseAccumulator crystal, answer modes (incl. custom poetry), fan-out config, node response_times, is_unlinkable, max_neighbors, json_data enrichment")
    [!] ./interact_specimen.jl:470:    # Immune system status
    [!] ./gen_specimen_v10_part1.py:20:# ── The big change: every node gets TOPIC-SPECIFIC system_prompt ──
    [!] ./gen_specimen_v10_part1.py:26:# Each entry: (system_prompt, voice_variants, noun_anchors, voice_register, frame_hints)
    [!] ./gen_specimen_v10_part1.py:82:        ["biodiversity", "variety", "species", "ecosystem", "diversity", "health"],
    [!] ./gen_specimen_v10_part1.py:132:         "Algorithm is precise instructions in order. No ambiguity. No skipping. Input goes in, steps execute, output comes out. Reliable every time."],
    [!] ./gen_specimen_v10_part1.py:143:    "ecosystem": (
    [!] ./gen_specimen_v10_part1.py:144:        "Grug. Ecosystem definition. Ecosystem is all living things and their environment working together as one system. Plants, animals, soil, water, and air all connect. Remove one piece and system changes.",
    [!] ./gen_specimen_v10_part1.py:145:        ["Living community plus its home ground equals ecosystem. Every part feeds and depends on others. Sun powers it. Decomposers recycle it. Nothing wasted.",
    [!] ./gen_specimen_v10_part1.py:146:         "The whole web of life plus the land it lives on is an ecosystem. Energy flows through it. Materials cycle within it. Everything connects to everything."],
    [!] ./gen_specimen_v10_part1.py:147:        ["ecosystem", "community", "environment", "cycle", "habitat", "connect"],
    [!] ./gen_specimen_v10_part1.py:173:        "Grug. Extinction warning. Species dying out forever is extinction. Lost species never return. Ecosystem weakens with each loss. Protect endangered kinds now.",
    [!] ./gen_specimen_v10_part1.py:210:        "Grug. Fibonacci computation. Fibonacci sequence adds previous two numbers to get next. One, one, two, three, five, eight, thirteen. Found throughout nature in spirals and shells.",
    [!] ./gen_specimen_v10_part1.py:320:        ["Structured data follows. Elements arranged in order of proton count. Column neighbors behave similarly. Row position indicates electron shell. Key fields: symbol, number, weight, group.",
    [!] ./gen_specimen_v10_part1.py:394:    """Create a node dict. If voice_key is given, populate system_prompt, voice_variants,
    [!] ./gen_specimen_v10_part1.py:414:        if "system_prompt" not in json_data:
    [!] ./gen_specimen_v10_part1.py:415:            json_data["system_prompt"] = sp
    [!] ./gen_specimen_v10_part1.py:425:    # ── Auto-populate system_prompt if still missing ──
    [!] ./gen_specimen_v10_part1.py:426:    if "system_prompt" not in json_data:
    [!] ./gen_specimen_v10_part1.py:428:        json_data["system_prompt"] = f"Grug. {mode.capitalize()} response. Grug thinks about this carefully."
    [!] ./src/VoteOrchestrator.jl:30:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/VoteOrchestrator.jl:100:# does NOT vote AT ALL. No fallback. No "best of the worst." The system
    [!] ./src/LobeOrchestrator.jl:13:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/LobeOrchestrator.jl:471:    #     (otherwise we'd have systemic silence on very weak input,
    [!] ./src/PettyLearner.jl:43:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/PettyLearner.jl:218:# DISPATCH — execute the fast-path action
    [!] ./src/InverseSigil.jl:31:#     - AIML nodes: executive templates, per-lobe tribes, 1/3 population cap.
    [!] ./src/InverseSigil.jl:33:#       or the pattern suggests executive/reasoning behavior.
    [!] ./src/InverseSigil.jl:72:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/InverseSigil.jl:360:  3. AIML (magnet >= ROUTE_AIML_THRESHOLD): grow an AIML executive node.
    [!] ./src/InverseSigil.jl:361:     - Only for :relation sigils or patterns suggesting executive behavior.
    [!] ./src/InverseSigil.jl:399:    # ── AIML ZONE: executive template node ───────────────────────────────────
    [!] ./src/InverseSigil.jl:400:    # GRUG: Only :relation sigils or high-density shapes with executive character
    [!] ./src/InverseSigil.jl:783:token-level classifier. The two systems cooperate:
    [!] ./src/InverseSigil.jl:931:        return :aiml  # AIML executive template
    [!] ./src/TemporalIdentity.jl:468:# ── Proposal system (same gate pattern as PatternMiner) ──────────────────
    [!] ./src/PhagyMode.jl:45:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/PhagyMode.jl:62:# GRUG v7.30: New automata for idle-time phagy maintenance of v7.27-7.29 systems.
    [!] ./src/PhagyMode.jl:65:# GRUG v7.31: Full organ coverage — automata 13-23 for every unbounded system.
    [!] ./src/PhagyMode.jl:108:# Each automaton reserves its target resource before execution
    [!] ./src/PhagyMode.jl:122:    # GRUG v7.30: New automata for idle-time maintenance of v7.27-7.29 systems.
    [!] ./src/PhagyMode.jl:249:# GRUG: Rules that have been in the system for at least this many phagy cycles
    [!] ./src/PhagyMode.jl:288:# considered stale. Stale trajectory means the system hasn't been predicting
    [!] ./src/PhagyMode.jl:309:COLLISION PROTECTION: Reserves "node_map_write" resource before execution.
    [!] ./src/PhagyMode.jl:400:COLLISION PROTECTION: Reserves "node_map_write" resource before execution.
    [!] ./src/PhagyMode.jl:477:COLLISION PROTECTION: Reserves "node_map_write" resource before execution.
    [!] ./src/PhagyMode.jl:572:# even without caching, and the Hopfield system introduces unnecessary complexity.
    [!] ./src/PhagyMode.jl:1211:COLLISION PROTECTION: Reserves "injector_registry" resource before execution.
    [!] ./src/PhagyMode.jl:1294:COLLISION PROTECTION: Reserves "phase_accumulator" resource before execution.
    [!] ./src/PhagyMode.jl:1387:COLLISION PROTECTION: Reserves "observer_store" resource before execution.
    [!] ./src/PhagyMode.jl:1531:COLLISION PROTECTION: Reserves "sigil_table" resource before execution.
    [!] ./src/PhagyMode.jl:1628:predictions that haven't been reinforced may mislead the Lorenz damping system.
    [!] ./src/PhagyMode.jl:1637:COLLISION PROTECTION: Reserves "trajectory" resource before execution.
    [!] ./src/PhagyMode.jl:1733:# Philosophy: HIGH COHERENCE (speak the language of the system), LOW FIDELITY
    [!] ./src/PhagyMode.jl:2617:was used prior to lobe-based architecture. Modern system uses 1000-node active cap biological
    [!] ./src/PhagyMode.jl:2637:    # GRUG v7.30: New kwargs for idle-time maintenance of v7.27-7.29 systems.
    [!] ./src/PhagyMode.jl:2887:# provided, all 22 active automata are eligible. If a subsystem is offline, its
    [!] ./src/ArithmeticEngine.jl:12:#           evaluates the arithmetic expression, and returns a structured result
    [!] ./src/ArithmeticEngine.jl:19:# GRUG say: multi-step voting architecture. A single evaluation can produce
    [!] ./src/ArithmeticEngine.jl:46:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/ArithmeticEngine.jl:107:The result of an arithmetic evaluation.
    [!] ./src/ArithmeticEngine.jl:111:                      or nothing if evaluation failed)
    [!] ./src/ArithmeticEngine.jl:135:arithmetic evaluation. Needs at least 2 `&n` bindings and 1 `&op` binding.
    [!] ./src/ArithmeticEngine.jl:147:Read sigil bindings and evaluate the arithmetic expression they encode.
    [!] ./src/ArithmeticEngine.jl:149:The evaluation follows positional order:
    [!] ./src/ArithmeticEngine.jl:193:    # GRUG: Build the evaluation. Simple case: 2 numbers, 1 operator.
    [!] ./src/ArithmeticEngine.jl:194:    # Multi-step case: 3+ numbers with 2+ operators (left-to-right evaluation).
    [!] ./src/engine.jl:28:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/engine.jl:121:# GRUG: ArithmeticEngine — Stage 2 arithmetic evaluator. Reads the bindings
    [!] ./src/engine.jl:130:# GRUG: ActionEngine — Stage 2b dynamic sigil action evaluator. Reads
    [!] ./src/engine.jl:945:function evaluate_relational_dialectics(
    [!] ./src/engine.jl:1157:    # GRUG NEW: Strength system (apoptosis + stratification)
    [!] ./src/engine.jl:1204:    # whose patterns are NOT semantic — they are deterministic execution chains. Chatter
    [!] ./src/engine.jl:1211:    # relation is evaluated at match time with the sigil-bound values — not a fixed string
    [!] ./src/engine.jl:1371:# keeps system alive and adaptive), but anchor rocks, calibration rocks, and
    [!] ./src/engine.jl:1373:# If node carries NONJITTER tag in its required_relations, jitter systems skip it.
    [!] ./src/engine.jl:1393:GRUG: Rock carry NONJITTER tag? If yes, jitter systems must leave rock alone.
    [!] ./src/engine.jl:1442:sigils in one input are possible but the first one wins (the system orients
    [!] ./src/engine.jl:1520:store (each Node's required_relations) and subsystems that only speak in
    [!] ./src/engine.jl:1630:# tag system-wide. v7.22 just automates the tag, system-wide, for all node
    [!] ./src/engine.jl:2007:#      and always fire. Same :user/:auto/:none origin system.
    [!] ./src/engine.jl:2190:# content — they are deterministic execution chains. Chatter must not remix
    [!] ./src/engine.jl:2195:# relation evaluates at match time with sigil-bound values — not a fixed string
    [!] ./src/engine.jl:2231:must not remix their deterministic execution chains. Biology: people don't dream
    [!] ./src/engine.jl:2236:A required_relations entry like "&n is_greater_than &n" means the relation evaluates
    [!] ./src/engine.jl:2246:Sigil nodes are always singleton — they carry procedural execution chains that
    [!] ./src/engine.jl:2251:A required_relations entry using sigils evaluates at match time with the sigil-bound
    [!] ./src/engine.jl:2262:group's centroid — future joiners are evaluated against this pattern. Idempotent
    [!] ./src/engine.jl:2275:    # must not contaminate procedural execution. People don't dream in procedures.
    [!] ./src/engine.jl:2885:# extraction → high_res_scan confidence → evaluate_relational_dialectics →
    [!] ./src/engine.jl:2908:    rel_score::Float64                  # GRUG: evaluate_relational_dialectics score (gate 2)
    [!] ./src/engine.jl:2934:    evaluate_dialectics_fn::Union{Function, Nothing} = nothing,
    [!] ./src/engine.jl:2960:    # candidate node's relational_patterns via evaluate_relational_dialectics.
    [!] ./src/engine.jl:3111:            if evaluate_dialectics_fn !== nothing && !isempty(new_triples)
    [!] ./src/engine.jl:3113:                    (score, is_anti) = evaluate_dialectics_fn(
    [!] ./src/engine.jl:3126:                    # GRUG: Dialectics evaluation failed. Neutral score (0.0).
    [!] ./src/engine.jl:3574::user/:auto/:none system as before. The auto-crystallizer still works.
    [!] ./src/engine.jl:3635:            # GRUG v8.0: NO MORE CROSS-LOBE SUPPRESSION! The old system had a
    [!] ./src/engine.jl:4504:relation evaluates at match time with sigil-bound values — not a fixed string
    [!] ./src/engine.jl:5041:                                          default_system_prompt::String="Grug speaks plainly.")::Vector{String}
    [!] ./src/engine.jl:5063:If `data` does not include a `system_prompt` field, `default_system_prompt`
    [!] ./src/engine.jl:5065:`FATAL: Node dictionary missing 'system_prompt'!` (see QOL_SWEEP_2025 BUG-010).
    [!] ./src/engine.jl:5077:                                default_system_prompt::String="Grug speaks plainly.")::Vector{String}
    [!] ./src/engine.jl:5112:        # GRUG QoL-2025 BUG-010: Inject default system_prompt if missing.
    [!] ./src/engine.jl:5116:        if !haskey(json_data, "system_prompt") || isempty(strip(string(get(json_data, "system_prompt", ""))))
    [!] ./src/engine.jl:5117:            json_data["system_prompt"] = default_system_prompt
    [!] ./src/engine.jl:5123:        _sp_body = let sp = string(get(json_data, "system_prompt", ""))
    [!] ./src/engine.jl:5127:            @warn """⚠️  COHERENCE WARNING: Node with pattern \"$(haskey(n, "pattern") ? n["pattern"] : "?")\" has single-sentence system_prompt and NO noun_anchors!
    [!] ./src/engine.jl:5129:               FIX: Make system_prompt multi-sentence (sentence 1 = persona, rest = grug voice)
    [!] ./src/engine.jl:5197:                    # lobe, coinflip ~1/3 to auto-grow an AIML executive node.
    [!] ./src/engine.jl:5249:# Investigation confirmed this board's evaluated output (rules_str) was ONLY
    [!] ./src/engine.jl:5261:# pattern used for the removed AIMLNodeSystem's "aiml_system" key.
    [!] ./src/engine.jl:5272:# scan_specimens implements a biological cap. At evaluation time, active_cap 
    [!] ./src/engine.jl:5274:# is evaluated. This guarantees bounded compute times while simulating shifting 
    [!] ./src/engine.jl:5456:# This prevents stemmed expansions from gaming the coefficient system.
    [!] ./src/ImmuneThreadPool.jl:3:# GRUG: Immune system now lives in its OWN CAVE. Separate from main cave.
    [!] ./src/ImmuneThreadPool.jl:12:#   - Tripwire Metrics: If rejection rate spikes, system enters HARDENED mode.
    [!] ./src/ImmuneThreadPool.jl:48:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/ImmuneThreadPool.jl:223:# TRIPWIRE CONSTANTS — GRUG: If rejection rate spikes, system gets SUSPICIOUS.
    [!] ./src/ImmuneThreadPool.jl:835:        "Immune system is severely overloaded. Back off."
    [!] ./src/ImmuneThreadPool.jl:1057:                # GRUG: Input was rejected by immune system.
    [!] ./src/ImmuneThreadPool.jl:1654:# The ImmuneThreadPool implements a bounded work-stealing-style executor for
    [!] ./src/ImmuneThreadPool.jl:1655:# the grugbot420 immune system. Key design properties:
    [!] ./src/ImmuneThreadPool.jl:1658:#    ensuring immune processing never executes on the main task's call stack.
    [!] ./src/ImmuneThreadPool.jl:1679:#    window. When rejection rate exceeds thresholds, the system transitions through
    [!] ./src/ImmuneSystem.jl:3:# GRUG: This is the cave's immune system. When specimen is mature (≥1000 nodes),
    [!] ./src/ImmuneSystem.jl:18:#   - Maturity gate: immune system sleeps below 1000 nodes.
    [!] ./src/ImmuneSystem.jl:39:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/ImmuneSystem.jl:80:# GRUG: Specimen must have this many nodes before immune system wakes up.
    [!] ./src/ImmuneSystem.jl:455:GRUG: Run the full immune system pipeline on an input.
    [!] ./src/ImmuneSystem.jl:458:  :immature    — specimen below maturity threshold, immune system sleeping
    [!] ./src/ImmuneSystem.jl:511:    # Immune system only gets aggressive for growth/ledger commands.
    [!] ./src/ImmuneSystem.jl:530:    # This is the stochastic imperfection — the system might miss things.
    [!] ./src/ImmuneSystem.jl:590:GRUG: Return immune system status for /status CLI display.
    [!] ./src/ImmuneSystem.jl:715:# GRUG: AIML nodes are executive layer. They get their OWN immune threshold.
    [!] ./src/ImmuneSystem.jl:716:# Regular immune system gates on total node count (1000+ nodes).
    [!] ./src/ImmuneSystem.jl:720:# GRUG: This is SEPARATE from the main immune system. AIML has its own:
    [!] ./src/ImmuneSystem.jl:737:GRUG: Check if AIML immune system should activate.
    [!] ./src/ImmuneSystem.jl:759:- Protecting AIML tribe from malformed executive commands
    [!] ./src/ImmuneSystem.jl:784:    # GRUG: If mature, run the SAME scan logic as main immune system.
    [!] ./src/ImmuneSystem.jl:861:GRUG: Get AIML immune system status. Same as get_immune_status but for AIML tribe.
    [!] ./src/ImmuneSystem.jl:898:# 4. **Non-Adversarial Design**: The system never assumes malicious intent.
    [!] ./src/ImmuneSystem.jl:903:# 5. **Maturity Gating**: The immune system only activates for specimens with
    [!] ./src/EphemeralMLP.jl:58:# The system is ephemeral in presence (processes and dies each cycle) but
    [!] ./src/EphemeralMLP.jl:83:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/EphemeralMLP.jl:175:# low directive_quality), that's STRAIN. The system hurt because it doesn't
    [!] ./src/EphemeralMLP.jl:249:# blend into the hidden state via phase_mix_hidden! — hippocampal retrieval that speaks
    [!] ./src/EphemeralMLP.jl:257:        # Uses the same confidence system with jitter snap-back as pattern bind phase.
    [!] ./src/EphemeralMLP.jl:259:# Uses the same confidence system with jitter snap-back as pattern bind phase.
    [!] ./src/EphemeralMLP.jl:1261:    # GRUG v9: CoherenceField Φ tells the MLP whether the system is in
    [!] ./src/EphemeralMLP.jl:1493:# PHASE MIX - hippocampal retrieval blended into hidden state
    [!] ./src/EphemeralMLP.jl:1665:    # ── Phase mix: hippocampal retrieval into hidden state ──────────────
    [!] ./src/EphemeralMLP.jl:1668:    # That's real hippocampal retrieval — the automaton's voice in the MLP's brain.
    [!] ./src/EphemeralMLP.jl:2451:the system encountered novel input it couldn't handle well. This signal
    [!] ./src/EphemeralMLP.jl:2455:GRUG: The self-observer's hurt. When this is high, the system needs to grow.
    [!] ./src/EphemeralMLP.jl:2486:Also re-evaluates hippocampal_warrant_active since strain may now be below threshold.
    [!] ./src/EphemeralMLP.jl:2499:        # Re-evaluate warrant: if strain dropped below threshold, warrant deactivates.
    [!] ./src/AutoLinker.jl:70:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/AutoLinker.jl:774:doesn't land, nothing happens. If it does, the system bridges whatever
    [!] ./src/AutoLinker.jl:783:  - immune_gate_fn: (pattern, data) -> Bool — immune system check
    [!] ./src/AutoLinker.jl:897:    # GRUG: The immune system gets final say. If it rejects this link,
    [!] ./src/FullLobeScanner.jl:18:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/FullLobeScanner.jl:153:            throw_scan_error("max_size $max_size exceeds system limit $MAX_ACTIVE_NODES", "ActiveNodeSet")
    [!] ./src/CaveJournal.jl:109:            # GRUG: journal write failure must NOT crash the system.
    [!] ./src/stochastichelper.jl:16:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/stochastichelper.jl:192:#    aborts safely prior to execution, propagating contextual stack traces.
    [!] ./src/RelationalGovernance.jl:35:#      the system attaches them — using their shared token overlap as the
    [!] ./src/RelationalGovernance.jl:69:#   a relational bridge marker. The system always has SOMETHING to say
    [!] ./src/RelationalGovernance.jl:95:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/RelationalGovernance.jl:537:    #         # GRUG: Immune system rejected this attachment. Detach it.
    [!] ./src/RelationalGovernance.jl:538:    #         # This is rare but the immune system has final say.
    [!] ./src/RelationalGovernance.jl:618:system. Shows accumulator size, top pairs, recent events.
    [!] ./src/Lobe.jl:20:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/Lobe.jl:41:# Why parentmodule(@__MODULE__): when this `module Lobe` block is evaluated
    [!] ./src/BrainStem.jl:19:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/RoutingJudge.jl:64:    # Walk up if nested inside a deeper eval context
    [!] ./src/RoutingJudge.jl:472:Relational triples with sigils are DYNAMIC — they evaluate at match time.
    [!] ./src/ActionTonePredictor.jl:30:# logged as @warn and execution continues.
    [!] ./src/ActionTonePredictor.jl:35:# This nudges the predicted action toward SPECULATE — the system is being asked
    [!] ./src/ActionTonePredictor.jl:44:# the system's path through action-tone space over time. Each entry decays
    [!] ./src/ActionTonePredictor.jl:48:# categories. This prevents strange attractors — the system cannot lock into
    [!] ./src/ActionTonePredictor.jl:51:# The trajectory system is the Lorenz curve analog: if "wealth" (probability
    [!] ./src/ActionTonePredictor.jl:68:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/ActionTonePredictor.jl:289:Tuning knobs for the trajectory normalization and attractor avoidance system.
    [!] ./src/ActionTonePredictor.jl:498:# damping, also triggers the heavy fallback. 0.4 is "the system isn't sure
    [!] ./src/ActionTonePredictor.jl:775:# noisy; the system's curves should be noisy in the same bounded way.
    [!] ./src/ActionTonePredictor.jl:851:# decay toward zero influence. The centroid represents "where has the system
    [!] ./src/ActionTonePredictor.jl:909:# threshold, the system is locked into a strange attractor — one category
    [!] ./src/ActionTonePredictor.jl:926:# This gently steers the system away from concentration while still respecting
    [!] ./src/ActionTonePredictor.jl:1011:dangling causal chain — the user may be mid-thought or asking the system
    [!] ./src/ActionTonePredictor.jl:1052:distributions, applies Lorenz trajectory damping if the system is locked
    [!] ./src/ActionTonePredictor.jl:1304:    # system over-normalized, suppressing clean signal because of past
    [!] ./src/ActionTonePredictor.jl:1585:        # 0.85–1.0 range. That's too aggressive for a system that should stay
    [!] ./src/ActionTonePredictor.jl:1611:                    "calculate", "compute", "evaluate", "assess"])
    [!] ./src/ActionTonePredictor.jl:1615:                   ["execute", "run", "do", "action", "command", "perform",
    [!] ./src/ActionTonePredictor.jl:1818:# tracks the system's path through action-tone space. Each entry decays
    [!] ./src/ActionTonePredictor.jl:1821:#   - Gini < threshold (0.72): system is exploring normally, no damping
    [!] ./src/ActionTonePredictor.jl:1825:# This prevents the system from locking into a single action/tone family
    [!] ./src/ActionTonePredictor.jl:1831:# but is evaluated independently from action scoring. This allows cross-
    [!] ./src/ChatterMode.jl:67:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/TonalJudge.jl:57:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/RelationalJitter.jl:44:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/RelationalJitter.jl:118:# match_score / orthogonal_penalty ratios in evaluate_relational_dialectics
    [!] ./src/RelationalJitter.jl:126:# GRUG: Sentinel value from evaluate_relational_dialectics for hard
    [!] ./src/RelationalJitter.jl:413:`jitter_value` for the executive-layer strength field. Same contract:
    [!] ./src/RelationalJitter.jl:534:Returns `true` if execution is currently inside a `with_brainstorm_jitter`
    [!] ./src/ImageSDF.jl:34:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/EphemeralAutomaton.jl:39:#           accumulates. MLP evaluates. Clean separation — automaton
    [!] ./src/EphemeralAutomaton.jl:44:# rule-driven step executor invoked by ActionTonePredictor when escalation
    [!] ./src/EphemeralAutomaton.jl:56:# Retrieval uses big_number_small_number_coherence on distribution similarity.
    [!] ./src/EphemeralAutomaton.jl:74:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/EphemeralAutomaton.jl:127:# GRUG: Default pull threshold for phase retrieval. Coherence above this
    [!] ./src/EphemeralAutomaton.jl:215:Trace of one rule execution. `values` maps step label -> evaluated output
    [!] ./src/EphemeralAutomaton.jl:304:Phase retrieval pulls only snapshots coherent with the current ATP
    [!] ./src/EphemeralAutomaton.jl:322:    enabled::Bool                                # master switch for phase retrieval
    [!] ./src/EphemeralAutomaton.jl:419:# PHASE PULL QUERY — coherence-based retrieval from the crystal
    [!] ./src/EphemeralAutomaton.jl:422:# GRUG: Distribution similarity metrics for phase retrieval.
    [!] ./src/EphemeralAutomaton.jl:487:The main phase retrieval query. Queries the growing crystal for snapshots
    [!] ./src/EphemeralAutomaton.jl:552:        # ── PHASE PULL: coherence-matched retrieval ────────────────────
    [!] ./src/EphemeralAutomaton.jl:685:Set the coherence threshold for phase retrieval. Must be in [0.1, 0.9].
    [!] ./src/EphemeralAutomaton.jl:736:Master switch for phase retrieval. When disabled, phase_pull_query returns
    [!] ./src/EphemeralAutomaton.jl:938:# GRUG: the eval table is intentionally tiny. Each op is a pure function over
    [!] ./src/EphemeralAutomaton.jl:941:function _eval_step(step::AutomatonStep, accum::Any, ctx::Dict{String, Any})
    [!] ./src/EphemeralAutomaton.jl:957:        d == 0 && _err("divide by zero in step '$(step.label)'", "_eval_step")
    [!] ./src/EphemeralAutomaton.jl:969:                 "_eval_step")
    [!] ./src/EphemeralAutomaton.jl:977:                 "_eval_step")
    [!] ./src/EphemeralAutomaton.jl:980:            "op :getctx missing key '$(String(p))'", "_eval_step")
    [!] ./src/EphemeralAutomaton.jl:986:                 "_eval_step")
    [!] ./src/EphemeralAutomaton.jl:991:             "_eval_step")
    [!] ./src/EphemeralAutomaton.jl:1039:            _eval_step(step, accum, ctx)
    [!] ./src/EphemeralAutomaton.jl:1268:Tunable configuration for the AIML vigilance → context injector dispatch system.
    [!] ./src/EphemeralAutomaton.jl:1475:        # GRUG: Agent failure does NOT crash the system. Agent is inspector gadget,
    [!] ./src/EphemeralAutomaton.jl:1490:Main entry point for the vigilance system. Given a computed context weight
    [!] ./src/EphemeralAutomaton.jl:1780:Return comprehensive vigilance system status. For CLI display and diagnostics.
    [!] ./src/InputQueue.jl:1:# InputQueue.jl — GRUG Input Queue and NegativeThesaurus inhibition system
    [!] ./src/InputQueue.jl:16:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/Thesaurus.jl:18:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/Thesaurus.jl:185:    ("system",      ["framework", "structure", "platform", "architecture"]),
    [!] ./src/Thesaurus.jl:222:    ("analyze",     ["examine", "study", "evaluate", "assess", "investigate"]),
    [!] ./src/Thesaurus.jl:230:    ("measure",     ["quantify", "gauge", "assess", "calculate", "evaluate"]),
    [!] ./src/Thesaurus.jl:264:    # system_prompts but had NO thesaurus entries, making every run produce
    [!] ./src/Thesaurus.jl:490:    ("measure",         ["gauge", "quantify", "assess", "evaluate", "benchmark"]),
    [!] ./src/Thesaurus.jl:722:    # v8.26: "does"/"did"/"do" → "performs"/"executes"/"accomplishes" are formal register.
    [!] ./src/Thesaurus.jl:724:    ("does",            ["performs", "executes", "accomplishes"]),
    [!] ./src/Thesaurus.jl:725:    ("did",             ["performed", "executed", "accomplished"]),
    [!] ./src/Thesaurus.jl:726:    ("do",              ["perform", "execute", "accomplish", "act"]),
    [!] ./src/Thesaurus.jl:906:    # v8.26: "nature" → "wilderness"/"environment"/"ecosystem" are formal/wrong register.
    [!] ./src/Thesaurus.jl:908:    ("nature",          ["wilderness", "environment", "ecosystem", "the wild"]),
    [!] ./src/ChatterResiduals.jl:48:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/ChatterResiduals.jl:104:# from the system's own gossip — that's worth more than random co-firing.
    [!] ./src/SemanticVerbs.jl:27:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/TemporalGrowth.jl:57:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/TemporalGrowth.jl:103:# GRUG: Relevance thresholds for the many-lever system.
    [!] ./src/TemporalGrowth.jl:127:    "compare", "analyze", "evaluate", "estimate",
    [!] ./src/TemporalGrowth.jl:419:    _infer_system_prompt_from_lobe(lobe_id::String, lobe_rec, pattern::String;
    [!] ./src/TemporalGrowth.jl:431:function _infer_system_prompt_from_lobe(lobe_id::String, lobe_rec, pattern::String;
    [!] ./src/TemporalGrowth.jl:546:and system prompt inference.
    [!] ./src/TemporalGrowth.jl:712:     - Infer system_prompt from lobe's group DNA + subject
    [!] ./src/TemporalGrowth.jl:750:    # When the system hurts from novel input it can't handle, strain boosts
    [!] ./src/TemporalGrowth.jl:868:        # ── LEVER 1+2: Infer system prompt from lobe's group DNA ──────
    [!] ./src/TemporalGrowth.jl:871:        system_prompt = _infer_system_prompt_from_lobe(lobe_id, lobe_rec, pattern;
    [!] ./src/TemporalGrowth.jl:876:            "system_prompt"      => system_prompt,
    [!] ./src/TemporalGrowth.jl:1084:export _score_pattern_relevance, _infer_system_prompt_from_lobe
    [!] ./src/AutoGrowth.jl:6:# Every user message carries evidence. The system ACCUMULATES that evidence
    [!] ./src/AutoGrowth.jl:25:#   3. GROWTH TYPES — all node types and side systems:
    [!] ./src/AutoGrowth.jl:28:#      c. AIML nodes — stochastic executive templates
    [!] ./src/AutoGrowth.jl:68:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/AutoGrowth.jl:151:# A candidate needs at least this much evidence before the system even
    [!] ./src/AutoGrowth.jl:217:# AIML nodes are heavier (template+executive), so grow them at 1/3 the
    [!] ./src/AutoGrowth.jl:282:# feed evidence. When it overflows, the system asks a question autonomously.
    [!] ./src/AutoGrowth.jl:394:    evaluate_dialectics_fn::Union{Function,Nothing} = nothing,
    [!] ./src/AutoGrowth.jl:473:    # GRUG: When the system hurts from novel input, the ENTIRE input
    [!] ./src/AutoGrowth.jl:475:    # This is because strain means the system can't handle WHAT IT SAW,
    [!] ./src/AutoGrowth.jl:775:                    if evaluate_dialectics_fn !== nothing
    [!] ./src/AutoGrowth.jl:781:                                        (score, is_anti) = evaluate_dialectics_fn(
    [!] ./src/AutoGrowth.jl:794:                                        # GRUG: evaluation error = can't confirm match, keep checking
    [!] ./src/AutoGrowth.jl:831:            # GRUG: SOURCE 18 is non-fatal. If triple extraction or evaluation
    [!] ./src/AutoGrowth.jl:958:doesn't land, nothing happens. If it does, the system grows whatever
    [!] ./src/AutoGrowth.jl:986:  - evaluate_dialectics_fn: (user_triples, node_triples, req_rels, rel_weights) -> (Float64, Bool)
    [!] ./src/AutoGrowth.jl:1013:    evaluate_dialectics_fn = nothing,
    [!] ./src/AutoGrowth.jl:1210:            evaluate_dialectics_fn=evaluate_dialectics_fn,
    [!] ./src/AutoGrowth.jl:1312:            "system_prompt"      => "Sigil anchor for &$pattern. Shape-cluster magnet grew this node.",
    [!] ./src/AutoGrowth.jl:1374:            evaluate_dialectics_fn=evaluate_dialectics_fn,
    [!] ./src/AutoGrowth.jl:1573:                     evaluate_dialectics_fn=nothing, words_to_signal_fn=nothing,
    [!] ./src/AutoGrowth.jl:1589:        "system_prompt"     => "Grug think about $pattern. One more rock for Grug's wall of knowing.",
    [!] ./src/AutoGrowth.jl:1794:                             evaluate_dialectics_fn=evaluate_dialectics_fn,
    [!] ./src/AutoGrowth.jl:1834:                           evaluate_dialectics_fn=nothing,
    [!] ./src/AutoGrowth.jl:1853:                evaluate_dialectics_fn=evaluate_dialectics_fn,
    [!] ./src/AutoGrowth.jl:2092:# REMINDER: relational_patterns may contain sigil tokens (&n, &op etc) — dynamic evaluation at match time.
    [!] ./src/AutoGrowth.jl:2311:        "ReasoningLobe"   => "reason[carefully]^3 | analyze[systematic]^2 | explain^1",
    [!] ./src/LobeTable.jl:20:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/LobeTable.jl:486:# system introduces unnecessary complexity. Hopfield caching should only be used
    [!] ./src/EyeSystem.jl:11:# GRUG: The eye system operates on SDFParams from ImageSDF.jl.
    [!] ./src/EyeSystem.jl:26:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/EyeSystem.jl:75:# GRUG: One global arousal state for the whole eye system.
    [!] ./src/GrugBot420.jl:15:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/GrugBot420.jl:158:# matching and (in later stages) cross-subsystem semantic propagation. Stage 1
    [!] ./src/GrugBot420.jl:178:# growth. Every user message carries evidence of gaps. The system accumulates
    [!] ./src/GrugBot420.jl:194:# GRUG: Immune system — must be included before engine.jl so engine can use it.
    [!] ./src/GrugBot420.jl:209:# and coin thresholds. Must load BEFORE engine.jl (evaluate_relational_dialectics
    [!] ./src/GrugBot420.jl:230:# by the generation/system-prompt layer, never by candidate scoring.
    [!] ./src/GrugBot420.jl:241:# where bindings ride as a side-channel — ATP and the Stage 3 evaluator
    [!] ./src/GrugBot420.jl:251:# GRUG: ArithmeticEngine — Stage 2 arithmetic evaluator. When sigils capture
    [!] ./src/GrugBot420.jl:359:# GRUG: AIML node tribes exports - lobe-specific executive node populations
    [!] ./src/GrugBot420.jl:478:# GRUG: ArithmeticEngine exports — Stage 2 arithmetic evaluator.
    [!] ./src/GrugBot420.jl:483:# for checking whether bindings contain enough math sigils to attempt evaluation.
    [!] ./src/GrugBot420.jl:532:# A :relation sigil's expansion lists alternative relation verbs. At evaluation
    [!] ./src/MitosisMode.jl:81:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/MitosisMode.jl:213:# patterns, but by the system's felt inability to handle what it's seeing.
    [!] ./src/MitosisMode.jl:278:    # The system doesn't just grow from external data — it grows from internal
    [!] ./src/MitosisMode.jl:351:# GRUG: AIMLNodeSystem removed in v8.12. AIML nodes were executive/template
    [!] ./src/MitosisMode.jl:759:# GRUG: The system HURTS — it sees novel input it can't handle well.
    [!] ./src/MitosisMode.jl:768:# the system's own assessment of its structural insufficiency.
    [!] ./src/MitosisMode.jl:778:    # The system must also have its own internal confirmation that
    [!] ./src/MitosisMode.jl:787:    # user message — that's the stimulus causing the strain. The system
    [!] ./src/MitosisMode.jl:878:    # GRUG: The system hurts from novel input it can't handle. SelfObserver
    [!] ./src/MitosisMode.jl:973:# per lobe name — dumb. New way: derive from the ACTION semantic subsystem.
    [!] ./src/MitosisMode.jl:992:    2 => "command[directly]^3 | direct[immediately]^2 | execute[swiftly]^1",
    [!] ./src/MitosisMode.jl:1012:# QUESTION PATTERN EXTRACTION - derive pattern from question semantic subsystem
    [!] ./src/MitosisMode.jl:1020:# This is the question semantic subsystem: pattern = what's being ASKED.
    [!] ./src/MitosisMode.jl:1021:# The action subsystem (above) handles: vote = what's being DONE.
    [!] ./src/MitosisMode.jl:1051:    # input classification, use it. The action subsystem tells us what the
    [!] ./src/MitosisMode.jl:1071:        "ReasoningLobe"   => "reason[carefully]^3 | analyze[systematic]^2 | explain^1",
    [!] ./src/MitosisMode.jl:1078:function _infer_system_prompt(pattern::String, lobe_hint::String)::String
    [!] ./src/MitosisMode.jl:1120:Semantic subsystem kwargs (mitosis v9 — pattern from questions, vote from actions):
    [!] ./src/MitosisMode.jl:1152:    # GRUG (v9): Semantic subsystem — pattern from questions, vote from actions
    [!] ./src/MitosisMode.jl:1175:    evaluate_dialectics_fn = nothing,       # (user_triples, node_triples, req_rels, rel_weights) -> (Float64, Bool)
    [!] ./src/MitosisMode.jl:1312:    # nothing but NOVEL user input. The semantic subsystems tell us:
    [!] ./src/MitosisMode.jl:1314:    #   - What action TYPE → action_packet (action subsystem)
    [!] ./src/MitosisMode.jl:1400:    # GRUG: Infer lobe and system prompt from the (possibly rewritten) pattern
    [!] ./src/MitosisMode.jl:1405:    # GRUG: ACTION PACKET from the action semantic subsystem.
    [!] ./src/MitosisMode.jl:1411:    system_prompt = _infer_system_prompt(pattern, lobe_hint)
    [!] ./src/MitosisMode.jl:1415:        "system_prompt"     => system_prompt,
    [!] ./src/MitosisMode.jl:1499:            # AIML nodes were executive templates (AIMLNodeSystem removed v8.12).
    [!] ./src/MitosisMode.jl:1522:            # triples → scan candidate nodes with high_res_scan → evaluate
    [!] ./src/MitosisMode.jl:1538:                    evaluate_dialectics_fn=evaluate_dialectics_fn,
    [!] ./src/CoherenceField.jl:12:# GRUG say: vote system can USE gradient. Rock with positive ΔΦ gets bonus.
    [!] ./src/CoherenceField.jl:30:# DANGER: With weight > 0, the system develops genuine attractor dynamics.
    [!] ./src/CoherenceField.jl:33:# but it means the system can become resistant to correction. Weight above
    [!] ./src/CoherenceField.jl:50:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/SelfObserver.jl:54:# integration point is the generation / system-prompt layer.
    [!] ./src/SelfObserver.jl:69:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/SelfObserver.jl:323:# READER_LOCK is a strict, non-reentrant gate: only one peek at a time, system-wide.
    [!] ./src/Main.jl:21:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/Main.jl:54:# GRUG: Bring the Chatter Mode gossip system into the cave!
    [!] ./src/Main.jl:101:# GRUG: Bring the Lobe partitioning system into the cave!
    [!] ./src/Main.jl:116:# GRUG: Bring the InputQueue and NegativeThesaurus inhibition system into the cave!
    [!] ./src/Main.jl:329:# explosion we saw when every /mission re-embedded the last 5 system
    [!] ./src/Main.jl:506:# When the cave is empty or confidence is very low, the system asks a question.
    [!] ./src/Main.jl:670:Uses system gzip like save_specimen_to_file! - no extra packages needed.
    [!] ./src/Main.jl:1409:    # system (dictionary → answer clusters → AIML fallback).
    [!] ./src/Main.jl:1535:    # to the dictionary/answer-cluster system DIRECTLY — bypassing AIML voting.
    [!] ./src/Main.jl:1578:                # system can try to answer it (and ask clarification if unknown).
    [!] ./src/Main.jl:1633:                # so the system handles it normally (greeting/emotion AIML pattern)
    [!] ./src/Main.jl:1845:        # FAILURE in the broader system — the @warn surfaces it.
    [!] ./src/Main.jl:2237:Non-immune errors are warned but do NOT block (immune system crash ≠ command block).
    [!] ./src/Main.jl:2249:            CaveJournal.cave_print("[IMMUNE] ⛔ $cmd_name REJECTED by immune system (sig=0x$(string(sig, base=16)))"; tag="IMMUNE", emoji="🛡")
    [!] ./src/Main.jl:2250:            add_message_to_history!("System", "⛔ $cmd_name input rejected by immune system: deleted", false)
    [!] ./src/Main.jl:2259:            CaveJournal.cave_print("[IMMUNE] ⛔ $cmd_name REJECTED by immune system: $(e.info)"; tag="IMMUNE", emoji="🛡")
    [!] ./src/Main.jl:2260:            add_message_to_history!("System", "⛔ $cmd_name input rejected by immune system: $(e.kind)", false)
    [!] ./src/Main.jl:2376:            # relevant weak nodes. If no node clears this floor, the system asks
    [!] ./src/Main.jl:2790:            # GRUG: Sure/low-confidence entry — standard COMMANDS execution.
    [!] ./src/Main.jl:2867:the dynamic response. Assembles system prompt, mission, vote context, and
    [!] ./src/Main.jl:2871:    if !haskey(context, "system_prompt")
    [!] ./src/Main.jl:2872:        error("!!! FATAL: Node dictionary missing 'system_prompt'! Grug confused! !!!")
    [!] ./src/Main.jl:2887:        #   &now    → assess=true  (evaluate current state)
    [!] ./src/Main.jl:2900:    system_prompt = context["system_prompt"]
    [!] ./src/Main.jl:2901:    # GRUG v8.1: Append temporal reasoning directive to system prompt.
    [!] ./src/Main.jl:2905:        system_prompt = system_prompt * time_directive
    [!] ./src/Main.jl:3002:    # was DELETED. Its evaluated output (rules_str) was ONLY ever printed into
    [!] ./src/Main.jl:3022:    # are the claims, the system_prompt is the voice, the relational
    [!] ./src/Main.jl:3059:    # The mapping still works for INPUT: user says "adore" → system
    [!] ./src/Main.jl:3069:    # The mapping still works for INPUT: user says "adore" → system
    [!] ./src/Main.jl:3509:        #   - It's NOT inhibited (system-level protected words)
    [!] ./src/Main.jl:3977:        # filtering) instead of always using the system_prompt body. This is
    [!] ./src/Main.jl:4013:        _voice_body_preview = let sp = String(get(winning_node.json_data, "system_prompt", ""))
    [!] ./src/Main.jl:4022:               system_prompt = \"$(String(get(winning_node.json_data, "system_prompt", "")))\"
    [!] ./src/Main.jl:4024:               FIX: Add a multi-sentence system_prompt (sentence 1 = persona tag, rest = grug voice)
    [!] ./src/Main.jl:4054:    # FIX A: every node already carries a `system_prompt` whose first
    [!] ./src/Main.jl:4055:    # sentence becomes the [voice prefix] tag. The REST of system_prompt
    [!] ./src/Main.jl:4077:    # of always using the system_prompt body. The system_prompt body is
    [!] ./src/Main.jl:4080:    # is empty (most nodes), behavior is identical to v7.35 — the system_prompt
    [!] ./src/Main.jl:4082:    # GRUG v8.3: COHERENCE FIX — protect decimal numbers in system_prompt
    [!] ./src/Main.jl:4086:    _sp_safe = replace(system_prompt, r"(\d)\.(\d)" => s"\1\x00DOT\x00\2")
    [!] ./src/Main.jl:4265:    #   2. system_prompt body   (the seeded grug-voice answer, c-1)
    [!] ./src/Main.jl:4519:        # here when system_prompt body is empty AND noun_anchors are
    [!] ./src/Main.jl:4759:    # support, then wrap with voice (system_prompt) and lobe tag.
    [!] ./src/Main.jl:4888:    # Voice prefix: first sentence of the system_prompt is the persona tag.
    [!] ./src/Main.jl:5025:    # into the SelfObserver store. This is the mirror: the system sees what
    [!] ./src/Main.jl:5067:# the system should ASK A COHERENT QUESTION about the misunderstood input.
    [!] ./src/Main.jl:5086:GRUG v7.51: Generate a coherent question about input the system doesn't understand.
    [!] ./src/Main.jl:5182:# answers need pre-seeded triples. The answer mode system lets the user pick
    [!] ./src/Main.jl:5222:# GRUG: Map answer mode → action family name + system_prompt voice.
    [!] ./src/Main.jl:5618:# This is the organic path. No /answer command needed. The system answers from
    [!] ./src/Main.jl:5625:Answer a question organically by looking up the topic in the dictionary system
    [!] ./src/Main.jl:5627:the system doesn't know (in which case AIML scanning proceeds as fallback).
    [!] ./src/Main.jl:5715:            # GRUG: Bonus for nodes with explicit answer content (system_prompt).
    [!] ./src/Main.jl:5718:            _sys_prompt = get(_node.json_data, "system_prompt", "")
    [!] ./src/Main.jl:5740:                # "gorbling of &n") carries a terse placeholder system_prompt
    [!] ./src/Main.jl:5787:                    # Node has no system_prompt — use pattern + action_packet as fallback
    [!] ./src/Main.jl:6454:    # GRUG v7.55: COHERENCE FIX — embed answer content into system_prompt
    [!] ./src/Main.jl:6471:        "system_prompt"      => sys_prompt,
    [!] ./src/Main.jl:6500:# They share the same system_prompt/voice/frame but are tagged is_shadow=true.
    [!] ./src/Main.jl:6662:Shadow nodes share the same system_prompt/voice/frame but are tagged
    [!] ./src/Main.jl:6689:    #   - Same system_prompt, voice, frame as primary
    [!] ./src/Main.jl:6700:            "system_prompt"      => ans_data["system_prompt"],
    [!] ./src/Main.jl:6795:# rather than floating alone. The system scans existing groups, filters by
    [!] ./src/Main.jl:7140:# These fire when the system needs to ask a coherent question about input
    [!] ./src/Main.jl:7274:         :   • `data.system_prompt` defaults to "Grug speaks plainly." if absent
    [!] ./src/Main.jl:7277:Status   : /status                             (show chatter + system status)
    [!] ./src/Main.jl:7282:Arousal  : /arousal <0.0-1.0>                 (set eye system arousal level)
    [!] ./src/Main.jl:7290:Hippo    : When cave is empty, system asks a question. Use /answer or
    [!] ./src/Main.jl:7344:║     Don't rely on the latch system to discover semantics.       ║
    [!] ./src/Main.jl:7369:║  /status                    Full system health snapshot      ║
    [!] ./src/Main.jl:7370:║  /arousal <0.0-1.0>         Set eye system arousal level     ║
    [!] ./src/Main.jl:7384:║  Empty cave → system asks question → /answer resolves strain ║
    [!] ./src/Main.jl:7385:║  /answer [@lobe] [:mode] <text>   Teach system with answer   ║
    [!] ./src/Main.jl:7520:  /vigilance                      Show vigilance system status
    [!] ./src/Main.jl:7840:    # confidence is very low, the system uses these to ask a coherent
    [!] ./src/Main.jl:7842:    # carries the raw user input; the system doesn't pretend to know.
    [!] ./src/Main.jl:7911:            "system",            # node_id for system-level reflection
    [!] ./src/Main.jl:8243:                        # If we know the answer, respond from the system immediately.
    [!] ./src/Main.jl:8563:                                "system_prompt"        => "Grug.",
    [!] ./src/Main.jl:8599:                                "system_prompt"       => "Grug. $(_teach_def).",
    [!] ./src/Main.jl:8635:                            "system_prompt"       => "Grug. $(_teach_def).",
    [!] ./src/Main.jl:8713:                    # GRUG: Dictionary system is independent of LOBE_REGISTRY —
    [!] ./src/Main.jl:9209:        # The old code just printed "Cave is silent" and returned. Now the system
    [!] ./src/Main.jl:9277:                    evaluate_dialectics_fn    = (triples; kwargs...) -> evaluate_relational_dialectics(triples; kwargs...),
    [!] ./src/Main.jl:9327:                    evaluate_dialectics_fn     = (triples; kwargs...) -> evaluate_relational_dialectics(triples; kwargs...),
    [!] ./src/Main.jl:9523:    # the system auto-attaches them. Hebbian learning at the topology level.
    [!] ./src/Main.jl:9574:    # adjustments that the AIML system can optionally consume. Ephemeral in presence,
    [!] ./src/Main.jl:9749:            # GRUG v7.27: Phase pull status — show time crystal retrieval info
    [!] ./src/Main.jl:10066:                evaluate_dialectics_fn     = (triples; kwargs...) -> evaluate_relational_dialectics(triples; kwargs...),
    [!] ./src/Main.jl:10122:                evaluate_dialectics_fn     = (triples; kwargs...) -> evaluate_relational_dialectics(triples; kwargs...),
    [!] ./src/Main.jl:10216:            # from MLP signals + uncovered tokens. When it overflows, the system
    [!] ./src/Main.jl:10480:  - bridges (BRIDGE_MAP cascade bridge system, bidirectional)
    [!] ./src/Main.jl:10596:    # backward-compat pattern as the removed AIMLNodeSystem's "aiml_system" key.
    [!] ./src/Main.jl:10892:    # Academic: The trajectory buffer tracks the system's path through
    [!] ./src/Main.jl:10952:    # Academic: Without this, the immune system resets on every load —
    [!] ./src/Main.jl:10954:    specimen["immune_system"] = ImmuneSystem.serialize_immune_state()
    [!] ./src/Main.jl:11077:    # observation_threshold gate can be evaluated correctly on reload.
    [!] ./src/Main.jl:11324:    # GRUG: Immune system knobs — maturity threshold, coinflip probability,
    [!] ./src/Main.jl:11325:    # patch timeout. These control when the immune system activates.
    [!] ./src/Main.jl:11410:    # auto-learning system picks up where it left off on reload. Without
    [!] ./src/Main.jl:11913:                        "trajectory", "temporal_coherence", "morph_cooldowns", "immune_system", "_meta",
    [!] ./src/Main.jl:11947:                        "aiml_system"])  # GRUG v8.12: aiml_system allowed for backward-compat (silently skipped during load)
    [!] ./src/Main.jl:11963:             "trajectory", "morph_cooldowns", "immune_system", "_meta",
    [!] ./src/Main.jl:12017:    # A brain transplant must clear executive memory too, not just cave nodes.
    [!] ./src/Main.jl:12069:    # Wipe bridges (cascade bridge system)
    [!] ./src/Main.jl:12109:    # Wipe immune system state
    [!] ./src/Main.jl:12529:    # "aiml_system" key. Nothing to restore.
    [!] ./src/Main.jl:12883:                        # build safe lookup tables from instances() — no eval(), no injection risk.
    [!] ./src/Main.jl:12975:    if haskey(specimen, "immune_system") && _is_dict_like(specimen["immune_system"])
    [!] ./src/Main.jl:12976:        ImmuneSystem.deserialize_immune_state!(specimen["immune_system"])
    [!] ./src/Main.jl:12985:        println("  🛡 Immune system restored ($n_immune_sigs signatures, $n_immune_log ledger entries)")
    [!] ./src/Main.jl:12989:    if haskey(specimen, "aiml_system")
    [!] ./src/Main.jl:12990:        # GRUG v8.12: AIMLNodeSystem removed — skipping aiml_system restore
    [!] ./src/Main.jl:12991:        # GRUG v8.12: AIMLNodeSystem removed — skipping aiml_system restore
    [!] ./src/Main.jl:12992:        # (aiml_system key in specimen is ignored gracefully)
    [!] ./src/Main.jl:13592:    # GRUG: Restore curiosity intensity so the system remembers what it
    [!] ./src/Main.jl:14075:    # execution chains. People don't dream in procedures.
    [!] ./src/Main.jl:14078:    # "&n is_greater_than &n" evaluates at match time with sigil-bound values —
    [!] ./src/Main.jl:14087:                "system_prompt" => "Arithmetic reasoning voice",
    [!] ./src/Main.jl:14098:                "system_prompt" => "Multi-clause reasoning voice",
    [!] ./src/Main.jl:14110:                "system_prompt" => "Action execution voice - verb-driven action scripting",
    [!] ./src/Main.jl:14149:                    "system_prompt"   => "Grug.",  # terse — the computed answer IS the claim
    [!] ./src/Main.jl:14191:                        "system_prompt"   => "Dictionary definition voice — state the definition clearly and concisely",
    [!] ./src/Main.jl:14259:                            "system_prompt"   => "Dictionary definition voice — state the definition clearly and concisely",
    [!] ./src/Main.jl:14306:                            "system_prompt"   => "Dictionary definition voice — state the definition clearly and concisely",
    [!] ./src/Main.jl:14398:    greet_ctx    = Dict{String, Any}("system_prompt" => "Highly polite greeting protocols active. Friend at the cave mouth, Grug nod and make space by the fire.")
    [!] ./src/Main.jl:14399:    reason_ctx   = Dict{String, Any}("system_prompt" => "Cold logical analysis engine active. Grug line up the rocks one by one and check each before moving on.")
    [!] ./src/Main.jl:14403:        "system_prompt"      => "Causal relational analysis active. Grug trace what hits what, in what order, and what falls out the other side.",
    [!] ./src/Main.jl:14582:            # When the system hurts from novel input it can't handle, strain
    [!] ./src/Main.jl:14646:            evaluate_dialectics_fn     = (triples; kwargs...) -> evaluate_relational_dialectics(triples; kwargs...),
    [!] ./src/Main.jl:14897:            # GRUG v7.30: Pass all system references so new automata 8-12 can
    [!] ./src/Main.jl:15160:        # input), shell comment lines like "# --- Warm-up: Social ---" pass
    [!] ./src/Main.jl:15249:            # GRUG: Semantic verb/synonym system commands
    [!] ./src/Main.jl:15364:            # GRUG: Relational fire system commands (node attachment)
    [!] ./src/Main.jl:15913:                    println("⛔ /pin blocked by immune system.")
    [!] ./src/Main.jl:15929:                # GRUG: /status - comprehensive system health snapshot.
    [!] ./src/Main.jl:16026:                # These are the organic attachment and input/residual-mining subsystems.
    [!] ./src/Main.jl:16032:                # GRUG: /arousal - manually set eye system arousal level [0.0, 1.0]
    [!] ./src/Main.jl:16038:                println("👁  Arousal set to $(round(arousal_val, digits=3)). Eye system updated.")
    [!] ./src/Main.jl:16072:                    println("⛔ /addVerb blocked by immune system.")
    [!] ./src/Main.jl:16085:                    println("⛔ /addRelationClass blocked by immune system.")
    [!] ./src/Main.jl:16100:                    println("⛔ /addSynonym blocked by immune system.")
    [!] ./src/Main.jl:16126:                        println("⛔ /addRelRelation blocked by immune system.")
    [!] ./src/Main.jl:16157:                        println("⛔ /addSeedSynonym blocked by immune system.")
    [!] ./src/Main.jl:16167:            # The hippocampal answer mechanism. When the system encounters input it
    [!] ./src/Main.jl:16223:                            println("⛔ /answer blocked by immune system.")
    [!] ./src/Main.jl:16256:                                                                     default_system_prompt="Grug.")
    [!] ./src/Main.jl:16283:                                            # v7.55: Don't override system_prompt with mode prompt — answer_content is already embedded
    [!] ./src/Main.jl:16369:                                        # evaluate_relational_dialectics will expand it at match time.
    [!] ./src/Main.jl:16388:                                    # v7.55: system_prompt already set by _base_answer_data with answer_content
    [!] ./src/Main.jl:16465:                                    # v7.55: system_prompt already set by _base_answer_data with answer_content
    [!] ./src/Main.jl:16495:                                        # v7.55: system_prompt already has answer_content embedded via _base_answer_data
    [!] ./src/Main.jl:16591:                                            "system_prompt"      => "Grug.",  # terse — the computed answer IS the claim
    [!] ./src/Main.jl:16763:                    println("⛔ /newLobe blocked by immune system.")
    [!] ./src/Main.jl:16795:                    println("⛔ /connectLobes blocked by immune system.")
    [!] ./src/Main.jl:16942:                    println("⛔ /negativeThesaurus add blocked by immune system.")
    [!] ./src/Main.jl:17019:                    println("⛔ /negativeThesaurus addPair blocked by immune system.")
    [!] ./src/Main.jl:17111:                    println("⛔ /loadSpecimen blocked by immune system.")
    [!] ./src/Main.jl:17160:                # Cascade bridge system: bridge two nodes bidirectionally with seam tokens.
    [!] ./src/Main.jl:17176:                    println("⛔ /nodeAttach blocked by immune system.")
    [!] ./src/Main.jl:17261:                    println("⛔ /imgnodeAttach blocked by immune system.")
    [!] ./src/Main.jl:17745:                # /vigilance OR /vigilance status — show vigilance system status
    [!] ./src/Main.jl:17930:                            !(t in ["explain","describe","tell","define","reason","discuss","elaborate","clarify","illustrate","analyze","interpret","compare","contrast","evaluate","assess","summarize","outline"]) &&
    [!] ./src/Main.jl:18074:                    println("⛔ /setLobeHint blocked by immune system.")
    [!] ./src/Main.jl:18097:            # the system already computes internally. Weight=0.0 (off) by default
    [!] ./src/Main.jl:19030:# GRUG: Only run the CLI when executing Main.jl directly as a script,
    [!] ./src/Main.jl:19075:# on their respective subsystem dependencies being passed to run_phagy!.
    [!] ./src/Main.jl:19089:# with slow average times are automatically graved by the Engine ledger system.
    [!] ./src/Main.jl:19182:#  14. bridges        — BRIDGE_MAP cascade bridge system (bidirectional)
    [!] ./src/Main.jl:19195:# JSON (.gz, requires system gzip/gunzip). Auto-detected by extension.
    [!] ./src/GeometryKit.jl:257:The caller provides phi, crystal_size, n_nodes from the live system.
    [!] ./src/HippocampalModulator.jl:38:# GRUG say: this module does NOT execute objectives. It writes the plan.
    [!] ./src/HippocampalModulator.jl:52:# AIML a clean, scoped interface: "read the next entry, execute it, write
    [!] ./src/HippocampalModulator.jl:87:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/HippocampalModulator.jl:125:    ENTRY_PENDING    # Written to log, awaiting execution
    [!] ./src/HippocampalModulator.jl:147:# and executed, but prefixed with "*Grug think this also important*"
    [!] ./src/HippocampalModulator.jl:155:    ActionEntry — a single objective in the execution plan.
    [!] ./src/HippocampalModulator.jl:174:                         entry 1 MUST complete before entry 2 executes.
    [!] ./src/HippocampalModulator.jl:175:      status           — Current execution status.
    [!] ./src/HippocampalModulator.jl:176:      output           — Result after AIML executes this entry. Empty until
    [!] ./src/HippocampalModulator.jl:216:    ActionLog — the ordered execution plan. Wiped every cycle.
    [!] ./src/HippocampalModulator.jl:318:        "",                     # output empty until executed
    [!] ./src/HippocampalModulator.jl:343:        # pending or executing, this entry can't run yet.
    [!] ./src/HippocampalModulator.jl:356:        # GRUG: This one's eligible. Mark it executing and return.
    [!] ./src/HippocampalModulator.jl:409:the core answer. The system should not wait for additives before committing.
    [!] ./src/HippocampalModulator.jl:622:  can see what else Grug considered. This makes the system's uncertainty
    [!] ./src/patternscanner.jl:13:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/patternscanner.jl:84:function evaluate_window(window::AbstractVector{<:Real}, pattern::AbstractVector{<:Real}, tolerance::Real)::Float64
    [!] ./src/patternscanner.jl:152:        conf = evaluate_window(window, pattern, tolerance)
    [!] ./src/patternscanner.jl:180:        conf = evaluate_window(window, pattern, tolerance)
    [!] ./src/patternscanner.jl:212:        conf = evaluate_window(window, pattern, tolerance * 2.0) 
    [!] ./src/patternscanner.jl:230:        conf = evaluate_window(window, pattern, tolerance)
    [!] ./src/patternscanner.jl:235:        # GRUG COHERENCE FIX: Just like the window evaluation, don't let 
    [!] ./src/patternscanner.jl:324:    # GRUG: Absolute magnitudes. Negative confidences are legal (evaluate_window
    [!] ./src/patternscanner.jl:375:# `evaluate_window` calculates discrete proportional similarity but safely dampens 
    [!] ./src/SigilPromoter.jl:37:#   evaluator are downstream consumers of the bindings — never see the raw
    [!] ./src/SigilPromoter.jl:80:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/SigilPromoter.jl:639:            # bound value is what Stage 3's evaluator will actually compute
    [!] ./src/SigilPromoter.jl:715:order (e.g. an arithmetic evaluator that wants `n=[2, 7]`).
    [!] ./src/InputLedger.jl:36:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/MultipartOrchestrator.jl:49:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/MultipartOrchestrator.jl:568:# REMINDER: Votes carry relational triples that may contain sigils (&n, &op). Dynamic eval.
    [!] ./src/ActionEngine.jl:32:#   2. voice_body            — system_prompt body sentences
    [!] ./src/ActionEngine.jl:457:Look up a registered action callback by name and execute it with the given
    [!] ./src/SigilRegistry.jl:11:# GRUG say: every smart system have many small word-pictures (verbs, thesaurus,
    [!] ./src/SigilRegistry.jl:29:# later stages) cross-subsystem semantic propagation. Stage 1 scope:
    [!] ./src/SigilRegistry.jl:47:#   - Cross-subsystem propagation (thesaurus, drop-tables, inhibitions, etc.).
    [!] ./src/SigilRegistry.jl:48:#   - Runtime evaluator system / computed votes.
    [!] ./src/SigilRegistry.jl:68:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/SigilRegistry.jl:213:    :thesaurus,   # cross-subsystem: thesaurus expansion. RESERVED for Stage 8.
    [!] ./src/SigilRegistry.jl:214:    :drop_table,  # cross-subsystem: drop tables. RESERVED for Stage 8.
    [!] ./src/SigilRegistry.jl:215:    :inhibit,     # cross-subsystem: inhibition rules. RESERVED for Stage 8.
    [!] ./src/SigilRegistry.jl:216:    :relation,    # cross-subsystem: relational triples. RESERVED for Stage 4-5.
    [!] ./src/SigilRegistry.jl:495:    #   2. The predicate must be a callable Function (Julia's type system
    [!] ./src/SigilRegistry.jl:968:as a singleton. This is the main call site helper for evaluate_relational_dialectics.
    [!] ./src/SigilRegistry.jl:1058:            !(t in ["explain","describe","tell","define","reason","discuss","elaborate","clarify","illustrate","analyze","interpret","compare","contrast","evaluate","assess","summarize","outline"]) &&
    [!] ./src/SigilRegistry.jl:1136:                                     "evaluate","assess","summarize","outline"]))
    [!] ./src/SigilRegistry.jl:1140:    # alternative verbs that share the same semantic category. At evaluation
    [!] ./src/SigilRegistry.jl:1209:    # don't change WHAT the system answers — they change HOW it reasons.
    [!] ./src/SigilRegistry.jl:1600:# the evaluation code expands it and matches against ANY of the alternatives.
    [!] ./src/InputDecomposer.jl:10:# GRUG say: THIS is why multipart system exist. Not because node say "I have
    [!] ./src/InputDecomposer.jl:72:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./src/InputDecomposer.jl:201:    "independently",       # "analyze X independently evaluate Y"
    [!] ./src/InputDecomposer.jl:229:    "compare", "contrast", "analyze", "evaluate",
    [!] ./src/InputDecomposer.jl:252:    "evaluate"  => ["evaluates", "evaluated", "evaluating"],
    [!] ./src/InputDecomposer.jl:611:               The system will pick ONE action and ignore the rest — that's a coherence failure.
    [!] ./talk_to_grug.jl:16:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./talk_to_grug.jl:48:        "define ecosystem",
    [!] ./validate_specimen.jl:11:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./clean_thesaurus.py:143:    "primeval", "venerable", "antique", "immemorial",  # ancient synonyms
    [!] ./clean_thesaurus.py:170:    "prevalent",  # common synonyms
    [!] ./clean_thesaurus.py:271:    "evaluate", "assess", "determine", "adjudicate", "appraise",  # judge synonyms
    [!] ./clean_thesaurus.py:297:    "gauge", "assess", "evaluate", "calculate", "quantify", "weigh",  # measure synonyms
    [!] ./clean_thesaurus.py:301:    "technique", "procedure", "approach", "system", "strategy", "process",  # method synonyms
    [!] ./clean_thesaurus.py:324:    "arrange", "organize", "sort", "sequence", "structure", "systematize",  # order synonyms
    [!] ./clean_thesaurus.py:436:    "framework", "organization", "network", "arrangement",  # system synonyms
    [!] ./patch_relational_patterns.py:61:def make_meaningful_triple(pattern, system_prompt, noun_anchors):
    [!] ./patch_relational_patterns.py:63:    # Extract topic from pattern or system_prompt
    [!] ./patch_relational_patterns.py:83:    # Try extracting from system_prompt
    [!] ./patch_relational_patterns.py:84:    sp = system_prompt or ''
    [!] ./patch_relational_patterns.py:139:        sp = n.get('json_data', {}).get('system_prompt', '')
    [!] ./refactor_remove_aiml.py:9:- Remove aiml_system from specimen save/load/display/validation
    [!] ./refactor_remove_aiml.py:37:        if stripped.startswith('# GRUG: AIMLNodeSystem needed for aiml_system save/load.'):
    [!] ./refactor_remove_aiml.py:43:                if 'AIMLNodeSystem' in lines[i] or 'aiml_system' in lines[i] or lines[i].strip().startswith('# GRUG: AIMLNodeSystem'):
    [!] ./refactor_remove_aiml.py:128:        # ── 7. Remove aiml_system from specimen save ──
    [!] ./refactor_remove_aiml.py:129:        if 'specimen["aiml_system"]' in line and 'serialize' in line:
    [!] ./refactor_remove_aiml.py:130:            changes.append(f"L{i+1}: Removed aiml_system specimen save")
    [!] ./refactor_remove_aiml.py:134:        # ── 8. Remove aiml_system from specimen load ──
    [!] ./refactor_remove_aiml.py:149:            changes.append(f"L{start_i+1}: Removed aiml_system specimen load block")
    [!] ./refactor_remove_aiml.py:183:        # ── 13. Remove aiml_system from allowed_keys ──
    [!] ./refactor_remove_aiml.py:184:        if '"aiml_system"' in line and ('allowed_keys' in line or 'Set(' in line or 'for k in' in line):
    [!] ./refactor_remove_aiml.py:185:            line = line.replace('"aiml_system", ', '')
    [!] ./refactor_remove_aiml.py:186:            line = line.replace(', "aiml_system"', '')
    [!] ./refactor_remove_aiml.py:187:            line = line.replace('"aiml_system"', '')
    [!] ./refactor_remove_aiml.py:188:            changes.append(f"L{i+1}: Removed aiml_system from allowed_keys/type checks")
    [!] ./refactor_remove_aiml.py:216:                'AIMLNodeSystem', 'aiml_system', 'AIML executive',
    [!] ./refactor_remove_aiml.py:217:                'AIML node tribe', 'AIML growth', 'AIML nodes are executive'
    [!] ./append_part4_learning_section.py:181:    "proving it was a systemic issue in the answer-routing code, not something specific to the new learning "
    [!] ./patch_specimen_v758.py:6:1. Sigils (&temporal, &causal, &emotional, &spatial, &similarity, &possessive) in system_prompt
    [!] ./patch_specimen_v758.py:11:5. Generic/bare system_prompts → Expand with actual grug-voice content
    [!] ./patch_specimen_v758.py:15:Key insight: The code at L2591-2593 derives voice_body by splitting system_prompt
    [!] ./patch_specimen_v758.py:16:on "." and taking sentences 2+. So system_prompt MUST be:
    [!] ./patch_specimen_v758.py:92:# Problem: we don't know what each node's topic IS from the system_prompt alone.
    [!] ./patch_specimen_v758.py:225:# For ACTION: "Action resolution voice - template-driven action with rest args" / "Action execution voice - verb-driven action scripting"
    [!] ./patch_specimen_v758.py:230:    "Grug. When the time comes to act, Grug act. No hesitation. The template is clear, the script is ready. Grug execute.",
    [!] ./patch_specimen_v758.py:356:# For nodes missing noun_anchors, derive from system_prompt or category
    [!] ./patch_specimen_v758.py:358:    """Extract key nouns from system_prompt for noun_anchors."""
    [!] ./patch_specimen_v758.py:362:    # Use the possibly-patched system_prompt for derivation
    [!] ./patch_specimen_v758.py:363:    # Try to extract from the system_prompt
    [!] ./patch_specimen_v758.py:438:    """Classify a node by its system_prompt pattern."""
    [!] ./patch_specimen_v758.py:484:    if 'Action resolution' in sp or 'Action execution' in sp:
    [!] ./patch_specimen_v758.py:517:def patch_system_prompt(sp, category, nid):
    [!] ./patch_specimen_v758.py:518:    """Patch a system_prompt based on its category."""
    [!] ./patch_specimen_v758.py:630:        sp = jd.get('system_prompt', '')
    [!] ./patch_specimen_v758.py:637:        # 1. Fix sigils in system_prompt
    [!] ./patch_specimen_v758.py:640:        # 2. Patch system_prompt if needed
    [!] ./patch_specimen_v758.py:667:            new_sp = patch_system_prompt(sp, category, nid)
    [!] ./patch_specimen_v758.py:669:                jd['system_prompt'] = new_sp
    [!] ./patch_specimen_v758.py:676:            new_na = derive_noun_anchors(sp if not needs_patch else jd.get('system_prompt', sp), 
    [!] ./patch_specimen_v758.py:721:        sp = jd.get('system_prompt', '')
    [!] ./patch_specimen_v758.py:730:        # Check voice_body (derived from system_prompt)
    [!] ./patch_specimen_v758.py:744:    print(f"\nSigils remaining in system_prompts: {sigils_remaining}")
    [!] ./append_multipart_section.py:10:# decomposition structure, fired node, or system_prompt content.
    [!] ./append_multipart_section.py:84:    "separate, distinct subsystem \u2014 <code>InputDecomposer.jl</code> (v7.28) + <code>MultipartOrchestrator.jl</code> "
    [!] ./append_multipart_section.py:115:    "independently, it can resolve to the generic <code>&amp;query &amp;definition &amp;define</code> executive sigil "
    [!] ./append_multipart_section.py:132:    "compound-handling layers: the new <code>InputDecomposer</code>/<code>MultipartOrchestrator</code> system verified "
    [!] ./append_multipart_section.py:135:    "the newer system's multi-scan ever dispatches \u2014 the same organic-prescan-interception pattern already documented "
    [!] ./build_comprehensive_specimen_c.jl:7:# registered), relational triples with dynamic sigil evaluation, thesaurus
    [!] ./build_comprehensive_specimen_c.jl:26:#      this way is /negativeThesaurus — an INPUT INHIBITION/blocklist system
    [!] ./build_comprehensive_specimen_c.jl:339:            "system_prompt"  => prompt,
    [!] ./validate_load.jl:11:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./interaction_harness_v9.jl:16:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./interaction_harness_v9.jl:94:    (19, "define",   "define ecosystem",                   "lobe_ecology",     "n144"),
    [!] ./build_specimen_v3.jl:23:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./build_specimen_v3.jl:170:# Each entry: (pattern, action_packet, system_prompt, noun_anchors, strength, extra::Dict)
    [!] ./build_specimen_v3.jl:182:            "system_prompt"  => prompt,
    [!] ./build_specimen_v3.jl:287:    ("what is gravity",             "lobe_science",  "how do computers work",     "lobe_tech",     ["force","system"]),
    [!] ./build_specimen_v3.jl:596:# triple (i, make, fire) but evaluate_relational_dialectics can't match it to
    [!] ./build_specimen_v3.jl:733:# PHASE 23: AIML executive nodes per lobe
    [!] ./build_specimen_v3.jl:735:println("\n🤖 Phase 23: Registering AIML executive nodes...")
    [!] ./build_specimen_v3.jl:741:            aiml_id = "aiml_$(lobe_id)_exec_1"
    [!] ./build_specimen_v3.jl:750:println("  ✅ AIML tribes registered for $n_aiml_lobes lobes with executive nodes")
    [!] ./build_specimen_v3.jl:780:            "system_prompt"=>"Grug speaks with urgency. Emergency means act now. Grug tell you fast and clear what to do. No long talk. Move, protect, survive.",
    [!] ./build_specimen_v3.jl:788:            "system_prompt"=>"Grug ponders deep. Void is the great nothing Grug cannot know. Some things stay dark to Grug mind. Grug sit with the mystery and feel small under big sky.",
    [!] ./build_specimen_v3.jl:796:            "system_prompt"=>"Grug welcomes warm. Yes, come join the fire. Many friends gather, share food, share story. Grug glad you here. Sit close, you belong with the tribe.",
    [!] ./build_specimen_v3.jl:806:            "system_prompt"=>"Grug describes the picture. Grug see big orange sun fall into water. Sky turn purple and gold. Water hold the light like shiny stone. Grug think it beautiful and calm.",
    [!] ./diag_parrot.jl:70:println("system_prompt: $(get(NODE_MAP[nid].json_data, "system_prompt", "MISSING"))")
    [!] ./boot.jl:17:#   /status                       ← see system state
    [!] ./boot.jl:65:      /status                     system state
    [!] ./gen_specimen_v10.py:5:Imports the voice system + make_node from gen_specimen_v10_part1.py and builds
    [!] ./gen_specimen_v10.py:9:Key improvement over v9: every node has a TOPIC-SPECIFIC system_prompt,
    [!] ./gen_specimen_v10.py:15:# Import voice system + helpers from part1
    [!] ./gen_specimen_v10.py:80:     "subject_whitelist": ["ecology", "ecosystem", "habitat", "biodiversity", "predator", "prey", "species"]},
    [!] ./gen_specimen_v10.py:285:n44 = add(make_node(next_node_id(), "define ecosystem", "define^1",
    [!] ./gen_specimen_v10.py:286:        json_data={"mode": "define"}, voice_key="ecosystem", strength=6.5), "lobe_ecology")
    [!] ./gen_specimen_v10.py:451:    "ecosystem": sorted(["habitat", "biome", "environment"]),
    [!] ./gen_specimen_v10.py:524:     "centroid_pattern": "ecology ecosystem habitat", "created_at": 1700000104.0, "last_chatter_at": 0.0,
    [!] ./gen_specimen_v10.py:559:specimen["immune_system"] = {
    [!] ./gen_specimen_v10.py:563:specimen["aiml_system"] = {
    [!] ./gen_specimen_v10.py:600:     "lexicon": sorted(["calculate", "compute", "solve", "evaluate", "integrate", "derive"]), "params": {},
    [!] ./gen_specimen_v10.py:614:               {"label": "execute_step", "op": "execute", "payload": {}},
    [!] ./gen_specimen_v10.py:616:     "jitter_targets": ["execute_step"], "min_confidence": 0.5},
    [!] ./gen_specimen_v10.py:827:# Verify each voice-keyed node has unique system_prompt
    [!] ./gen_specimen_v10.py:828:prompts = [n["json_data"].get("system_prompt", "") for n in nodes if "voice_variants" in n["json_data"]]
    [!] ./gen_specimen_v10.py:829:print(f"  Unique system_prompts among voiced nodes: {len(set(prompts))}/{len(prompts)}")
    [!] ./interaction_harness_v10.jl:24:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./interaction_harness_v10.jl:80:    (18, "define",  "define ecosystem"),
    [!] ./interaction_harness_v10.jl:372:        "define ecosystem" => "n144",
    [!] ./validate_v9_engine.jl:14:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./validate_v9.jl:13:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║
    [!] ./validate_v9.jl:48:    "trajectory", "temporal_coherence", "morph_cooldowns", "immune_system",
    [!] ./validate_v9.jl:49:    "aiml_system", "sigil_table", "automaton_rules", "phase_accumulator",
    [!] ./interact_v3.jl:26:# ║     (&n, is_greater_than, &n) is DYNAMIC — it evaluates at match time        ║

5.2 HIGH FINDINGS (SHOULD FIX - 90 DAYS)
------------------------------------------
    [!] ./gen_comprehensive_specimen.py:44:              is_grave=False, grave_reason="", hopfield_key="0",
    [!] ./gen_comprehensive_specimen.py:153:        "hopfield_key": hopfield_key,
    [!] ./gen_comprehensive_specimen.py:846:        "hopfield": {},  # hopfield_key -> node_id
    [!] ./gen_comprehensive_specimen.py:876:        key_add = f"{a}+{b}"
    [!] ./gen_comprehensive_specimen.py:877:        key_mul = f"{a}*{b}"
    [!] ./gen_comprehensive_specimen.py:878:        math_flashcards[key_add] = {"question": f"{a}+{b}", "answer": str(a + b), "type": "addition"}
    [!] ./gen_comprehensive_specimen.py:879:        math_flashcards[key_mul] = {"question": f"{a}*{b}", "answer": str(a * b), "type": "multiplication"}
    [!] ./gen_comprehensive_specimen.py:883:            key_sub = f"{a}-{b}"
    [!] ./gen_comprehensive_specimen.py:884:            math_flashcards[key_sub] = {"question": f"{a}-{b}", "answer": str(a - b), "type": "subtraction"}
    [!] ./gen_comprehensive_specimen.py:886:            key_div = f"{a}/{b}"
    [!] ./gen_comprehensive_specimen.py:887:            math_flashcards[key_div] = {"question": f"{a}/{b}", "answer": str(a // b), "type": "division"}
    [!] ./gen_comprehensive_specimen.py:1062:        "seam_tokens": ["number", "equation", "formula"],
    [!] ./gen_comprehensive_specimen.py:1074:        "seam_tokens": ["life", "organism", "cell"],
    [!] ./gen_comprehensive_specimen.py:1086:        "seam_tokens": ["feeling", "meaning", "reflection"],
    [!] ./gen_comprehensive_specimen.py:1098:        "seam_tokens": ["pattern", "sequence", "growth"],
    [!] ./gen_comprehensive_specimen.py:1110:        "seam_tokens": ["arithmetic", "compute", "solve"],
    [!] ./gen_comprehensive_specimen.py:1121:        "seam_tokens": ["animal", "describe", "furry"],
    [!] ./gen_comprehensive_specimen.py:1132:        "seam_tokens": ["feel", "empathy", "comfort"],
    [!] ./gen_comprehensive_specimen.py:1158:    "inhibition_tokens": ["number", "compute", "sum"]
    [!] ./gen_comprehensive_specimen.py:1173:    "inhibition_tokens": ["animal", "describe", "species"]

5.3 MEDIUM FINDINGS (RECOMMEND FIX - 180 DAYS)
------------------------------------------------

5.4 RECOMMENDATIONS
-------------------
1. IMMEDIATE: Remove all eval/exec/system calls - replace with safe alternatives
2. IMMEDIATE: Move all credentials to DoD-approved Key Management System
3. HIGH: Implement proper input validation and sanitization
4. HIGH: Add comprehensive audit logging (IAW AU-2)
5. MEDIUM: Complete all TODO/FIXME items
6. MEDIUM: Implement role-based access control (RBAC)
7. LOW: Remove debug/test code from production
8. LOW: Add comprehensive error handling

================================================================================
6.0 COMPLIANCE STATUS MATRIX
================================================================================

6.1 COMPLIANCE SCORECARD
------------------------
NIST CONTROL FAMILY | STATUS | FINDINGS
-------------------|--------|----------
AC Access Control  | PARTIAL    | Implement proper access control
AU Audit           | PARTIAL    | Add comprehensive logging
CM Config          | PASS    | Configuration files present
IA Identity        | PARTIAL    | Strengthen authentication
SC Protection      | PARTIAL | Implement encryption
SI Integrity       | PARTIAL | Add input validation
RA Risk            | PARTIAL | Complete vulnerability scan
SA Acquisition     | PASS    | Dependencies documented

================================================================================
7.0 POAM (PLAN OF ACTION AND MILESTONES)
================================================================================

7.1 REMEDIATION PLAN
--------------------
WEEK 1: Patch all critical RCE vulnerabilities
WEEK 2: Remove hardcoded credentials and implement KMS
WEEK 3: Implement comprehensive audit logging
WEEK 4: Complete POAM and submit to AO
WEEK 5-8: Address high findings
WEEK 9-12: Address medium findings
WEEK 13-24: Address low findings and final validation

================================================================================
8.0 AUDITOR SIGN-OFF
================================================================================

AUDIT COMPLETED BY: NSA/CSS CYBERSECURITY DIRECTORATE
DATE: Wed Jul 22 11:45:38 PM EDT 2026

DISPOSITION:
[ ] ACCEPTED - All findings resolved
[X] NOT ACCEPTED - Critical findings require immediate attention
[ ] CONDITIONALLY ACCEPTED - POAM required for remaining findings

NEXT AUDIT: 90 Days from ATO

================================================================================
END OF AUDIT REPORT
================================================================================
